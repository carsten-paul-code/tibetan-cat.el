#+TITLE: Function Reference — tibetan-cat.el
#+SUBTITLE: Auto-generated from source — do not hand-edit; re-run generate-function-reference.py
#+DATE: 2026-06-11T10:19:51
#+OPTIONS: toc:2 num:nil

Source tree: ~/Users/cp/tibetan-cat.el~  ·  69 modules  ·  1281 forms

Regenerated automatically by:

#+begin_src bash
python3 ~/emacs-zettelkasten/generate-function-reference.py /Users/cp/tibetan-cat.el /Users/cp/tibetan-cat.el/REFERENCE.org
#+end_src

* Quick Index

  - [[#INIT-EXAMPLE.el][INIT-EXAMPLE.el]]  (1)
  - [[#analysis/tibetan-classroom.el][analysis/tibetan-classroom.el]]  (11)
  - [[#analysis/tibetan-clause-analysis.el][analysis/tibetan-clause-analysis.el]]  (15)
  - [[#analysis/tibetan-clause-segmenter.el][analysis/tibetan-clause-segmenter.el]]  (20)
  - [[#analysis/tibetan-enhanced-display.el][analysis/tibetan-enhanced-display.el]]  (16)
  - [[#analysis/tibetan-enhanced-parser.el][analysis/tibetan-enhanced-parser.el]]  (16)
  - [[#analysis/tibetan-interlinear.el][analysis/tibetan-interlinear.el]]  (21)
  - [[#analysis/tibetan-mitra-translation.el][analysis/tibetan-mitra-translation.el]]  (24)
  - [[#analysis/tibetan-particles-bialek.el][analysis/tibetan-particles-bialek.el]]  (8)
  - [[#analysis/tibetan-sentence-tree.el][analysis/tibetan-sentence-tree.el]]  (10)
  - [[#analysis/tibetan-translation-engine.el][analysis/tibetan-translation-engine.el]]  (10)
  - [[#analysis/tibetan-translation-suggest.el][analysis/tibetan-translation-suggest.el]]  (2)
  - [[#analysis/tibetan-verb-classifier.el][analysis/tibetan-verb-classifier.el]]  (5)
  - [[#config/tibetan-keybindings.el][config/tibetan-keybindings.el]]  (3)
  - [[#config/tibetan-menu.el][config/tibetan-menu.el]]  (1)
  - [[#core/tibetan-dharmamitra-api.el][core/tibetan-dharmamitra-api.el]]  (13)
  - [[#core/tibetan-dharmamitra-translation.el][core/tibetan-dharmamitra-translation.el]]  (12)
  - [[#core/tibetan-org-structure.el][core/tibetan-org-structure.el]]  (37)
  - [[#core/tibetan-phonetics.el][core/tibetan-phonetics.el]]  (17)
  - [[#core/tibetan-sanskrit-parallel-dharmamitra.el][core/tibetan-sanskrit-parallel-dharmamitra.el]]  (22)
  - [[#core/tibetan-sanskrit-parallel.el][core/tibetan-sanskrit-parallel.el]]  (24)
  - [[#core/tibetan-steinert.el][core/tibetan-steinert.el]]  (13)
  - [[#core/tibetan-text-classifier.el][core/tibetan-text-classifier.el]]  (9)
  - [[#core/tibetan-thesaurus.el][core/tibetan-thesaurus.el]]  (34)
  - [[#core/tibetan-utils.el][core/tibetan-utils.el]]  (13)
  - [[#core/tibetan-vocabulary-detailed.el][core/tibetan-vocabulary-detailed.el]]  (40)
  - [[#core/tibetan-vocabulary.el][core/tibetan-vocabulary.el]]  (48)
  - [[#core/tibetan-wylie.el][core/tibetan-wylie.el]]  (4)
  - [[#core/tibetan-zettel.el][core/tibetan-zettel.el]]  (51)
  - [[#data/tibetan-glossary-loader.el][data/tibetan-glossary-loader.el]]  (14)
  - [[#doc-prep/tibetan-doc-display.el][doc-prep/tibetan-doc-display.el]]  (13)
  - [[#doc-prep/tibetan-doc-format.el][doc-prep/tibetan-doc-format.el]]  (18)
  - [[#doc-prep/tibetan-doc-prep-init.el][doc-prep/tibetan-doc-prep-init.el]]  (3)
  - [[#doc-prep/tibetan-doc-prep.el][doc-prep/tibetan-doc-prep.el]]  (34)
  - [[#doc-prep/tibetan-document-genres.el][doc-prep/tibetan-document-genres.el]]  (9)
  - [[#doc-prep/tibetan-document-prep-claude.el][doc-prep/tibetan-document-prep-claude.el]]  (13)
  - [[#doc-prep/tibetan-document-prep-wizard.el][doc-prep/tibetan-document-prep-wizard.el]]  (23)
  - [[#doc-prep/tibetan-ocr-correct.el][doc-prep/tibetan-ocr-correct.el]]  (14)
  - [[#doc-prep/tibetan-ocr-runner.el][doc-prep/tibetan-ocr-runner.el]]  (17)
  - [[#doc-prep/tibetan-ocr-validate.el][doc-prep/tibetan-ocr-validate.el]]  (23)
  - [[#doc-prep/tibetan-segment-migrate.el][doc-prep/tibetan-segment-migrate.el]]  (12)
  - [[#doc-prep/tibetan-sentence-structure.el][doc-prep/tibetan-sentence-structure.el]]  (17)
  - [[#doc-prep/tibetan-wylie-ingest.el][doc-prep/tibetan-wylie-ingest.el]]  (26)
  - [[#persist/tibetan-analysis-claude.el][persist/tibetan-analysis-claude.el]]  (63)
  - [[#persist/tibetan-analysis-combine.el][persist/tibetan-analysis-combine.el]]  (14)
  - [[#persist/tibetan-analysis-combined.el][persist/tibetan-analysis-combined.el]]  (15)
  - [[#persist/tibetan-analysis-persist.el][persist/tibetan-analysis-persist.el]]  (131)
  - [[#persist/tibetan-analysis-sanskrit.el][persist/tibetan-analysis-sanskrit.el]]  (22)
  - [[#persist/tibetan-auto-analysis.el][persist/tibetan-auto-analysis.el]]  (28)
  - [[#persist/tibetan-claude-queue.el][persist/tibetan-claude-queue.el]]  (21)
  - [[#persist/tibetan-compound-analysis.el][persist/tibetan-compound-analysis.el]]  (29)
  - [[#persist/tibetan-sentence-claude.el][persist/tibetan-sentence-claude.el]]  (23)
  - [[#persist/tibetan-sentence-persist.el][persist/tibetan-sentence-persist.el]]  (56)
  - [[#persist/tibetan-structure-reorg.el][persist/tibetan-structure-reorg.el]]  (15)
  - [[#philology/tibetan-bialek-textbook-refs.el][philology/tibetan-bialek-textbook-refs.el]]  (2)
  - [[#philology/tibetan-madhyamaka-terms.el][philology/tibetan-madhyamaka-terms.el]]  (6)
  - [[#philology/tibetan-verse-philology.el][philology/tibetan-verse-philology.el]]  (13)
  - [[#scripts/regenerate-tigress-analysis.el][scripts/regenerate-tigress-analysis.el]]  (4)
  - [[#scripts/test-analysis-output.el][scripts/test-analysis-output.el]]  (1)
  - [[#setup/tibetan-cat-setup.el][setup/tibetan-cat-setup.el]]  (15)
  - [[#setup/tibetan-cat-ui.el][setup/tibetan-cat-ui.el]]  (23)
  - [[#spec/generate-docs.el][spec/generate-docs.el]]  (1)
  - [[#spec/generate-living-doc.el][spec/generate-living-doc.el]]  (10)
  - [[#spec/run-specs.el][spec/run-specs.el]]  (1)
  - [[#spec/suites/particle-analysis-spec.el][spec/suites/particle-analysis-spec.el]]  (1)
  - [[#spec/suites/tigress-story-spec.el][spec/suites/tigress-story-spec.el]]  (5)
  - [[#spec/tibetan-bdd.el][spec/tibetan-bdd.el]]  (22)
  - [[#tibetan-cat.el][tibetan-cat.el]]  (8)
  - [[#workspace/tibetan-sentence-workspace.el][workspace/tibetan-sentence-workspace.el]]  (16)

* INIT-EXAMPLE.el
:PROPERTIES:
:CUSTOM_ID: INIT-EXAMPLE.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/INIT-EXAMPLE.el
:END:

** ~cp/setup-tibetan-cat~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 13
:SIGNATURE: (cp/setup-tibetan-cat)
:INTERACTIVE: t
:END:

Setup Tibetan Computer-Assisted Translation system.


* analysis/tibetan-classroom.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-classroom.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-classroom.el
:END:

** ~tibetan-cache-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 36
:SIGNATURE: (tibetan-cache-key seg-id tibetan-text)
:END:

Generate cache key from segment ID and content hash.

** ~tibetan-get-cached-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 40
:SIGNATURE: (tibetan-get-cached-analysis seg-id tibetan-text)
:END:

Get cached analysis for segment, or nil if not cached.

** ~tibetan-cache-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 45
:SIGNATURE: (tibetan-cache-analysis seg-id tibetan-text wylie translation vocab grammar suggestion)
:END:

Cache analysis results for segment.

** ~tibetan-get-dharmamitra-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 55
:SIGNATURE: (tibetan-get-dharmamitra-translation tibetan-text)
:END:

Get DharmaMitra translation for TIBETAN-TEXT.
Returns translation string or error message.

** ~tibetan-extract-and-analyze-verbs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 74
:SIGNATURE: (tibetan-extract-and-analyze-verbs tibetan-text)
:END:

Extract verbs from TIBETAN-TEXT and analyze them using Hill lexicon.
Returns list of verb entries found, or nil if verb classifier not available.

** ~tibetan-segment-info~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 98
:SIGNATURE: (tibetan-segment-info &optional silent fast)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show segment analysis in RIGHT window.
Displays:
- Tibetan text
- Wylie transliteration
- DharmaMitra translation (unless FAST is non-nil)
- Vocabulary with meanings
- Context-aware grammatical analysis

Works with both:
- Org structure: *** Segment N
- Old markers: 〔seg:...〕

Bound to C-c u i.

If SILENT is non-nil, return nil instead of raising error when not in segment.
If FAST is non-nil, skip slow DharmaMitra translation (for auto-mode).

** ~tibetan-auto-update~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 350
:SIGNATURE: (tibetan-auto-update)
:END:

Auto-update analysis when in auto-analysis mode.
Called by post-command-hook. Uses persistent analysis (C-c u A).

** ~tibetan-toggle-auto~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 368
:SIGNATURE: (tibetan-toggle-auto)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Toggle auto-analysis mode.
When enabled, persistent analysis updates automatically as you navigate.
Bound to C-c u E.

** ~tibetan-analysis-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 31
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for segment analysis results.
Key: (segment-id . content-hash)
Value: (wylie translation vocabulary grammar suggestion)

** ~tibetan-auto-mode~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 343
:DEFAULT: nil
:END:

Non-nil if auto-analysis mode is enabled.
Auto-analysis automatically updates segment analysis as you navigate.

** ~tibetan-auto-last-segment~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 347
:DEFAULT: nil
:END:

Last segment ID analyzed in auto mode, to avoid redundant updates.


* analysis/tibetan-clause-analysis.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-clause-analysis.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-clause-analysis.el
:END:

** ~tibetan-clause--clean-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 56
:SIGNATURE: (tibetan-clause--clean-text text)
:END:

Clean TEXT for analysis by normalizing punctuation.

** ~tibetan-clause--split-syllables~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 66
:SIGNATURE: (tibetan-clause--split-syllables text)
:END:

Split TEXT into syllables by tsheg (་).

** ~tibetan-clause--is-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 71
:SIGNATURE: (tibetan-clause--is-verb-p word)
:END:

Check if WORD is a known or likely verb.
Uses verb database if available, otherwise falls back to heuristics.

** ~tibetan-clause--get-converb-type~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 94
:SIGNATURE: (tibetan-clause--get-converb-type particle)
:END:

Get the converb type for PARTICLE.

** ~tibetan-clause--strip-particle~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 100
:SIGNATURE: (tibetan-clause--strip-particle word particle)
:END:

Strip PARTICLE suffix from WORD and return the stem.

** ~tibetan-detect-converbs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 111
:SIGNATURE: (tibetan-detect-converbs text)
:END:

Detect converbs in TEXT.
Returns a list of plists with :particle, :type, :stem, :position, and :word.

Converbs can appear in two forms:
1. As standalone particles: བཞུགས་ནས (two syllables: བཞུགས + ནས)
2. As suffixes attached to verb stems: ཐོས་པས (one syllable with suffix)

This function handles both cases.

** ~tibetan-find-main-verb~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 159
:SIGNATURE: (tibetan-find-main-verb text)
:END:

Find the main verb in TEXT.
Returns a plist with :verb, :type, :position, :stem.
The main verb is typically the final verb not marked with a converb particle.

** ~tibetan-scope-converbs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 220
:SIGNATURE: (tibetan-scope-converbs text)
:END:

Determine scoping relationships between converbs and main verbs in TEXT.
Returns a list of plists, each with :converb and :main-verb.
Each converb is paired with the verb it modifies.

** ~tibetan-build-clause-tree~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 245
:SIGNATURE: (tibetan-build-clause-tree text)
:END:

Build a hierarchical clause tree from TEXT.
Returns a plist representing the clause structure:
  :type - ='clause='
  :text - original text
  :main-verb - the main verb plist
  :dependent-clauses - list of dependent clause plists

** ~tibetan-format-clause-tree~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 278
:SIGNATURE: (tibetan-format-clause-tree tree &optional indent)
:END:

Format TREE for display with optional INDENT level.

** ~tibetan-clause--get-converb-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 307
:SIGNATURE: (tibetan-clause--get-converb-translation type)
:END:

Get English translation hint for converb TYPE.

** ~tibetan-analyze-clause-structure~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 321
:SIGNATURE: (tibetan-analyze-clause-structure text)
:END:

Analyze clause structure in TEXT and return formatted analysis.
This is the main entry point for clause analysis, identifying main verbs
and converbial constructions.

** ~tibetan-converb-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 31
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-final-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 44
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-negative-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 48
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-clause-segmenter.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-clause-segmenter.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-clause-segmenter.el
:END:

** ~tibetan-clause-seg--verb-at~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 120
:SIGNATURE: (tibetan-clause-seg--verb-at verbs pos)
:END:

Return the verb entry in VERBS whose source-pos = POS, or nil.

** ~tibetan-clause-seg--suffix-type~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 124
:SIGNATURE: (tibetan-clause-seg--suffix-type verb)
:END:

Return the converb type associated with VERB's suffix, or nil.

** ~tibetan-clause-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 129
:SIGNATURE: (tibetan-clause-segment words verbs &optional _mwu)
:END:

Segment WORDS into clauses using VERBS (from Round-1 extractor).

Returns a list of clause alists — see commentary of this file.
Each verb becomes exactly one clause.  The clause's span runs from the
position after the previous clause's end up to and including the verb
position (plus any trailing converb particle or final particle).

** ~tibetan-clause-seg--multichar-suffix-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 270
:SIGNATURE: (tibetan-clause-seg--multichar-suffix-p pair)
:END:

Non-nil when PAIR's particle is longer than one character and can
therefore be safely matched as a suffix on another word.

Single-character case particles (ས / ལ / ར / ན) are ambiguous with
syllable-final consonants (e.g. `གཞིས' ends in ས as final consonant,
not as an agglutinated ergative).  We therefore allow them to match
only as standalone words (via the earlier `assoc' lookups), never
via `string-suffix-p'.  Bialek's particle analyzer documents the
same rule at `analysis/tibetan-particles-bialek.el' (§terminative).

** ~tibetan-clause-seg--classify-word~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 282
:SIGNATURE: (tibetan-clause-seg--classify-word w)
:END:

Return a symbol for W indicating its grammatical role.
Possible return values: \='case-suffix, \='case-standalone, \='negator,
\='determiner, \='final, \='converb, or nil (= content/head
candidate).  Detects the longest matching suffix.  Single-character
case particles match only as standalone — see
`tibetan-clause-seg--multichar-suffix-p'.

** ~tibetan-clause-seg--case-of~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 309
:SIGNATURE: (tibetan-clause-seg--case-of w)
:END:

Return the case symbol for W, preferring the longest matching
particle/suffix.  Only multi-character particles may match as a
suffix (see `tibetan-clause-seg--multichar-suffix-p').

** ~tibetan-clause-seg--case-particle~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 325
:SIGNATURE: (tibetan-clause-seg--case-particle w)
:END:

Return the particle string at the end of W (the literal substring
that the case-of helper recognised), or nil if W carries no case.

Standalone particle (W itself is a case particle) → return W.
Suffix particle (multi-char, see `--multichar-suffix-p') → return the
matched suffix.  Used by callers that need the particle text in
addition to the case symbol — e.g. legacy renderers that print
`form(MARKER)' annotations.

** ~tibetan-clause-seg--case-strip~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 348
:SIGNATURE: (tibetan-clause-seg--case-strip w)
:END:

If W ends in a case particle longer than itself, return the stem;
otherwise return W unchanged.  Only multi-character particles may
match — a final ས / ལ / ར / ན on a stem is NOT stripped, since
single-character case particles are ambiguous with syllable-final
consonants.

** ~tibetan-clause-seg--verb-position-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 370
:SIGNATURE: (tibetan-clause-seg--verb-position-p pos verbs)
:END:

/(no docstring)/

** ~tibetan-clause-seg--mwu-verbal-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 373
:SIGNATURE: (tibetan-clause-seg--mwu-verbal-p mwu-entry)
:END:

Return non-nil when an MWU entry's `english' meta describes a verb.

MWU-ENTRY has the shape `(start end form meta)' where META is an
alist (typically containing an `english' key).  An MWU is treated as
verb-bearing when its English gloss starts with `to <lowercase>',
`pf./pres./fut./ft./imp.', or contains the word `verb' / `Verb'.
Single source of truth used by both the NP chunker (which then
SKIPS the MWU entirely — verbal MWUs are predicates, not NPs) and
the verb extractor (which then attempts head-verb detection).

The lowercase-letter requirement after `to ' is intentional:
directional / terminative glosses like `to Ko ron sa' or
`to the temple' would otherwise spuriously match.

** ~tibetan-clause-seg--mwu-span-at~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 404
:SIGNATURE: (tibetan-clause-seg--mwu-span-at pos mwu)
:END:

Return (start . end) if POS starts an MWU in MWU, else nil.
MWU entries are (start end form meta).

** ~tibetan-np-chunk~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 410
:SIGNATURE: (tibetan-np-chunk words &optional clauses mwu)
:END:

Chunk WORDS into noun phrases, optionally bounded by CLAUSES.
MWU spans are treated as atomic heads.  Returns a list of NP alists
(see commentary).

** ~tibetan-clause-seg--lookup-case-frame~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 666
:SIGNATURE: (tibetan-clause-seg--lookup-case-frame lemma)
:END:

Return the `case_frame' string for LEMMA via the verb classifier, or nil.

** ~tibetan-analyze-round2~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 676
:SIGNATURE: (tibetan-analyze-round2 words verbs &optional mwu)
:END:

Run Round-2 Parts A+B (clauses + NPs).  Returns an alist:
  ((clauses . ...) (nps . ...)).
The former `argument-structure' key is RETIRED — use
`tibetan-analyze-sentence' (tibetan-sentence-tree.el) for the
verb-first slot-filled tree.

** ~tibetan-clause-seg--converb-suffixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 57
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-clause-seg--final-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 74
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-clause-seg--case-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 78
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-clause-seg--negators~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 96
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-clause-seg--nominaliser-suffixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 99
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-clause-seg--determiners~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 113
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-enhanced-display.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-enhanced-display.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-enhanced-display.el
:END:

** ~tibetan-verb-detect--minimal-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 70
:SIGNATURE: (tibetan-verb-detect--minimal-entry word &optional meaning source)
:END:

Minimal verb-entry alist for WORD when the Hill DB has no full record.
Compatible with the `alist-get' consumers used throughout the analyzer.

SOURCE (a symbol) tags the classification path that produced this
entry — `closed-set' for the curated minor-verb / modal /
reporting sets, `vocab-fallback' for dictionary-gloss heuristic
hits.  Renderers use this tag to filter low-confidence
vocab-fallback entries (e.g. uddāna-verse `རྣམས' / `སྡོམ' that
look verbal in Bialek but are actually non-verbs in context)
from clause segmentation while keeping curated closed-set verbs
that legitimately drive clauses (`གསོལ', `མཛད', `བྱུང', …).
Defaults to `closed-set' for backward compatibility with callers
that don't pass the arg yet.

** ~tibetan-verb-detect--vocab-says-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 121
:SIGNATURE: (tibetan-verb-detect--vocab-says-verb-p word)
:END:

Return the gloss string if `tibetan-vocab-lookup-detailed' for WORD
looks like a verb (primary gloss starts with `to ', `pf.', `pres.',
`fut.', `imp.', or contains the word `verb').

Returns nil — overriding any leading `to X' hit — when the gloss
also contains `particle' / `marker' / `postposition' /
`nominaliser' / `enclitic' tokens.  In that case the dictionary
itself is flagging the word as a non-verb and the leading `to X'
is misleading polysemy-flattening from a multilingual TSV
source (see
`tibetan-verb-detect--non-verb-gloss-tokens-re').

** ~tibetan-verb-detect--lookup~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 177
:SIGNATURE: (tibetan-verb-detect--lookup candidate)
:END:

Try to resolve CANDIDATE to a verb entry.
Search order:
 0. Reject if CANDIDATE is a bare nominaliser/enclitic particle.
 1. Hill 2010 DB (full record via `tibetan-verb-lookup').
 2. Closed minor-verb / modal / reporting set (minimal record,
    tagged `(source . closed-set)').
 3. Vocabulary dictionary — only when the gloss looks verbal AND
    doesn't flag the word a non-verb via
    `tibetan-verb-detect--non-verb-gloss-tokens-re'.  Tagged
    `(source . vocab-fallback)' so renderers can filter low-
    confidence hits from clause segmentation.

** ~tibetan-verb-detect--strip-negation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 215
:SIGNATURE: (tibetan-verb-detect--strip-negation word)
:END:

If WORD starts with `མ་' or `མི་' followed by at least one more syllable,
return (BARE . t).  Otherwise (WORD . nil).  The tsheg boundary is
mandatory so monosyllabic verbs (མཐོང, མི) are never mis-analysed.

** ~tibetan-verb-detect--progressive~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 225
:SIGNATURE: (tibetan-verb-detect--progressive word)
:END:

Try to locate a verb hit for WORD, stripping negation and particles.
Returns a plist (:entry E :source-form WORD :negated BOOL :suffix-type S)
or nil.  The progressive candidate list is:
  (stripped word, strip-particles once, strip-particles twice, head syllable).

** ~tibetan-analyze-zero-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 271
:SIGNATURE: (tibetan-analyze-zero-markers verbs multiword-units words)
:END:

Analyze zero-marked NPs to distinguish TOPIC vs. ABSOLUTIVE functions.
Returns list of zero-marker analyses with function, gloss, and notes.
Returns nil if no zero markers found or if inputs are invalid.

** ~tibetan-analyze-arguments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 368
:SIGNATURE: (tibetan-analyze-arguments verb multiword-units words &optional all-verbs)
:END:

Analyze argument structure for VERB given MULTIWORD-UNITS and WORDS.
Returns list of argument alists with keys: role, marker, form,
english, function.
When ALL-VERBS is given, arguments are restricted to the clause ending
at VERB — i.e. only units positioned AFTER the previous verb in the
sentence and BEFORE (or at) this verb.  Without ALL-VERBS, falls back
to the legacy behaviour of collecting every unit before VERB.

** ~tibetan-segment-info-enhanced~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 533
:SIGNATURE: (tibetan-segment-info-enhanced &optional silent)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show enhanced segment analysis.
Uses compound/proper noun recognition and accurate particle detection.
If SILENT is non-nil, return nil instead of error when not in segment.

** ~tibetan-build-compound-aware-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1020
:SIGNATURE: (tibetan-build-compound-aware-segments words multiword-units)
:END:

Build segmentation list showing compounds as units.
WORDS is the raw syllable list, MULTIWORD-UNITS are recognized compounds.
Returns list of strings for display.

** ~tibetan-get-claimed-indices~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1051
:SIGNATURE: (tibetan-get-claimed-indices multiword-units)
:END:

Get set of syllable indices that are part of compounds.
Returns hash table for fast lookup.

** ~tibetan-extract-verbs-compound-aware~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1060
:SIGNATURE: (tibetan-extract-verbs-compound-aware tibetan-text words multiword-units)
:END:

Extract verbs from TIBETAN-TEXT, compound-, particle-, and chain-aware.

Returns a list of verb-entry alists.  Each entry carries the usual
Hill 2010 fields (lemma, meaning, stems, transitivity, volitionality,
case_frame, indigenous_class) plus Round-1/Round-3 annotations:
  source-form    as-appears-in-text
  source-pos     first syllable index in WORDS (nil if unknown)
  negated        t when ma-/mi- negation applies (stripped from within
                 the form, preceding separate syllable, or intrinsic for
                 `མེད' / `མིན')
  suffix-type    morphological suffix description, or nil
  minimal        t when only a closed-set / vocab fallback gave the hit
  is-modal       t for auxiliary verbs (dgos, chog, thub…)
  is-reporter    t for reporting verbs (zer, gsungs…)
  modal-of       lemma of a following modal, when this verb chains into one
  reports-p      lemma of a following reporting verb, when chained
  conditional    t when the verb is negated AND a `ན' conditional follows
                 within 1–2 positions (pattern `མ་X་ན')

Strategy:
 1. Scan 3- and 2-syllable windows for multi-syllable Hill DB entries.
 2. For each multiword unit try progressive verb detection on the whole
    unit; if that fails, try its head-with-strip, then each constituent.
 3. For each unclaimed single syllable, try progressive detection.
 4. Link modal / reporting chains based on adjacent syllable positions.
 5. Tag conditional negations (`མ་X་ན').

** ~tibetan-verb-detect--modal-set~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 38
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-verb-detect--reporting-set~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 44
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-verb-detect--minor-verb-set~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 48
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-verb-detect--non-verb-gloss-tokens-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 97
:DEFAULT: (concat "\\b\\("
          "particle"
          "\\|marker"
          "\\|postposition"
          "\\|nominali[sz]er"
  
:END:

Regexp matching tokens in a dictionary gloss that flag the entry as
a non-verb regardless of any leading `to X' polysemy hit.

Live test on gotrapatala.org seg-005 (the uddāna verse `སྡོམ་ལ་
། གཞི་དང་རྟགས་དང་ཕྱོགས་རྣམས་དང་༎') showed the Bialek
dictionary entries

  rnams   → `to choke | plural marker'
  dag     → `to bind; plural particle'

driving false-positive verb classifications even though the
SAME gloss labels the word a `marker' / `particle'.  When this
regexp matches the gloss, `--vocab-says-verb-p' returns nil —
the dictionary's own non-verb classification overrides the
leading `to X' polysemy flattening.

** ~tibetan-verb-detect--nominalizer-set~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 168
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-enhanced-parser.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-enhanced-parser.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-enhanced-parser.el
:END:

** ~tibetan-load-json-dict~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 46
:SIGNATURE: (tibetan-load-json-dict filename)
:END:

Load JSON dictionary from FILENAME, return as hash table.

** ~tibetan-load-dictionaries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 63
:SIGNATURE: (tibetan-load-dictionaries)
:END:

Load compound and proper noun dictionaries from JSON files.
Creates empty hash tables if files don't exist or tibetan-cat-data-dir is unset.

** ~tibetan-segment-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 89
:SIGNATURE: (tibetan-segment-text text)
:END:

Segment TEXT into tsheg-delimited units.
Returns list of word strings.

** ~tibetan-enhanced-parser--case-particle-tail-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 107
:SIGNATURE: (tibetan-enhanced-parser--case-particle-tail-p joined)
:END:

Return non-nil if JOINED (a `་'-joined string) ends in a case/converb
particle listed in `tibetan-enhanced-parser--case-particle-tails'.

** ~tibetan-enhanced-parser--negation-plus-hill-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 115
:SIGNATURE: (tibetan-enhanced-parser--negation-plus-hill-verb-p joined)
:END:

Return non-nil when JOINED is `མ-V' or `མི-V' with V a Hill-DB verb.

Live test on gotrapatala.org seg-008 (`སྤྱོད་དང་རབ་གནས་ཐ་མ་
ཡིན་༎') showed the MWU finder bundling `མ་ཡིན' as a Steinert
2-syllable MWU, hiding the copula `ཡིན' from verb extraction.
The verb extractor's `prev-word-negates-p' mechanism already
handles `མ' / `མི' + verb correctly when they are SEPARATE
words; bundling them as an MWU only causes harm.

This predicate is consumed by the MWU finder's steinert-hit
branch — when it returns non-nil for a candidate JOINED, the
2-syllable Steinert hit is rejected so the MWU finder falls
through to length-1 and the verb extractor sees `མ' / `མི'
and the verb as separate words.

Returns nil for:
  - non-2-syllable forms (`མ་ཡིན་པ' falls outside)
  - heads other than `མ' / `མི' (`ཐ་མ' has tail `མ' but head
    is `ཐ', not negation)
  - tails not present in the Hill-DB verb table — closed-set
    minor verbs (`མཛད', `གསོལ', `བྱུང', …) are intentionally
    out of scope, so user-facing genuine `མ-V' / `མི-V'
    Steinert idioms involving honorific or aspectual verbs
    keep their MWU bundling.

Defensive: returns nil when Hill-DB lookup is unavailable
(`tibetan-verb-lookup' not fbound) — the predicate becomes a
no-op rather than raising.

** ~tibetan-enhanced-parser--verb-tail-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 154
:SIGNATURE: (tibetan-enhanced-parser--verb-tail-p joined)
:END:

Return non-nil if the LAST tsheg-syllable of JOINED is a Hill-DB verb.

Used by the MWU finder to stop a multi-syllable (≥3) unit from absorbing
a finite verb — e.g. `དྲུང་དུ་ཕྱིན' (a Rangjung-Yeshe phrasal entry)
bundling the past verb `ཕྱིན' ("went").  The verb belongs to clause /
sentence-structure analysis, not the noun phrase, so the candidate is
rejected and the finder falls through to a shorter length, leaving the
verb a separate word.  Defensive no-op when `tibetan-verb-lookup' is
unavailable.

** ~tibetan-find-multiword-units~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 172
:SIGNATURE: (tibetan-find-multiword-units words)
:END:

Find multi-word compound/proper noun units in WORDS list.
Uses longest-match-first strategy.
Returns list of (start-index . end-index . entry-data) tuples.

** ~tibetan-analyze-word-unit~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 398
:SIGNATURE: (tibetan-analyze-word-unit word _prev-word _next-word)
:END:

Analyze single tsheg-delimited WORD with context.
PREV-WORD and NEXT-WORD provide context.
Returns alist with analysis data.

** ~tibetan-particle-function~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 437
:SIGNATURE: (tibetan-particle-function particle)
:END:

Return function description for PARTICLE.

** ~tibetan-find-verb-nominalizer-units~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 455
:SIGNATURE: (tibetan-find-verb-nominalizer-units words)
:END:

Find verb+nominalizer constructions in WORDS list.
Patterns like སྨྲས་པ (said + nominalizer) should be grouped together.
Returns list of (start-index end-index form data) tuples.

** ~tibetan-parse-enhanced~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 499
:SIGNATURE: (tibetan-parse-enhanced text)
:END:

Enhanced parsing of TEXT.
Returns structured analysis with:
- Multi-word units (compounds, proper nouns)
- Verb+nominalizer constructions (སྨྲས་པ, etc.)
- Word-level analysis
- Particle identification (only at boundaries)
- Context-aware verb identification.

** ~tibetan-cat-data-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 27
:DEFAULT: nil
:END:

Base directory for Tibetan CAT data files.

** ~tibetan-comprehensive-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 32
:END:

/(no docstring)/

** ~tibetan-compounds-dict~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 38
:DEFAULT: nil
:END:

Hash table of Tibetan compound terms.
Key: Tibetan text, Value: alist with wylie, english, sanskrit, category.

** ~tibetan-proper-nouns-dict~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 42
:DEFAULT: nil
:END:

Hash table of Tibetan proper nouns.
Key: Tibetan text, Value: alist with wylie, english, sanskrit, category.

** ~tibetan-enhanced-parser--case-particle-tails~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 95
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-interlinear.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-interlinear.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-interlinear.el
:END:

** ~tibetan-interlinear--portfolio-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 89
:SIGNATURE: (tibetan-interlinear--portfolio-key bialek-type)
:END:

Map a Bialek analysis type label to a Portfolio lookup key.
BIALEK-TYPE is e.g. "GENITIVE (GEN)" or "CONVERBIAL: ABLATIVE CONVERB".
Returns a normalised key like "genitive" or "converb-nas".

** ~tibetan-interlinear--parse-portfolio~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 114
:SIGNATURE: (tibetan-interlinear--parse-portfolio file)
:END:

Parse the Bialek Portfolio org FILE into a structured alist.
Returns an alist suitable for `tibetan-interlinear--portfolio-cache'.

** ~tibetan-interlinear--get-portfolio~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 225
:SIGNATURE: (tibetan-interlinear--get-portfolio)
:END:

Return the parsed Portfolio, using cache when valid.

** ~tibetan-interlinear--split-word-particle~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 284
:SIGNATURE: (tibetan-interlinear--split-word-particle tibetan-word bialek-tag)
:END:

Split TIBETAN-WORD into (STEM . PARTICLE-INFO) using BIALEK-TAG.
BIALEK-TAG is the grammatical role string from the Word/Particle List,
e.g. "GENITIVE (GEN)" or "CONCESSIVE PARTICLE" or nil.

Returns one of:
  (STEM . (PARTICLE-TIBETAN . SHORT-LABEL))  — stem + clitic particle
  (nil  . (WORD . SHORT-LABEL))              — pure standalone particle
  (WORD . nil)                                — no particle detected

The pure-standalone-particle case fires when the whole word IS a
known particle (e.g. `ནས' on its own, not bolted onto a verb) AND
the bialek analysis tags it as such.  The Interlinear renderer
emits these as a compact `nas [ABL]' — no dictionary gloss, no
jump link — matching how stem-final particles are rendered.

** ~tibetan-interlinear--sanitize-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 340
:SIGNATURE: (tibetan-interlinear--sanitize-gloss gloss)
:END:

Make GLOSS safe to embed inside `[...]' without forming an org link.

Rangjung Yeshe and other glossaries contain entries whose gloss is
itself wrapped in square brackets — e.g. `[value-big]', `[R]', or
`[accusative, adverbial]'.  When the interlinear renderer wraps
such a gloss in `[...]' the resulting `[[value-big]]' is read by
org-mode as a link target, and `org-export-dispatch' aborts with
`Unable to resolve link: "value-big"'.  Replacing `[...]' with
`(...)' inside the gloss keeps the content readable while
guaranteeing no accidental double-bracket ever reaches the export
layer.  Called for its value; does not mutate input.

** ~tibetan-interlinear--format-gloss-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 357
:SIGNATURE: (tibetan-interlinear--format-gloss-entry wylie-stem term-anchor
                                                 short-meaning
                                                 particle-wylie particle-label
                                                 &optional curated-p zettel-id)
:END:

Format one interlinear entry.
WYLIE-STEM is the Wylie of the lexical part.
TERM-ANCHOR is the internal org-link target slug (e.g. `term-bdag-la')
  matching a `<<term-bdag-la>>' target in the Detailed Dictionary
  section — click the Wylie to jump to the full entry below.  Pass
  nil to render plain Wylie without a link (useful for pure function
  words that have no Detailed Dictionary entry).
SHORT-MEANING is the English gloss or nil.
PARTICLE-WYLIE is the Wylie of the particle part or nil.
PARTICLE-LABEL is the short Bialek label ("GEN", "CONC", etc.) or nil.
Optional CURATED-P, when non-nil, marks the entry as coming from
the per-document Resources / Custom vocabulary list.  The entry
gets a leading `★' and is given a longer truncation budget (60
vs 30 chars) so hand-curated glosses aren't cut mid-phrase.
Optional ZETTEL-ID (Phase 2 of zettel-in-translation-workflow,
2026-04-24), when non-nil, indicates a zettel exists for the
token.  The link target switches to `id:ZETTEL-ID' (org's id
protocol — resolves via `.org-id-locations'), and a `♦' marker
follows the link.  ZETTEL-ID and CURATED-P are independent:
both markers can appear (`♦ ★') when a token has both a zettel
AND the displayed gloss came from Resources/Custom.

The English gloss (or Bialek label) is rendered OUTSIDE the org link
so the clickable area covers only the Wylie, keeping the readable
English visible as plain text.  Any brackets inside the gloss are
rewritten to parens so the enclosing `[...]' wrapper never produces
a second `[' / `]' pair that org-mode would parse as a link.
Returns something like:

  [[term-anchor][stem]] ★ [gloss] particle [LABEL]
  [[id:ZETTEL-ID][stem]] ♦ [gloss] particle [LABEL]
  [[id:ZETTEL-ID][stem]] ♦ ★ [gloss] particle [LABEL]

** ~tibetan-interlinear--prefer-target-lang~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 458
:SIGNATURE: (tibetan-interlinear--prefer-target-lang meaning)
:END:

Pass 5c (2026-04-22): return MEANING with the target-language half
of a bilingual `DE // EN' gloss selected.

Target language is driven by the dynamic variable
`tibetan-analysis--target-lang' (set from the source document's
`#+TIBETAN_TARGET_LANG:' header — `"de"' or `"en"'; nil
defaults to English-preferred for backward compatibility).

Non-bilingual glosses (no ` // ' separator) pass through verbatim.

Convention: bilingual glosses in this codebase are stored as
`GERMAN // ENGLISH' — the German half appears first because
most user-curated Resources entries were written German-primary.
With two + halves (multi-sense on each side using `;' / `,'),
the split just picks the first / last chunk by position: German
is first, English is last.

** ~tibetan-interlinear--prefer-english~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 485
:SIGNATURE: (tibetan-interlinear--prefer-english meaning)
:END:

If MEANING is a bilingual `DE // EN' gloss, return just the English half.
Otherwise return MEANING unchanged.

User-curated Resources entries routinely carry both halves
separated by ` // ' — e.g. `Personenname; der baumwollgewandete
[Yogin] aus der Mid-la-Familie // name of a person; the
cotton-clad [yogin] from the Mid la family'.  Without this
helper the interlinear would truncate the German half and leave
the reader with a stub like `Personenname; der', which is
semantically worthless at a glance.  We strip everything up to
and including the first ` // ' and keep what follows, recursively
in case the entry packs multiple `//' separators (take the last
segment, which in practice is always English).

This is an EXPLICIT English picker — it ignores
`tibetan-analysis--target-lang' and always returns the English
half.  Callers that want to honour the document's target-lang
should use `tibetan-interlinear--prefer-target-lang' instead.
Kept as a back-compat entry point; the Interlinear / CAT-Gloss
pipeline now routes through `--prefer-target-lang'.

** ~tibetan-interlinear--truncate-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 510
:SIGNATURE: (tibetan-interlinear--truncate-gloss meaning max-len)
:END:

Truncate MEANING to MAX-LEN characters for interlinear display.
Tries to cut at a word boundary.  Bilingual `DE // EN' glosses are
reduced to the target-language half first (per
`tibetan-analysis--target-lang', default English) so the
truncation never strands the reader on half a phrase in the
other language.

** ~tibetan-interlinear-generate-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 526
:SIGNATURE: (tibetan-interlinear-generate-gloss _vocab-pairs enriched-vocab-pairs
                                            bialek-analysis _tibetan-text
                                            &optional curated-words-hash)
:END:

Generate the Interlinear Gloss section content.
_VOCAB-PAIRS is the raw (tibetan . meaning) alist from segmentation
 (accepted for caller symmetry but currently unused — enriched pairs
 carry the short meanings the renderer needs).
ENRICHED-VOCAB-PAIRS is ((tibetan-clean . short-meaning) ...) from the
vocabulary loop in `tibetan-analysis-generate-content'.
BIALEK-ANALYSIS is the result of `tibetan-analyze-grammar-bialek'.
_TIBETAN-TEXT is the original Tibetan string (for verse structure —
reserved for shad/verse-break reinsertion; currently unused).
Optional CURATED-WORDS-HASH is a hash (word → t) of tokens whose
primary dictionary hit came from per-document Resources / Custom.
When non-nil, those entries get a leading `★' and a longer
truncation (60 chars vs 30) so the hand-curated gloss isn't cut
off mid-phrase.
Returns a string ready to insert after the `** Interlinear Gloss' heading.

** ~tibetan-interlinear-generate-particle-overview~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 661
:SIGNATURE: (tibetan-interlinear-generate-particle-overview bialek-analysis
                                                        _enriched-vocab-pairs)
:END:

Generate the Particle Overview section content.
BIALEK-ANALYSIS is the result of `tibetan-analyze-grammar-bialek'.
_ENRICHED-VOCAB-PAIRS is accepted for caller symmetry but is not
currently consulted — the portfolio lookup keys off the Bialek
particle tag alone.
Returns a string ready to insert after the `** Particle Overview' heading.

** ~tibetan-interlinear-portfolio-function-snippet~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 749
:SIGNATURE: (tibetan-interlinear-portfolio-function-snippet portfolio-key sub-id)
:END:

Return the Portfolio snippet `(SUB-TITLE . DESCRIPTION)' for
PORTFOLIO-KEY + SUB-ID, or nil if not found.
PORTFOLIO-KEY is a normalised key like "terminative",
"converb-nas" (see `tibetan-interlinear--portfolio-key').  SUB-ID
is a section identifier string like "1.5.1" or "2.11.2".

Walks the parsed Portfolio's `:functions' alist (which stores
`((SUB-NUM . SUB-TITLE) . DESCRIPTION)' entries) and returns the
first match.  When SUB-ID doesn't correspond to a parsed sub-section
— Claude may have emitted a broader `1.5' when the Portfolio only
has `1.5.1'..`1.5.9' — returns nil so the caller can fall back to
the top-level `:intro' or omit the snippet.

Used by `*** Particles in This Segment' in the Grammar renderer
(Pass 6c) to attach Portfolio text per particle occurrence, so the
analysis file is self-contained for readers without access to the
user's Portfolio source.

** ~tibetan-interlinear-portfolio-reference-block~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 777
:SIGNATURE: (tibetan-interlinear-portfolio-reference-block)
:END:

Return a markdown-style reference block listing every parsed
Portfolio section + sub-section with its ACTUAL numbering from the
user's Portfolio file.

The block is designed to be injected into the Claude system prompt
so Claude's `## Particles' output uses EXACTLY the section IDs the
user's Portfolio defines — not Bialek-canonical numbers that may
diverge (Carsten's Portfolio has Terminative at §1.6; some Bialek
textbook editions have it at §1.5).  Without this the Grammar
section's `*** Particles in This Segment' renderer fails to resolve
Portfolio snippets for correctly-tagged particles, because
`tibetan-interlinear-portfolio-function-snippet' looks up IDs in
the parsed cache.

Returns an empty string when no Portfolio is loaded — callers
concatenate unconditionally and the prompt simply omits the block.

Format:
  Portfolio section reference (use these EXACT numbers in ...):
    §1.1 Genitive — 1.1.1 Genitive Attribute; 1.1.2 Genitive with...
    §1.3 Ergative — 1.3.1 Subject of Transitive Verbs; ...
    §1.5 Dative
    §1.6 Terminative — 1.6.1 Place / Location; 1.6.3 Time; ...

** ~tibetan-interlinear--section-lt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 838
:SIGNATURE: (tibetan-interlinear--section-lt a b)
:END:

Compare two Portfolio section-number strings numerically.
`1.2' < `1.10'; `1.9' < `2.1'.  Simple numeric tuple compare —
each dot-separated component is parsed as an integer.

** ~tibetan-interlinear--truncate-para~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 854
:SIGNATURE: (tibetan-interlinear--truncate-para text max-len)
:END:

Truncate TEXT to approximately MAX-LEN characters at a sentence boundary.

** ~tibetan-interlinear-insert-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 868
:SIGNATURE: (tibetan-interlinear-insert-sections enriched-vocab-pairs
                                             bialek-analysis
                                             tibetan-text
                                             &optional vocab-pairs
                                             curated-words-hash)
:END:

Insert Interlinear Gloss and Particle Overview sections at point.
This is the main entry point called from the analysis generation pipeline.
ENRICHED-VOCAB-PAIRS, BIALEK-ANALYSIS, TIBETAN-TEXT as for the
individual generators.  Optional VOCAB-PAIRS for future use.

Optional CURATED-WORDS-HASH is a hash (word → t) of tokens whose
primary dictionary hit came from the per-document Resources /
Custom vocabulary list.  When passed, the Interlinear generator
prepends `★' to those entries so students see at a glance which
glosses are authoritative, hand-curated for this document.

** ~tibetan-interlinear-portfolio-file~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 43
:DEFAULT: nil
:END:

Path to the Bialek Portfolio org file.
When nil, the Particle Overview section will still be generated
using the inline Bialek analysis data, but without Portfolio excerpts.

** ~tibetan-interlinear-emit-particle-overview~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 48
:DEFAULT: nil
:END:

When non-nil, `tibetan-interlinear-insert-sections' emits the legacy
`** Particle Overview' level-2 section.  Default nil (Pass 6b,
2026-04-22): the per-particle Portfolio reference has moved into
`** Grammar / *** Particles in This Segment' rendered by
`tibetan-analysis--render-grammar-section'.  Kept as an escape
hatch for tests and callers that still want the old standalone
section.

** ~tibetan-interlinear--portfolio-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 78
:DEFAULT: nil
:END:

Cached alist of parsed Portfolio entries.
Each entry is (KEY . plist) where plist has:
  :section   — section number string ("1.1", "2.4")
  :title     — heading text ("Genitive", "Verb Stem + /ste/")
  :intro     — introductory paragraph(s) before sub-sections
  :functions — alist of ((sub-section-number . sub-title) . description)

** ~tibetan-interlinear--portfolio-file-stamp~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 86
:DEFAULT: nil
:END:

Modification time of the Portfolio file when cache was built.

** ~tibetan-interlinear--particle-patterns~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 251
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-mitra-translation.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-mitra-translation.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-mitra-translation.el
:END:

** ~tibetan-mitra-clear-cache~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 92
:SIGNATURE: (tibetan-mitra-clear-cache)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Clear the translation cache.

** ~tibetan-mitra--url-retrieve~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 102
:SIGNATURE: (tibetan-mitra--url-retrieve url &optional timeout)
:END:

Like `url-retrieve-synchronously' for URL, with TLS certificate
verification ENFORCED.

Emacs' default `gnutls-verify-error' is nil, so a forged/MITM cert
would NOT abort an https connection — the HuggingFace `Authorization:
Bearer' token would be sent and the response trusted over an unverified
channel.  Binding it to t makes a cert failure signal (callers already
run inside condition-case).  Harmless for the localhost Ollama / local-
server http endpoints.

** ~tibetan-mitra--ollama-translate~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 120
:SIGNATURE: (tibetan-mitra--ollama-translate tibetan-text)
:END:

Translate TIBETAN-TEXT using Ollama backend.

** ~tibetan-mitra--ollama-available-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-mitra--ollama-available-p)
:END:

Check if Ollama is available and the model is loaded.

** ~tibetan-mitra--huggingface-translate~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 168
:SIGNATURE: (tibetan-mitra--huggingface-translate tibetan-text)
:END:

Translate TIBETAN-TEXT using Hugging Face Inference API.

** ~tibetan-mitra--local-server-translate~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 203
:SIGNATURE: (tibetan-mitra--local-server-translate tibetan-text)
:END:

Translate TIBETAN-TEXT using local Python server.

** ~tibetan-mitra-translate~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 225
:SIGNATURE: (tibetan-mitra-translate tibetan-text)
:END:

Translate TIBETAN-TEXT using Gemma-2-Mitra-E.
Uses the backend specified in `tibetan-mitra-backend'.

** ~tibetan-mitra-translate-async~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 252
:SIGNATURE: (tibetan-mitra-translate-async tibetan-text callback)
:END:

Translate TIBETAN-TEXT asynchronously, calling CALLBACK with result.

** ~tibetan-mitra-translate-region~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 271
:SIGNATURE: (tibetan-mitra-translate-region start end)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Translate Tibetan text in region from START to END using Mitra.

** ~tibetan-mitra-translate-segment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 284
:SIGNATURE: (tibetan-mitra-translate-segment)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Translate current Tibetan segment using Mitra.

** ~tibetan-mitra-translate-dwim~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 300
:SIGNATURE: (tibetan-mitra-translate-dwim)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Translate Tibetan text: region if active, else current segment.

** ~tibetan-mitra-setup-ollama~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 312
:SIGNATURE: (tibetan-mitra-setup-ollama)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Display instructions for setting up Mitra in Ollama.

** ~tibetan-mitra-check-status~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 334
:SIGNATURE: (tibetan-mitra-check-status)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Check status of Mitra translation backend.

** ~tibetan-mitra-format-for-display~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 363
:SIGNATURE: (tibetan-mitra-format-for-display translation)
:END:

Format Mitra TRANSLATION for display in analysis buffer.

** ~tibetan-mitra-backend~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 38
:TYPE: '(choice (const
:GROUP: tibetan-mitra
:DEFAULT: 'ollama
:END:

Backend to use for Mitra translation.
Options: `ollama', `huggingface', `local-server'.

** ~tibetan-mitra-ollama-url~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 46
:TYPE: 'string
:GROUP: tibetan-mitra
:DEFAULT: "http://localhost:11434"
:END:

URL for Ollama API.

** ~tibetan-mitra-ollama-model~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 51
:TYPE: 'string
:GROUP: tibetan-mitra
:DEFAULT: "gemma2-mitra"
:END:

Model name in Ollama for Mitra.
You need to create this model first - see `tibetan-mitra-setup-ollama'.

** ~tibetan-mitra-huggingface-model~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 57
:TYPE: 'string
:GROUP: tibetan-mitra
:DEFAULT: "billingsmoore/gemma-2-2b-mitra-e"
:END:

Hugging Face model identifier.

** ~tibetan-mitra-huggingface-token~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 62
:TYPE: '(choice (const
:GROUP: tibetan-mitra
:DEFAULT: nil
:END:

Hugging Face API token. Set via M-x customize or in init.el.
Get your token at: https://huggingface.co/settings/tokens

** ~tibetan-mitra-local-server-url~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 69
:TYPE: 'string
:GROUP: tibetan-mitra
:DEFAULT: "http://localhost:5000"
:END:

URL for local Python translation server.

** ~tibetan-mitra-timeout~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 74
:TYPE: 'integer
:GROUP: tibetan-mitra
:DEFAULT: 30
:END:

Timeout in seconds for translation requests.

** ~tibetan-mitra-cache-enabled~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 79
:TYPE: 'boolean
:GROUP: tibetan-mitra
:DEFAULT: t
:END:

Whether to cache translation results.

** ~tibetan-mitra--cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 88
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for Mitra translations.

** ~tibetan-mitra~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 33
:END:

Gemma-2-Mitra-E translation integration.


* analysis/tibetan-particles-bialek.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-particles-bialek.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-particles-bialek.el
:END:

** ~tibetan-particles-safe-substring~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 20
:SIGNATURE: (tibetan-particles-safe-substring str start &optional end)
:END:

Safely extract substring from STR between START and END.
Returns empty string if indices are out of range or invalid.

** ~tibetan-particles-bialek--ablative-suffix-splittable-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 66
:SIGNATURE: (tibetan-particles-bialek--ablative-suffix-splittable-p word suffix)
:END:

Non-nil when WORD ending in ablative SUFFIX (`ནས' or `ལས') should be
split into stem + ablative, rather than being a lexeme that merely ends
in those letters.

Rejects two false-positive cases:
  - the stem after stripping SUFFIX is a single bare letter (e.g.
    གནས → ག): genuine attached ablatives leave a multi-letter stem,
    while real case/converb ablatives normally arrive STANDALONE (after
    a tsheg, so the stem is empty) — both of which are allowed here;
  - WORD is itself a known verb (Hill 2010 DB), mirroring the
    པས/བས causal-converb guard.

A standalone particle (WORD = SUFFIX, empty stem) is allowed — the
caller's standalone path handles it.

** ~tibetan-analyze-cases-bialek~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 90
:SIGNATURE: (tibetan-analyze-cases-bialek tibetan-text)
:END:

Analyze case particles using Bialek's terminology.
Returns list of (particle word case function translation-guide bialek-ref).

** ~tibetan-analyze-converbs-bialek~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 419
:SIGNATURE: (tibetan-analyze-converbs-bialek tibetan-text)
:END:

Analyze CONVERBIAL CONSTRUCTIONS - major emphasis in Bialek.

Converbial constructions are dependent clauses that express:
- Temporal sequence ('having done X, then...')
- Cause/reason ('because of X')
- Simultaneity ('while doing X')
- Manner ('by means of X')

Returns list of (particle word type function translation-guide bialek-ref).

** ~tibetan-tokenize-into-words~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 614
:SIGNATURE: (tibetan-tokenize-into-words tibetan-text)
:END:

Tokenize TIBETAN-TEXT into word-level tokens using dictionary lookup.
Returns a list of strings, where each string may be a multi-syllable
compound (e.g., "རྣལ་འབྱོར") or a single syllable.
Uses greedy matching (longest match first) via the vocabulary system.

This is used to prevent the grammar analysis from incorrectly parsing
particles inside compound words (e.g., ལ in རྣལ་འབྱོར is NOT dative).

** ~tibetan--syllable-internal-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 658
:SIGNATURE: (tibetan--syllable-internal-p syllable tokens)
:END:

Return non-nil if SYLLABLE appears only as part of a multi-syllable token.
TOKENS is the list from `tibetan-tokenize-into-words'.
A syllable is internal if it appears inside a compound but never as
a standalone token.

** ~tibetan-analyze-grammar-bialek~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 682
:SIGNATURE: (tibetan-analyze-grammar-bialek tibetan-text)
:END:

Complete grammar analysis using Bialek's framework.
Combines case analysis and converbial construction analysis.

False positives from single-character particles (ལ, ར, ན) inside
compound words are prevented at the source: the case and converb
analyzers only match these as standalone syllables, not as suffixes.
Multi-character particles (གྱི, དུ, ནས, སྟེ, etc.) are distinctive
enough to be reliably detected as suffixes.

Returns list of all grammatical features detected.

** ~tibetan-particles-bialek--single-s-erg-stems~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 38
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-sentence-tree.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-sentence-tree.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-sentence-tree.el
:END:

** ~tibetan-frame-slots~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 65
:SIGNATURE: (tibetan-frame-slots frame)
:END:

Return the ordered slot plists for FRAME (a case-frame string).
Each element is (:role R :cases (C...) :required B).  nil for an
unknown or nil FRAME — the caller then treats every NP as an adjunct.

** ~tibetan-frame--attach-genitives~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 80
:SIGNATURE: (tibetan-frame--attach-genitives nps)
:END:

Fold each GEN-cased NP into the FOLLOWING NP as a possessor.
Returns a new NP list where the following NP carries an extra
`(possessors . (NP ...))' alist entry.  A GEN NP with no following NP
(dangling, e.g. sentence-final) is kept as-is.  Input NPs are the
chunker alists; output preserves their shape.

** ~tibetan-frame--merge-coordinates~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 100
:SIGNATURE: (tibetan-frame--merge-coordinates nps)
:END:

Merge each COM-cased NP with the IMMEDIATELY FOLLOWING NP.
`X dang Y' is a coordination: the pair becomes one NP whose head is
"X дང Y"-style concatenation, carrying the SECOND NP's case (the
coordination's shared case marking sits on the final conjunct in
Tibetan) plus `(coordinated . t)'.  A dangling COM NP is kept as-is.

** ~tibetan-frame--np-distance~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 137
:SIGNATURE: (tibetan-frame--np-distance np verb-pos)
:END:

Distance from NP's end to VERB-POS (smaller = nearer the verb).

** ~tibetan-frame-fill-slots~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 141
:SIGNATURE: (tibetan-frame-fill-slots slots nps verb-pos)
:END:

Fill SLOTS (from `tibetan-frame-slots') with candidate NPS.
VERB-POS is the head verb's word position, used for proximity
tie-breaks.  Returns (:slots FILLED :adjuncts LEFTOVER) where each
filled slot plist gains `:filler NP-or-nil' and `:elided B'.

Algorithm (saturation — each slot at most once, each NP at most once):
1. Explicitly-cased NPs claim matching slots: the agent slot takes the
   LEFTMOST ERG; other slots take their case's NPs in textual order.
2. Bare NPs (case nil) fill still-open ABS slots: a single open ABS
   slot takes the bare NP NEAREST the verb (patient position); the
   Abs-Abs copula's two slots fill in textual order.
3. NPs left over become adjuncts; required unfilled slots are elided.

** ~tibetan-sentence-tree--build-node~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 222
:SIGNATURE: (tibetan-sentence-tree--build-node clause nps)
:END:

Build a tree node for CLAUSE from its candidate NPS.
Runs the NP post-passes (genitive attachment, dang-coordination),
looks up the verb's Hill case frame, and fills the frame slots.
Returns the node plist (without :complements/:converbs — the caller
attaches those).

** ~tibetan-sentence-tree--complement-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 244
:SIGNATURE: (tibetan-sentence-tree--complement-p clause words)
:END:

Non-nil when CLAUSE's verb is immediately followed by a standalone
terminative particle — the V+TERM complement pattern (byed du …).

** ~tibetan-analyze-sentence~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 254
:SIGNATURE: (tibetan-analyze-sentence words verbs &optional mwu)
:END:

Verb-first analysis of WORDS/VERBS: return the head-driven tree.

The FINAL clause's verb is the root.  Its Hill case frame is filled
from the root clause's NPs (with the matrix-ERG-claiming rule: an
unfilled agent slot may claim the leftmost ERG NP from a complement
clause's span — V+du complements essentially never carry their own
overt ergative agent).  Non-final clauses attach as :complements
(V+TERM pattern) or :converbs (with their Bialek label), each
recursively slot-filled from its own remaining NPs.

Tree node shape:
  (:verb V :frame STR|nil :clause CLAUSE
   :slots (...) :adjuncts (NP ...)
   :complements ((:complement-case TERM :node NODE) ...)
   :converbs    ((:label SYM :particle STR :node NODE) ...))

Returns nil when the substrate is unavailable or no clause is found.

** ~tibetan-frame--slot-specs~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 38
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence-tree--term-particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 218
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-translation-engine.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-translation-engine.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-translation-engine.el
:END:

** ~tibetan-cat--get-arguments-from-structure~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 34
:SIGNATURE: (tibetan-cat--get-arguments-from-structure structure-text)
:END:

Parse STRUCTURE-TEXT from analysis to extract arguments.
Returns alist of (role . (form . english)).

** ~tibetan-cat--gloss-word~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 62
:SIGNATURE: (tibetan-cat--gloss-word tibetan-word &optional multiword-units)
:END:

Get English gloss for TIBETAN-WORD.
Checks MULTIWORD-UNITS first, then vocabulary.
Returns nil for empty or nil input.

** ~tibetan-cat--build-verb-phrase~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 92
:SIGNATURE: (tibetan-cat--build-verb-phrase verb-form verb-meaning _verb-info)
:END:

Build English verb phrase from VERB-FORM, VERB-MEANING, and VERB-INFO.

** ~tibetan-cat--build-noun-phrase~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 103
:SIGNATURE: (tibetan-cat--build-noun-phrase form english role _marker)
:END:

Build English noun phrase from FORM, ENGLISH, ROLE, and MARKER.

** ~tibetan-cat-generate-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 134
:SIGNATURE: (tibetan-cat-generate-translation tibetan-text &optional parsed verbs)
:END:

Generate CAT-suggested translation for TIBETAN-TEXT.
Uses PARSED analysis and VERBS if provided, otherwise generates them.
Returns a string with the suggested translation.

** ~tibetan-cat--build-structured-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 237
:SIGNATURE: (tibetan-cat--build-structured-translation tibetan-text words multiword-units)
:END:

Build a structured English translation from TIBETAN-TEXT.
Uses WORDS and MULTIWORD-UNITS for vocabulary lookup.
Analyzes grammatical structure to produce proper English word order.

** ~tibetan-cat-insert-translation~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 347
:SIGNATURE: (tibetan-cat-insert-translation)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Generate and insert CAT translation for current segment.
Works within an analysis buffer to fill in the CAT Suggested line.

** ~tibetan-cat-translate-region~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 376
:SIGNATURE: (tibetan-cat-translate-region start end)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Generate CAT translation for Tibetan text in region from START to END.

** ~tibetan-cat-translation-word-order~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 20
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-cat-sentence-particles~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 125
:DEFAULT: '
:END:

/(no docstring)/


* analysis/tibetan-translation-suggest.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-translation-suggest.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-translation-suggest.el
:END:

** ~tibetan-suggest-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 22
:SIGNATURE: (tibetan-suggest-translation _tibetan-text vocab-list grammar-list)
:END:

Suggest a translation based on VOCAB-LIST and GRAMMAR-LIST.

VOCAB-LIST: list of (tibetan-word . english-meaning)
GRAMMAR-LIST: Bialek grammar analysis from tibetan-analyze-grammar-bialek

Returns a suggested translation with grammatical notes.

** ~tibetan-explain-converb-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 80
:SIGNATURE: (tibetan-explain-converb-translation grammar-list)
:END:

Generate specific translation guidance for converbial constructions.
This helps students understand HOW to translate converbs correctly.


* analysis/tibetan-verb-classifier.el
:PROPERTIES:
:CUSTOM_ID: analysis/tibetan-verb-classifier.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/analysis/tibetan-verb-classifier.el
:END:

** ~tibetan-verb-database--initialize~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 27
:SIGNATURE: (tibetan-verb-database--initialize)
:END:

Initialize the verb database. Always rebuilds from scratch.

** ~tibetan-verb-lookup~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 887
:SIGNATURE: (tibetan-verb-lookup word)
:END:

Look up WORD in the verb database.
Returns verb entry alist if found, nil otherwise.

** ~tibetan-is-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 903
:SIGNATURE: (tibetan-is-verb-p word)
:END:

Return non-nil if WORD is a recognized verb.

** ~tibetan-verb-matched-stem~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 907
:SIGNATURE: (tibetan-verb-matched-stem word entry)
:END:

Return which stem of ENTRY the surface WORD matches.
One of the symbols `present', `past', `future', `imperative', or nil
when WORD matches no stem field of ENTRY.  WORD is cleaned of trailing
punctuation / tsheg first (same as `tibetan-verb-lookup').  Used to
annotate the Verb Classification output, e.g. "byas = perfect (past)
stem of byed".

** ~tibetan-verb-database~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 24
:DEFAULT: nil
:END:

Hash table of Tibetan verbs with grammatical information.


* config/tibetan-keybindings.el
:PROPERTIES:
:CUSTOM_ID: config/tibetan-keybindings.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/config/tibetan-keybindings.el
:END:

** ~tibetan-increase-text-scale~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 426
:SIGNATURE: (tibetan-increase-text-scale)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Increase Tibetan text scale by 0.1.

** ~tibetan-decrease-text-scale~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 432
:SIGNATURE: (tibetan-decrease-text-scale)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Decrease Tibetan text scale by 0.1 (minimum 1.0).

** ~tibetan-thesaurus-prefix-map~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 265
:DEFAULT: (make-sparse-keymap)
:END:

Keymap for thesaurus commands — prefix `C-c u z'.


* config/tibetan-menu.el
:PROPERTIES:
:CUSTOM_ID: config/tibetan-menu.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/config/tibetan-menu.el
:END:

** ~tibetan-cat-context-menu-map~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 253
:DEFAULT: (let ((map (make-sparse-keymap "Tibetan")))
    (define-key map [segment-info] '(menu-item "Segment Info" tibetan-segmen
:END:

Context menu for Tibetan CAT operations.


* core/tibetan-dharmamitra-api.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-dharmamitra-api.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-dharmamitra-api.el
:END:

** ~tibetan-dharmamitra-api--cache-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 91
:SIGNATURE: (tibetan-dharmamitra-api--cache-key endpoint body)
:END:

Return cache key for ENDPOINT (string) + BODY (JSON string).
Body hash decouples key length from input length and folds
semantically-equivalent requests together.

** ~tibetan-dharmamitra-api-clear-cache~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 98
:SIGNATURE: (tibetan-dharmamitra-api-clear-cache)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Clear the DharmaMitra API in-memory cache.

** ~tibetan-dharmamitra-api--parse-sse-stream~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (tibetan-dharmamitra-api--parse-sse-stream stream)
:END:

Parse an OpenAI-style Server-Sent-Events STREAM string.

Walks lines starting with `data: ', extracts JSON payloads, and
concatenates the `choices[0].delta.content' field of each chunk.
Stops at `data: [DONE]'.  Malformed JSON and non-data lines
(`event:', `id:', heartbeat comments) are silently skipped.

Returns the accumulated content as a string (possibly empty if
the stream contained no content chunks).  Never signals.

This is the pure parsing kernel behind
`tibetan-dharmamitra-api-chat-translate'; the public function
adds caching and HTTP I/O around it.

** ~tibetan-dharmamitra-api--build-chat-translate-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 150
:SIGNATURE: (tibetan-dharmamitra-api--build-chat-translate-body text &rest args)
:END:

Build JSON request body for the chat-translate endpoint.

TEXT is the source content.  ARGS is a plist supporting:
  :target-lang     Default `english' — passed verbatim to DM.
  :input-encoding  Default `auto' — auto / tibetan / wylie / iast / dev / hk.

Returns a JSON-encoded string ready for the request body.

** ~tibetan-dharmamitra-api--build-search-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 170
:SIGNATURE: (tibetan-dharmamitra-api--build-search-body input &rest args)
:END:

Build JSON request body for the primary search endpoint.

INPUT is the search query (text in any supported language).
ARGS is a plist supporting:
  :source-lang    Default `auto' — auto / bo / sa / zh / pa / all.
                  Sets `filter_source_language' on the request.
  :target-lang    Default `all' — same enum.  Sets
                  `filter_target_language'.
  :include-files  Optional list of source IDs to constrain the
                  corpus to specific works (e.g.
                  `("SA_T06_bsa034" "BO_T06_D4037")').
  :max-depth      Default 50 — caps the number of returned hits.

Returns a JSON-encoded string.

** ~tibetan-dharmamitra-api--http-post~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 206
:SIGNATURE: (tibetan-dharmamitra-api--http-post endpoint body)
:END:

POST BODY (string) to ENDPOINT (relative path under base URL).

Returns response body as a UTF-8 decoded string, or signals an
error.  Synchronous; respects
`tibetan-dharmamitra-api-timeout'.

Tests stub this function via `cl-letf' so callers exercise the
rest of the pipeline without touching the network.

** ~tibetan-dharmamitra-api-chat-translate~  /function (cl)/  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 248
:SIGNATURE: (tibetan-dharmamitra-api-chat-translate text &key (target-lang "english") (input-encoding "auto"))
:AUTOLOAD: t
:END:

Translate TEXT via DharmaMitra's chat-translate endpoint.

TARGET-LANG defaults to `english'.  INPUT-ENCODING defaults to
`auto' — DM auto-detects IAST / Devanagari / Tibetan Unicode /
Wylie.  Override only when auto-detection misbehaves on edge
cases (mixed-script input).

Returns the translation as a string, or nil on:
  - HTTP failure (network, 4xx/5xx, timeout)
  - empty SSE stream (no content chunks)
  - JSON parse failure on every chunk

Cached by request body hash; identical re-invocations return
without an HTTP call.  Use
`tibetan-dharmamitra-api-clear-cache' to invalidate.

** ~tibetan-dharmamitra-api-search~  /function (cl)/  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 313
:SIGNATURE: (tibetan-dharmamitra-api-search input &key (source-lang "auto") (target-lang "all")
           include-files (max-depth 50))
:AUTOLOAD: t
:END:

Search the DharmaMitra primary corpus for INPUT.

INPUT is a query in any supported language; encoding is
auto-detected.  See
`tibetan-dharmamitra-api--build-search-body' for the keyword
arguments — :source-lang, :target-lang, :include-files,
:max-depth.

Returns a list of result alists, each with keys `id', `lang',
`source', `segmentnr', `text', `text_new', `title', `summary',
`src_link', `query'.  Returns nil on:
  - HTTP failure
  - empty results array
  - JSON parse failure

Cached by request body hash; clear with
`tibetan-dharmamitra-api-clear-cache'.

** ~tibetan-dharmamitra-api-base-url~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 58
:TYPE: 'string
:GROUP: tibetan-dharmamitra-api
:DEFAULT: "https://dharmamitra.org/api-search"
:END:

Base URL for the DharmaMitra search backend.
The relative paths `/chat-translate/v1/chat/completions' and
`/primary/' are appended to this for the two endpoints.

** ~tibetan-dharmamitra-api-token~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 66
:TYPE: 'string
:GROUP: tibetan-dharmamitra-api
:DEFAULT: "sthiramati"
:END:

Bearer token for DharmaMitra API.  The default `sthiramati' is
the publicly-documented token; override only if DharmaMitra
issues a new one or you have a private deployment.

** ~tibetan-dharmamitra-api-timeout~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 73
:TYPE: 'integer
:GROUP: tibetan-dharmamitra-api
:DEFAULT: 30
:END:

Timeout in seconds for individual DharmaMitra HTTP requests.
chat-translate streams typically complete in 1-2 seconds; search
takes 2-7 seconds.  The default 30 leaves headroom for slow links.

** ~tibetan-dharmamitra-api--cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 84
:DEFAULT: (make-hash-table :test 'equal)
:END:

In-memory cache for DharmaMitra API responses.
Keyed on a `(endpoint . sha256-of-body)' cons cell so identical
requests dedupe across the public API; cleared by
`tibetan-dharmamitra-api-clear-cache'.

** ~tibetan-dharmamitra-api~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 53
:END:

DharmaMitra HTTP client configuration.


* core/tibetan-dharmamitra-translation.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-dharmamitra-translation.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-dharmamitra-translation.el
:END:

** ~tibetan-dharmamitra-translation--sanitize-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 33
:SIGNATURE: (tibetan-dharmamitra-translation--sanitize-body text)
:END:

Neutralise org structural markers in untrusted TEXT before it is
inserted into an analysis buffer.

DharmaMitra responses are network/LLM text.  A body line beginning with
`*' would otherwise become a top-level org heading mid-file — silently
restructuring the document and confusing later regenerate passes that
treat top-level headings as section boundaries.  Prefix any line-leading
run of `*' with a space so the line renders as body text / a list item
instead of a heading.  Returns TEXT unchanged when it carries no such
line; nil-safe.

** ~tibetan-dharmamitra-translation--write-nested-tibetan-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 52
:SIGNATURE: (tibetan-dharmamitra-translation--write-nested-tibetan-section analysis-file translation)
:END:

Write the DharmaMitra Tibetan TRANSLATION into the level-2
`** DharmaMitra Translation' section inside `* Tibetan Analysis'.

The renderer emits a `** DharmaMitra Translation\n[Awaiting…]'
placeholder during analysis generation;  this writer locates the
placeholder and replaces its body with the real translation, plus
a `:PROPERTIES:' drawer carrying `:LAST_TRANSLATED:'.

When the placeholder is absent (legacy files generated before
the 2026-05-19 layout revision), creates the heading immediately
after `** Translation' / `** Claude Translation' inside
`* Tibetan Analysis'.  When the parent is also absent, falls
back to top-level `* DharmaMitra Translation (Tibetan)' (the
pre-layout-revision behaviour) for safety.

Returns t on success, nil otherwise.

** ~tibetan-dharmamitra-translation--delete-legacy-toplevel-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 122
:SIGNATURE: (tibetan-dharmamitra-translation--delete-legacy-toplevel-section buffer source-lang)
:END:

Delete any legacy top-level `* DharmaMitra Translation (SOURCE-LANG)'
section in BUFFER.  Idempotent — no-op when absent.  Used by the
Tibetan-side migration to clear the top-level section before
reinserting the body at the new nested location.

** ~tibetan-dharmamitra-translation--insert-after-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 140
:SIGNATURE: (tibetan-dharmamitra-translation--insert-after-translation buffer translation)
:END:

Insert a fresh `** DharmaMitra Translation' block inside
`* Tibetan Analysis'.

Placement:
  (a) immediately after `** Translation' or `** Claude
      Translation' (whichever appears first) — the canonical
      side-by-side layout;
  (b) when no Translation sibling exists, at the end of the
      `* Tibetan Analysis' subtree — better than dropping the
      body or punting to top-level.

Returns t when `* Tibetan Analysis' is present, nil otherwise.
The caller falls back to top-level placement when this returns
nil.

** ~tibetan-dharmamitra-translation--write-toplevel-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 185
:SIGNATURE: (tibetan-dharmamitra-translation--write-toplevel-section buffer-or-file translation source-lang)
:END:

Write `* DharmaMitra Translation (SOURCE-LANG)' at top-level.

BUFFER-OR-FILE is either a buffer or a file path.  Used for
Sanskrit (which stays at top-level in parallel-Sanskrit mode)
and as a fallback for Tibetan in legacy files without a
`* Tibetan Analysis' parent.

Creates or replaces the section.  Body is TRANSLATION;  property
drawer carries `:LAST_TRANSLATED:' for freshness tracking.

** ~tibetan-dharmamitra-translation--write-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 224
:SIGNATURE: (tibetan-dharmamitra-translation--write-section analysis-file translation source-lang)
:END:

Dispatch the DharmaMitra writer based on SOURCE-LANG.

Tibetan side (`source-lang' = "Tibetan") writes into the
level-2 `** DharmaMitra Translation' inside `* Tibetan Analysis',
sitting as a peer of `** Translation' (the Claude rendering).
Sanskrit side stays at top-level `* DharmaMitra Translation
(Sanskrit)' for parallel-mode workflow symmetry with the legacy
Sanskrit pipeline.

Returns t on success, nil otherwise.

** ~tibetan-dharmamitra-translation-needs-request-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 263
:SIGNATURE: (tibetan-dharmamitra-translation-needs-request-p analysis-file &optional source-lang)
:END:

Return t when ANALYSIS-FILE lacks a populated DM translation section.

SOURCE-LANG defaults to `"Tibetan"'.

Returns:
  t   when the section `* DharmaMitra Translation (SOURCE-LANG)' is
      absent OR exists with empty / whitespace-only body
  nil when the section is present and populated, OR ANALYSIS-FILE
      is missing (caller can't write anyway)

Heading location (matters — write/read must agree):
  - Tibetan side:  since §5.20 (2026-05-19) the writer puts the
    translation in the NESTED level-2 `** DharmaMitra Translation'
    inside `* Tibetan Analysis'.  This predicate therefore checks
    that heading FIRST, and accepts the legacy top-level
    `* DharmaMitra Translation (Tibetan)' for unmigrated files.
    (Before this fix it checked ONLY the legacy top-level heading,
    never matched the nested section the writer actually fills, and
    so always returned t — re-firing DM on every file open.)
  - Other languages (Sanskrit):  top-level
    `* DharmaMitra Translation (SOURCE-LANG)'.

A body that is empty OR a `[Awaiting…' / `[Requesting…' placeholder
counts as still-needing, so a freshly-rendered file (which carries the
`[Awaiting DharmaMitra…]' placeholder) is correctly filled.

Used by the existing-file open path so re-running `C-c u A' on a
file that already has a DM translation doesn't re-fire the API
unnecessarily.  Explicit reanalysis (`C-c u R') passes FORCE to
`tibetan-dharmamitra-translation-fire-for-segment' to refresh anyway.

** ~tibetan-dharmamitra-translation-needs-fire-on-open-p~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 333
:SIGNATURE: (tibetan-dharmamitra-translation-needs-fire-on-open-p analysis-file)
:AUTOLOAD: t
:END:

Return t when ANALYSIS-FILE needs ANY DharmaMitra section fired.

Specifically:  t when EITHER `* DharmaMitra Translation (Tibetan)'
OR `* DharmaMitra Translation (Sanskrit)' is missing or empty.

The umbrella `tibetan-dharmamitra-translation-fire-for-segment'
does internal per-language gating, so callers can fire the
umbrella whenever this predicate returns t — the populated side
is correctly skipped, the missing side is fired.

Bug fix 2026-05-04 (post-§5.18 layout-revision):  the existing-
file open path's DM auto-fire gate previously checked ONLY the
Tibetan side via `--needs-request-p filepath "Tibetan"'.  When
the Tibetan DM was populated (yesterday's run) but Sanskrit was
absent (DM Sanskrit fire missed), the gate returned nil and
`* DharmaMitra Translation (Sanskrit)' stayed permanently
missing.  This predicate triggers when EITHER side needs work.

** ~tibetan-dharmamitra-translation-fire-tibetan~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 357
:SIGNATURE: (tibetan-dharmamitra-translation-fire-tibetan tibetan-text analysis-file)
:AUTOLOAD: t
:END:

Translate TIBETAN-TEXT via DharmaMitra; write to ANALYSIS-FILE.

Calls `tibetan-dharmamitra-api-chat-translate' on TIBETAN-TEXT
with target-lang `english' and writes the result into the
analysis file's `* DharmaMitra Translation (Tibetan)' section.

No-op when:
  - TIBETAN-TEXT is nil / empty
  - chat-translate returns nil (HTTP error / empty response)

The latter avoids overwriting a previous good translation with
a failed one — same defensive pattern as Claude's restore path
(CLAUDE.md §6 — preserve user content).

Returns t on successful write, nil otherwise.

** ~tibetan-dharmamitra-translation-fire-sanskrit~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 387
:SIGNATURE: (tibetan-dharmamitra-translation-fire-sanskrit sanskrit-text analysis-file)
:AUTOLOAD: t
:END:

Translate SANSKRIT-TEXT (IAST) via DharmaMitra; write result to
ANALYSIS-FILE's `* DharmaMitra Translation (Sanskrit)' section.

No-op when:
  - SANSKRIT-TEXT is empty / nil
  - SANSKRIT-TEXT is a Sanskrit-alignment placeholder marker
    recognised by
    `tibetan-sanskrit-parallel--placeholder-text-p'
    (`[Sanskrit alignment pending …]',
     `[Sanskrit alignment exhausted …]',
     `[No Sanskrit counterpart …]').  In practice the walker
    already returns nil for placeholders before we get here,
    but this defence-in-depth check guards against direct
    callers that bypass the walker.
  - chat-translate returns nil (HTTP error / empty response)

DM auto-detects the input encoding (IAST / Devanagari).  If
your sibling carries Devanagari instead of IAST, the same call
works — the function is encoding-agnostic.

Returns t on successful write, nil otherwise.

** ~tibetan-dharmamitra-translation-fire-for-segment~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 429
:SIGNATURE: (tibetan-dharmamitra-translation-fire-for-segment tibetan-text analysis-file &optional source-file seg-id force)
:AUTOLOAD: t
:END:

Fire DharmaMitra translation(s) for a segment.

Fires the Tibetan translation when TIBETAN-TEXT is non-empty AND
either FORCE is non-nil OR the Tibetan DM section is empty / a
placeholder (`tibetan-dharmamitra-translation-needs-request-p').  In
other words a POPULATED Tibetan DM section is left alone unless FORCE
is given — this is what stops the request storm on file open, where
the segment already has a DM translation.  Pass FORCE non-nil from the
explicit-refresh path (`C-c u R').

Additionally fires Sanskrit translation when:
  - SOURCE-FILE is non-nil
  - SOURCE-FILE has `#+SOURCE_MODE: parallel-sanskrit'
  - The segment SEG-ID has a `**** Sanskrit' sibling on the source
  - The sibling's IAST text is not a placeholder marker
  - FORCE is non-nil OR the Sanskrit DM section needs a request

Sanskrit parallel-mode gating is delegated to
`tibetan-sanskrit-parallel-plist-for-segment-id', which returns
nil for any of the above failures.

This is the call site's main entry point — replaces the
single-language `fire-tibetan' calls.

** ~tibetan-dharmamitra-translation-fire-tibetan-sentence~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 481
:SIGNATURE: (tibetan-dharmamitra-translation-fire-tibetan-sentence sentence-text sent-num seg-nums child-files sent-file &optional force)
:END:

Fire ONE DharmaMitra translation for a whole SENTENCE (§5.40).
SENTENCE-TEXT is the concatenated sentence; SENT-NUM / SEG-NUMS
identify it; CHILD-FILES are the child seg-NNN.org paths and
SENT-FILE the sent-NNN.org path (nil to skip).  The full sentence
translation is written into EVERY child's nested `** DharmaMitra
Translation' slot AND the sent file, under a `(Sentence N — segments
A–B)' label that travels INSIDE the body (so the §5.28 sanitizer and
the §5.20 preserve machinery handle it untouched).

Fire gate: FORCE, or ANY child still needs a DM request.  Landing
gate per file: FORCE or that file needs it — a populated section is
never overwritten by a non-FORCE fire (§5.29 policy).  An empty API
response writes nothing (preserve pattern, like --fire-tibetan).


* core/tibetan-org-structure.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-org-structure.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-org-structure.el
:END:

** ~tibetan-org-at-segment-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 67
:SIGNATURE: (tibetan-org-at-segment-p)
:END:

Return t if point is in or under a Segment heading.
Detects segments at any heading level (typically *** in the
fresh-prep layout, **** in the section + sentence-wrap layout
produced by `tibetan-add-sentence-structure').
Works both when cursor is on the heading line or in the content
below it.

** ~tibetan-org-at-sentence-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 85
:SIGNATURE: (tibetan-org-at-sentence-p)
:END:

Return t if point is in or under a Sentence heading.
Detects sentences at any heading level (typically ** in the
fresh-prep layout, *** in the section + sentence-wrap layout
produced by `tibetan-add-sentence-structure').
Works both when cursor is on the heading line or in the content
below it.

** ~tibetan-org--read-subtree-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 105
:SIGNATURE: (tibetan-org--read-subtree-body)
:END:

Read the body text of the current org subtree.

The body is the text between the heading line and the first
child heading (any depth), with a leading `:PROPERTIES: … :END:'
drawer skipped.  Returns the trimmed body string, or nil when
the body is empty / whitespace-only.

Point need not be on the heading line — `org-back-to-heading'
moves back automatically.  Internally uses
`save-restriction' + `org-narrow-to-subtree' so callers with
existing narrowing remain unaffected.

Used by `tibetan-org-get-segment-text' (segment Tibetan text) and
`tibetan-sanskrit-parallel-text-for-segment' (Sanskrit sibling
text).  Extracted 2026-04-27 as part of Phase 1 of the
sanskrit-parallel-workflow feature so both walkers share the
same drawer-skip + child-stop semantics — there is no second
copy of this pattern to drift.

** ~tibetan-org-get-segment-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 156
:SIGNATURE: (tibetan-org-get-segment-text)
:END:

Get Tibetan text from current segment.
Works when positioned anywhere in or under a *** Segment heading.
Returns nil if not in a segment (no error raised).

Skips an `:PROPERTIES: … :END:' drawer immediately following the
heading (used e.g. for `:FOLIO:' markers on Yogācārabhūmi segments)
so the drawer body is not mistaken for Tibetan text.  Also skips any
child `**** Working Translation' / other subheadings — only the
segment's own body text is returned.

Internally delegates to `tibetan-org--read-subtree-body' (the
shared helper that implements the drawer-skip + child-stop body
extraction).  Pre-Phase 1 of sanskrit-parallel-workflow this
function carried its own copy of that logic; the helper was
extracted 2026-04-27 so segment and Sanskrit walkers share one
pattern.

** ~tibetan-org-get-segment-folio~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 179
:SIGNATURE: (tibetan-org-get-segment-folio)
:END:

Return the `:FOLIO:' property of the current segment, or nil.

Pairs with `tibetan-org-get-segment-text' (the §5.8.2 drawer-aware
extractor):  the drawer is correctly stripped from the returned
Tibetan TEXT, but the folio reference itself is read here so
callers (e.g. `tibetan-analysis-create-file') can thread it into
the analysis file's `* Tibetan Text' heading.

Drawer-skip side-effect P6 (CLAUDE.md §6, addressed 2026-05-05):
without this helper, newly-created analysis files for YBh
segments lost the `:FOLIO:' reference because the source's
drawer was stripped during text extraction.

Returns nil cleanly when:
  - cursor is not under a `*** Segment' heading,
  - the segment has no `:PROPERTIES:' drawer,
  - the drawer exists but lacks a `:FOLIO:' key.

** ~tibetan-org-get-segment-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 212
:SIGNATURE: (tibetan-org-get-segment-id)
:END:

Get segment ID (number) from current segment.

** ~tibetan-org-at-paragraph-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 237
:SIGNATURE: (tibetan-org-at-paragraph-p)
:END:

Return t if point is in or under a `** §N' paragraph heading.
Walks up the outline via `org-up-heading-safe' so cursor position
inside any child (Tibetisch, Wylie, Übersetzung, …) still counts.

** ~tibetan-org-get-paragraph-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 260
:SIGNATURE: (tibetan-org-get-paragraph-id)
:END:

Return paragraph number (integer) for the current §-subtree, or nil.
Walks up headings until it finds one matching `** §N'.

** ~tibetan-org--goto-paragraph-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 279
:SIGNATURE: (tibetan-org--goto-paragraph-heading)
:END:

Move point to the enclosing `** §N' heading line.
Returns t on success, nil if not in a paragraph.

** ~tibetan-org-get-paragraph-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 295
:SIGNATURE: (tibetan-org-get-paragraph-text)
:END:

Return concatenated Tibetan text of the current paragraph's Tibetisch child.
Looks for a `*** Tibetisch' or `*** Tibetisch (...)' subheading and
returns its body (whitespace-trimmed).  Returns nil if no such child
exists, or if point is not in a paragraph.

** ~tibetan-org--paragraph-reference-section-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 347
:SIGNATURE: (tibetan-org--paragraph-reference-section-p heading)
:END:

Return non-nil if HEADING names a reference-translation sub-section.
A reference is any sub-section that is NOT the source (Tibetisch)
and NOT a structural / scratch / philological sibling.

** ~tibetan-org-get-paragraph-references~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 357
:SIGNATURE: (tibetan-org-get-paragraph-references)
:END:

Return alist of (heading . body) for reference-translation siblings.
Walks every direct `***'-child of the enclosing `** §N' paragraph
heading.  Skips the Tibetan source, transliteration, user draft,
and philological-note sections (see
`tibetan-org--paragraph-source-prefixes' and
`tibetan-org--paragraph-non-reference-prefixes').  Empty bodies are
filtered out.  Returns nil when point is not inside a paragraph or
when no reference-translation siblings exist.

Designed for the Rgyan-comparative.org layout
  ** §N
  *** Tibetisch (B2)        ← skipped (source)
  *** Wylie                 ← skipped
  *** Lopez 2006            ← extracted
  *** Wangjié & Mulligan    ← extracted
  *** Eigene Übersetzung    ← skipped (user draft)
  *** Apparat und Notizen   ← skipped (philology)
but generic to any §-based document with translation siblings.

** ~tibetan-org-get-sentence-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 412
:SIGNATURE: (tibetan-org-get-sentence-segments)
:END:

Get all segment texts from current sentence.
Returns a list of strings, one per segment.
Works when positioned anywhere in or under a sentence.

** ~tibetan-org-get-sentence-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 448
:SIGNATURE: (tibetan-org-get-sentence-text)
:END:

Get combined Tibetan text from all segments in current sentence.
Returns a single string with all segment texts joined.
Works when positioned anywhere in or under a sentence.

** ~tibetan-org-get-sentence-data~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 456
:SIGNATURE: (tibetan-org-get-sentence-data)
:END:

Get sentence data for analysis.
Returns cons cell (ID . TEXT) where ID is ='Sentence N=' and TEXT is
combined segments. Works when positioned on a ** Sentence heading.

** ~tibetan-org-get-sentence-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 466
:SIGNATURE: (tibetan-org-get-sentence-id)
:END:

Get sentence ID (number) from current sentence.
Works when positioned anywhere in or under a sentence.

** ~tibetan-org-get-parent-section-name~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 481
:SIGNATURE: (tibetan-org-get-parent-section-name)
:END:

Get the name of the parent Section (level 2 heading).
Walks up from the current heading until reaching level 2 — the
canonical Section level in both supported layouts.  Useful for
files that don't use ='Sentence N=' format (e.g.,
='[30] Introduction=' style headings).

In the fresh-prep layout this returns the level-2 heading
directly (which is often ='Sentence N=' — that is the documented
fallback behaviour preserved for backward compatibility).  In the
section + sentence-wrap layout it walks up past the level-3
Sentence to return the actual Section name.

** ~tibetan-org-next-segment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 511
:SIGNATURE: (tibetan-org-next-segment)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Move to next segment heading.

** ~tibetan-org-previous-segment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 518
:SIGNATURE: (tibetan-org-previous-segment)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Move to previous segment heading.

** ~tibetan-org-next-sentence~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 525
:SIGNATURE: (tibetan-org-next-sentence)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Move to next sentence heading.

** ~tibetan-org-previous-sentence~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 535
:SIGNATURE: (tibetan-org-previous-sentence)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Move to previous sentence heading.

** ~tibetan-org-show-segment-info~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 547
:SIGNATURE: (tibetan-org-show-segment-info)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show information about current segment.

** ~tibetan-org-show-sentence-info~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 562
:SIGNATURE: (tibetan-org-show-sentence-info)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show information about current sentence.

** ~tibetan-org-segment-for-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 574
:SIGNATURE: (tibetan-org-segment-for-analysis)
:END:

Get segment text for analysis.
Returns segment text if in org structure, nil otherwise.
This integrates with tibetan-classroom.el.

** ~tibetan-org-segments-for-workspace~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 582
:SIGNATURE: (tibetan-org-segments-for-workspace)
:END:

Get sentence segments for workspace.
Returns list of segment texts if in org structure, nil otherwise.
This integrates with tibetan-sentence-workspace.el.

** ~tibetan-prepare-document~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 594
:SIGNATURE: (tibetan-prepare-document &optional title)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Prepare current buffer for Tibetan translation work.
Segments the Tibetan text and adds org-mode headers.

Call this after pasting Tibetan text into a new .org buffer.
The function will:
1. Add org-mode headers (title, startup options)
2. Group segments into sentences (by main verb / double-shad)
3. Create * Sentence N / ** Segment N structure

Structure created:
​  * Title
  ** Sentence 1
  *** Segment 1
  text...
  *** Segment 2
  text...
  ** Sentence 2
  ...

Workflow:
- C-c u A on *** Segment: word-by-word analysis
- C-c u A on ** Sentence: clause structure analysis

With prefix arg or TITLE, prompts for document title.

** ~tibetan--group-into-sentences~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 693
:SIGNATURE: (tibetan--group-into-sentences text)
:END:

Group Tibetan TEXT into sentences, each containing multiple segments.
Returns list of lists: ((seg1 seg2) (seg3 seg4 seg5) ...).

Uses fast heuristic sentence boundary detection:
1. Double-shad (།།) - always ends sentence
2. Final particles (སོ། འོ། etc.) - ends sentence
3. Copula/existentials at end (ཡིན། མེད། etc.) - ends sentence
4. Converb particles (ནས། ཏེ། ཅིང། etc.) - do NOT end sentence

This is the FAST version used during document preparation.
For more accurate analysis, use C-c u A on individual segments.

** ~tibetan--segment-ends-sentence-fast-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 725
:SIGNATURE: (tibetan--segment-ends-sentence-fast-p segment)
:END:

Fast check if SEGMENT ends a sentence (no verb analysis).
Uses simple pattern matching - much faster than full verb analysis.

** ~tibetan--segment-has-main-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 740
:SIGNATURE: (tibetan--segment-has-main-verb-p segment)
:END:

Check if SEGMENT likely contains a main verb (sentence-final).
Uses clause analysis when available, otherwise falls back to heuristics.

A segment has a main verb if:
1. It ends with double-shad (།།)
2. It ends with a final particle (སོ། འོ། etc.)
3. It contains a verb that is NOT followed by a converb particle
4. It contains copula (ཡིན/མིན) or existential (ཡོད/མེད/འདུག/རེད)

** ~tibetan--segment-is-converb-only-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 763
:SIGNATURE: (tibetan--segment-is-converb-only-p segment)
:END:

Check if SEGMENT ends with a converb particle (dependent clause marker).
Such segments cannot end a sentence - they connect to the following clause.

Converb particles include:
- Ablative: ནས (having V-ed, after V-ing)
- Coordinative: ཏེ/སྟེ/དེ (V-ed and...)
- Simultaneous: ཞིང/ཅིང/ཤིང (while V-ing)
- Causal: པས/བས (because V-ed) - NOTE: can also be agentive
- Concessive: ཀྱང/ཡང/འང (even though V)

** ~tibetan--segment-ends-sentence-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 800
:SIGNATURE: (tibetan--segment-ends-sentence-p segment)
:END:

Determine if SEGMENT marks a sentence boundary.
Returns t if the segment should end the current sentence.

Sentence ends are marked by:
1. Double-shad (།།) - always a boundary
2. Final particles (སོ། འོ། etc.)
3. Main verb without following converb or subordinating particle

Does NOT end sentence if:
- Segment ends with a converb particle (dependent clause)
- Segment ends with temporal/conditional markers (ཚེ། ན། etc.)

** ~tibetan--segment-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 832
:SIGNATURE: (tibetan--segment-text text)
:END:

Segment Tibetan TEXT into translation units.
Splits on:
1. Hard line breaks (\n) - each line becomes a segment
2. Double-shad (།།) - sentence boundaries
3. Single shad (།) - clause boundaries (only if no line breaks found)

Returns list of segment strings.

** ~tibetan-prepare-document-from-region~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 873
:SIGNATURE: (tibetan-prepare-document-from-region start end &optional title)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Prepare Tibetan text in region for translation work.
Creates segments from the selected Tibetan text.
Result replaces the region content.

** ~tibetan-prepare-new-document~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 888
:SIGNATURE: (tibetan-prepare-new-document)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Create a new buffer and prepare it for Tibetan translation.
Prompts for title and then for the Tibetan text to paste.

** ~tibetan-org--paragraph-heading-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 233
:DEFAULT: "§\\([0-9]+\\)"
:END:

Regex capturing the paragraph number from a `** §N' heading.

** ~tibetan-org--paragraph-source-prefixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 329
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-org--paragraph-non-reference-prefixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 335
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-phonetics.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-phonetics.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-phonetics.el
:END:

** ~tibetan-phonetics--starts-with-root~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 94
:SIGNATURE: (tibetan-phonetics--starts-with-root s)
:END:

Return the longest Wylie root prefix of S, or nil when none match.

** ~tibetan-phonetics--parse-syllable~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 151
:SIGNATURE: (tibetan-phonetics--parse-syllable wylie)
:END:

Parse WYLIE single-syllable string into a plist.

Returns `(:initial CLUSTER :vowel VOWEL :suffix SFX :post-suffix POST)'.
CLUSTER is everything before the vowel (prefix + super + root +
subscript);  VOWEL is one of `a' / `i' / `e' / `o' / `u' (implicit
`a' when none written);  SFX and POST may be empty strings.

Strips Wylie punctuation (`/' `།' etc.) before parsing — the
caller is expected to preserve punctuation independently.

** ~tibetan-phonetics--parse-cluster~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 189
:SIGNATURE: (tibetan-phonetics--parse-cluster cluster)
:END:

Parse initial CLUSTER string into a plist of components.

Returns `(:prefix P :super S :root R :subscript SUB)'.  Each
slot is a string (possibly empty).  Greedy left-to-right;
recognises the standard Tibetan prefix / superscript / root /
subscript hierarchy.

** ~tibetan-phonetics--root-thl~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 279
:SIGNATURE: (tibetan-phonetics--root-thl root)
:END:

THL initial for ROOT, or ROOT itself if not in the table.

** ~tibetan-phonetics--initial-cluster-thl~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 311
:SIGNATURE: (tibetan-phonetics--initial-cluster-thl root subscript)
:END:

Convert ROOT + SUBSCRIPT to a THL initial string.

Tries the override table first (handles palatalisations and
retroflexes);  otherwise concatenates root's THL value with
subscript verbatim (graceful degradation when subscript is
something unusual).

** ~tibetan-phonetics--umlaut-vowel~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 341
:SIGNATURE: (tibetan-phonetics--umlaut-vowel vowel)
:END:

Return the umlauted form of VOWEL.

** ~tibetan-phonetics--final-thl~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 349
:SIGNATURE: (tibetan-phonetics--final-thl vowel suffix post-suffix)
:END:

Compute the THL vowel + final-consonant for VOWEL + SUFFIX + POST-SUFFIX.

Umlaut is normally triggered ONLY by the suffix, never by the
post-suffix.  This matters for forms like `tshogs' (suffix `g',
post-suffix `s'):  the `g' blocks the `s'-driven umlaut, so the
vowel `o' stays unchanged and the final realises as `-k'.

EXCEPTION:  the genitive compound suffix `'i' (a-chung + i,
written `'i' in Wylie) DOES trigger umlaut on the stem vowel
and both characters are silent.  `pa'i' → `pä', `po'i' →
`pö', `bu'i' → `bü', etc.  Without this rule, every genitive-
ending word reads as if it had no genitive marker:  `chos kyi
rgyal po'i sa' (a high-frequency YBh construction) would
collapse from `chö kyi gyel pö sa' to `chö kyi gyel po sa'.

** ~tibetan-phonetics--curated-override~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 398
:SIGNATURE: (tibetan-phonetics--curated-override wylie)
:END:

Return the curated `:phonetics:' value for WYLIE from the
thesaurus, or nil when absent / module unavailable.

Lets the user pin a specific phonetic rendering for tricky cases
(Madhyamaka terms, Sanskrit loanwords, monastic-tradition
readings) via the per-word zettel.  When the rule engine's
output is wrong for a word, the fix is to populate the zettel's
`:phonetics:' field rather than complicate the rules table.

** ~tibetan-wylie-syllable-to-phonetic~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 422
:SIGNATURE: (tibetan-wylie-syllable-to-phonetic wylie)
:AUTOLOAD: t
:END:

Convert a single-syllable Wylie string WYLIE to THL phonetic.

Returns the phonetic syllable as a string;  nil when WYLIE is
nil / empty.

Tries the curated thesaurus override first.  Falls back to the
rule engine:  parses into prefix / superscript / root /
subscript / vowel / suffix / post-suffix, computes the initial
cluster + vowel + final, composes the result.

When the syllable can't be parsed as Wylie (e.g. random Roman
text), the input is returned verbatim — graceful degradation
better than crashing the rendering pipeline.

** ~tibetan-to-phonetics~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 461
:SIGNATURE: (tibetan-to-phonetics text)
:AUTOLOAD: t
:END:

Convert TEXT (Tibetan Unicode OR Wylie) to a THL phonetic string.

Tibetan Unicode is detected by the presence of code-points in
the Tibetan block (U+0F00–U+0FFF) and run through
`tibetan-to-wylie-fixed' first.  Already-Wylie input is used
directly.

Splits on whitespace + tsheg (`་').  Punctuation (shad `།',
slash `/') is preserved verbatim — useful for keeping the
visual rhythm of a clause when reading aloud.  Each syllable
converts via `tibetan-wylie-syllable-to-phonetic'.

Returns the phonetic line as a single space-separated string.
Nil-safe.

** ~tibetan-phonetics--consonant-roots~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 77
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--valid-prefix-combos~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 100
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--valid-super-combos~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 130
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--root-thl~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 267
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--cluster-overrides~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 283
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--umlaut-table~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 329
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-phonetics--umlauting-suffixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 345
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-sanskrit-parallel-dharmamitra.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-sanskrit-parallel-dharmamitra.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-sanskrit-parallel-dharmamitra.el
:END:

** ~tibetan-sanskrit-parallel-dm--shape-result~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 44
:SIGNATURE: (tibetan-sanskrit-parallel-dm--shape-result alist rank)
:END:

Convert a DharmaMitra search-result ALIST to a candidate plist.
RANK is the 1-based position in the search response.

Pure function — no I/O.  Robust to missing alist keys: each
slot defaults to nil / empty string so downstream callers always
receive a fully-populated plist.

** ~tibetan-sanskrit-parallel-dm-candidates-for-tibetan~  /function (cl)/  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 58
:SIGNATURE: (tibetan-sanskrit-parallel-dm-candidates-for-tibetan tibetan-text source-file &key (max-candidates 5))
:AUTOLOAD: t
:END:

Find Sanskrit alignment candidates for TIBETAN-TEXT in DM corpus.

Workflow:
  1. Read `#+DM_SANSKRIT_SOURCE:' from SOURCE-FILE.  Returns nil
     when missing — without a target work, searching the whole
     DM corpus would surface unrelated hits (e.g. high-rank
     prajñāpāramitā passages instead of Bodhisattvabhūmi).
  2. Translate TIBETAN-TEXT → English via
     `tibetan-dharmamitra-api-chat-translate'.  Returns nil if
     translation fails (HTTP error / empty response).
  3. Search the DM primary corpus with the English translation,
     filtered to the configured Sanskrit work, with
     `:source-lang sa' so only Sanskrit hits are returned.  Returns
     nil if search fails or yields no hits.
  4. Take the top MAX-CANDIDATES (default 5) hits, shape each as a
     plist `(:id ID :text STR :segmentnr STR :title STR :rank N)'.

Returns the candidate list, or nil for any failure mode (missing
header, empty Tibetan input, translate failure, search failure,
zero hits, missing/unreadable SOURCE-FILE).

Both underlying API calls are cached by request body hash, so
repeated invocations with the same inputs cost nothing after the
first.

** ~tibetan-sanskrit-parallel-dm--build-claude-pick-prompt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 129
:SIGNATURE: (tibetan-sanskrit-parallel-dm--build-claude-pick-prompt tibetan-text candidates)
:END:

Build the disambiguation prompt for CANDIDATES given TIBETAN-TEXT.

Pure function — no I/O.  The output is plain text Claude can
read; the response is expected back in the `## Choice' / `##
Reason' schema parsed by
`tibetan-sanskrit-parallel-dm--parse-claude-pick-response'.

** ~tibetan-sanskrit-parallel-dm--parse-claude-pick-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 172
:SIGNATURE: (tibetan-sanskrit-parallel-dm--parse-claude-pick-response response candidates)
:END:

Parse RESPONSE against CANDIDATES into a `(:chosen-rank N :reason STR)' plist.

Pure function — no I/O.  Returns nil when:
  - RESPONSE is nil / empty / not a string
  - `## Choice' heading is missing or carries no integer
  - parsed rank is outside [1, length-of-CANDIDATES]

When parsing succeeds, returns the plist; the calling
`claude-pick' uses it to look up the matching candidate.

** ~tibetan-sanskrit-parallel-dm--ask-claude-sync~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 210
:SIGNATURE: (tibetan-sanskrit-parallel-dm--ask-claude-sync prompt)
:END:

Synchronously call Claude with PROMPT.  Returns response string or nil.

Uses gptel under the hood (same Claude infrastructure as
`tibetan-analysis--request-claude-translation') but blocks on
`accept-process-output' until the callback fires or
`tibetan-sanskrit-parallel-dm-claude-pick-timeout' seconds
elapse.  No queue — this runs outside `tibetan-claude-queue' so
it doesn't compete with translation requests for slots.

Phase 9 (2026-04-30):  calls `tibetan-analysis--ensure-gptel-ready'
before `gptel-request' so the Anthropic API key is loaded from
`~/.authinfo' into `gptel-api-key'.  Without this step every
Claude call fails with `No \='gptel-api-key\=' found in the
auth source' and the realign command silently falls back to the
top hit on every multi-candidate segment.

Returns nil for any failure (gptel not loaded, HTTP error,
timeout, empty response).  Tests `cl-letf' this function so the
caller orchestration can be exercised without network.

** ~tibetan-sanskrit-parallel-dm-claude-pick~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 256
:SIGNATURE: (tibetan-sanskrit-parallel-dm-claude-pick tibetan-text candidates)
:AUTOLOAD: t
:END:

Ask Claude to identify the correct Sanskrit parallel from CANDIDATES.

CANDIDATES is the list of plists returned by
`tibetan-sanskrit-parallel-dm-candidates-for-tibetan'.

Returns a plist:
  (:chosen-id ID :chosen-text TEXT :chosen-rank N :reason STR)

The `:reason' field carries either Claude's explanation or a
fallback marker — `single-candidate', `claude-unavailable-...',
`claude-response-unparseable-...' — so the realign command's
dry-run preview shows what happened.  Returns nil when CANDIDATES
is empty.

When CANDIDATES has exactly one entry, returns it directly without
calling Claude (no disambiguation needed).

** ~tibetan-sanskrit-parallel-dm--normalise-whitespace~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 325
:SIGNATURE: (tibetan-sanskrit-parallel-dm--normalise-whitespace s)
:END:

Normalise whitespace in S for proposal-comparison purposes.
Trims leading/trailing whitespace and collapses runs of
whitespace into single spaces.  Empty / nil → empty string.

** ~tibetan-sanskrit-parallel-dm--build-proposal~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 334
:SIGNATURE: (tibetan-sanskrit-parallel-dm--build-proposal seg-id tibetan-text current-sanskrit pick)
:END:

Build a realign proposal plist for SEG-ID.

PICK is the plist returned by
`tibetan-sanskrit-parallel-dm-claude-pick' (or nil when no
candidates were found).  CURRENT-SANSKRIT is the body of the
segment's existing `**** Sanskrit' sibling (or nil / empty when
the segment has none yet).

Returns:
  (:seg-id N :tibetan-text TIB :current-sanskrit CUR
   :proposed-sanskrit PROP :proposed-rank R :proposed-segmentnr SN
   :reason STR :status SYM)

Status values:
  `change'         — current and proposed differ (after whitespace
                     normalisation)
  `unchanged'      — current ≈ proposed; no edit needed
  `no-candidates'  — PICK was nil (DM returned no hits, translate
                     failed, or no DM_SANSKRIT_SOURCE configured)

Pure function — no I/O.  The caller (the document walker)
handles I/O around it.

Phase 8 (2026-04-30):  the `:proposed-sanskrit' value is the
extracted matching clause from `:chosen-clause' when present
and non-empty;  otherwise it falls back to the full candidate
text from `:chosen-text'.  This fixes the granularity issue
where DM corpus segments are coarser than Tibetan segments —
Claude extracts just the line(s) that parallel the Tibetan
instead of the whole multi-line corpus chunk.

** ~tibetan-sanskrit-parallel-dm--collect-segment-ids~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 390
:SIGNATURE: (tibetan-sanskrit-parallel-dm--collect-segment-ids source-file)
:END:

Return sorted list of segment IDs found in SOURCE-FILE.
Walks every `**** Segment N' heading.

** ~tibetan-sanskrit-parallel-dm-realign-document-proposals~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 404
:SIGNATURE: (tibetan-sanskrit-parallel-dm-realign-document-proposals source-file)
:AUTOLOAD: t
:END:

Return a list of realign proposals for every segment in SOURCE-FILE.

For each `**** Segment N' heading, runs the three-stage pipeline
(read segment data → DM candidates → Claude pick → build proposal)
and collects the result.  Order matches the source file's
segment IDs (sorted ascending).

Returns nil when SOURCE-FILE is missing / unreadable.  On per-
segment failures (translate / search failure, no candidates),
the proposal carries `:status' `no-candidates' so the preview
buffer can list it; the walker continues to the next segment.

This is the orchestrator for `realign-document'.  Phase 5 will
wrap it in an interactive command that calls this, hands the
result to the preview renderer, and (with `C-u') applies the
changes.

** ~tibetan-sanskrit-parallel-dm--count-status~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 441
:SIGNATURE: (tibetan-sanskrit-parallel-dm--count-status proposals status)
:END:

Count proposals in PROPOSALS with `:status' equal to STATUS.

** ~tibetan-sanskrit-parallel-dm--render-preview-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 446
:SIGNATURE: (tibetan-sanskrit-parallel-dm--render-preview-buffer source-file proposals)
:END:

Render a `*Sanskrit Realign Preview*' buffer for PROPOSALS.

SOURCE-FILE is the path of the source document being aligned;
displayed in the buffer header for context.  PROPOSALS is the
list returned by
`tibetan-sanskrit-parallel-dm-realign-document-proposals'.

The buffer shows:
  - a summary line with counts (changes / unchanged /
    no-candidates)
  - per-proposal blocks with seg-id, Tibetan, current Sanskrit,
    proposed Sanskrit, Claude reason, status

Returns the buffer.  Caller decides whether to display, kill, or
read from it; the renderer does not display the buffer itself.

** ~tibetan-sanskrit-parallel-dm--write-dharmamitra-sanskrit-to-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 525
:SIGNATURE: (tibetan-sanskrit-parallel-dm--write-dharmamitra-sanskrit-to-analysis analysis-file proposal)
:END:

Write PROPOSAL's realign output to ANALYSIS-FILE.

Creates or replaces a top-level `* Sanskrit (DharmaMitra)'
section.  The section's property drawer carries:

  :DM_SEGMENTNR:   the chosen DharmaMitra segment ID
  :DM_RANK:        the rank within the candidates list
  :CLAUDE_REASON:  the reason string from claude-pick (or
                   fallback marker)
  :LAST_REALIGN:   today's date stamp (YYYY-MM-DD)

The body is the proposed Sanskrit text.

Top-level placement (outside `* Auto-Analysis') ensures the
section survives reanalysis without changes to the existing
preserve-list machinery — `regenerate-auto' only rebuilds the
`* Auto-Analysis' subtree.

Returns t on success, nil when ANALYSIS-FILE is missing or
unwritable.

** ~tibetan-sanskrit-parallel-dm--sanitize-drawer-value~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 582
:SIGNATURE: (tibetan-sanskrit-parallel-dm--sanitize-drawer-value value)
:END:

Collapse VALUE to a single line for safe use inside an org
`:PROPERTIES:' drawer.  DM segment numbers / ranks and Claude's reason
are untrusted text; an embedded newline followed by `:KEY: val' or
`:END:' would forge a property entry or close the drawer early.  All
whitespace runs (including newlines) collapse to a single space; the
result is trimmed.  Nil-safe.

** ~tibetan-sanskrit-parallel-dm--sanitize-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 592
:SIGNATURE: (tibetan-sanskrit-parallel-dm--sanitize-body text)
:END:

Neutralise org heading injection in untrusted body TEXT: prefix any
line-leading run of `*' with a space so it renders as body text / a
list item rather than a top-level heading.  Nil-safe.

** ~tibetan-sanskrit-parallel-dm--render-dharmamitra-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 598
:SIGNATURE: (tibetan-sanskrit-parallel-dm--render-dharmamitra-section proposal)
:END:

Render PROPOSAL as the body text of a `* Sanskrit (DharmaMitra)'
section.  Pure function — no I/O.  Used by the analysis-file
writer; also useful for tests that want to inspect the output
shape without going through file I/O.

Untrusted DM / Claude fields are sanitised before insertion: drawer
values (segment number / rank / reason) are collapsed to one line, and
the Sanskrit body's line-leading `*' is neutralised — so a response
cannot forge property-drawer entries or inject an org heading.

** ~tibetan-sanskrit-parallel-dm--apply-proposal~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 627
:SIGNATURE: (tibetan-sanskrit-parallel-dm--apply-proposal analysis-file proposal)
:END:

Apply PROPOSAL to ANALYSIS-FILE if its `:status' is `change'.

Skips proposals with status `unchanged' (no edit needed) or
`no-candidates' (DM had nothing to propose; we don't blank the
existing section).  Phase 7 (2026-04-30):  ANALYSIS-FILE is
the per-segment seg-NNN.org analysis file, NOT the source.

Returns the writer's return value (t / nil) on `change',
otherwise nil.

** ~tibetan-sanskrit-parallel-dm--apply-proposals~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 643
:SIGNATURE: (tibetan-sanskrit-parallel-dm--apply-proposals source-file proposals)
:END:

Apply every PROPOSAL in PROPOSALS whose `:status' is `change'.

Resolves each proposal's analysis file via
`tibetan-analysis-get-filepath' (which uses
`tibetan-analysis-folder' / current buffer's directory).
SOURCE-FILE is visited in a buffer first so the path resolver
gets the right context.

Returns count of successfully applied proposals.  SOURCE-FILE
itself is NEVER modified — verified by regression test.

** ~tibetan-sanskrit-parallel-dm--resolve-source-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 688
:SIGNATURE: (tibetan-sanskrit-parallel-dm--resolve-source-file)
:END:

Return the source file for the realign command.
If the current buffer is an analysis seg-NNN.org file, follow
its `#+SOURCE:' link.  Otherwise treat the current buffer as
the source.  Falls back to a prompt.

** ~tibetan-sanskrit-parallel-dm-realign-document~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 700
:SIGNATURE: (tibetan-sanskrit-parallel-dm-realign-document &optional apply)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

DharmaMitra-driven re-alignment of all `**** Sanskrit' siblings.

Without prefix arg: pops a `*Sanskrit Realign Preview*' buffer
showing per-segment proposals (current vs proposed Sanskrit,
Claude reason).  Re-run with `C-u' to apply the changes.

With `C-u' prefix: applies every proposal whose `:status' is
`change' to the source file's `**** Sanskrit' siblings, then
displays a summary buffer.

Source file is resolved from the current buffer (analysis or
source); falls back to a prompt.  Requires
`#+DM_SANSKRIT_SOURCE:' on the source file's headers — without
it the per-segment pipeline returns no candidates and every
proposal lands as `no-candidates'.

The pipeline calls DharmaMitra (chat-translate + search) and
Claude (disambiguation) once per segment.  Plan ~3 sec per
segment; gotrapatala (97 segments) is roughly 5 minutes.

** ~tibetan-sanskrit-parallel-dm-realign-segment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 747
:SIGNATURE: (tibetan-sanskrit-parallel-dm-realign-segment &optional apply)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

DharmaMitra-driven re-alignment of the `**** Sanskrit' sibling
for the segment under cursor.

Without prefix arg: pops a preview buffer showing the proposal
for this single segment.  With `C-u': applies it directly.

Like `tibetan-sanskrit-parallel-dm-realign-document' but
narrowed to one segment — useful for spot-checking a specific
segment without firing the full document pipeline.

** ~tibetan-sanskrit-parallel-dm-claude-pick-timeout~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 123
:TYPE: 'integer
:GROUP: tibetan-cat
:DEFAULT: 30
:END:

Seconds to wait for Claude's pick response before falling back
to the top candidate.


* core/tibetan-sanskrit-parallel.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-sanskrit-parallel.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-sanskrit-parallel.el
:END:

** ~tibetan-sanskrit-parallel--has-devanagari-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 77
:SIGNATURE: (tibetan-sanskrit-parallel--has-devanagari-p str)
:END:

Return non-nil when STR contains a baseline Devanagari codepoint.

Tested range is U+0900–U+097F (the standard Devanagari block).
Vedic Extensions (U+1CD0–U+1CFF) and Devanagari Extended
(U+A8E0–U+A8FF) are intentionally excluded — for prose
Yogācārabhūmi reading the baseline block is sufficient and the
heuristic stays predictable.  Future Vedic-quoting sources can
opt into the broader range without disturbing this default.

Returns nil for nil, non-string, or empty input.

** ~tibetan-sanskrit-parallel--placeholder-text-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 140
:SIGNATURE: (tibetan-sanskrit-parallel--placeholder-text-p str)
:END:

Return non-nil when STR is a Sanskrit-alignment placeholder marker.

Checks STR against
`tibetan-sanskrit-parallel--placeholder-prefixes' via
`string-prefix-p'.  Defensive against nil and non-string
input — returns nil for both.

Used by `--parse-body' to short-circuit placeholder bodies into
nil so the walker reports `no Sanskrit available'.  Other
modules (DM fire-sanskrit, Claude prompt user-block) can call
this directly to apply the same gate without re-implementing
the prefix list.

** ~tibetan-sanskrit-parallel--parse-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 161
:SIGNATURE: (tibetan-sanskrit-parallel--parse-body body)
:END:

Parse BODY (a string) into a Sanskrit-text plist or nil.

Splits BODY on newlines.  The first non-empty trimmed line is
taken as IAST.  If the second non-empty line contains Devanagari
codepoints (per `tibetan-sanskrit-parallel--has-devanagari-p')
it is taken as Devanagari; otherwise it is dropped.  Subsequent
lines are ignored.

Returned plist:
  (:iast STRING                  ; trimmed line 1
   :devanagari STRING-or-nil     ; trimmed line 2 if Devanagari
   :script-source SYMBOL)        ; `iast-line' or `iast-and-devanagari'

Returns nil when:
  - BODY is nil, empty, or contains no IAST line.
  - The IAST line is a placeholder marker recognised by
    `tibetan-sanskrit-parallel--placeholder-text-p' (alignment-
    fix work, 2026-04-30).  Placeholder segments thus look the
    same to downstream consumers as segments that have no
    `**** Sanskrit' sibling at all.

** ~tibetan-sanskrit-parallel-text-for-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 205
:SIGNATURE: (tibetan-sanskrit-parallel-text-for-segment)
:END:

Return Sanskrit-text plist for the current `**** Segment' subtree.

When point is in or under a `**** Segment' heading, scans the
same-level siblings within the enclosing Sentence subtree for a
heading whose text starts with `Sanskrit'.  Reads that subtree's
body via `tibetan-org--read-subtree-body' and parses it via
`tibetan-sanskrit-parallel--parse-body'.

Walking semantics:
  - Stops at the next `**** Segment' sibling (Sanskrit belongs
    to the segment that immediately precedes it).
  - Stops at any heading shallower than the current Segment
    (Sentence boundary).
  - Skips intervening siblings such as `**** Working Translation'
    so a Sanskrit heading placed AFTER Working Translation is
    still found.

Returns nil when:
  - Point is not in or under a Segment.
  - The Segment has no Sanskrit sibling within its Sentence.
  - The Sanskrit sibling has an empty body.

** ~tibetan-sanskrit-parallel-text-for-segment-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 273
:SIGNATURE: (tibetan-sanskrit-parallel-text-for-segment-id source-file seg-id)
:END:

Return Sanskrit-text plist for SEG-ID inside SOURCE-FILE, or nil.

Opens SOURCE-FILE in a temporary org buffer, navigates to the
heading `Segment SEG-ID', and delegates to
`tibetan-sanskrit-parallel-text-for-segment'.

Returns nil when SOURCE-FILE is nil/empty/missing, when SEG-ID
is not an integer, when the file has no matching `Segment N'
heading, or when the Segment has no Sanskrit sibling.  Never
errors — wrapped in `condition-case' so callers can use the
result directly without guarding.

** ~tibetan-sanskrit-parallel-plist-for-segment-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 316
:SIGNATURE: (tibetan-sanskrit-parallel-plist-for-segment-id source-file seg-id)
:END:

Return the Sanskrit plist for SEG-ID in SOURCE-FILE — gated by mode.

This is a convenience wrapper for analysis call sites
(`tibetan-auto-analyze-document', `tibetan-analysis-reanalyze-
file', the per-segment open / reanalyse paths).  Returns:

  - the walker plist
    `(:iast STR :devanagari STR-or-nil :script-source SYM)'
    when SOURCE-FILE has `#+SOURCE_MODE: parallel-sanskrit' AND
    the segment has a `**** Sanskrit' sibling.

  - nil otherwise — including when SOURCE-FILE is not in
    parallel mode (today's behaviour preserved byte-for-byte for
    Tibetan-only documents), when the file is missing, or when
    SEG-ID is not an integer.

Call sites bind `tibetan-analysis--sanskrit-text-for-render' to
this function's return value and the renderer fires conditionally
in `tibetan-analysis--render-sanskrit-source' (Phase 2).

** ~tibetan-sanskrit-parallel-text-for-sentence~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 345
:SIGNATURE: (tibetan-sanskrit-parallel-text-for-sentence source-file seg-ids)
:END:

Return an aggregated Sanskrit plist for a SENTENCE in SOURCE-FILE.

SEG-IDS is a list of integer segment ids that constitute the
sentence.  Walks each child segment, collects its Sanskrit
plist via `tibetan-sanskrit-parallel-text-for-segment-id'
(which already filters out placeholder bodies — see
`tibetan-sanskrit-parallel--placeholder-text-p').  Concatenates
the surviving IAST lines (separated by blank lines) and the
surviving Devanagari lines (same).

Returns:
  - `(:iast CONCAT-IAST :devanagari CONCAT-DEV-or-nil
     :script-source SYM)'  when at least one child segment has
    real (non-placeholder) Sanskrit content.
  - nil when SOURCE-FILE is non-parallel, when SEG-IDS is empty,
    or when every child segment's Sanskrit is missing /
    placeholder.

Used by the sentence-level dispatch (sent-NNN.org
reanalyse paths) to fire ONE Sanskrit Claude call covering the
whole sentence — granularity matching the sentence-scoped
Tibetan analysis.

Devanagari aggregation rule: if ANY child segment has
Devanagari, the aggregated `:devanagari' is the concatenation
of those that have it (segments without Devanagari contribute
nothing).  When NO child has Devanagari, `:devanagari' is nil
and `:script-source' is `iast-line'.

Mode-gated:  returns nil when SOURCE-FILE lacks
`#+SOURCE_MODE: parallel-sanskrit'.  Same gate as
`tibetan-sanskrit-parallel-plist-for-segment-id'.

** ~tibetan-cat-set-source-mode~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 431
:SIGNATURE: (tibetan-cat-set-source-mode source-file mode)
:INTERACTIVE: t
:END:

Set `#+SOURCE_MODE:' to MODE on SOURCE-FILE.

When SOURCE-FILE already has a `#+SOURCE_MODE:' line, the value
is replaced in place.  Otherwise the new line is inserted right
after the first `#+TITLE:' line so document metadata stays
contiguous at the top of the file.  When neither header is
present, the mode header is prepended to the buffer.

Re-analyses of this document pick up the new value automatically
via `tibetan-analysis--read-source-metadata' — no manual reload
needed.  Interactive callers get a final-state message.

Errors when MODE is nil / empty, or when SOURCE-FILE is nil /
non-existent / not writable.

** ~tibetan-cat-clear-source-mode~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 485
:SIGNATURE: (tibetan-cat-clear-source-mode source-file)
:INTERACTIVE: t
:END:

Remove the `#+SOURCE_MODE:' header from SOURCE-FILE, if present.

No-op (and no error) when the header is absent.  Returns the
document to today's Tibetan-only behaviour: the next reanalyse
emits no `** Sanskrit Source' section, the Claude system prompt
reverts to Tibetan-only translation, and the user prompt drops
the Sanskrit (primary) block.

Errors when SOURCE-FILE is nil / non-existent / not writable.

** ~tibetan-cat-toggle-source-mode-parallel~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 519
:SIGNATURE: (tibetan-cat-toggle-source-mode-parallel)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Toggle `#+SOURCE_MODE: parallel-sanskrit' on the current source file.

When the source file is in parallel-sanskrit mode, the header is
removed (back to today's Tibetan-only behaviour).  Otherwise the
header is set (or replaced if a different mode token was
present) to `parallel-sanskrit'.

Source file resolution:
  1. If the current buffer is an analysis file (seg-NNN*.org),
     follow its `#+SOURCE:' link to the source document.
  2. Otherwise, treat the current buffer's file as the source.
  3. If neither resolves, prompt for a path.

Bound to `C-c u z P' (parallel-Sanskrit) under the existing
`C-c u z' source-document prefix.  Sibling of `C-c u z L'
(target language).

** ~tibetan-cat--source-mode-parallel-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 558
:SIGNATURE: (tibetan-cat--source-mode-parallel-p source-file)
:END:

Return non-nil iff SOURCE-FILE has `#+SOURCE_MODE: parallel-sanskrit'.

The header is read directly from the file (no `persist/'
dependency) — this lets `core/' callers ask the question without
pulling in the analysis pipeline.

The persist-layer metadata reader,
`tibetan-analysis--read-source-metadata', exposes the same value
on its `:source-mode' plist key for callers that already build
the full metadata plist (Phase 3 Claude prompt builder).

Safe when SOURCE-FILE is nil, empty, or missing — returns nil.

** ~tibetan-sanskrit-parallel-segment-data-for-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 588
:SIGNATURE: (tibetan-sanskrit-parallel-segment-data-for-id source-file seg-id)
:END:

Return `(:tibetan STR :current-sanskrit STR)' for SEG-ID in SOURCE-FILE.

Walks SOURCE-FILE's `**** Segment N' headings, narrows to the
matching one, extracts the Tibetan body via
`tibetan-org-get-segment-text' and the current `**** Sanskrit'
sibling's body via `tibetan-sanskrit-parallel-text-for-segment'.

Returns nil when:
  - SOURCE-FILE is nil / unreadable
  - SEG-ID has no matching `**** Segment SEG-ID' heading

The `:current-sanskrit' value is nil when the segment has no
`**** Sanskrit' sibling yet (legacy or fresh document).  The
realign workflow treats nil current-sanskrit as a fill, not a
change.

** ~tibetan-sanskrit-parallel--reset-delete-section-in-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 695
:SIGNATURE: (tibetan-sanskrit-parallel--reset-delete-section-in-buffer heading-text level)
:END:

Delete the section under HEADING-TEXT at org LEVEL in the current buffer.
Stops the deletion at the next heading at LEVEL-or-shallower or
end of buffer.  Returns t if a section was deleted, nil
otherwise.  Idempotent — re-running on a buffer where the
section is already absent is a safe no-op.

** ~tibetan-sanskrit-parallel--reset-squash-blank-runs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 732
:SIGNATURE: (tibetan-sanskrit-parallel--reset-squash-blank-runs)
:END:

Collapse runs of 3+ blank lines down to a single blank line.
The deletion above can leave "\n\n\n\n" gaps where two
adjacent target sections both got removed; this restores normal
spacing.  Operates on the current buffer.

** ~tibetan-sanskrit-parallel--reset-sanskrit-sections-in-file~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 743
:SIGNATURE: (tibetan-sanskrit-parallel--reset-sanskrit-sections-in-file filepath)
:AUTOLOAD: t
:END:

Remove Sanskrit-side sections from FILEPATH, save, return count.

Removes every section listed in
`tibetan-sanskrit-parallel--reset-level2-headings' (level-2
inside `* Auto-Analysis') and
`tibetan-sanskrit-parallel--reset-level1-headings' (level-1
top-level).  See the commentary above for the full list and
the preserved-content invariants.

Returns the integer count of sections removed (0 when the file
was already clean).  Idempotent: re-running on a clean file
returns 0 and leaves the file byte-identical.

The buffer is saved only when at least one section was
removed — clean files don't generate a no-op save event that
would alter `mtime' or trigger external file-watchers.

** ~tibetan-sanskrit-parallel--reset-sanskrit-sections-in-folder~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 790
:SIGNATURE: (tibetan-sanskrit-parallel--reset-sanskrit-sections-in-folder folder)
:AUTOLOAD: t
:END:

Run `--reset-sanskrit-sections-in-file' on every `seg-NNN.org'
and `sent-NNN.org' file in FOLDER.

Returns a plist summary:
  (:files-touched     N
   :files-clean       M
   :sections-removed  K)

When FOLDER is nil, missing, or not a directory, returns
  (:error "reason …")
so the interactive command can report the failure without an
exception.

Files outside the seg/sent naming convention are silently
skipped — the reset target is a parallel-Sanskrit reading's
analysis folder, not arbitrary org files.

** ~tibetan-sanskrit-parallel-reset-sanskrit-sections-in-folder~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 832
:SIGNATURE: (tibetan-sanskrit-parallel-reset-sanskrit-sections-in-folder folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive entry point — reset Sanskrit-side sections in FOLDER.

Removes every `** Sanskrit Source', `** Claude Translation
(Sanskrit)', `** Claude Translation (Combined)', `** Claude
Divergence', and `* DharmaMitra Translation (Sanskrit)' from
every `seg-NNN.org' / `sent-NNN.org' analysis file in FOLDER.

Designed for after-the-fact cleanup when the parallel-Sanskrit
source's `**** Sanskrit' alignment is bulk-changed (e.g. marked
as `[Sanskrit alignment pending …]' for class-time editorial
verification).  Existing analysis files generated against the
old alignment carry Sanskrit-side sections that no longer
match; this command removes them cleanly so the next reanalyse
regenerates them — or, when the source is now pending, omits
them.

Prompts for confirmation.  Default FOLDER is the current
analysis folder per `tibetan-analysis-get-folder' (when
available); otherwise the user is prompted for a directory.

Reports the summary in `*Messages*' on completion.

** ~tibetan-sanskrit-parallel--strip-legacy-dm-sanskrit-in-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 902
:SIGNATURE: (tibetan-sanskrit-parallel--strip-legacy-dm-sanskrit-in-file filepath)
:END:

Remove the legacy `* Sanskrit (DharmaMitra)' section from FILEPATH.

Returns the integer count of sections removed (0 when the file
was already clean).  Idempotent — re-running on a clean file
returns 0 and leaves the file byte-identical.

The buffer is saved only when at least one section was removed,
so a clean file's `mtime' isn't disturbed.

Reuses `--reset-delete-section-in-buffer' for the actual deletion
(it already handles property drawer + body capture and stops at
the next level-1 heading).

** ~tibetan-sanskrit-parallel--strip-legacy-dm-sanskrit-in-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 935
:SIGNATURE: (tibetan-sanskrit-parallel--strip-legacy-dm-sanskrit-in-folder folder)
:END:

Run `--strip-legacy-dm-sanskrit-in-file' on every `seg-NNN.org'
and `sent-NNN.org' file in FOLDER.

Returns a plist summary:
  (:files-touched     N
   :files-clean       M
   :sections-removed  K)

When FOLDER is nil, missing, or not a directory, returns
  (:error "reason …")
so the interactive command can report the failure cleanly.

Files outside the seg/sent naming convention are silently
skipped — the cleanup target is a parallel-Sanskrit reading's
analysis folder.

** ~tibetan-sanskrit-parallel-strip-legacy-dm-sanskrit-in-folder~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 977
:SIGNATURE: (tibetan-sanskrit-parallel-strip-legacy-dm-sanskrit-in-folder folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive entry point — strip legacy `* Sanskrit (DharmaMitra)'.

One-time post-§5.17 cleanup that removes the legacy
`* Sanskrit (DharmaMitra)' section from every `seg-NNN.org' /
`sent-NNN.org' file in FOLDER.  This section was the §5.15
DM-realign output and is now superseded by `* Sanskrit Text'
(driven by the canonically-aligned source file).

The Tibetan-side and Sanskrit-side DharmaMitra Translation
sections (`* DharmaMitra Translation (Tibetan)' /
`* DharmaMitra Translation (Sanskrit)') are NOT touched —
those are translator outputs, not realign provenance.  The
new `* Sanskrit Text' section is also NOT touched.

Returns the summary plist from
`--strip-legacy-dm-sanskrit-in-folder'.  Reports a friendly
message; on error reports the `:error' string instead of
throwing.

** ~tibetan-cat-source-mode-default-parallel~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 420
:TYPE: 'string
:GROUP: tibetan-cat
:DEFAULT: "parallel-sanskrit"
:END:

The mode token written by `tibetan-cat-toggle-source-mode-parallel'.

Currently `parallel-sanskrit' is the only implemented mode.
Future modes (`parallel-pali', `commentary-with-base', …) would
extend this list and route through the same toggle command via
a per-mode interactive variant — for now the constant default
keeps the toggle one-keystroke.

** ~tibetan-sanskrit-parallel--placeholder-prefixes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 97
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sanskrit-parallel--reset-level2-headings~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 676
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sanskrit-parallel--reset-level1-headings~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 688
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-steinert.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-steinert.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-steinert.el
:END:

** ~tibetan-steinert-available-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 71
:SIGNATURE: (tibetan-steinert-available-p)
:END:

Return non-nil if Steinert lookup is usable in this Emacs session.

** ~tibetan-steinert--db~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 77
:SIGNATURE: (tibetan-steinert--db)
:END:

Return the open SQLite handle, opening it on first call.
Returns nil (and warns once) if the DB is unavailable.

** ~tibetan-steinert-close~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 93
:SIGNATURE: (tibetan-steinert-close)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Close the Steinert DB connection if open (does not delete the file).

** ~tibetan-steinert--to-wylie-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (tibetan-steinert--to-wylie-key word)
:END:

Return the Wylie lookup key for WORD (Tibetan script OR already-Wylie).
Returns nil if conversion isn't possible.

** ~tibetan-steinert-lookup~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 133
:SIGNATURE: (tibetan-steinert-lookup word &optional limit)
:END:

Look WORD up in the Steinert DB.
Return a list of plists (:source :wylie :gloss :sanskrit), or nil.
LIMIT defaults to `tibetan-steinert-max-hits'.

** ~tibetan-steinert-sanskrit-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 159
:SIGNATURE: (tibetan-steinert-sanskrit-for word)
:END:

Return a combined Sanskrit string for WORD, or nil.
Queries only the Sanskrit-indexed sources for cleanliness.

** ~tibetan-steinert-url~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 194
:SIGNATURE: (tibetan-steinert-url wylie-term)
:END:

Return a Steinert web-dictionary URL for WYLIE-TERM, or nil.
The URL encodes a JSON hash fragment that the single-page app at
`https://dictionary.christian-steinert.de/' routes to a search result.
WYLIE-TERM should be a lowercased Wylie string (e.g. "mnyam med").
Strips trailing particles like \='s, \='i suffixes for cleaner lookups.

** ~tibetan-steinert-url-org~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 217
:SIGNATURE: (tibetan-steinert-url-org wylie-term)
:END:

Return an org-mode link to the Steinert web dictionary for WYLIE-TERM.
Format: [[url][Steinert]].  Returns nil if WYLIE-TERM is empty.

** ~tibetan-steinert-db-path~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 36
:TYPE: '(choice (const
:GROUP: tibetan
:DEFAULT: (let* ((here (or load-file-name buffer-file-name))
         (base (and here (file-name-directory here))))
    (and base 
:END:

Path to the Steinert SQLite database.
Nil or a missing file disables Steinert lookup.

** ~tibetan-steinert-max-hits~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 45
:TYPE: 'integer
:GROUP: tibetan
:DEFAULT: 25
:END:

Cap on glossary rows returned by `tibetan-steinert-lookup'.
Keeps the Detailed Dictionary section from exploding on very common words.

** ~tibetan-steinert--sanskrit-sources~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 51
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-steinert--db~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 66
:DEFAULT: nil
:END:

Cached open SQLite handle. Opened lazily on first lookup.

** ~tibetan-steinert--warned-missing~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 69
:DEFAULT: nil
:END:

/(no docstring)/


* core/tibetan-text-classifier.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-text-classifier.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-text-classifier.el
:END:

** ~tibetan-detect-text-type~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 64
:SIGNATURE: (tibetan-detect-text-type)
:END:

Detect text type from current buffer.
Checks for explicit classification header first, then uses heuristics.
Returns symbol: classical, madhyamaka-verse, kagyu-verse, etc.

** ~tibetan-get-explicit-classification~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 71
:SIGNATURE: (tibetan-get-explicit-classification)
:END:

Get explicit text classification from buffer header.
Looks for:
  #+TIBETAN_TEXT_TYPE: madhyamaka-verse
  or
  # TIBETAN_TEXT_TYPE: classical
Returns symbol or nil.

** ~tibetan-classify-by-heuristics~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 85
:SIGNATURE: (tibetan-classify-by-heuristics)
:END:

Classify text type using heuristics when no explicit header.
Analyzes:
- Presence of verse markers/meter
- Technical terminology
- Sentence structure
Returns symbol: classical, madhyamaka-verse, etc.

** ~tibetan-text-has-verse-structure-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 116
:SIGNATURE: (tibetan-text-has-verse-structure-p text)
:END:

Check if TEXT has verse structure.
Looks for:
- Short lines (likely 7-syllable)
- Regular line breaks
- Line-final particles (common in verse)

** ~tibetan-text-has-madhyamaka-terms-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 130
:SIGNATURE: (tibetan-text-has-madhyamaka-terms-p text)
:END:

Check if TEXT contains Madhyamaka technical terms.

** ~tibetan-text-has-kagyu-terms-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 139
:SIGNATURE: (tibetan-text-has-kagyu-terms-p text)
:END:

Check if TEXT contains Kagyü/Mahāmudrā terms.

** ~tibetan-text-has-bialek-structure-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 146
:SIGNATURE: (tibetan-text-has-bialek-structure-p text)
:END:

Check if TEXT has Bialek classroom text structure.
Looks for:
- Sentence markers
- Segment markers
- Org-mode headings

** ~tibetan-get-text-type-info~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 160
:SIGNATURE: (tibetan-get-text-type-info text-type)
:END:

Get information about TEXT-TYPE.
Returns plist with :description, :features, :tools.

** ~tibetan-text-types~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 28
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-thesaurus.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-thesaurus.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-thesaurus.el
:END:

** ~tibetan-thesaurus--first-match~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 108
:SIGNATURE: (tibetan-thesaurus--first-match regexp)
:END:

Return the first `\1' group match of REGEXP in the current buffer,
or nil if no match.  Search is from `point-min' forward; caller
guards buffer state.

** ~tibetan-thesaurus--placeholder-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 118
:SIGNATURE: (tibetan-thesaurus--placeholder-p s)
:END:

Return non-nil if S looks like a Kramer placeholder field.
Placeholders are bracketed hints like `[to be added]', `[to be
researched]', or `[TBD]' — the parser treats them as unset.

** ~tibetan-thesaurus--clean-value~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 124
:SIGNATURE: (tibetan-thesaurus--clean-value s)
:END:

Return S trimmed, or nil if S is empty / placeholder.

** ~tibetan-thesaurus--parse-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-thesaurus--parse-file path)
:END:

Parse a thesaurus zettel at PATH into a plist, or return nil.

Returned plist keys:
  :id         — org ID string (from the `:ID:' property).
  :sanskrit   — Sanskrit term (from `#+title:'; the zettel's title
                also serves as the filename-style Sanskrit lemma).
  :wylie      — Wylie transliteration (from `- Wylie: ...' under
                the `** Tibetan' sub-heading).  This is the KEY
                the thesaurus indexes on.
  :primary-en — English primary translation (or nil if placeholder).
  :primary-de — German primary translation  (or nil if placeholder).
  :path       — absolute file path, used by editing commands.

Returns nil when PATH doesn't exist or has no `Wylie:' field —
the latter signals a malformed / stub zettel that should be
skipped from the index rather than crashing the indexer.

** ~tibetan-thesaurus--extract-field~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 175
:SIGNATURE: (tibetan-thesaurus--extract-field section-title field-label)
:END:

Return the FIELD-LABEL value inside the `** SECTION-TITLE' heading
of the current buffer, or nil.  E.g.
  SECTION-TITLE = "English", FIELD-LABEL = "Primary translation"
looks for:
  `** English' … `- Primary translation: <value>'
and returns <value>.  The search is bounded to the next level-2
heading so `- Primary translation:' lines in later sections don't
bleed into the result.

** ~tibetan-thesaurus--build-index~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 203
:SIGNATURE: (tibetan-thesaurus--build-index &optional dir)
:END:

Walk DIR (default: `tibetan-thesaurus-directory'), parse every
matching zettel, and return a hash keyed by Wylie string.

Values are LISTS of entry plists — the same Wylie can appear in
multiple zettels when the user wants to document distinct senses
or sources for the term.

Missing / unreadable directory → empty hash (not nil) so callers
can iterate safely in either state.

** ~tibetan-thesaurus--ensure-index~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 250
:SIGNATURE: (tibetan-thesaurus--ensure-index)
:END:

Return the thesaurus index, building / rebuilding as needed.

** ~tibetan-thesaurus-reload~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 261
:SIGNATURE: (tibetan-thesaurus-reload)
:INTERACTIVE: t
:END:

Clear the cached thesaurus index.  The next `tibetan-thesaurus-
lookup' call rebuilds it from disk.  Call this after editing a
zettel in the thesaurus directory or changing the directory path.

** ~tibetan-thesaurus-lookup~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 272
:SIGNATURE: (tibetan-thesaurus-lookup wylie)
:END:

Return a list of thesaurus entries matching the WYLIE key.
Each entry is a plist as documented on
`tibetan-thesaurus--parse-file'.  When no thesaurus is configured
or no match exists, returns nil — the multisource framework then
falls through to the next-ranked source silently.

** ~tibetan-thesaurus--slugify-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 288
:SIGNATURE: (tibetan-thesaurus--slugify-wylie wylie)
:END:

Turn WYLIE into a filename-safe slug.
Spaces become `-', apostrophes are dropped, anything outside
`[a-z0-9-]' is replaced with `-'.  Multi-dash runs collapse to
one.  Empty input returns the literal `entry' so the caller still
gets a valid filename component.

** ~tibetan-thesaurus-new-entry~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 303
:SIGNATURE: (tibetan-thesaurus-new-entry wylie &key sanskrit english german)
:END:

Create a new thesaurus zettel for WYLIE and return its absolute path.
Optional fields prefill the scaffold:
  :sanskrit — `#+title:' and `** Sanskrit / - Term:' values
  :english  — `** English / - Primary translation:'
  :german   — `** German / - Primary translation:'

Placement: `tibetan-thesaurus-directory' / TIMESTAMP--WYLIE__user.org
where TIMESTAMP is the current time in Denote's `%Y%m%dT%H%M%S'
shape and WYLIE is the Wylie slugified via `--slugify-wylie'.
The `:ID:' property equals TIMESTAMP so org's `id:' link scheme
resolves cleanly.

Errors:
  · `tibetan-thesaurus-directory' unset  → user-error.
  · File already exists                  → user-error (never
    overwrites existing content; a rare timestamp collision
    on the same second means the user should retry).

Side-effect: the cached thesaurus index is invalidated so the
next `tibetan-thesaurus-lookup' sees the new entry without a
manual `tibetan-thesaurus-reload'.

** ~tibetan-thesaurus-new-entry-interactively~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 374
:SIGNATURE: (tibetan-thesaurus-new-entry-interactively)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Prompt for a Wylie term and create a new thesaurus zettel.
Thin interactive wrapper around `tibetan-thesaurus-new-entry'.
Uses the Wylie on the current line (when available via
`--wylie-at-point') as the default, so in an analysis buffer
the user can `C-c u z n RET' to create an entry for the term
the cursor is already on.

** ~tibetan-thesaurus--wylie-at-point~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 398
:SIGNATURE: (tibetan-thesaurus--wylie-at-point)
:END:

Return the Wylie string visible on the current line, or nil.

Looks for a `[WYLIE]' token bounded by square brackets — the
convention used by the Detailed Dictionary head line (`◆ TIBETAN
[mthu] ★ …') and the Interlinear Gloss (`[[term-mthu][mthu]] …').
Multi-word Wylie inside a single bracket pair (e.g. `rnam par
shes pa') is returned whole.

Returns nil when the current line has no `[...]' match — the
caller can then prompt the user for a Wylie interactively.

** ~tibetan-thesaurus-edit-at-point~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 433
:SIGNATURE: (tibetan-thesaurus-edit-at-point)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open the thesaurus zettel for the Wylie term on the current line.
When multiple zettels are indexed for the same Wylie key, prompt
the user to pick one.  When no entry exists, offer to create a
new one via `tibetan-thesaurus-new-entry'.

Falls back to prompting the user for a Wylie when the current
line has no bracketed token.

** ~tibetan-thesaurus-initialize-from-kramer~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 477
:SIGNATURE: (tibetan-thesaurus-initialize-from-kramer)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Seed `tibetan-thesaurus-directory' from the Kramer glossary.
Copies every `*.org' file under
`tibetan-thesaurus-kramer-source-directory' into
`tibetan-thesaurus-directory', preserving the original Kramer
source intact.  Destination files that ALREADY EXIST are skipped
— a user who has started editing their thesaurus will never lose
their work on a second init call.

Both paths must be configured in advance via
`M-x customize-group RET tibetan-thesaurus'; missing config
raises a user-error with guidance.

Returns a plist `(:copied N :skipped-existing M :source SRC-DIR
:dest DEST-DIR)' summarising the run.

** ~tibetan-thesaurus--parse-last-analyzed~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 558
:SIGNATURE: (tibetan-thesaurus--parse-last-analyzed analysis-file)
:END:

Return the `#+LAST_ANALYZED:' date as an Emacs time value, or nil.
Date format expected: `YYYY-MM-DD' (what
`tibetan-analysis-create-file' writes).  Returns nil on malformed
or missing header so callers can decide the fallback.

** ~tibetan-thesaurus--tibetan-text-of~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 574
:SIGNATURE: (tibetan-thesaurus--tibetan-text-of analysis-file)
:END:

Return the body of the `* Tibetan Text' section of ANALYSIS-FILE,
or an empty string.  Used by audit/affected queries to scan
segment content for Wylie matches.

** ~tibetan-thesaurus--wylie-mentioned-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 592
:SIGNATURE: (tibetan-thesaurus--wylie-mentioned-p wylie text)
:END:

Return non-nil if WYLIE appears as a token sequence in TEXT.
Tokenised match (see `--tokenise-wylie-text') so `mthu' doesn't
match `mthun' / `mthuri' and multi-syllable keys are compared
syllable-by-syllable.  Case-sensitive.

** ~tibetan-thesaurus-segments-affected-by-zettel~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 599
:SIGNATURE: (tibetan-thesaurus-segments-affected-by-zettel zettel-path analysis-dir)
:END:

Return the list of analysis files under ANALYSIS-DIR whose
Tibetan Text contains the Wylie key of the thesaurus ZETTEL-PATH.

Drives `tibetan-thesaurus-rerun-affected' — after editing one
zettel, this finds exactly the segments that need re-analysis
and leaves the rest untouched.

When ZETTEL-PATH is unreadable or has no `- Wylie:' field, or
ANALYSIS-DIR doesn't exist, returns nil.

** ~tibetan-thesaurus-audit-folder~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 623
:SIGNATURE: (tibetan-thesaurus-audit-folder analysis-dir)
:AUTOLOAD: t
:END:

Scan ANALYSIS-DIR for analysis files whose last-analyzed date
predates a thesaurus zettel's modification time for a term that
appears in the analysis's Tibetan Text.  Return a list of plists:

  (:analysis-file PATH
   :stale-terms    (WYLIE1 WYLIE2 ...)
   :zettel-paths   (Z1 Z2 ...))

Each item flags one analysis as stale and names the Wylie terms
whose thesaurus entries post-date it.  An empty list means every
analysis in the folder is up-to-date with the current thesaurus.

Returns nil when no thesaurus is configured — without an index
there's nothing to compare against.

Interactive callers use `tibetan-thesaurus-audit-folder-display'
(below) to open a formatted results buffer; this function is
the data-only core.

** ~tibetan-thesaurus-rerun-affected-by-zettel~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 677
:SIGNATURE: (tibetan-thesaurus-rerun-affected-by-zettel zettel-path analysis-dir)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-analyse every segment under ANALYSIS-DIR whose Tibetan Text
contains the Wylie key of ZETTEL-PATH.  Preserves Claude sections
(`:re-request-claude nil') — only the parser-side output is
refreshed from the new thesaurus gloss.  Returns the list of
re-analysed file paths.

Typical use: after editing a thesaurus zettel to correct a gloss,
  M-x tibetan-thesaurus-rerun-affected-by-zettel
gets every analysis that mentions the term picking up the new
rendering without blindly batch-regenerating the whole folder.

** ~tibetan-thesaurus-translation-gaps~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 720
:SIGNATURE: (tibetan-thesaurus-translation-gaps analysis-dir)
:AUTOLOAD: t
:END:

Scan ANALYSIS-DIR and return a list of thesaurus entries whose
bilingual gloss is INCOMPLETE (either German or English primary
still a placeholder) AND which are referenced by at least one
segment's Tibetan Text.

Return format: list of plists, sorted by descending frequency:

  (:wylie WYLIE
   :frequency N
   :missing  "de" | "en" | "both"
   :zettel-path PATH
   :sanskrit SKT              ;; may be nil / placeholder
   :primary-en GLOSS-OR-NIL
   :primary-de GLOSS-OR-NIL)

Primes the German-target document-prep workflow: fill the
high-frequency gaps first, so the Interlinear + CAT Gloss render
cleanly for the document's most-used terms as early as possible.

Returns nil when no thesaurus is configured or the folder is
empty.

** ~tibetan-thesaurus--tokenise-wylie-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 797
:SIGNATURE: (tibetan-thesaurus--tokenise-wylie-text text)
:END:

Split TEXT into a flat list of Wylie tokens.
Uses whitespace, tsheg (`་'), slash (`/' — the shad marker),
and Tibetan punctuation as delimiters.  Case-sensitive.

** ~tibetan-thesaurus--count-mentions~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 804
:SIGNATURE: (tibetan-thesaurus--count-mentions wylie text)
:END:

Count word-boundary-safe occurrences of WYLIE in TEXT.

Tokenise TEXT (whitespace, `་', `/', punctuation) and scan for
consecutive token matches against the tokens of WYLIE.  Handles
multi-syllable Wylie keys like `rnam par shes pa' cleanly —
each of its 4 syllable-tokens must appear consecutively in the
text.  Word boundaries are implicit from tokenisation, so
`mthu' never matches inside `mthun'.

Case-sensitive matching so `DE' in metadata doesn't match `de'
in Wylie.

** ~tibetan-thesaurus-translation-gaps-display~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 839
:SIGNATURE: (tibetan-thesaurus-translation-gaps-display analysis-dir)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive `*Thesaurus Gaps*' buffer listing bilingual-incomplete
thesaurus entries referenced by segments in ANALYSIS-DIR, sorted
by descending frequency.  Each line names the Wylie, frequency,
which side is missing, Sanskrit term, and the zettel path.

Click (org-link) on a zettel path to jump in for editing.

Shows `All thesaurus entries are bilingual-complete.' when no
gaps remain for the folder.

** ~tibetan-thesaurus-audit-folder-display~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 886
:SIGNATURE: (tibetan-thesaurus-audit-folder-display analysis-dir)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive wrapper around `tibetan-thesaurus-audit-folder' that
opens a formatted *Thesaurus Audit* buffer listing every stale
analysis and the Wylie terms that triggered the flag.  Users
invoke this after a thesaurus editing session to see the
surface area of the change.

** ~tibetan-thesaurus-directory~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 57
:TYPE: '(choice (const
:GROUP: tibetan-thesaurus
:DEFAULT: nil
:END:

Directory where the user's thesaurus zettels live.
Each `*.org' file under this directory is a single term entry in
the format described in the module commentary.  Set this to your
thesaurus location — typically a subdir of your Buddhist-studies
zettelkasten, separate from the original Kramer glossary source.

When nil or unset, `tibetan-thesaurus-lookup' returns nil for
every query and the multisource framework silently falls through
to the next-ranked source (Resources / Steinert / etc.).

** ~tibetan-thesaurus-file-pattern~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 71
:TYPE: 'string
:GROUP: tibetan-thesaurus
:DEFAULT: "*.org"
:END:

Glob pattern under `tibetan-thesaurus-directory' that matches
individual thesaurus zettel files.  Default `*.org' picks up every
org file in the directory.

** ~tibetan-thesaurus-kramer-source-directory~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 78
:TYPE: '(choice (const nil
:GROUP: tibetan-thesaurus
:DEFAULT: nil
:END:

Source directory holding Jowita Kramer's Yogācāra glossary
zettels (seed for `tibetan-thesaurus-directory').  Used ONLY by
the one-shot `tibetan-thesaurus-initialize-from-kramer' command
to copy the Kramer files into a fresh thesaurus directory.  After
initialisation the thesaurus is the user's own — Kramer stays
intact at this path for reference.

This directory typically sits inside a larger zettelkasten holding
many non-Kramer files, so the copy step filters by
`tibetan-thesaurus-kramer-source-pattern' (default
`*kramer-glossary*.org') to avoid pulling in unrelated zettels.

** ~tibetan-thesaurus-kramer-source-pattern~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 93
:TYPE: 'string
:GROUP: tibetan-thesaurus
:DEFAULT: "*kramer-glossary*.org"
:END:

Glob pattern matching Kramer-glossary zettel files inside
`tibetan-thesaurus-kramer-source-directory'.  Only files matching
this pattern are copied by
`tibetan-thesaurus-initialize-from-kramer'.  The default
`*kramer-glossary*.org' matches the filename convention of
Carsten's 342-entry Kramer zettel set while leaving the rest of
his zettelkasten untouched.

** ~tibetan-thesaurus--index~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 234
:DEFAULT: nil
:END:

Cached thesaurus index (hash, Wylie → list of entry plists).
Built lazily by `tibetan-thesaurus--ensure-index'; invalidated by
`tibetan-thesaurus-reload' when the user edits entries or changes
`tibetan-thesaurus-directory'.

** ~tibetan-thesaurus--index-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 240
:DEFAULT: nil
:END:

Directory the cached index was built from.  When
`tibetan-thesaurus-directory' changes (or any zettel file's mtime
postdates the cache), a re-index is triggered.

** ~tibetan-thesaurus--index-build-time~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 245
:DEFAULT: nil
:END:

`current-time' value at which the cache was populated, used for
mtime-based invalidation.  A future refinement can compare this
against individual file modification stamps.

** ~tibetan-thesaurus~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 53
:END:

User-editable multilingual thesaurus for Tibetan translation work.


* core/tibetan-utils.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-utils.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-utils.el
:END:

** ~tibetan-get-current-line-as-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 23
:SIGNATURE: (tibetan-get-current-line-as-segment)
:END:

Get current line as a segment (for plain text without markers).
Returns cons cell (line-number . line-text) if line contains Tibetan text,
nil otherwise.

** ~tibetan-get-current-segment-any-format~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 43
:SIGNATURE: (tibetan-get-current-segment-any-format)
:END:

Get current segment ID and text from ANY format.
Tries in order:
1. Org structure (*** Segment N)
2. Old markers (〔seg:...〕)
3. Plain text line (for text-only documents)

Returns cons cell (seg-id . seg-text) or nil.

** ~tibetan-get-current-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 81
:SIGNATURE: (tibetan-get-current-segment)
:END:

Get current segment ID and text.
Handles both old format =〔seg:ID〕= and new format =〔seg〕=.
Returns cons cell (seg-id . seg-text) if point is within a segment,
nil otherwise.

** ~tibetan--get-segment-new-format~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 94
:SIGNATURE: (tibetan--get-segment-new-format pos)
:END:

Get segment in new format 〔seg〕...〔/seg〕 at POS.
Returns (line-number . text) or nil.

** ~tibetan--get-segment-old-format~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 108
:SIGNATURE: (tibetan--get-segment-old-format pos)
:END:

Get segment in old format 〔seg:ID〕...〔/seg〕 at POS.
Returns (id . text) or nil.

** ~tibetan-get-all-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 121
:SIGNATURE: (tibetan-get-all-segments)
:END:

Get all segments in current buffer.
Handles both old format 〔seg:ID〕 and new format 〔seg〕.
Returns list of cons cells (seg-id . seg-text).

** ~tibetan-normalize-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-normalize-text text)
:END:

Normalize Tibetan TEXT for processing.
Replaces spaces with tsheg, removes extra whitespace.

** ~tibetan-split-into-syllables~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 158
:SIGNATURE: (tibetan-split-into-syllables text)
:END:

Split Tibetan TEXT into syllables.
Returns list of syllables.

** ~tibetan-clean-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 165
:SIGNATURE: (tibetan-clean-text text)
:END:

Remove Tibetan punctuation from TEXT.
Keeps only syllables and tsheg.

** ~tibetan-display-in-side-window~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 175
:SIGNATURE: (tibetan-display-in-side-window buffer-name)
:END:

Display BUFFER-NAME in a side window (right or below).
Returns the window.

** ~tibetan-close-side-window~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 186
:SIGNATURE: (tibetan-close-side-window buffer-name)
:END:

Close side window displaying BUFFER-NAME.

** ~tibetan-insert-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 198
:SIGNATURE: (tibetan-insert-section title &optional level)
:END:

Insert org-mode section with TITLE at LEVEL (default 2).

** ~tibetan-insert-separator~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 203
:SIGNATURE: (tibetan-insert-separator)
:END:

Insert a visual separator line.


* core/tibetan-vocabulary-detailed.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-vocabulary-detailed.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-vocabulary-detailed.el
:END:

** ~tibetan-vocab-clear-cache~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 51
:SIGNATURE: (tibetan-vocab-clear-cache)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Clear the detailed vocabulary cache.
Use this after reloading vocabulary code so that updated lookup logic
(e.g. verb-stem enrichment) takes effect for already-queried words.

** ~tibetan-vocab--first-sense-bracket-aware~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 64
:SIGNATURE: (tibetan-vocab--first-sense-bracket-aware text)
:END:

Return the prefix of TEXT up to the first `;', `,', or `.' that
appears at bracket-depth zero.  `[]' and `()' nest, so a comma
inside `[accusative, adverbial …]' does NOT terminate the sense.

Returns the full TEXT if no terminator is found at depth zero.

** ~tibetan-vocab--split-senses-bracket-aware~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 89
:SIGNATURE: (tibetan-vocab--split-senses-bracket-aware text)
:END:

Split TEXT into senses at `;' separators that sit at bracket-depth
zero.  `[]' and `()' nest, so the `;' inside `[Skt; loan]' does NOT
split (M5, Fable-5 audit).  Returns a list of trimmed senses.

** ~tibetan-vocab--mostly-tibetan-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 107
:SIGNATURE: (tibetan-vocab--mostly-tibetan-p text)
:END:

Return non-nil when TEXT is a Tibetan-script EXAMPLE, not a gloss.
True when TEXT contains at least one Tibetan-block character AND no
Latin letter.  A genuine gloss is always written in English or
German (Latin script); a sense that is pure Tibetan is a usage
example baked into the dictionary entry and must not surface as the
word's meaning.

** ~tibetan-vocab--parse-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 125
:SIGNATURE: (tibetan-vocab--parse-entry raw-entry)
:END:

Parse RAW-ENTRY string into structured format.
Returns plist with :primary :detailed :sanskrit.

** ~tibetan-vocab--get-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 192
:SIGNATURE: (tibetan-vocab--get-wylie tibetan-word)
:END:

Get Wylie transliteration for TIBETAN-WORD.

** ~tibetan-vocab--tibetan-from-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 200
:SIGNATURE: (tibetan-vocab--tibetan-from-wylie wylie)
:END:

Get Tibetan script from WYLIE transliteration.

** ~tibetan-vocab--extract-stem-reference~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 219
:SIGNATURE: (tibetan-vocab--extract-stem-reference text)
:END:

Return the base Wylie form referenced in TEXT.
For example \=`byed\=' for input containing \=`pf. of byed\='.
Returns nil if no stem reference is found.

** ~tibetan-vocab--already-has-gloss-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 232
:SIGNATURE: (tibetan-vocab--already-has-gloss-p text)
:END:

Return non-nil if TEXT already contains a gloss after the stem reference,
i.e. a semicolon or the German//English separator following the reference.

** ~tibetan-vocab--lookup-base-meaning~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 249
:SIGNATURE: (tibetan-vocab--lookup-base-meaning wylie-base)
:END:

Return a short English meaning for WYLIE-BASE.
Prefers the Hill 2010 verb table (authoritative for common verbs like
byed/gtong/dzugs), then falls back to `tibetan-vocab-lookup-detailed'.
The verb table is consulted first because general dictionaries (RY,
DharmaMitra) often return idiomatic senses for bare verbs.

** ~tibetan-vocab--enrich-with-base~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 268
:SIGNATURE: (tibetan-vocab--enrich-with-base entry)
:END:

If ENTRY's :primary is a bare stem reference (e.g. "pf. of byed"),
look up the base form and append its primary meaning.
Returns a possibly-modified copy of ENTRY. Recursion-safe via a
dynamic flag `tibetan-vocab--enriching'.

** ~tibetan-vocab--mwu-exists-p~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 322
:SIGNATURE: (tibetan-vocab--mwu-exists-p compound)
:AUTOLOAD: t
:END:

Return non-nil when COMPOUND (a tsheg-joined Tibetan string) is
the EXACT key of an entry in any consulted dictionary.

The strict MWU existence check, shared by
`tibetan-extract-vocabulary' (Interlinear) and
`tibetan-vocab-extract-detailed' (Detailed Dictionary) so both
agree on which syllable groupings form MWUs.  Critical:  this
does NOT fall back to particle-stripped substring matches —
unlike `tibetan-lookup-word' (which strips trailing particles
like `སོ' / `ནས' before hashing) or `tibetan-vocab-lookup-detailed'
(which dispatches into `tibetan-lookup-word-in-rangjung-yeshe',
which itself calls `tibetan-strip-particles').

That strip-fallback was the root cause of the Tibetisch IV
seg-049 export crash (2026-05-18):  the greedy 3-syll MWU
candidate `rnams ngo so' returned the `rnams' gloss after the
strip pass, the MWU loop accepted the bogus 3-token group, and
the Interlinear emitted a dangling `[[term-rnams-ngo-so][...]]'
link.  `org-export-dispatch' then aborted on the unresolved
target.

Implementation:  direct `gethash' against each consulted hash
table, plus exact-key DB probes for Steinert and Thesaurus.
NO `tibetan-strip-particles' fallback anywhere.  Returns the
matching entry on the first hit (any non-nil value);  nil when
all consulted sources lack the EXACT key.

Consulted sources, in priority order:
  Resources → Custom → bundled glossary → Rangjung Yeshe
  → Steinert → Thesaurus
all via strict exact-key lookups.

** ~tibetan-vocab-lookup-detailed~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 388
:SIGNATURE: (tibetan-vocab-lookup-detailed word)
:END:

Look up WORD and return detailed dictionary entry.
Tries: Resources vocab, custom vocab, bundled glossaries, DharmaMitra.
WORD can be Tibetan script or Wylie.
Returns plist with :primary :detailed :sanskrit :wylie :source, or nil.

** ~tibetan-vocab-extract-detailed~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 522
:SIGNATURE: (tibetan-vocab-extract-detailed tibetan-text)
:END:

Extract detailed vocabulary from TIBETAN-TEXT.
Returns list of plists, each with keys
:tibetan :wylie :primary :detailed :sanskrit :source.
Uses greedy matching: tries longer compounds first (4, 3, 2 syllables)
before falling back to single syllables.  Provides a consistent
format for both the classroom view (C-c u i) and the persistent
analysis (C-c u A).

** ~tibetan-vocab-format-entry-short~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 662
:SIGNATURE: (tibetan-vocab-format-entry-short entry)
:END:

Format ENTRY plist as short one-line summary.
Format: ཚིག /*wylie*/ — primary meaning

** ~tibetan-vocab-format-entry-full~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 673
:SIGNATURE: (tibetan-vocab-format-entry-full entry)
:END:

Format ENTRY plist as full dictionary entry.
Format:
  ཚིག /*wylie*/
    Primary: meaning
    Full: detailed explanation
    Sanskrit: term (if available)
    Source: glossary name

** ~tibetan-vocab-format-list-short~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 700
:SIGNATURE: (tibetan-vocab-format-list-short vocab-list)
:END:

Format VOCAB-LIST as short vocabulary section for analysis files.

** ~tibetan-vocab-format-list-full~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 704
:SIGNATURE: (tibetan-vocab-format-list-full vocab-list)
:END:

Format VOCAB-LIST as full dictionary section.

** ~tibetan-vocab--dedup-fingerprint~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 739
:SIGNATURE: (tibetan-vocab--dedup-fingerprint text)
:END:

Return a normalised fingerprint of TEXT for gloss deduplication.
Lower-cases, collapses whitespace, strips punctuation, and truncates
to 80 chars.  Two entries with the same fingerprint are treated as
near-duplicates and only the first is shown.

** ~tibetan-vocab--looks-like-sanskrit-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 760
:SIGNATURE: (tibetan-vocab--looks-like-sanskrit-p text)
:END:

Return non-nil when TEXT appears to be genuine Sanskrit content
rather than a Wylie cross-reference masquerading in the Sanskrit field.

Heuristic:
  1. Devanāgarī characters (U+0900–U+097F) → Sanskrit.  True positive.
  2. Any IAST diacritic (see `tibetan-vocab--sanskrit-diacritics') →
     Sanskrit.  Real Sanskrit transliteration nearly always has them.
  3. Curly-brace Wylie citation `{...}' → Wylie cross-ref, not Sanskrit.
  4. Leading `=' sign — common Steinert convention for Wylie
     cross-references (`= {gzhi yis/}', `= ste') — not Sanskrit.
  5. Otherwise, if every token is ASCII lowercase without diacritics,
     assume Wylie (e.g. `ste', `bdag nyid').

False positives are cheaper than false negatives (losing real Sanskrit),
so when in doubt we fall through to `t'.

** ~tibetan-vocab--make-source-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 795
:SIGNATURE: (tibetan-vocab--make-source-entry seen source-name raw-gloss
                                              native-sanskrit wylie)
:END:

Build a source-entry plist for RAW-GLOSS, or return nil to skip.

SEEN is a hash-table tracking gloss fingerprints already emitted; if
the fingerprint of this entry is present (or empty), nil is returned
and SEEN is left untouched.  On success, the fingerprint is recorded
in SEEN and a plist (:source :primary :detailed :sanskrit :wylie)
is returned.

Sanskrit is preferred from NATIVE-SANSKRIT (the source row's own
field), falling back to whatever the parser extracts from the gloss
text — never synthesised from elsewhere.

** ~tibetan-vocab-rank-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 852
:SIGNATURE: (tibetan-vocab-rank-source source)
:END:

Return an integer rank for SOURCE (a :source plist value).  Lower
is better.  Used by `tibetan-vocab--sort-entries-by-rank' to pick the
best dictionary entry per term — the one whose gloss is surfaced by
the Interlinear Gloss and appears first in the Detailed Dictionary.

Rank layers (2026-04-22):

  1 — Thesaurus (user-curated via zettelkasten — reserved for Pass 5b).
  2 — Resources / Custom (per-document vocabulary list).
  3 — Corpus-specific (e.g. `22-Yoghacharabhumi-glossary' when
      reading a YBh text — set via `tibetan-vocab--corpus').
  4 — `Steinert/01-Hopkins2015' (modern scholarly Buddhist).
  5 — `Steinert/43-84000Dict' (84000 canonical translations).
  6 — `Steinert/02-RangjungYeshe' (Buddhist practice vocabulary).
  7 — `Rangjung Yeshe' (standalone, separate integration).
  8 — `Steinert/08-IvesWaldo', `Steinert/07-JimValby',
      `Steinert/09-DanMartin', `Steinert/10-RichardBarron' etc.
  9 — `Bundled' (synthetic local glossary).
 10 — `DharmaMitra' (last-resort API fallback).
 99 — any unrecognised source.

** ~tibetan-vocab--sort-entries-by-rank~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 902
:SIGNATURE: (tibetan-vocab--sort-entries-by-rank entries)
:END:

Return ENTRIES sorted by source rank, preserving original order
within ties.  Emacs' `sort' is stable, so two entries with the same
rank stay in insertion order.

** ~tibetan-vocab-find-sanskrit~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 911
:SIGNATURE: (tibetan-vocab-find-sanskrit entries)
:END:

Return the first genuine Sanskrit gloss found in ENTRIES, or nil.
Walks the ranked list (or any list) and returns the first `:sanskrit'
field that passes `tibetan-vocab--looks-like-sanskrit-p' — filtering
out Wylie cross-references that masquerade in the Sanskrit slot.

** ~tibetan-vocab-best-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 924
:SIGNATURE: (tibetan-vocab-best-entry entries)
:END:

Return the top-ranked entry from ENTRIES (or nil).  Shortcut for
`(car (tibetan-vocab--sort-entries-by-rank ENTRIES))'.

** ~tibetan-vocab-term-anchor~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 929
:SIGNATURE: (tibetan-vocab-term-anchor wylie)
:END:

Return a stable org-mode anchor slug for WYLIE.
Used by the Interlinear Gloss renderer as the target of its internal
link (`[[term-bdag-la][bdag la]]') pointing at the matching
`<<term-bdag-la>>' target in the Detailed Dictionary section below.

The slug is lowercased, spaces and apostrophes become dashes, and
anything outside `[-a-z0-9]' is dropped.  Prefixed with `term-' so
the namespace doesn't collide with user-defined org targets.

Examples:
  (tibetan-vocab-term-anchor "bdag la")     \=> "term-bdag-la"
  (tibetan-vocab-term-anchor "rnal \='byor")  \=> "term-rnal-byor"
  (tibetan-vocab-term-anchor "byang chub sems dpa\='") \=>
    "term-byang-chub-sems-dpa"

** ~tibetan-vocab-detailed-cap-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 966
:SIGNATURE: (tibetan-vocab-detailed-cap-entries entries)
:END:

Return ENTRIES trimmed per `tibetan-vocab-detailed-max-sources'.
Resources and Custom entries (user-curated) are always kept; the cap
only applies to subsequent automatic-dictionary sources.  Order is
preserved.

Example: with max=3 and entries
  (Resources Custom Steinert/01 Steinert/02 Rangjung DharmaMitra)
returns
  (Resources Custom Steinert/01 Steinert/02 Rangjung)
(2 curated kept + 3 automatic = 5 total).

** ~tibetan-vocab-multisource-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 990
:SIGNATURE: (tibetan-vocab-multisource-entries word)
:END:

Return an ordered, deduplicated list of dictionary entries for WORD.
Each element is a plist (:source :primary :detailed :sanskrit :wylie).

Priority order:
  1. Resources (provided per-document vocabulary list)
  2. Custom (user list)
  3. Steinert aggregated DB — several top hits
  4. Rangjung Yeshe standalone
  5. Bundled glossaries (Hopkins / Bialek)
  6. DharmaMitra

Near-duplicates are skipped, so polysemous entries like ཡུལ do not
produce ten variants of the same gloss.  Sanskrit is emitted only
when the source entry carries it natively.

** ~tibetan-vocab-detailed-max-sources~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 953
:TYPE: 'integer
:GROUP: tibetan-cat
:DEFAULT: 3
:END:

Maximum number of source entries emitted per term in the Detailed
Dictionary.  Resources and Custom entries are ALWAYS preserved — the
cap only trims subsequent automatic-dictionary hits (Steinert /
Rangjung Yeshe / Bundled / DharmaMitra).  Raise this to see the
underlying polysemy in full; lower it to compact the section.

Default 3 strikes a balance: one curated source (if present) + up to
three reference glosses covers the sense space for class reading
without turning each entry into a wall of near-duplicates.

** ~tibetan-detailed-vocab-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 45
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for detailed vocabulary entries.
Key: Tibetan or Wylie term
Value: plist with :primary :detailed :sanskrit :wylie :source

** ~tibetan-vocab--stem-ref-pattern~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 212
:DEFAULT: "\\b\\(?:pf\\.\\|Pf\\.\\|pres\\.\\|pres\\|fut\\.\\|Fut\\.\\|ft\\.\\|imp\\.\\|Imp\\.\\)[ \t]+\\(?:of\\|von\\|zu\\)[ \t]+\
:END:

Regex matching verb-stem references in vocabulary entries.
Matches patterns such as \=`pf. of byed\=', \=`Pf. von rgyal du dzugs\=',
\=`fut. of sleb pa\=', \=`Imp. von gtong\='.  The base Wylie form is
captured in group 1.

** ~tibetan-vocab--enriching~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 245
:DEFAULT: nil
:END:

Non-nil while a stem-reference base lookup is in progress; disables
further enrichment to prevent recursion cycles.

** ~tibetan-current-resources-vocab~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 316
:END:

/(no docstring)/

** ~tibetan-current-custom-vocab~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 317
:END:

/(no docstring)/

** ~tibetan-comprehensive-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 318
:END:

/(no docstring)/

** ~tibetan-rangjung-yeshe-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 319
:END:

/(no docstring)/

** ~tibetan-vocab--steinert-cap~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 732
:DEFAULT: 5
:END:

Maximum number of Steinert rows surfaced per word in the multi-source
Detailed Dictionary block.  Lower values produce tighter blocks at the
cost of variety for deeply polysemous terms; higher values restore the
wider coverage.  Tuned down from the original 8 after real-segment
review showed words like ཡུལ / ཤོ / ལ producing overly long blocks.

** ~tibetan-vocab--sanskrit-diacritics~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 751
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-vocab--corpus~  /variable (buffer-local)/
:PROPERTIES:
:KIND: defvar-local
:LINE: 834
:DEFAULT: nil
:END:

Per-buffer corpus-specific Steinert sub-dictionary promoted by the
ranker to rank-2 slot (below Thesaurus / Resources / Custom).  Set
by the reader from the source file's `#+TIBETAN_CORPUS:' header —
e.g. `Yogacarabhumi' picks `22-Yoghacharabhumi-glossary' as the
corpus source.  Unset: the ranker still works, just without the
corpus boost.

** ~tibetan-vocab--corpus-source-map~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 842
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-vocabulary.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-vocabulary.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-vocabulary.el
:END:

** ~tibetan-get-buffer-dict-priority~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 95
:SIGNATURE: (tibetan-get-buffer-dict-priority)
:END:

Get dictionary priority for current buffer.
Checks for #+TIBETAN_DICT_PRIORITY header first, falls back to
`tibetan-dictionary-priority' customization.

** ~tibetan--lookup-in-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 107
:SIGNATURE: (tibetan--lookup-in-source source word &optional word-stripped)
:END:

Look up WORD in dictionary SOURCE.  Try WORD-STRIPPED as fallback.
Returns meaning string or nil.

** ~tibetan-vocab-safe-substring~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 147
:SIGNATURE: (tibetan-vocab-safe-substring str start &optional end)
:END:

Safely extract substring from STR between START and END.
Returns empty string if indices are out of range or invalid.

** ~tibetan-rangjung-yeshe-get-path~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 188
:SIGNATURE: (tibetan-rangjung-yeshe-get-path)
:END:

Get path to Rangjung Yeshe dictionary file.
Returns the Tibetan-keyed version if it exists, otherwise Wylie version.

** ~tibetan-rangjung-yeshe-load~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 205
:SIGNATURE: (tibetan-rangjung-yeshe-load)
:END:

Load Rangjung Yeshe dictionary (lazy, called on first lookup).
This is a large dictionary (162k entries) so we load it on-demand.
Stores entries under both Tibetan and Wylie keys for flexible lookup.

** ~tibetan-lookup-word-in-rangjung-yeshe~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 242
:SIGNATURE: (tibetan-lookup-word-in-rangjung-yeshe word)
:END:

Look up WORD in the combined 64-source dictionary or Rangjung Yeshe.
Tries the combined dictionary first (if loaded from init-tibetan-legacy.el),
then falls back to the local Rangjung Yeshe copy.
Returns meaning if found, nil otherwise.

** ~tibetan--steinert-maybe-hint-build~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 295
:SIGNATURE: (tibetan--steinert-maybe-hint-build)
:END:

Print a one-time hint pointing at the DB build script.
Only fires when the SQLite backend is present but no DB is on disk,
so users who have intentionally disabled Steinert never see it.

** ~tibetan-lookup-word-in-steinert~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 307
:SIGNATURE: (tibetan-lookup-word-in-steinert word)
:END:

Look up WORD in the Steinert dictionary collection.
Returns a single definition string tagged with its source
(e.g. "...explanation... [01-Hopkins2015]"), or nil when the
word is absent / the DB is unavailable.

This is the legacy-shaped entry point used by the priority
dispatcher in `tibetan--lookup-in-source'. The richer plist API
lives in `tibetan-steinert-lookup' in core/tibetan-steinert.el.

** ~tibetan-get-custom-vocab-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 339
:SIGNATURE: (tibetan-get-custom-vocab-file)
:END:

Get custom vocabulary file path from #+TIBETAN_VOCAB_FILE header.
Returns absolute path or nil if not set.

** ~tibetan-find-resources-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 352
:SIGNATURE: (tibetan-find-resources-folder)
:END:

Find Resources folder relative to the current context.
Looks in: ./Resources, ../Resources, ../../Resources — relative to
the current buffer's file, or to `default-directory' when the buffer
is not visiting a file (e.g. headless batch reanalysis runs
`generate-content' in `*scratch*', where `buffer-file-name' is nil;
without this fallback the curated Resources glosses were silently
dropped from every regenerated file — 2026-06-03 corpus wipe).
Returns absolute path or nil.

** ~tibetan-parse-wordlist-pdf~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 371
:SIGNATURE: (tibetan-parse-wordlist-pdf pdf-path)
:END:

Parse a Tibetan wordlist PDF (Wortliste format).
Format: Wylie or Tibetan term followed by German // English definitions.
Returns hash-table with both Wylie and Tibetan keys.

** ~tibetan--store-vocab-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 439
:SIGNATURE: (tibetan--store-vocab-entry vocab-table term def)
:END:

Store TERM with DEF in VOCAB-TABLE under both Wylie and Tibetan keys.
Strips folio markers like (12a5) and collapses extra whitespace.

** ~tibetan-parse-wordlist-txt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 462
:SIGNATURE: (tibetan-parse-wordlist-txt txt-path)
:END:

Parse a plain text Tibetan wordlist file.
Format: One entry per line, Tibetan/Wylie term followed by definition.
Supports formats:
  - TAB-separated: term<TAB>definition
  - Colon-separated: term: definition
  - Dash-separated: term - definition
  - Indented: term on non-indented line, definition on indented line(s) below
Returns hash-table with both Wylie and Tibetan keys.

** ~tibetan-parse-wordlist-org~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 542
:SIGNATURE: (tibetan-parse-wordlist-org org-path)
:END:

Parse an org-mode Tibetan wordlist file.
Format: Headings are terms, content below is definition.
​  * term
    definition
Or table format:
  | term | definition |
Returns hash-table with both Wylie and Tibetan keys.

** ~tibetan-load-resources-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 580
:SIGNATURE: (tibetan-load-resources-vocab)
:END:

Load vocabulary from Resources folder if present.
Looks for wordlist files (PDF, TXT, ORG) in the Resources folder.
Priority: TXT > ORG > PDF (PDF requires pdftotext).

** ~tibetan-lookup-word-in-resources-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 632
:SIGNATURE: (tibetan-lookup-word-in-resources-vocab word)
:END:

Look up WORD in current Resources vocabulary.
Returns meaning if found, nil otherwise.

** ~tibetan-parse-vocab-pdf~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 649
:SIGNATURE: (tibetan-parse-vocab-pdf pdf-path)
:END:

Parse a vocabulary PDF file and return a hash-table of entries.
Supports two formats:
1. Tibetan script: ཚིག་མཛོད།\n   definition
2. Wylie transliteration: shes rab\n   definition
Both Tibetan and Wylie keys are stored for lookup.

** ~tibetan--maybe-store-vocab-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 718
:SIGNATURE: (tibetan--maybe-store-vocab-entry vocab-table term def)
:END:

Store TERM with DEF in VOCAB-TABLE under Wylie and (if convertible)
Tibetan keys, when TERM is non-nil and DEF non-empty.  No-op otherwise.

** ~tibetan-load-custom-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 730
:SIGNATURE: (tibetan-load-custom-vocab)
:END:

Load custom vocabulary for current buffer if #+TIBETAN_VOCAB_FILE is set.
Caches the parsed vocabulary for reuse.

** ~tibetan-lookup-word-in-custom-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 746
:SIGNATURE: (tibetan-lookup-word-in-custom-vocab word)
:END:

Look up WORD in current buffer's custom vocabulary.
Tries Tibetan script first, then converts to Wylie for lookup.
Returns meaning if found, nil otherwise.

** ~tibetan-strip-particles~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 770
:SIGNATURE: (tibetan-strip-particles word)
:END:

Strip common particles and punctuation from WORD to find root form.
Only strips unambiguous multi-character particles to avoid false positives.
Also trims any trailing tsheg (་) or whitespace so that stripped forms
match the canonical (tsheg-free) keys used in the Resources and other
dictionaries.

Verb-stem guard: if WORD is itself a known Hill-DB verb form (past,
imperative, etc.), it is returned unchanged.  Without this guard
`བསླབས' (past of སློབ) and `སླེབས' (past of སླེབ) would match the
`བས' causal-converb pattern and get split into nonsense roots
(`བསླ' / `སླེ') that fail every dictionary lookup downstream.

** ~tibetan-lookup-word-in-local-glossary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 816
:SIGNATURE: (tibetan-lookup-word-in-local-glossary word)
:END:

Look up WORD in local comprehensive glossary.
Tries Tibetan script first, then converts to Wylie for lookup.
Returns meaning if found, nil otherwise.

** ~tibetan-lookup-word-in-dharmamitra~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 840
:SIGNATURE: (tibetan-lookup-word-in-dharmamitra word)
:END:

Look up WORD in DharmaMitra with caching (positive AND negative).
Returns meaning if found, nil otherwise.

A miss is cached as the sentinel `none' so repeated lookups of an
out-of-dictionary word during a batch do not re-hit the network on
every call (previously only hits were cached).

** ~tibetan-format-bilingual-meaning~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 869
:SIGNATURE: (tibetan-format-bilingual-meaning english german)
:END:

Format ENGLISH and GERMAN meanings, English first, German in brackets.
If only one is available, return that. If both, format as
='english (DE: german)='.

** ~tibetan-extract-english-from-bilingual~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 885
:SIGNATURE: (tibetan-extract-english-from-bilingual meaning)
:END:

Extract English part from a bilingual meaning string.
Handles formats like ='German // English=' or ='German (English)='.

** ~tibetan--collect-bilingual~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 903
:SIGNATURE: (tibetan--collect-bilingual word &optional word-stripped)
:END:

Collect English and German meanings for WORD from all sources.
Respects `tibetan-dictionary-priority' for ordering.
Returns (ENGLISH-MEANING . GERMAN-MEANING) cons cell.

** ~tibetan-lookup-word~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 930
:SIGNATURE: (tibetan-lookup-word word &optional prev-word)
:END:

Look up WORD with optional PREV-WORD for compound detection.
Returns meaning with bilingual merge (English + German).
Dictionary priority is controlled by `tibetan-dictionary-priority'
and can be overridden per-buffer with #+TIBETAN_DICT_PRIORITY.

** ~tibetan-extract-vocab--tail-is-particle-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 977
:SIGNATURE: (tibetan-extract-vocab--tail-is-particle-p joined)
:END:

Return non-nil when JOINED (a `་'-joined Tibetan compound) ends
in a case or converb particle listed in
`tibetan-extract-vocab--particle-tails'.

** ~tibetan-extract-vocab--head-is-particle-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 986
:SIGNATURE: (tibetan-extract-vocab--head-is-particle-p joined)
:END:

Return non-nil when JOINED starts with a case or converb particle
listed in `tibetan-extract-vocab--particle-tails'.

A particle can never legitimately be the HEAD of a compound — it's
always attached to a preceding content word.  This rejection
prevents the greedy compound loop from stranding a particle with
the following word: e.g. after `བདག་ལ' has split into stem + LA,
the next iteration starts at `ལ' and must not be allowed to pick
up `ལ་སྟོད' ("Latö, western Tsang") as a 2-syllable compound.

** ~tibetan-extract-vocab--tail-is-verb-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1003
:SIGNATURE: (tibetan-extract-vocab--tail-is-verb-p joined)
:END:

Return non-nil when JOINED (a `་'-joined compound of ≥2 syllables)
ends in a syllable that `tibetan-verb-lookup' recognises as a finite
Hill-DB verb.

Used to stop the greedy Interlinear MWU loop from gluing an
auto-sourced phrasal dictionary entry onto a following verb — e.g.
the Rangjung-Yeshe entry `དྲུང་དུ་ཕྱིན' was absorbing the verb `ཕྱིན'
("went"), hiding it from clause / Verb-Classification analysis and
surfacing the entry's Tibetan example sentence as the token's gloss
(Milarepa Segment 39).

Mirrors `tibetan-enhanced-parser--verb-tail-p' for the second MWU
loop.  No-op (returns nil) when the verb classifier isn't loaded.

** ~tibetan-extract-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1023
:SIGNATURE: (tibetan-extract-vocabulary tibetan-text)
:END:

Extract vocabulary from TIBETAN-TEXT with meanings.
Returns list of (word . meaning) pairs.

MWU detection uses `tibetan-vocab--mwu-exists-p' (the strict,
exact-key check shared with `tibetan-vocab-extract-detailed') so
the Interlinear Gloss and Detailed Dictionary agree on which
syllable groupings form MWUs.  Without this agreement, the
Interlinear used to emit `[[term-X][label]]' links whose `<<term-X>>'
anchors didn't exist in DD, breaking `org-export-dispatch'
(Tibetisch IV seg-049 report, 2026-05-18).

Greedy matching:  tries 4 / 3 / 2 syllable compounds before
falling back to single.  Rejects any compound whose head OR tail
syllable is a case / converb particle (`tibetan-extract-vocab--
{head,tail}-is-particle-p').

The legacy `verb+converb' merging path (which used to combine
`phyar' + `nas' into a `phyar nas [converb]' display token) has
been REMOVED — DD never produced a matching anchor for such
combinations, so they showed up as dangling links.  Verb
morphology is rendered separately by Particle Map, Clause
Structure, and Grammar.

Per-stem GLOSS lookup falls back through
`tibetan-lookup-word' (particle-stripping bilingual lookup).
Strict existence check guards MWU DETECTION;  loose gloss lookup
populates the final `(word . meaning)' cell.

** ~tibetan-vocabulary-initialize~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1141
:SIGNATURE: (tibetan-vocabulary-initialize)
:END:

Initialize vocabulary by loading bundled glossaries.
Called automatically when tibetan-vocabulary is loaded.
Set `tibetan-skip-external-glossaries' to non-nil to skip loading.

As of the glossary-loader unification (2026-04) there is one canonical
bundled module — `tibetan-glossary-loader' — which `tibetan-cat.el'
already `require's at boot.  That module's `load-all-glossaries' is
idempotent (guards on `tibetan-glossaries-loaded'), so calling it
here is safe even though it has already been auto-called when the
loader module was required.  The legacy external-path fallback is
kept for users who install the package without the bundled data dir.

** ~reload-all-glossaries~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1166
:SIGNATURE: (reload-all-glossaries)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Reload all glossaries, forcing a fresh read from disk.
Uses the canonical `load-all-glossaries' with the FORCE flag, which
resets the idempotence guard and re-emits every bundled source.

** ~tibetan-vocab-extract-short-meaning~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1194
:SIGNATURE: (tibetan-vocab-extract-short-meaning full-meaning)
:END:

Extract a short, readable meaning from FULL-MEANING.
Returns just the core translation without lengthy explanations.

** ~tibetan-vocab-has-extended-info-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1216
:SIGNATURE: (tibetan-vocab-has-extended-info-p full-meaning)
:END:

Check if FULL-MEANING has extended information worth showing.

** ~tibetan-vocab-format-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1222
:SIGNATURE: (tibetan-vocab-format-entry tibetan-word full-meaning &optional format-type)
:END:

Format a single vocabulary entry for display.
TIBETAN-WORD is the Tibetan text.
FULL-MEANING is the full dictionary meaning.
FORMAT-TYPE controls output style:
  nil or ='compact=  - Short one-line format: Tibetan *wylie* — short-meaning
  ='org-compact=     - Org format with short meaning (for vocab section)
  ='org-detailed=    - Org format with full meaning (for detailed section)
  ='full=            - Plain text with full meaning (for classroom detailed)
Returns formatted string.

** ~tibetan-vocab-format-list~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1264
:SIGNATURE: (tibetan-vocab-format-list vocab-pairs &optional format-type)
:END:

Format a list of vocabulary pairs for display.
VOCAB-PAIRS is list of (tibetan . meaning) cons cells.
FORMAT-TYPE is passed to `tibetan-vocab-format-entry'.
Returns formatted string.

** ~tibetan-vocab-format-detailed-list~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1276
:SIGNATURE: (tibetan-vocab-format-detailed-list vocab-pairs &optional for-org)
:END:

Format vocabulary list with FULL dictionary entries.
VOCAB-PAIRS is list of (tibetan . meaning) cons cells.
FOR-ORG if non-nil, uses org-mode formatting.
Returns formatted string with complete dictionary information.

** ~tibetan-dictionary-priority~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 52
:TYPE: '(repeat (choice (const
:GROUP: tibetan
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-dharmamitra-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 169
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for DharmaMitra word translations to avoid repeated API calls.

** ~tibetan-custom-vocab-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 172
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for custom vocabulary files.
Key: absolute path to vocab file
Value: hash-table of (tibetan-term . definition)

** ~tibetan-rangjung-yeshe-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 181
:DEFAULT: nil
:END:

Hash table for Rangjung Yeshe dictionary entries.
Lazy-loaded on first lookup to avoid slow startup.

** ~tibetan-rangjung-yeshe-loaded~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 185
:DEFAULT: nil
:END:

Whether Rangjung Yeshe dictionary has been loaded.

** ~tibetan-current-custom-vocab~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 274
:DEFAULT: nil
:END:

Current buffer's custom vocabulary hash-table, or nil if none.

** ~tibetan--steinert-build-hint-shown~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 292
:DEFAULT: nil
:END:

Non-nil once the one-shot build-hint message has been emitted.

** ~tibetan-resources-vocab-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 332
:DEFAULT: (make-hash-table :test 'equal)
:END:

Cache for Resources folder vocabulary.
Key: directory path, Value: hash-table of (term . definition)

** ~tibetan-current-resources-vocab~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 336
:DEFAULT: nil
:END:

Current buffer's Resources vocabulary hash-table, or nil if none.

** ~tibetan-extract-vocab--particle-tails~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 959
:DEFAULT: '
:END:

/(no docstring)/


* core/tibetan-wylie.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-wylie.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-wylie.el
:END:

** ~tibetan-safe-substring~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 14
:SIGNATURE: (tibetan-safe-substring str start &optional end)
:END:

Safely extract substring from STR between START and END.
Returns empty string if indices are out of range.

** ~tibetan-to-wylie-fixed~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 32
:SIGNATURE: (tibetan-to-wylie-fixed tibetan-text)
:END:

Convert Tibetan Unicode text to Wylie transliteration.
FIXED: Properly handles implicit ='a=' vowels after each consonant.

** ~tibetan-is-prefix~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 79
:SIGNATURE: (tibetan-is-prefix char next-char)
:END:

Check if CHAR is a valid prefix before NEXT-CHAR in Tibetan.
Prefixes don't get implicit ='a=' vowel.

** ~tibetan-syllable-to-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 97
:SIGNATURE: (tibetan-syllable-to-wylie syllable)
:END:

Convert a single Tibetan syllable to Wylie.
Handles implicit ='a=' vowels correctly after each consonant.
Recognizes Tibetan syllable structure: [prefix] ROOT [subscript]
[vowel] [suffix]


* core/tibetan-zettel.el
:PROPERTIES:
:CUSTOM_ID: core/tibetan-zettel.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/core/tibetan-zettel.el
:END:

** ~tibetan-zettel--glossar-tagged-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 94
:SIGNATURE: (tibetan-zettel--glossar-tagged-p path)
:END:

Return non-nil when PATH's filename carries the Denote `__glossar' tag.
Checks the filename base (with extension); matches the tag in
any position of the Denote tag chain — first (`...__glossar.org'),
middle (`...__glossar__buddhist-term.org'), or last.

** ~tibetan-zettel--has-wylie-property-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (tibetan-zettel--has-wylie-property-p content)
:END:

Return non-nil when CONTENT has a `:wylie:' property with a value.
CONTENT is a string (typically the first N bytes of a zettel file).
A bare `:wylie:' line with no value does NOT count — we need the
lookup key.

** ~tibetan-zettel--translation-relevant-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 117
:SIGNATURE: (tibetan-zettel--translation-relevant-p path)
:END:

Return non-nil when PATH is a translation-relevant zettel.
Detection: `__glossar' filename tag OR `:wylie:' property (OR
semantics).  Reads only the first 4096 bytes of the file for the
property check so huge files don't pathologically slow the
indexer.

** ~tibetan-zettel--extract-property~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 133
:SIGNATURE: (tibetan-zettel--extract-property content key)
:END:

Return the value of `:KEY:' property from CONTENT, or nil.
Case-insensitive match on KEY.  Returns the first match; zettels
are expected to have at most one PROPERTIES drawer.  Whitespace
is trimmed from the value.

** ~tibetan-zettel--has-heading-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-zettel--has-heading-p content heading)
:END:

Return non-nil when CONTENT has a heading matching HEADING.
HEADING is the literal text after the stars (e.g. `Claude
Explanation').  Matches any level of heading (`*' to `*****').

** ~tibetan-zettel--v1-extract-primary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 164
:SIGNATURE: (tibetan-zettel--v1-extract-primary content section)
:END:

In CONTENT, find `** SECTION' and return the `- Primary translation:'
value within it, or nil.  SECTION is e.g. "English" / "German".

** ~tibetan-zettel--parse-v2~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 181
:SIGNATURE: (tibetan-zettel--parse-v2 content path)
:END:

Parse CONTENT at PATH as a v2 (properties-drawer) zettel.
Returns a plist, or nil if no `:wylie:' property is found (can't
index without a key).

** ~tibetan-zettel--parse-v1~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 212
:SIGNATURE: (tibetan-zettel--parse-v1 content path)
:END:

Parse CONTENT at PATH as a Pass-5b Kramer-format zettel.
Returns a plist with the same shape as the v2 parser, or nil if
no `- Wylie:' field is found.

** ~tibetan-zettel--parse-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 240
:SIGNATURE: (tibetan-zettel--parse-file path)
:END:

Parse the zettel at PATH into a plist, or return nil.
Tries the v2 (properties) schema first; falls back to the v1
(Kramer sections) schema.  Returns nil when neither parses — e.g.
the file is not translation-relevant (no `:wylie:' property AND
no `- Wylie:' line) or is unreadable.

The returned plist has the shape documented in the module
commentary.

** ~tibetan-zettel--build-index~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 271
:SIGNATURE: (tibetan-zettel--build-index &optional dir)
:END:

Walk DIR (default `tibetan-zettel-directory') and return a fresh
hash: Wylie → plist.

Non-translation-relevant files are skipped silently.  When the
same Wylie appears in multiple zettels, the LAST one wins — the
user is expected to merge duplicates; Phase 6's migration pass
surfaces conflicts.

** ~tibetan-zettel--ensure-index~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 290
:SIGNATURE: (tibetan-zettel--ensure-index)
:END:

Build the index lazily on first use, OR when the directory changed.

** ~tibetan-zettel-reload~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 300
:SIGNATURE: (tibetan-zettel-reload)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Force a fresh index build of `tibetan-zettel-directory'.
Call after edits that happened outside Emacs (git pull, external
tooling).  In-Emacs edits via the Phase 2+ commands should
invalidate the cache automatically.

** ~tibetan-zettel-lookup~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 318
:SIGNATURE: (tibetan-zettel-lookup wylie)
:END:

Return the zettel plist for WYLIE, or nil.
WYLIE is normalised (trimmed, lowercased) before lookup.  The
returned plist shape matches `tibetan-zettel--parse-file' — see
the module commentary.

** ~tibetan-zettel--slug-from-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 356
:SIGNATURE: (tibetan-zettel--slug-from-wylie wylie)
:END:

Return a Denote filename slug from WYLIE.
Rules: lowercase, apostrophes dropped, non-alphanumeric → dashes,
consecutive dashes collapsed, leading/trailing dashes trimmed.

Examples:
  `bdag'                 → `bdag'
  `tshad med bzhi po'    → `tshad-med-bzhi-po'
  `'khor ba'             → `khor-ba'
  `Byang Chub Kyi Sems'  → `byang-chub-kyi-sems'

** ~tibetan-zettel--denote-timestamp~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 381
:SIGNATURE: (tibetan-zettel--denote-timestamp)
:END:

Return a Denote-style `YYYYMMDDTHHMMSS' timestamp, guaranteed
unique against previous calls within the running Emacs session.

Production case: clock advances normally, every call returns a
later value than the last.

Collision case: two creates in the same second → the second one
gets `last + 1 second' instead of the clock value, preserving
uniqueness.  Users will essentially never notice; tests that
batch-create benefit directly.

** ~tibetan-zettel--denote-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 410
:SIGNATURE: (tibetan-zettel--denote-filename timestamp slug)
:END:

Assemble the canonical auto-created zettel filename.
`<TIMESTAMP>--<SLUG>__glossar__buddhist-term.org'

Matches the Denote convention so the file participates in
whatever Denote wiring the zettelkasten already has (search,
renaming, backlinks); carries both the `__glossar' translation-
relevant tag AND a `__buddhist-term' tag distinguishing auto-
created entries from hand-written ones.

** ~tibetan-zettel--extract-term-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 421
:SIGNATURE: (tibetan-zettel--extract-term-gloss body)
:END:

From an 84000Definitions body string BODY, pull the short gloss
immediately after the `<term>' tag.  Returns the trimmed gloss or
nil.

The 84000 body format is:

  <term> SHORT-GLOSS (Skt: ...): LONG-EXPLANATION...

We take everything between `<term> ' and the first `(' or `:' or
newline, trimmed — this is the short reference-translation that
the auto-created `:preferred-en:' field is seeded with.

** ~tibetan-zettel--create-for-term~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 438
:SIGNATURE: (tibetan-zettel--create-for-term &key wylie script sanskrit 84000-body preferred-en
          preferred-de source-analysis-file)
:END:

Create a new Denote-format zettel for the Buddhist term with WYLIE.
Returns the created file's absolute path, or nil on failure (bad
args, unwritable directory).

Required:
  :WYLIE — the term's Wylie, used for both the PROPERTIES drawer
           and the filename slug.

Optional (any may be nil):
  :SCRIPT             — Tibetan script for the drawer.
  :SANSKRIT           — Skt lemma (e.g. `caturapramāṇa').
  :84000-BODY         — Steinert's 84000Definitions paragraph.
                        Written into the `* 84000 Definitions'
                        cache section; auto-derives a short gloss
                        for :preferred-en if PREFERRED-EN is nil.
  :PREFERRED-EN       — explicit English preferred translation.
                        Falls back to extracting the short gloss
                        from 84000-BODY; falls back further to
                        `[to be researched]'.
  :PREFERRED-DE       — explicit German (default `[to be researched]').
                        Per design decision #6, the German stays
                        empty for user curation — auto-populating
                        would defeat the translation-gaps report.
  :SOURCE-ANALYSIS-FILE — path of the segment whose analysis
                        triggered creation.  When given, a back-
                        link is written into the `* Back-links'
                        section pointing at that file.

On success, reloads the index so `tibetan-zettel-lookup' sees the
new entry immediately.

** ~tibetan-zettel--read-source-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 529
:SIGNATURE: (tibetan-zettel--read-source-id analysis-file)
:END:

Return the top-level `:ID:' property of ANALYSIS-FILE, or nil.
Used to build back-links from auto-created zettels to the segment
analysis file that triggered their creation.

** ~tibetan-zettel-maybe-create-for-buddhist-term~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 539
:SIGNATURE: (tibetan-zettel-maybe-create-for-buddhist-term &key wylie script sanskrit 84000-body preferred-en
          preferred-de source-analysis-file)
:END:

If a zettel doesn't exist for WYLIE, create one per user prefs.
Behaviour dispatches on `tibetan-zettel-auto-create-on-buddhist-term':

  - nil      → return nil without prompting or writing.
  - `always' → create immediately.
  - `ask'    → prompt `y-or-n-p' showing WYLIE + the derived
               short gloss; create on y, skip on n.

Returns the created zettel's path, or nil when nothing was written.
Idempotent: if a zettel already exists for WYLIE (hit in the
current index), returns nil immediately without prompting.

See `tibetan-zettel--create-for-term' for the full argument list.

** ~tibetan-zettel--prompt-version-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 596
:SIGNATURE: (tibetan-zettel--prompt-version-hash prompt)
:END:

Return a 12-char SHA-256 hex prefix of PROMPT, suitable as the
`:prompt-version:' property value.

Short enough to fit on a property line, long enough that
collisions across realistic prompt revisions are negligible.  When
Phase 5 reads this property and compares against the current
prompt's hash, mismatch → bypass cache, fetch fresh from Claude,
overwrite the cached entry with the new response and stamps.

** ~tibetan-zettel--claude-explanation-empty-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 607
:SIGNATURE: (tibetan-zettel--claude-explanation-empty-p content)
:END:

Return non-nil if CONTENT's `* Claude Explanation' section body
is empty / placeholder (`[awaiting first analysis]' or just
whitespace).  Used to gate cache writes — a populated section is
left strictly alone.

Searches for the Phase-3 scaffold marker line, since the Claude
Explanation heading itself can appear anywhere; the body region
is what we inspect.

** ~tibetan-zettel--cache-claude-explanation~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 624
:SIGNATURE: (tibetan-zettel--cache-claude-explanation path explanation &key model prompt-version)
:END:

Write EXPLANATION into PATH's `* Claude Explanation' section.

Phase 4 contract (idempotent): empty / placeholder section →
write; populated section with same prompt-version → leave alone.

Phase 5 extension (smart invalidation): populated section with
DIFFERENT cached `:prompt-version:' than the PROMPT-VERSION arg
→ OVERWRITE.  Means a system-prompt revision propagates to all
cached entries on next reference, without a manual
`claude-invalidate-all'.

Stamps the zettel's PROPERTIES drawer with `:claude-cached:'
(today's date), `:claude-model:' (MODEL or `(unknown)') and
`:prompt-version:' (PROMPT-VERSION or `unknown').  Stamps are
removed-and-reinserted on each write so re-runs leave a clean
drawer.

Returns t when an update was written, nil when skipped (already
populated AND prompt-version matches).

** ~tibetan-zettel--extract-claude-vocab-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 706
:SIGNATURE: (tibetan-zettel--extract-claude-vocab-gloss line)
:END:

Return the QUOTED gloss field from a Claude Vocabulary LINE.
Format Claude emits is

  wylie-key, part-of-speech, "gloss", commentary

Only the third field (between the first pair of double quotes)
is the cacheable short explanation.  Returns nil when the line
lacks a quoted field.

** ~tibetan-zettel--current-prompt-version~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 718
:SIGNATURE: (tibetan-zettel--current-prompt-version)
:END:

Return the live `tibetan-analysis--claude-system-prompt' hash, or
nil when the prompt symbol is unavailable.  Used by the Phase 5
reader and stale-p detector when the caller doesn't supply an
explicit `:current-prompt-version'.

** ~tibetan-zettel--read-cached-prompt-version~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 728
:SIGNATURE: (tibetan-zettel--read-cached-prompt-version path)
:END:

Return PATH zettel's cached `:prompt-version:' property, or nil.

** ~tibetan-zettel--cache-stale-p~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 737
:SIGNATURE: (tibetan-zettel--cache-stale-p entry &key current-prompt-version)
:END:

Return non-nil when ENTRY's cached prompt-version differs from
the current live system prompt's hash.  ENTRY is a plist returned
by `tibetan-zettel-lookup'.

`:current-prompt-version' override takes precedence over the live
hash; used by tests to pin a specific value.

Returns nil when no cache exists yet (no `:prompt-version:'
property in the zettel) — vacuously fresh.

** ~tibetan-zettel--read-claude-explanation~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 754
:SIGNATURE: (tibetan-zettel--read-claude-explanation entry &key current-prompt-version)
:END:

Return ENTRY's cached `* Claude Explanation' body, or nil.

Returns nil when:
  · the section is empty / placeholder (no cache),
  · OR the cached `:prompt-version:' differs from the current
    (or supplied via `:current-prompt-version') — stale entries
    are invisible to the reader so the analysis flow falls back
    to fresh data and the writer overwrites on next reference.

ENTRY is a plist returned by `tibetan-zettel-lookup'.

** ~tibetan-zettel--cache-claude-vocabulary~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 791
:SIGNATURE: (tibetan-zettel--cache-claude-vocabulary vocab-alist &key model prompt-version)
:END:

Walk VOCAB-ALIST (parsed `## Vocabulary' from a Claude response —
each entry is (WYLIE-KEY . FULL-LINE)) and update zettels in place.

For each entry whose WYLIE-KEY hits an existing zettel, calls
`--cache-claude-explanation' with the line's quoted gloss field.
Skips entries with no matching zettel and entries whose gloss
field can't be extracted.

MODEL and PROMPT-VERSION are passed through to the writer for the
property stamps.  When the caller doesn't know either, default
values are used.

Returns the count of zettels actually updated (excludes skipped
and no-match entries).

** ~tibetan-zettel--parse-buddhist-term-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 850
:SIGNATURE: (tibetan-zettel--parse-buddhist-term-body line-end section-end)
:END:

Return the body excerpt following a `- SCRIPT [wylie]' line.
Starts at LINE-END (end of the header line) and reads following
indented lines until the next `- ' bullet or SECTION-END, joining
them with spaces.  Trimmed.

** ~tibetan-zettel-auto-create-from-current-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 868
:SIGNATURE: (tibetan-zettel-auto-create-from-current-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Walk the current analysis buffer's `*** Buddhist Terms' section and
offer to create a zettel for every term that doesn't already have
one.  Gated by `tibetan-zettel-auto-create-on-buddhist-term' — see
that defcustom for per-term prompt behaviour.

Signals a `user-error' when the current buffer has no
`*** Buddhist Terms' heading (either the analysis hasn't been
generated yet or the segment has no <term>-tagged tokens).

Back-links on created zettels point at the current analysis
file's `:ID:' (if present) or path.

** ~tibetan-zettel--migrate-one-pass5b~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 936
:SIGNATURE: (tibetan-zettel--migrate-one-pass5b path target-dir dry-run)
:END:

Migrate a single Pass-5b file at PATH into TARGET-DIR.
Returns a symbol describing the outcome:
  `migrated' — wrote a new v2 zettel.
  `skipped'  — wylie already has a zettel in TARGET-DIR.
  `error'    — couldn't parse PATH (no `- Wylie:' field, etc.).

DRY-RUN non-nil → don't actually write; just compute and return
the symbol that WOULD have been the outcome.

** ~tibetan-zettel-migrate-thesaurus~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 995
:SIGNATURE: (tibetan-zettel-migrate-thesaurus &optional source-dir target-dir dry-run)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Migrate Pass-5b Thesaurus zettels from SOURCE-DIR into TARGET-DIR.
SOURCE-DIR defaults to `tibetan-thesaurus-directory' (the legacy
location, typically `~/Documents/tibetan-thesaurus/').
TARGET-DIR defaults to `tibetan-zettel-directory'
(`~/buddhist-studies/knowledge/zettelkasten/').

Walks every .org file in SOURCE-DIR; for each, parses via the
Pass-5b reader and writes a v2-schema zettel into TARGET-DIR.  The
new file is named `<ID>--<slug>__glossar.org' so Phase-1 detection
picks it up.  The original PROPERTIES `:ID:' is preserved when
present.  The new file carries `:migrated-from:' and `:migrated-on:'
properties for traceability.

Idempotent: if the target zettelkasten already has an entry for
the wylie, the source is skipped (its file in SOURCE-DIR is left
untouched — user can delete after verifying).

DRY-RUN non-nil prints what would happen without writing.

Returns a plist `(:migrated N :skipped M :errors K [:dry-run t])'
suitable for messaging.  Reloads `tibetan-zettel--index' on
success so the migrated zettels are immediately reachable via
`tibetan-zettel-lookup'.

Interactive: prompts for SOURCE-DIR + TARGET-DIR, defaults from
the variables above.  Pass C-u for dry-run.

** ~tibetan-zettel--read-source-label~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1082
:SIGNATURE: (tibetan-zettel--read-source-label analysis-file)
:END:

Compute a short label for ANALYSIS-FILE — the file's basename
(without extension), used as link text in back-link entries.

** ~tibetan-zettel--add-back-link~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1088
:SIGNATURE: (tibetan-zettel--add-back-link zettel-path source-id source-label)
:END:

Append `[[id:SOURCE-ID][SOURCE-LABEL]]' to ZETTEL-PATH's
`* Back-links' section.  Idempotent — when an entry with the same
SOURCE-ID is already present, no write happens.

Returns t on a fresh add, nil on dedup-skip.

** ~tibetan-zettel--collect-zettel-id-links~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1132
:SIGNATURE: (tibetan-zettel--collect-zettel-id-links buffer)
:END:

Return the list of zettel `:ID:' strings referenced by
`[[id:...]]' org links in BUFFER.  Deduped, order-preserved.

Used by `--update-back-links-from-current-analysis' to find every
zettel touched by an analysis (the Phase-2 Interlinear emits
`[[id:ZETTEL-ID][wylie]]' links for tokens with zettels).

** ~tibetan-zettel--zettel-path-for-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1151
:SIGNATURE: (tibetan-zettel--zettel-path-for-id zettel-id)
:END:

Return the on-disk path for the zettel whose `:ID:' is ZETTEL-ID,
or nil.  Walks the index — fast — rather than calling org-id-find
which would re-scan the world.

** ~tibetan-zettel-update-back-links-from-current-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1165
:SIGNATURE: (tibetan-zettel-update-back-links-from-current-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Walk the current analysis buffer's `[[id:ZETTEL-ID]]' links
(emitted by Phase-2's Interlinear renderer) and add a back-link
to every referenced zettel pointing at the current analysis's
own `:ID:'.

Signals `user-error' when the buffer has no top-level :ID:
property — back-links need a stable target.  Returns the count of
zettels that gained a fresh link (deduped).

Bound to `C-c u z L' (NOT to be confused with `C-c u z l' which
lists thesaurus entries — different module).  Also reachable from
the Tibetan menu.

** ~tibetan-zettel-gc-back-links~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1206
:SIGNATURE: (tibetan-zettel-gc-back-links)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Walk every zettel's `* Back-links' section and prune entries whose
`[[id:...]]' target no longer resolves via `org-id-find'.

Manual / on-demand: never automatic, since back-link removal is
irreversible without grep-bisecting the zettel's history.  Pair
with the existing `auto-create' (Phase 3) and `migrate-thesaurus'
(Phase 6) workflows for a clean zettelkasten.

Returns `(:scanned N :pruned M)' as message + value.

** ~tibetan-zettel-directory~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 70
:TYPE: 'directory
:GROUP: tibetan-zettel
:DEFAULT: "~/buddhist-studies/knowledge/zettelkasten/"
:END:

Directory holding the zettelkasten's translation-relevant zettels.
Default points at the post-reorg location (2026-04-24); older
configurations with the bare `~/buddhist-studies/zettelkasten/'
path should update after the buddhist-studies reorg.

The indexer walks this directory recursively, parses each .org
file passing `tibetan-zettel--translation-relevant-p', and keys
entries on their `:wylie:' field (or `- Wylie:' for legacy
Pass-5b zettels).

** ~tibetan-zettel-auto-create-on-buddhist-term~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 345
:TYPE: '(choice (const
:GROUP: tibetan-zettel
:DEFAULT: 'ask
:END:

Behaviour when a `<term>'-tagged token has no zettel yet.
See module commentary for semantics.  Default `ask' surfaces every
opportunity as a y-or-n-p prompt; switch to `always' once the
<term>-tag heuristic has proven itself on your corpus (i.e. no
more rejections).

** ~tibetan-zettel--glossar-tag-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 88
:DEFAULT: "__glossar\\(__\\|\\.org\\'\\)"
:END:

Regex matching the Denote `__glossar' file tag.
The trailing alternation (`__' or `.org' end-anchor) prevents
`__glossary' / `__glossar-archive' from matching as the exact tag.

** ~tibetan-zettel--wylie-property-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 103
:DEFAULT: "^[ \t]*:wylie:[ \t]+\\(\\S-.*?\\)[ \t]*$"
:END:

Regex matching a `:wylie: VALUE' property line.
Leading whitespace tolerated (org's PROPERTIES drawers indent);
trailing whitespace trimmed by the capture group's `.*?'.

** ~tibetan-zettel--v1-wylie-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 154
:DEFAULT: "^[ \t]*-[ \t]*Wylie:[ \t]*\\(.+?\\)[ \t]*$"
:END:

Regex matching the `- Wylie: VALUE' line in Pass-5b Kramer format.

** ~tibetan-zettel--v1-primary-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 158
:DEFAULT: "^\\*\\*[ \t]+%s[ \t]*$\\([^*]*?\\)^\\*\\*"
:END:

Template: regex for a `** SECTION' body, bounded to the next `**'.
Used to scope `- Primary translation:' lookups to the correct
section (English vs German).

** ~tibetan-zettel--index~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 260
:DEFAULT: nil
:END:

Hash: normalised Wylie key → entry plist.
See `tibetan-zettel--parse-file' for the plist shape.  Built lazily
on first `tibetan-zettel-lookup' via `tibetan-zettel--ensure-index',
or force-rebuilt via `tibetan-zettel-reload'.

** ~tibetan-zettel--index-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 266
:DEFAULT: nil
:END:

Directory the current `tibetan-zettel--index' was built from.
When this differs from the current `tibetan-zettel-directory', the
cache is invalidated on next access.

** ~tibetan-zettel--last-denote-timestamp~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 374
:DEFAULT: nil
:END:

Last `--denote-timestamp' value handed out, for collision avoidance.
Two zettels created in rapid succession would otherwise share an
ID (the YYYYMMDDTHHMMSS format has one-second resolution).  When
the clock would produce a value equal to or earlier than this,
the function bumps to one second later instead.

** ~tibetan-zettel--buddhist-terms-heading-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 839
:DEFAULT: "^\\*\\*\\* Buddhist Terms[ \t]*$"
:END:

Regex for the U3-emitted Buddhist Terms subsection heading.

** ~tibetan-zettel--buddhist-term-line-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 843
:DEFAULT: "^- \\(.+?\\) \\[\\(.+?\\)\\][ \t]*$"
:END:

Regex for a `- SCRIPT [wylie]' entry inside Buddhist Terms.
Capture 1: Tibetan script.  Capture 2: Wylie.  The body line(s)
follow on subsequent lines indented by at least two spaces; see
`tibetan-zettel--parse-buddhist-term-body' for extraction.

** ~tibetan-zettel~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 65
:END:

Integration of the zettelkasten into the CAT translation workflow.


* data/tibetan-glossary-loader.el
:PROPERTIES:
:CUSTOM_ID: data/tibetan-glossary-loader.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/data/tibetan-glossary-loader.el
:END:

** ~tibetan-glossary--load-tsv~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 41
:SIGNATURE: (tibetan-glossary--load-tsv filepath &optional skip-existing)
:END:

Load a tab-separated glossary from FILEPATH into comprehensive vocabulary.
If SKIP-EXISTING is non-nil, don't overwrite existing entries.
Format: Tibetan<TAB>English[<TAB>Notes]
Lines starting with # are comments.

** ~load-hopkins-glossary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 77
:SIGNATURE: (load-hopkins-glossary)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load Hopkins glossary from bundled data.

** ~load-unified-glossary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 85
:SIGNATURE: (load-unified-glossary)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load unified Tibetan glossary from bundled data.

** ~load-madhyamaka-glossary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 93
:SIGNATURE: (load-madhyamaka-glossary)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load specialized Madhyamaka glossary from bundled data.

** ~load-common-vocabulary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 101
:SIGNATURE: (load-common-vocabulary)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load common vocabulary from bundled data.

** ~load-tibetan-english-glossary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (load-tibetan-english-glossary)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load the large tibetan-english glossary (TSV format).

** ~tibetan-load-rangjung-yeshe~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 121
:SIGNATURE: (tibetan-load-rangjung-yeshe)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load Rangjung Yeshe dictionary (~162K entries).
Called automatically on first vocabulary lookup.

** ~load-all-glossaries~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 163
:SIGNATURE: (load-all-glossaries &optional force)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Load all available glossaries into the comprehensive vocabulary.
Rangjung Yeshe is loaded separately on first use.

Idempotent: returns immediately if `tibetan-glossaries-loaded' is
already non-nil.  Pass FORCE non-nil (interactively, with a prefix
argument) to discard the existing hash and reload everything.

** ~lookup-tibetan-comprehensive~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 191
:SIGNATURE: (lookup-tibetan-comprehensive word)
:END:

Look up WORD in the comprehensive vocabulary.

** ~add-to-tibetan-vocabulary~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 197
:SIGNATURE: (add-to-tibetan-vocabulary tibetan english)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Add TIBETAN term with ENGLISH translation to vocabulary.

** ~tibetan-glossary-data-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 21
:DEFAULT: (expand-file-name "glossaries/"
                    (file-name-directory (or load-file-name buffer-file-name)))
:END:

Directory containing glossary data files.

** ~tibetan-comprehensive-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 30
:DEFAULT: (make-hash-table :test 'equal :size 20000)
:END:

Hash table containing all Tibetan vocabulary from multiple sources.

** ~tibetan-rangjung-yeshe-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 33
:DEFAULT: nil
:END:

Hash table for Rangjung Yeshe dictionary (162K entries).
Loaded on first use to avoid slow startup.

** ~tibetan-glossaries-loaded~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 155
:DEFAULT: nil
:END:

Non-nil after `load-all-glossaries' has populated
`tibetan-comprehensive-vocabulary'.  Guards against repeated reloads
when callers (e.g. `tibetan-analysis--ensure-vocabulary' invoked on
every reanalysis) trigger the function defensively.  Set to nil and
call `load-all-glossaries' again to force a fresh load.


* doc-prep/tibetan-doc-display.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-doc-display.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-doc-display.el
:END:

** ~tibetan-doc-display-mode~  /minor mode/  ↗
:PROPERTIES:
:KIND: define-minor-mode
:LINE: 109
:SIGNATURE: (tibetan-doc-display-mode …)
:AUTOLOAD: t
:END:

Minor mode for displaying Tibetan prepared documents.
Makes segment markers smaller and less obtrusive.
Makes Tibetan script larger for better readability (superscript/subscript).

** ~tibetan-set-text-scale~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 31
:SIGNATURE: (tibetan-set-text-scale factor)
:INTERACTIVE: t
:END:

Set the scale factor for Tibetan text to FACTOR.
Interactive: prompts for factor (default 1.4).
Updates the face and re-fontifies current buffer.

** ~tibetan-doc-display--maybe-enable~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 136
:SIGNATURE: (tibetan-doc-display--maybe-enable)
:END:

Enable tibetan-doc-display-mode if buffer contains Tibetan text.
Activates for any .org file with Tibetan Unicode characters (U+0F00-U+0FFF).

** ~tibetan-doc-toggle-markers~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 154
:SIGNATURE: (tibetan-doc-toggle-markers)
:INTERACTIVE: t
:END:

Toggle visibility of segment markers in current buffer.

** ~tibetan-doc-hide-markers~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 163
:SIGNATURE: (tibetan-doc-hide-markers)
:INTERACTIVE: t
:END:

Hide segment markers completely (invisible).

** ~tibetan-doc-show-markers~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 172
:SIGNATURE: (tibetan-doc-show-markers)
:INTERACTIVE: t
:END:

Show all segment markers (remove invisible property).

** ~tibetan-text-scale-factor~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 17
:TYPE: 'number
:GROUP: tibetan-cat
:DEFAULT: 1.8
:END:

Scale factor for Tibetan text display.
Values > 1.0 make Tibetan text larger for better readability.
Recommended: 1.6-2.0 for comfortable reading of stacked letters.
Use C-c u + / C-c u - to adjust interactively.

** ~tibetan-doc-font-lock-keywords~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 74
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-script-font-lock-keywords~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 98
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-script-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 25
:END:

/(no docstring)/

** ~tibetan-doc-marker-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 48
:END:

/(no docstring)/

** ~tibetan-doc-marker-tag-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 56
:END:

/(no docstring)/

** ~tibetan-doc-marker-attr-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 63
:END:

/(no docstring)/


* doc-prep/tibetan-doc-format.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-doc-format.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-doc-format.el
:END:

** ~tibetan-doc-format--split-at-shad~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 38
:SIGNATURE: (tibetan-doc-format--split-at-shad text)
:END:

Split TEXT at single shad (།), creating one line per segment.
Preserves the shad at end of each segment.

** ~tibetan-doc-format--split-at-double-shad~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 46
:SIGNATURE: (tibetan-doc-format--split-at-double-shad text)
:END:

Split TEXT at double shad (༎), keeping single shad within lines.
Preserves the double-shad at end of each segment.

** ~tibetan-doc-format--split-preserve~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 54
:SIGNATURE: (tibetan-doc-format--split-preserve text)
:END:

Split TEXT preserving original line breaks.

** ~tibetan-doc-format--split-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 58
:SIGNATURE: (tibetan-doc-format--split-text text line-break-style)
:END:

Split TEXT according to LINE-BREAK-STYLE.
Returns list of text segments.

** ~tibetan-doc-format--add-segment-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 71
:SIGNATURE: (tibetan-doc-format--add-segment-markers lines &optional start-num with-trans)
:END:

Add 〔seg:N〕 markers to LINES.
START-NUM is the starting segment number (default 1).
When WITH-TRANS is non-nil, also append an empty 〔trans:N〕 ... 〔/trans〕
block after each segment so the class translation can be inserted inline.
Returns list of marked lines.

** ~tibetan-doc-format-get-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 87
:SIGNATURE: (tibetan-doc-format-get-translation text seg-num)
:END:

Extract the translation text for SEG-NUM from TEXT.
Looks for a 〔trans:N〕 ... 〔/trans〕 block and returns the trimmed content,
or nil if no block is found or the block is empty.

** ~tibetan-doc-format--extract-folio-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 103
:SIGNATURE: (tibetan-doc-format--extract-folio-markers text)
:END:

Extract folio markers from TEXT.
Looks for patterns like [1a], [1b], (1a), (1b), F.1a, etc.
Returns alist of ((position . folio-num) ...).

** ~tibetan-doc-format--generate-header~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 124
:SIGNATURE: (tibetan-doc-format--generate-header source-info options)
:END:

Generate org file header from SOURCE-INFO and OPTIONS.

** ~tibetan-doc-format--generate-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 151
:SIGNATURE: (tibetan-doc-format--generate-body text options)
:END:

Generate org body from TEXT with OPTIONS.

Recognised option keys:
  :line-break     `shad' | `double-shad' | `preserve'
  :segments       t | nil (wrap each line in 〔seg:N〕 ... 〔/seg〕)
  :translations   t | nil (append an empty 〔trans:N〕 block after
                  each segment; defaults to t when :segments is t)

** ~tibetan-doc-format~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 192
:SIGNATURE: (tibetan-doc-format text source-info options &optional output-path)
:AUTOLOAD: t
:END:

Format TEXT into org document.

SOURCE-INFO is plist with:
  :source-file   Original file path
  :title         Document title
  :folio-range   Range of folios (e.g., "1a-5b")

OPTIONS is plist with:
  :line-break    `shad' | `double-shad' | `preserve'
  :segments      t | nil (add segment markers)
  :translations  t | nil (append empty 〔trans:N〕 blocks after each
                 segment for inline class translations; defaults to the
                 value of :segments)
  :folio-style   `heading' | `inline' | `property'
  :text-type     `classical' | `madhyamaka-verse' | `sutra' | `commentary'

OUTPUT-PATH is optional file path. If nil, generates path from source.

Returns path to created file.

** ~tibetan-doc-format--generate-output-path~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 224
:SIGNATURE: (tibetan-doc-format--generate-output-path source-info)
:END:

Generate output file path from SOURCE-INFO.

** ~tibetan-doc-format-buffer~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 240
:SIGNATURE: (tibetan-doc-format-buffer)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Format current buffer's Tibetan text into org structure.
Prompts for formatting options.

** ~tibetan-doc-format-region~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 254
:SIGNATURE: (tibetan-doc-format-region start end)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Format region between START and END into org document.

** ~tibetan-doc-format--prompt-options~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 265
:SIGNATURE: (tibetan-doc-format--prompt-options)
:END:

Prompt user for formatting options.
Returns options plist.

** ~tibetan-doc-format-clean-ocr-artifacts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 285
:SIGNATURE: (tibetan-doc-format-clean-ocr-artifacts text)
:END:

Remove common OCR artifacts from TEXT.
Removes extra spaces, normalizes punctuation, etc.

** ~tibetan-doc-format-shad~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 25
:DEFAULT: "།"
:END:

Tibetan single shad punctuation.

** ~tibetan-doc-format-double-shad~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 28
:DEFAULT: "༎"
:END:

Tibetan double shad (nyis shad) punctuation.

** ~tibetan-doc-format-tsheg~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 31
:DEFAULT: "་"
:END:

Tibetan tsheg (syllable separator).


* doc-prep/tibetan-doc-prep-init.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-doc-prep-init.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-doc-prep-init.el
:END:

** ~tibetan-doc-prep-init-status~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 51
:SIGNATURE: (tibetan-doc-prep-init-status)
:INTERACTIVE: t
:END:

Show status of doc-prep module components.

** ~tibetan-doc-prep-init--load-glossaries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 88
:SIGNATURE: (tibetan-doc-prep-init--load-glossaries)
:END:

Load comprehensive glossaries if available.

** ~tibetan-comprehensive-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 33
:END:

/(no docstring)/


* doc-prep/tibetan-doc-prep.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-doc-prep.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-doc-prep.el
:END:

** ~tibetan-doc-prep--resolve-work-in-progress-target~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 124
:SIGNATURE: (tibetan-doc-prep--resolve-work-in-progress-target source-path)
:END:

Return the absolute target path for SOURCE-PATH after relocation.

Consults `tibetan-doc-prep-work-in-progress-folder':
  · nil          → returns SOURCE-PATH unchanged (no move).
  · relative     → resolved as a sibling of the source's parent
                   directory:  `.../parent/<folder>/<basename>'.
  · absolute     → used directly as the destination directory.

The returned path is always absolute.  Does not create the
destination directory — the move helper does that.

** ~tibetan-doc-prep-move-to-work-in-progress~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 149
:SIGNATURE: (tibetan-doc-prep-move-to-work-in-progress source-path)
:END:

Relocate SOURCE-PATH into the configured work-in-progress folder.

Resolves the destination via
`tibetan-doc-prep--resolve-work-in-progress-target', creates the
destination directory if absent, and renames the file across.
Returns the new absolute path.

Idempotent:  when SOURCE-PATH already resides at the resolved
destination (or
`tibetan-doc-prep-work-in-progress-folder' is nil), the function
is a no-op and returns SOURCE-PATH unchanged.

Existing destination files are overwritten — callers (the wizard,
the Wylie ingest wrapper) confirm with the user before invoking.

** ~tibetan-doc-prep--select-from-list~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 201
:SIGNATURE: (tibetan-doc-prep--select-from-list prompt choices)
:END:

Display PROMPT and let user select from CHOICES.
CHOICES is alist of (display-string . value).
Returns selected value.

** ~tibetan-doc-prep--yes-or-no~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 209
:SIGNATURE: (tibetan-doc-prep--yes-or-no prompt)
:END:

Ask yes/no question with PROMPT.

** ~tibetan-doc-prep--select-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 218
:SIGNATURE: (tibetan-doc-prep--select-source)
:END:

Interactive source selection.
Returns plist with :type and :path or :text.

** ~tibetan-doc-prep--get-ocr-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 256
:SIGNATURE: (tibetan-doc-prep--get-ocr-text source)
:END:

Get OCR text from SOURCE.
If source is already text, return it.
Otherwise, run BDRC OCR.

** ~tibetan-doc-prep--validate-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 287
:SIGNATURE: (tibetan-doc-prep--validate-text text)
:END:

Validate TEXT against dictionaries.
Returns validation result plist.

** ~tibetan-doc-prep--ai-correct~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 304
:SIGNATURE: (tibetan-doc-prep--ai-correct text validation)
:END:

Correct TEXT using Claude AI, informed by VALIDATION.
Returns corrected text.

** ~tibetan-doc-prep--review-flagged~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 345
:SIGNATURE: (tibetan-doc-prep--review-flagged validation)
:END:

Display flagged items from VALIDATION for review.

** ~tibetan-doc-prep--show-changes~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 362
:SIGNATURE: (tibetan-doc-prep--show-changes changes)
:END:

Display CHANGES made by AI correction.

** ~tibetan-doc-prep--select-format-options~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 381
:SIGNATURE: (tibetan-doc-prep--select-format-options)
:END:

Interactive selection of formatting options.
Returns options plist.

** ~tibetan-doc-prep--format-document~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 405
:SIGNATURE: (tibetan-doc-prep--format-document text source)
:END:

Format TEXT into org document.
SOURCE contains source info.
Returns path to created file.

** ~tibetan-doc-prep--basic-format~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 428
:SIGNATURE: (tibetan-doc-prep--basic-format text output-file options title)
:END:

Basic document formatting fallback.
Formats TEXT to OUTPUT-FILE with OPTIONS and TITLE.

** ~tibetan-doc-prep--show-summary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 465
:SIGNATURE: (tibetan-doc-prep--show-summary source validation output-file)
:END:

Show summary of document preparation.
SOURCE is the input source, VALIDATION is the validation result,
OUTPUT-FILE is the created file path.

** ~tibetan-doc-prep-wizard~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 485
:SIGNATURE: (tibetan-doc-prep-wizard)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive wizard for complete Tibetan document preparation.
Guides through: Source → OCR → Validation → AI Correction → Formatting.

** ~tibetan-doc-prep-ocr~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 518
:SIGNATURE: (tibetan-doc-prep-ocr source-file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Run OCR on SOURCE-FILE only.

** ~tibetan-doc-prep-validate~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 532
:SIGNATURE: (tibetan-doc-prep-validate)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Validate current buffer or region against dictionaries.

** ~tibetan-doc-prep-correct~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 544
:SIGNATURE: (tibetan-doc-prep-correct)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

AI-correct current buffer or region.

** ~tibetan-doc-prep-format~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 560
:SIGNATURE: (tibetan-doc-prep-format)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Format current buffer into org document.

** ~tibetan-doc-prep-quick-pecha~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 575
:SIGNATURE: (tibetan-doc-prep-quick-pecha _file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Quick preparation for pecha scans with defaults.
FILE is the PDF or image to process.

** ~tibetan-doc-prep-quick-verse~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 586
:SIGNATURE: (tibetan-doc-prep-quick-verse _file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Quick preparation for verse texts.
FILE is the PDF or image to process.

** ~tibetan-doc-prep-default-line-break~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 62
:TYPE: '(choice (const
:GROUP: tibetan-doc-prep
:DEFAULT: 'shad
:END:

Default line break style for document formatting.
Options: `shad' (།), `double-shad' (༎), `preserve' (keep original).

** ~tibetan-doc-prep-default-segments~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 70
:TYPE: 'boolean
:GROUP: tibetan-doc-prep
:DEFAULT: t
:END:

Whether to add segment markers by default.

** ~tibetan-doc-prep-default-folio-style~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 75
:TYPE: '(choice (const
:GROUP: tibetan-doc-prep
:DEFAULT: 'heading
:END:

Default style for folio markers.
Options: `heading' (* Folio 1a), `inline' ([F:1a]), `property' (drawer).

** ~tibetan-doc-prep-default-text-type~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 83
:TYPE: '(choice (const
:GROUP: tibetan-doc-prep
:DEFAULT: 'classical
:END:

Default text type for new documents.

** ~tibetan-doc-prep-output-directory~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 91
:TYPE: '(choice (const
:GROUP: tibetan-doc-prep
:DEFAULT: nil
:END:

Default output directory for prepared documents.
If nil, uses same directory as source file.

** ~tibetan-doc-prep-work-in-progress-folder~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 98
:TYPE: '(choice (const
:GROUP: tibetan-doc-prep
:DEFAULT: "work in progress"
:END:

Sibling folder name that receives a freshly prepared source file.

§5.27 Phase 2 follow-up (2026-05-27):  after ingest (Wylie
conversion / OCR import / paste-in) the prepared `.org' file is
relocated UP one level into a sibling folder of this name so the
buddhist-studies workflow folders mirror the established Milarepa
pattern:

  <semester>/<class>/<raw-input>/foo.org       ←  source (ingest input)
  <semester>/<class>/work in progress/foo.org  ←  prepared file
  <semester>/<class>/work in progress/analysis/seg-NNN-foo.org

Set to nil to disable the post-ingest move entirely (the prepared
file then stays in its source directory).  An absolute path is
also accepted — when provided, the prepared file lands there
unconditionally instead of in a relative sibling of the source.

** ~cat-mode-map~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 23
:DEFAULT: nil
:END:

Keymap for cat-mode (declared for byte-compiler).

** ~tibetan-doc-prep--current-source~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 185
:DEFAULT: nil
:END:

Current source file/text being processed.

** ~tibetan-doc-prep--raw-text~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 188
:DEFAULT: nil
:END:

Raw OCR/input text.

** ~tibetan-doc-prep--validation-result~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 191
:DEFAULT: nil
:END:

Result of dictionary validation.

** ~tibetan-doc-prep--corrected-text~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 194
:DEFAULT: nil
:END:

AI-corrected text.

** ~tibetan-doc-prep-map~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 605
:DEFAULT: (let ((map (make-sparse-keymap)))
    ;; §5.27 Phase 6:  unified document-prep wizard (top-of-map).
    (define-key map 
:END:

Keymap for document preparation commands.

Key bindings:
  d   Unified document-prep wizard (§5.27 — Wylie / Tibetan-script /
      existing / OCR + metadata + analysis kickoff in one flow)
  D   Apply cached Claude metadata suggestions to current buffer
  y   Wylie ingest (validate + convert);  C-u → in-place + relocate
  Y   Wylie ingest — validate only (no conversion)
  o   Legacy OCR / Format wizard (subsumed by `d' for new prep)
  r   OCR step only
  v   Validate buffer
  c   AI correct buffer
  f   Format to org
  p   Quick pecha preset
  V   Quick verse preset
  a   Open BDRC OCR app
  w   OCR workflow (guided)
  s   Show OCR status
  i c Import from clipboard
  i f Import from file
  i b Import from buffer

** ~tibetan-doc-prep~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 57
:END:

Tibetan document preparation pipeline.


* doc-prep/tibetan-document-genres.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-document-genres.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-document-genres.el
:END:

** ~tibetan-document-genres-keys~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 123
:SIGNATURE: (tibetan-document-genres-keys)
:END:

Return the list of KEY symbols from the taxonomy, in order.

** ~tibetan-document-genres-labels~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 127
:SIGNATURE: (tibetan-document-genres-labels)
:END:

Return the list of `:label' strings from the taxonomy, in order.

** ~tibetan-document-genres--entry-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-document-genres--entry-for key)
:END:

Return the PLIST for KEY (a symbol), or nil when unknown.

** ~tibetan-document-genres-label-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 136
:SIGNATURE: (tibetan-document-genres-label-for key)
:END:

Return the display label for KEY, or nil when unknown.

** ~tibetan-document-genres-description-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 140
:SIGNATURE: (tibetan-document-genres-description-for key)
:END:

Return the one-line description for KEY, or nil when unknown.

** ~tibetan-document-genres-claude-hint-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 144
:SIGNATURE: (tibetan-document-genres-claude-hint-for key)
:END:

Return the Claude system-prompt hint for KEY, or nil when unknown.
Wizard / prompt builders append the returned line to Claude's
system prompt when the document carries a known genre.

** ~tibetan-document-genres-tibetan-for~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 150
:SIGNATURE: (tibetan-document-genres-tibetan-for key)
:END:

Return the Wylie/Tibetan name for KEY, or nil when unknown.
The legacy keys `classical' and `madhyamaka-verse' have no
Tibetan-name analogue and return the empty string.

** ~tibetan-document-genres-read~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 160
:SIGNATURE: (tibetan-document-genres-read &optional default prompt)
:END:

Read a genre interactively via `completing-read'.

Returns the KEY symbol of the chosen genre.  DEFAULT (when
provided as a KEY symbol) preselects its label;  unrecognised
DEFAULT falls through to no preselection.  PROMPT defaults to
`"Genre: "'.

The completing-read input is a `:label' string — Carsten's vertico/
ivy/marginalia UI honours the alist mapping back to keys.

** ~tibetan-document-genre-taxonomy~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 27
:DEFAULT: '
:END:

/(no docstring)/


* doc-prep/tibetan-document-prep-claude.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-document-prep-claude.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-document-prep-claude.el
:END:

** ~tibetan-document-prep--extract-body-excerpt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 75
:SIGNATURE: (tibetan-document-prep--extract-body-excerpt source-file)
:END:

Return the first ~N words of SOURCE-FILE's `* Tibetan Text' body.

N is `tibetan-document-prep-claude-excerpt-words'.  Returns nil
when the file is unreadable, the section is absent, or the body
is empty.  Skips org metadata headers and section headings — the
excerpt is pure body text, ready for Claude.

Words are split on whitespace;  Tibetan tshegs (U+0F0B) and
shads (U+0F0D) are preserved within each whitespace-separated
chunk so the excerpt remains parseable as Tibetan.

** ~tibetan-document-prep--build-system-prompt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 112
:SIGNATURE: (tibetan-document-prep--build-system-prompt)
:END:

Return the system prompt instructing Claude to emit JSON metadata.

Enumerates the 14 canonical genre keys so Claude picks from the
exact set the wizard's selectbox understands;  asks for a tightly
shaped JSON response so the parser stays simple.

** ~tibetan-document-prep--build-user-prompt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-document-prep--build-user-prompt excerpt)
:END:

Wrap EXCERPT in the user-prompt frame Claude expects.

** ~tibetan-document-prep--strip-code-fence~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 156
:SIGNATURE: (tibetan-document-prep--strip-code-fence response)
:END:

Return RESPONSE with surrounding ```json … ``` fences removed.

Claude occasionally wraps a JSON response in code fences despite
explicit instructions.  We strip them so the parser sees clean
JSON.  Idempotent — input without fences passes through.

** ~tibetan-document-prep--parse-claude-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 171
:SIGNATURE: (tibetan-document-prep--parse-claude-response response)
:END:

Parse Claude's JSON RESPONSE into a metadata plist.

Returns:
  (:genre KEY-SYMBOL-OR-NIL
   :author STRING-OR-NIL
   :context LIST-OF-STRINGS)

Unknown / malformed JSON → nil (caller falls back to no-suggestion
behaviour).  Unknown genre keys (not in the taxonomy) → :genre nil,
other fields preserved.  Author = "unknown" → :author nil so the
wizard knows to fall through to the manual prompt.

** ~tibetan-document-prep--upsert-single-line-header~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 231
:SIGNATURE: (tibetan-document-prep--upsert-single-line-header key value)
:END:

In the current buffer, set `#+KEY: VALUE' (single-line header).

Replaces an existing matching line in place;  otherwise inserts
after the last existing `#+TIBETAN_…' / `#+TITLE:' line so the
metadata block stays contiguous.

** ~tibetan-document-prep--append-context-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 262
:SIGNATURE: (tibetan-document-prep--append-context-lines lines)
:END:

In the current buffer, append `#+TIBETAN_CLAUDE_CONTEXT: <line>'
for each entry in LINES (a list of strings).  Existing context
lines are preserved — the new ones go AFTER the last one (or
after the final `#+TIBETAN_…' header when none exist yet).

** ~tibetan-document-prep--apply-suggestions~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 305
:SIGNATURE: (tibetan-document-prep--apply-suggestions source-file plist)
:END:

Write the suggestion PLIST's headers into SOURCE-FILE.

Three header kinds are touched (each only when its value is
non-nil):
  · :genre   → `#+TIBETAN_TEXT_TYPE:'      (replaces existing)
  · :author  → `#+TIBETAN_AUTHOR:'         (replaces existing)
  · :context → appends one `#+TIBETAN_CLAUDE_CONTEXT:' per line
               (existing context lines are preserved)

No-op when SOURCE-FILE is unreadable or PLIST is nil.  Returns the
SOURCE-FILE path on success.

** ~tibetan-document-prep--ask-claude~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 341
:SIGNATURE: (tibetan-document-prep--ask-claude source-file callback)
:END:

Submit a Claude metadata-suggestion request for SOURCE-FILE.

Builds the excerpt + prompts, dispatches via
`tibetan-claude-queue-submit' (so throttle / retry / rate-limit
handling is shared with the rest of the Claude pipeline), and on
success invokes CALLBACK with the parsed suggestion plist (or nil
when Claude returned an unparseable / empty body).

Signals a `user-error' when the prerequisites are missing
(gptel not loaded, queue not loaded, source unreadable, body
section absent) so callers see the actionable hint without a
silent failure.

** ~tibetan-document-prep-apply-claude-suggestions~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 401
:SIGNATURE: (tibetan-document-prep-apply-claude-suggestions)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Apply the cached Claude metadata suggestions to the current buffer.

Reads from `tibetan-document-prep--claude-suggestions' (buffer-
local;  set by the wizard's async callback).  Writes the
suggestion plist's `:genre' / `:author' / `:context' fields into
the buffer's file as `#+TIBETAN_TEXT_TYPE:' / `#+TIBETAN_AUTHOR:' /
`#+TIBETAN_CLAUDE_CONTEXT:' headers, then reverts the buffer to
pick up the new content.

When no cached suggestions exist (Claude hasn't returned yet, or
the wizard wasn't run) the command prints a polite message and
returns nil.  Use \[tibetan-document-prep-wizard] to kick off a
fresh metadata fetch.

** ~tibetan-document-prep-claude-excerpt-words~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 45
:TYPE: 'integer
:GROUP: tibetan-document-prep-claude
:DEFAULT: 200
:END:

Word count of the Tibetan excerpt sent to Claude for metadata.

200 words is enough for a confident genre signal (verse meter,
honorific register, formulaic openings) without bloating the
context window.  Increase if Claude's suggestions feel under-
informed;  decrease to save tokens.

** ~tibetan-document-prep--claude-suggestions~  /variable (buffer-local)/
:PROPERTIES:
:KIND: defvar-local
:LINE: 59
:DEFAULT: nil
:END:

Buffer-local plist of the latest Claude metadata suggestions.

Shape:
  (:genre KEY        ; symbol from `tibetan-document-genre-taxonomy'
                     ; or nil when Claude couldn't decide
   :author STR       ; suggested author name, or "unknown"
   :context LIST)    ; list of 1-3 context strings

Set by the wizard's async callback when Claude returns;  consumed
by `tibetan-document-prep-apply-claude-suggestions'.

** ~tibetan-document-prep-claude~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 40
:END:

Async Claude metadata pre-fill for the document-preparation wizard.


* doc-prep/tibetan-document-prep-wizard.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-document-prep-wizard.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-document-prep-wizard.el
:END:

** ~tibetan-document-prep-wizard--read-route~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 69
:SIGNATURE: (tibetan-document-prep-wizard--read-route)
:END:

Prompt the user for an INPUT ROUTE.  Returns the route key symbol.

** ~tibetan-document-prep-wizard--run-wylie-ingest~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 76
:SIGNATURE: (tibetan-document-prep-wizard--run-wylie-ingest &optional class-mode)
:END:

Prompt for a Wylie `.org' source, validate + convert + relocate.
Returns the final source-file path (post-relocation into the
configured `work in progress/' folder).

CLASS-MODE (`reading' default | `grammar') controls the
shad-normalisation grain so the resulting segments match the
intended class type:  reading-class = one segment per paragraph,
grammar-class = one segment per `| |'-delimited sentence.  See
`tibetan-wylie-ingest-normalize-shads' for the details.

When the source is bare (no `* Tibetan Text' heading) the wizard
OFFERS to auto-wrap it before validation — bare Wylie files
copy-pasted from a transcription don't carry the org wrapper
the converter requires, so this catches the common case without
forcing the user to hand-edit.

** ~tibetan-document-prep-wizard--collect-source-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 157
:SIGNATURE: (tibetan-document-prep-wizard--collect-source-file route
                                                         &optional class-mode)
:END:

Acquire a source-file path for the given ROUTE.
Dispatches to the appropriate ingest helper;  returns the final
absolute path.  CLASS-MODE is threaded through to the Wylie
route so shad normalisation matches the intended segment grain
(reading = paragraphs, grammar = sentences).

** ~tibetan-document-prep-wizard--read-target-lang~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 183
:SIGNATURE: (tibetan-document-prep-wizard--read-target-lang source-file)
:END:

Prompt for `#+TIBETAN_TARGET_LANG:'.  Defaults to existing value
or `de'.  Writes the header on the source file via
`tibetan-document-prep--upsert-single-line-header'.

** ~tibetan-document-prep-wizard--fire-async-claude~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 199
:SIGNATURE: (tibetan-document-prep-wizard--fire-async-claude source-file)
:END:

Submit the async Claude metadata-suggestion call.

Caches the response in `tibetan-document-prep--claude-suggestions'
on the source's visiting buffer (creating it on the fly when the
source isn't open).  Returns immediately — the wizard continues
without waiting for Claude's reply.

** ~tibetan-document-prep-wizard--read-genre~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 226
:SIGNATURE: (tibetan-document-prep-wizard--read-genre source-file)
:END:

Prompt for `#+TIBETAN_TEXT_TYPE:' using the canonical selectbox.

When Claude's async suggestion has already arrived (cached in
the source buffer's local var) its `:genre' is used as the
completing-read default;  otherwise existing-header value or
`classical' is the fallback.

** ~tibetan-document-prep-wizard--read-author~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 251
:SIGNATURE: (tibetan-document-prep-wizard--read-author source-file)
:END:

Prompt for `#+TIBETAN_AUTHOR:'.  Default = Claude suggestion if
present, otherwise existing header value, otherwise empty string.

** ~tibetan-document-prep-wizard--read-context-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 277
:SIGNATURE: (tibetan-document-prep-wizard--read-context-lines source-file)
:END:

Prompt for `#+TIBETAN_CLAUDE_CONTEXT:' lines (multi-line).

When Claude's async suggestion has arrived its `:context' list is
offered as the default — user accepts with RET, edits inline, or
clears and types fresh lines.  Empty input ends the loop.

Lines are appended via `tibetan-document-prep--append-context-
lines' so existing context lines are preserved.

** ~tibetan-document-prep-wizard--read-class-mode-fresh~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 318
:SIGNATURE: (tibetan-document-prep-wizard--read-class-mode-fresh)
:END:

Prompt for class mode WITHOUT a source-file lookup.
Used by the wizard's source-acquisition phase, where the source
doesn't yet exist on disk in its final form (Wylie ingest still
pending).  Returns the chosen symbol (`grammar' | `reading').

The default is `reading' — Carsten's stated preferred class
format and the more common case (per the §5.22-final user-
feedback note in CLAUDE.md).

** ~tibetan-document-prep-wizard--read-class-mode~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 337
:SIGNATURE: (tibetan-document-prep-wizard--read-class-mode source-file)
:END:

Prompt for `#+TIBETAN_CLASS_MODE:'.  Default = existing value
or `grammar'.  Returns the chosen symbol (`grammar' | `reading').

** ~tibetan-document-prep-wizard--read-sentence-detail~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 363
:SIGNATURE: (tibetan-document-prep-wizard--read-sentence-detail source-file)
:END:

Prompt for `#+TIBETAN_SENTENCE_DETAIL:'.  Returns chosen symbol.

** ~tibetan-document-prep-wizard--edit-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 387
:SIGNATURE: (tibetan-document-prep-wizard--edit-source source-file edit-fn)
:END:

Apply EDIT-FN (a no-arg buffer-editing function) to SOURCE-FILE.

When SOURCE-FILE already has a visiting buffer — e.g. the one
`tibetan-document-prep-wizard--fire-async-claude' opens to hold the
cached Claude suggestion — edit THAT buffer and save it, so the buffer
stays in sync with disk.  Otherwise edit the file on disk via a temp
buffer.

This is the fix for the stale-buffer clobber:  header writes used to go
straight to disk (write-region) while the async-Claude buffer stayed
stale;  the later kickoff steps (find-file-noselect + save-buffer) then
wrote that stale buffer back, dropping the headers.  Routing every
source mutation through the visiting buffer keeps them consistent.

** ~tibetan-document-prep-wizard--write-header~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 411
:SIGNATURE: (tibetan-document-prep-wizard--write-header source-file key value)
:END:

Upsert `#+KEY: VALUE' into SOURCE-FILE.  Re-uses the Phase-4
header writer so the wizard and the Claude-apply command share
one implementation.  Edits through the file's visiting buffer when one
exists (see `tibetan-document-prep-wizard--edit-source').

** ~tibetan-document-prep-wizard--scaffold-resources~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 439
:SIGNATURE: (tibetan-document-prep-wizard--scaffold-resources source-file)
:END:

Create `Resources/vocabulary.org' next to SOURCE-FILE if absent.
Returns the path of the (created or pre-existing) file.

** ~tibetan-document-prep-wizard--run-sentence-structure~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 456
:SIGNATURE: (tibetan-document-prep-wizard--run-sentence-structure source-file)
:END:

Open SOURCE-FILE and run `tibetan-add-sentence-structure' on it.
Demotes `*** Segment N' headings under `*** Sentence N' wrappers
ready for analysis.

** ~tibetan-document-prep-wizard--maybe-run-auto-analyze~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 466
:SIGNATURE: (tibetan-document-prep-wizard--maybe-run-auto-analyze source-file
                                                            class-mode)
:END:

Offer to run `tibetan-auto-analyze-document' on SOURCE-FILE.
When CLASS-MODE is `reading', additionally offer to run
`tibetan-sentence-create-all' afterwards.

** ~tibetan-document-prep-wizard--format-summary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 487
:SIGNATURE: (tibetan-document-prep-wizard--format-summary state)
:END:

Render a human-readable summary of the wizard's STATE.

** ~tibetan-document-prep-wizard~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 543
:SIGNATURE: (tibetan-document-prep-wizard)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Unified document-preparation wizard (§5.27).

Walks through:
  1. Input route (Wylie / Tibetan-script / existing / OCR).
  2. (Wylie route) validate + convert via pyewts, relocate to
     `work in progress/'.
  3. Target language → `#+TIBETAN_TARGET_LANG:'.
  4. Fire async Claude metadata suggestion (does NOT block).
  5. Genre selectbox → `#+TIBETAN_TEXT_TYPE:'.
  6. Author + context lines → `#+TIBETAN_AUTHOR:' + N x
     `#+TIBETAN_CLAUDE_CONTEXT:'.
  7. Class mode → `#+TIBETAN_CLASS_MODE:'.
  8. (Reading class-mode) sentence detail → `#+TIBETAN_SENTENCE_DETAIL:'.
  9. Resources/vocabulary.org scaffold.
  10. Sentence-structure demotion (`*** Segment N' →
      `*** Sentence N' wrappers).
  11. Offer to run `tibetan-auto-analyze-document' +
      (reading mode) `tibetan-sentence-create-all'.
  12. Final summary buffer.

When Claude's async suggestion arrives mid-wizard it's cached on
the source's visiting buffer;  later wizard steps (genre, author,
context) consult it as a default.  Post-wizard the user can run
`M-x tibetan-document-prep-apply-claude-suggestions' to apply any
suggestions not yet incorporated.

** ~tibetan-document-prep-wizard--state~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 52
:DEFAULT: nil
:END:

Transient plist of the in-flight wizard run.  Cleared on
completion.  Carries `:source-file', `:input-route', whatever
metadata has been gathered so far.  Internal to the wizard
flow.

** ~tibetan-document-prep-wizard--routes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 62
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-document-prep-wizard--class-modes~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 313
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-document-prep-wizard--sentence-details~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 358
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-document-prep-wizard--vocabulary-template~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 425
:DEFAULT: "#+TITLE: Per-document vocabulary
#+OPTIONS: toc:nil num:nil

* Vocabulary

| Term | Definition |
|------+------------|

:END:

Starter content for `Resources/vocabulary.org'.  Org-table
format matches the parser in `tibetan-parse-wordlist-org' —
filled-in rows are auto-loaded by `tibetan-load-resources-vocab'.


* doc-prep/tibetan-ocr-correct.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-ocr-correct.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-ocr-correct.el
:END:

** ~tibetan-ocr-correct--format-flagged~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 61
:SIGNATURE: (tibetan-ocr-correct--format-flagged validation)
:END:

Format flagged items from VALIDATION for prompt.

** ~tibetan-ocr-correct--build-prompt~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 88
:SIGNATURE: (tibetan-ocr-correct--build-prompt text validation)
:END:

Build correction prompt from TEXT and VALIDATION result.

** ~tibetan-ocr-correct--parse-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-ocr-correct--parse-response response)
:END:

Parse Claude RESPONSE into corrected text and changes list.
Returns plist: (:text "..." :changes (list of plists))

** ~tibetan-ocr-correct--gptel-available-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 172
:SIGNATURE: (tibetan-ocr-correct--gptel-available-p)
:END:

Check if gptel is available and configured.
Self-loads the API key from `~/.authinfo' (or `$ANTHROPIC_API_KEY')
via `tibetan-analysis--ensure-gptel-ready' if persist module is
loaded -- so a fresh Emacs session need not have run an analysis
command first.  Same pattern as
`tibetan-sanskrit-parallel-dharmamitra'.

** ~tibetan-ocr-correct--call-claude~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 185
:SIGNATURE: (tibetan-ocr-correct--call-claude prompt callback)
:END:

Call Claude with PROMPT, invoke CALLBACK with response.

** ~tibetan-ocr-correct~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 202
:SIGNATURE: (tibetan-ocr-correct text validation)
:AUTOLOAD: t
:END:

Correct TEXT using Claude AI, informed by VALIDATION result.
Returns plist: (:text "corrected" :changes (list))

** ~tibetan-ocr-correct--show-diff~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 248
:SIGNATURE: (tibetan-ocr-correct--show-diff _original corrected changes)
:END:

Show diff between ORIGINAL and CORRECTED text with CHANGES list.

** ~tibetan-ocr-correct-buffer~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 287
:SIGNATURE: (tibetan-ocr-correct-buffer)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Correct OCR errors in current buffer using Claude AI.

** ~tibetan-ocr-correct-region~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 316
:SIGNATURE: (tibetan-ocr-correct-region start end)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Correct OCR errors in region between START and END.

** ~tibetan-ocr-correct-model~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 29
:TYPE: 'string
:GROUP: tibetan-ocr-correct
:DEFAULT: "claude-sonnet-4-20250514"
:END:

Claude model to use for OCR correction.
Sonnet is recommended for balance of speed and accuracy.

** ~tibetan-ocr-correct-show-diff~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 35
:TYPE: 'boolean
:GROUP: tibetan-ocr-correct
:DEFAULT: t
:END:

Whether to show diff of changes after correction.

** ~tibetan-ocr-correct-auto-accept~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 40
:TYPE: 'boolean
:GROUP: tibetan-ocr-correct
:DEFAULT: nil
:END:

If non-nil, automatically accept AI corrections without confirmation.

** ~tibetan-ocr-correct--system-prompt~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 49
:DEFAULT: "You are an expert in Classical Tibetan orthography, helping to correct OCR errors.
You have deep knowledge of:
- Tibeta
:END:

System prompt for Claude OCR correction.

** ~tibetan-ocr-correct~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 24
:END:

Claude AI correction for Tibetan OCR.


* doc-prep/tibetan-ocr-runner.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-ocr-runner.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-ocr-runner.el
:END:

** ~tibetan-ocr--find-app~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 55
:SIGNATURE: (tibetan-ocr--find-app)
:END:

Find BDRC OCR application.

** ~tibetan-ocr--get-models-dir~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 65
:SIGNATURE: (tibetan-ocr--get-models-dir)
:END:

Get path to OCR models directory.

** ~tibetan-ocr--model-name~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 87
:SIGNATURE: (tibetan-ocr--model-name model)
:END:

Get OCR model directory name for MODEL symbol.

** ~tibetan-ocr-available-p~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 97
:SIGNATURE: (tibetan-ocr-available-p)
:AUTOLOAD: t
:END:

Check if BDRC OCR is installed and available.

** ~tibetan-ocr-status~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 102
:SIGNATURE: (tibetan-ocr-status)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show BDRC OCR installation status.

** ~tibetan-ocr-open-app~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 125
:SIGNATURE: (tibetan-ocr-open-app &optional file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open BDRC Tibetan OCR app, optionally with FILE.
If FILE is provided, the app opens with that file ready for OCR.

** ~tibetan-ocr-import-from-file~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 150
:SIGNATURE: (tibetan-ocr-import-from-file file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Import OCR results from FILE (text file exported from BDRC OCR).

** ~tibetan-ocr-import-from-clipboard~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 161
:SIGNATURE: (tibetan-ocr-import-from-clipboard)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Import OCR results from clipboard.
Use this after copying text from BDRC OCR app.

** ~tibetan-ocr-import-from-buffer~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 182
:SIGNATURE: (tibetan-ocr-import-from-buffer)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Import OCR text from current buffer or region.

** ~tibetan-ocr-workflow~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 197
:SIGNATURE: (tibetan-ocr-workflow &optional source-file)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Guided workflow for OCR processing.
1. Opens BDRC OCR app with SOURCE-FILE
2. Waits for user to process in app
3. Imports results

Returns OCR result plist.

** ~tibetan-ocr-run~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 251
:SIGNATURE: (tibetan-ocr-run source &optional _model)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Process SOURCE through BDRC OCR.
MODEL is currently ignored (user selects in GUI app).
For GUI app, this launches the workflow and prompts for import.
Returns plist: (:text "..." :source "..." :model ...)

** ~tibetan-ocr-app-path~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 40
:TYPE: 'file
:GROUP: tibetan-ocr-runner
:DEFAULT: "/Applications/BDRC Tibetan OCR.app"
:END:

Path to BDRC OCR application.
On macOS this is the .app bundle.

** ~tibetan-ocr--macos-app-paths~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 50
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-ocr--model-names~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 75
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-ocr--last-source~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 146
:DEFAULT: nil
:END:

Last source file processed.

** ~tibetan-ocr-import-map~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 292
:DEFAULT: (let ((map (make-sparse-keymap)))
    (define-key map (kbd "c") #'tibetan-ocr-import-from-clipboard)
    (define-key map
:END:

Keymap for OCR import commands (C-c o i prefix).

** ~tibetan-ocr-runner~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 35
:END:

BDRC OCR runner settings.


* doc-prep/tibetan-ocr-validate.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-ocr-validate.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-ocr-validate.el
:END:

** ~tibetan-ocr-validate--load-json~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 118
:SIGNATURE: (tibetan-ocr-validate--load-json file)
:END:

Load JSON FILE into hash table.

** ~tibetan-ocr-validate--ensure-loaded~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 126
:SIGNATURE: (tibetan-ocr-validate--ensure-loaded)
:END:

Ensure validation data is loaded.

** ~tibetan-ocr-validate--strip-particle~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 152
:SIGNATURE: (tibetan-ocr-validate--strip-particle word)
:END:

Strip particle suffix from WORD, return (root . particle) or (word . nil).

** ~tibetan-ocr-validate--check-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 162
:SIGNATURE: (tibetan-ocr-validate--check-vocabulary syllable)
:END:

Check if SYLLABLE is in comprehensive vocabulary.
Returns source name if found, nil otherwise.

** ~tibetan-ocr-validate--check-compounds~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 170
:SIGNATURE: (tibetan-ocr-validate--check-compounds syllable)
:END:

Check if SYLLABLE is part of a compound.
Returns source name if found, nil otherwise.

** ~tibetan-ocr-validate--check-proper-nouns~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 177
:SIGNATURE: (tibetan-ocr-validate--check-proper-nouns syllable)
:END:

Check if SYLLABLE is a proper noun.
Returns source name if found, nil otherwise.

** ~tibetan-ocr-validate--is-particle~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 184
:SIGNATURE: (tibetan-ocr-validate--is-particle syllable)
:END:

Check if SYLLABLE is a standalone particle.

** ~tibetan-ocr-validate--valid-syllable-structure-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 188
:SIGNATURE: (tibetan-ocr-validate--valid-syllable-structure-p syllable)
:END:

Check if SYLLABLE has valid Tibetan syllable structure.
Basic check for valid character combinations.

** ~tibetan-ocr-validate-syllable~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 199
:SIGNATURE: (tibetan-ocr-validate-syllable syllable)
:END:

Validate single SYLLABLE against all sources.
Returns plist: (:status valid|unknown|suspicious
                :source "vocabulary"|"compound"|etc.|nil
                :suggestions (list))

** ~tibetan-ocr-validate--edit-distance~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 258
:SIGNATURE: (tibetan-ocr-validate--edit-distance s1 s2)
:END:

Calculate Levenshtein edit distance between S1 and S2.

** ~tibetan-ocr-validate--apply-confusion~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 281
:SIGNATURE: (tibetan-ocr-validate--apply-confusion syllable)
:END:

Generate variants of SYLLABLE using confusion pairs.

** ~tibetan-ocr-validate--find-similar~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 292
:SIGNATURE: (tibetan-ocr-validate--find-similar syllable)
:END:

Find dictionary entries similar to SYLLABLE.
Uses confusion pairs and edit distance.

** ~tibetan-ocr-validate-text~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 324
:SIGNATURE: (tibetan-ocr-validate-text text)
:AUTOLOAD: t
:END:

Validate TEXT against all dictionaries.
Returns plist:
  :total       Total syllables checked
  :valid       List of (syllable . source) for valid syllables
  :unknown     List of unknown syllables (structurally valid)
  :suspicious  List of suspicious syllables (possible OCR errors)
  :details     List of full validation results

** ~tibetan-ocr-validation-report~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 365
:SIGNATURE: (tibetan-ocr-validation-report validation-result)
:AUTOLOAD: t
:END:

Generate human-readable report from VALIDATION-RESULT.

** ~tibetan-ocr-validate-data-dir~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 31
:TYPE: 'directory
:GROUP: tibetan-ocr-validate
:DEFAULT: (expand-file-name "data" (file-name-directory
                            (directory-file-name
                         
:END:

Directory containing validation data files.

** ~tibetan-ocr-validate--compounds~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 43
:DEFAULT: nil
:END:

Hash table of compound words from compounds.json.

** ~tibetan-ocr-validate--proper-nouns~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 46
:DEFAULT: nil
:END:

Hash table of proper nouns from proper_nouns.json.

** ~tibetan-ocr-validate--loaded~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 49
:DEFAULT: nil
:END:

Whether validation data has been loaded.

** ~tibetan-ocr-validate--particles~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 56
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-ocr-validate--particle-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 81
:DEFAULT: (concat "\\(" (mapconcat #'regexp-quote tibetan-ocr-validate--particles "\\|") "\\)$")
:END:

Regex matching particle suffixes.

** ~tibetan-ocr-validate--confusion-pairs~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 89
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-ocr-validate--vowel-marks~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 110
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-ocr-validate~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 26
:END:

Tibetan OCR validation settings.


* doc-prep/tibetan-segment-migrate.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-segment-migrate.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-segment-migrate.el
:END:

** ~tibetan-migrate--format-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 46
:SIGNATURE: (tibetan-migrate--format-segment n tibetan trans)
:END:

Format one migrated segment block.
N is the segment number (string).  TIBETAN and TRANS are the
raw texts extracted from the inline tags; leading/trailing
whitespace is trimmed.  An empty TRANS still produces a
"**** Working Translation" heading so the translator can fill
it in later.

** ~tibetan-migrate--convert-string~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 61
:SIGNATURE: (tibetan-migrate--convert-string text)
:END:

Return TEXT with every inline segment block replaced by Org headings.
Operates on strings directly (not via `replace-match') to avoid
edge cases in Emacs' replacement machinery when the replacement
spans multiple lines.

** ~tibetan-migrate-inline-segments-to-headings~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 81
:SIGNATURE: (tibetan-migrate-inline-segments-to-headings)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Convert all inline 〔seg:N〕 / 〔trans:N〕 markers in the current buffer
to the heading-based structure *** Segment N / **** Working Translation.

Idempotent: a buffer with no remaining inline tags is left unchanged.

** ~tibetan-migrate--split-and-flatten-title-headings~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 140
:SIGNATURE: (tibetan-migrate--split-and-flatten-title-headings text)
:END:

Step 1 of the bare-seg migration.

For every heading line of the shape `^\*+ <title>〔/seg〕$', emit TWO
lines in its place:

  1. `〔/seg〕' on its own line (so the previous segment closes cleanly
     at the start of the next regex iteration).
  2. `** <title>' — the heading flattened to uniform level 2,
     regardless of the original `*' / `**' / `***' depth.

Heading lines WITHOUT an inline 〔/seg〕 (e.g. `* Document Info',
`* Text', `* NOTES') are left alone — they are structural, not
Tibetan section titles.

** ~tibetan-migrate--hoist-metadata-from-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 159
:SIGNATURE: (tibetan-migrate--hoist-metadata-from-body body)
:END:

Split BODY into hoisted metadata and remaining narrative.

Walks BODY line by line and returns a cons cell (METAS . REST):
  METAS — a list of `#+KEYWORD: value' lines encountered (in order).
  REST  — BODY with those lines removed, trailing blank lines
          collapsed, then trimmed.

Used by the bare-seg migrator so any `#+KEYWORD:' lines that the
original pipeline accidentally embedded in a segment body (typically
seg:1 containing the document's `#+TITLE' etc.) can be promoted to
the document header and the segment body can be evaluated on what
remains.

** ~tibetan-migrate--convert-bare-seg-blocks~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 182
:SIGNATURE: (tibetan-migrate--convert-bare-seg-blocks text)
:END:

Step 2 of the bare-seg migration.

Scan TEXT for `〔seg:N〕 … 〔/seg〕' blocks, hoist any `#+KEYWORD:'
lines from their bodies, and emit `*** Segment M' headings for the
non-empty survivors (M counts from 1 in the order encountered).

Returns a cons cell (METAS . OUT) where:
  METAS — hoisted `#+KEYWORD:' lines across all segments, in order.
  OUT   — TEXT with every seg-block replaced; original `N' values
          are discarded and replaced with the fresh sequence.

A segment whose body becomes empty after metadata hoisting is
dropped entirely — it does NOT get a `*** Segment M' heading.

** ~tibetan-migrate--collapse-excess-blank-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 218
:SIGNATURE: (tibetan-migrate--collapse-excess-blank-lines text)
:END:

Collapse runs of 3+ blank lines in TEXT down to 2.
Keeps the output tidy after the heading-splitting pass has inserted
`〔/seg〕' lines that later disappear, and after segments are removed.

** ~tibetan-migrate--convert-bare-string~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 224
:SIGNATURE: (tibetan-migrate--convert-bare-string text)
:END:

Return TEXT migrated from the bare-seg layout to `*** Segment M'.

Pipeline:
  1. Split + flatten Tibetan-title heading lines.
  2. Convert `〔seg:N〕 … 〔/seg〕' blocks to `*** Segment M' headings,
     hoisting `#+KEYWORD:' metadata lines out of segment bodies.
  3. Prepend the hoisted metadata block above the first heading line.
  4. Collapse excess blank-line runs.

The transformation is idempotent: a TEXT already in the target
shape contains no `〔' characters, so every helper short-circuits.

** ~tibetan-migrate-bare-segments-to-headings~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 255
:SIGNATURE: (tibetan-migrate-bare-segments-to-headings)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Convert `〔seg:N〕 … 〔/seg〕' markers in the current buffer to
`*** Segment M' headings, flatten Tibetan-title heading lines to
`** Section', and hoist `#+KEYWORD:' metadata to the document header.

Use this for documents prepared with the bare-seg pipeline (no
`〔trans:N〕' pairs), typically identified by inline `〔/seg〕' markers
at the end of Tibetan-title heading lines.  See also
`tibetan-migrate-inline-segments-to-headings' for the paired variant.

Idempotent: a buffer without any `〔' markers is left unchanged.

** ~tibetan-migrate--segment-block-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 33
:DEFAULT: (concat "〔seg:\\([0-9]+\\)〕"
          "\\(\\(?:.\\|\n\\)*?\\)"
          "〔/seg〕"
          "[[:space:]\n]*"
          
:END:

Regex matching one 〔seg:N〕 … 〔/seg〕 + 〔trans:N〕 … 〔/trans〕 block.
Group 1: the segment number.
Group 2: the Tibetan text.
Group 3: the translation text.

** ~tibetan-migrate--bare-seg-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 126
:DEFAULT: (concat "〔seg:\\([0-9]+\\)〕"
          "\\(\\(?:.\\|\n\\)*?\\)"
          "〔/seg〕")
:END:

Regex matching one 〔seg:N〕 … 〔/seg〕 block (no trans required).
Group 1: the original segment number (discarded on migration).
Group 2: the body text between markers.

** ~tibetan-migrate--keyword-line-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 134
:DEFAULT: "^#\\+[A-Z][A-Z0-9_]*:.*$"
:END:

Regex matching an Org `#+KEYWORD: value' document-level metadata line.
Deliberately excludes block markers like `#+BEGIN_SRC' (which have no
colon immediately after the keyword name).


* doc-prep/tibetan-sentence-structure.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-sentence-structure.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-sentence-structure.el
:END:

** ~tibetan--last-syllable~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 155
:SIGNATURE: (tibetan--last-syllable text)
:END:

Return the final tsheg-separated syllable of TEXT, or nil if none.
Strips trailing shad, tsheg, and whitespace before splitting.

** ~tibetan--last-two-syllables~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 165
:SIGNATURE: (tibetan--last-two-syllables text)
:END:

Return the last two tsheg-separated syllables of TEXT joined by ་,
or nil if fewer than two syllables remain after stripping shad/tsheg.

** ~tibetan-is-sentence-boundary-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 176
:SIGNATURE: (tibetan-is-sentence-boundary-p text)
:END:

Determine whether TEXT ends at a sentence boundary.

Return value:
  nil       — not a sentence boundary
  \='strong — high-confidence boundary (double shad, sentence-final
              particle, known finite verb)
  \='weak   — probable boundary (bare single shad on a multi-syllable
              phrase, after ruling out converb/case/topic/coordinator).
              In rnam-thar prose this is most commonly a finite past
              verb that is not in the explicit finite-verb list.

Both \='strong and \='weak are truthy, so legacy callers that use
the result in `when'/`if' continue to work.  Callers that want to
gate on confidence (e.g. the auto-segmenter defaulting to strong-only)
can test the symbol explicitly with `eq'.

Detection layers, in order:
  1. Double shad (།།, ༎)                                 → strong
  2. No shad at all                                       → nil
  3. Two-syllable non-boundary patterns (e.g. པ་ལ།)       → nil
  4. Converb / clause-connective last syllable            → nil
  5. Case particle last syllable                          → nil
  6. Topic marker last syllable (ནི།)                     → nil
  7. Sentence-final particle / finite verb                → strong
  7b. Dialogue two-syllable pattern (ན་རེ།, ཅེས་སྐད།)    → strong
  7c. Quotative single-syllable closer (ཅེས།, ཞེས།)       → strong
  8. Bare single shad on multi-syllable phrase            → weak
  9. Single-syllable phrase (likely fused ergative)       → nil

Steps 7b and 7c handle Milarepa-style dialogue framing: `X ན་རེ།'
opens a quoted section (short framing sentence), and `X ཅེས།' or
`X ཞེས།' closes one.  Without these, the detector fell through to
the weak tier and required manual confirmation for every reported-
speech boundary — costly on a rnam-thar corpus where dialogue is
the dominant structure.

** ~tibetan--segment-boundary-at-point~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 243
:SIGNATURE: (tibetan--segment-boundary-at-point)
:END:

Return the confidence symbol of the segment heading at point.
One of nil, \='strong, or \='weak.  Reads the segment body from
the next line up to the following Org heading.

** ~tibetan--collect-segment-boundaries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 255
:SIGNATURE: (tibetan--collect-segment-boundaries)
:END:

Walk the current buffer and return a plist per `*** Segment' heading.
Each plist has the shape (:seg-num N :pos POS :confidence CONF) where
CONF is one of nil, \='strong, or \='weak.  Returns them in
document order.

** ~tibetan-detect-sentence-boundaries~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 271
:SIGNATURE: (tibetan-detect-sentence-boundaries &optional include-weak)
:INTERACTIVE: t
:END:

Preview sentence boundaries in the current buffer.

With no prefix argument, show only STRONG boundaries (double shad,
sentence-final particles, finite verbs).  With prefix argument
(C-u) also show WEAK boundaries (bare single shad on multi-syllable
phrases — typically finite past verbs not in the explicit list).

The preview lists each sentence start and tags its trigger as
[strong] or [weak], and ends with a summary count of each kind.

** ~tibetan-add-sentence-structure~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 327
:SIGNATURE: (tibetan-add-sentence-structure &optional include-weak)
:INTERACTIVE: t
:END:

Add *** Sentence N headings to organize segments into sentences.

By default, only STRONG boundaries split sentences (double shad,
sentence-final particles, finite verbs).  With a prefix argument
(C-u) also split at WEAK boundaries (bare single shad on a
multi-syllable phrase — typically finite past verbs not in the
explicit list).

The strong-only default is deliberately conservative: rnam-thar
prose has many ambiguous clause-final verbs, and under-segmentation
is cheaper to fix interactively (via `tibetan-mark-sentence-start')
than over-segmentation.

Transforms:
  ** Section
  *** Segment 1
  *** Segment 2
  ...
Into:
  ** Section
  *** Sentence 1
  **** Segment 1
  **** Segment 2
  *** Sentence 2
  **** Segment 3

** ~tibetan-mark-sentence-start~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 405
:SIGNATURE: (tibetan-mark-sentence-start)
:INTERACTIVE: t
:END:

Mark current segment as starting a new sentence.
Inserts a *** Sentence heading above the current segment and demotes
the segment heading from *** to ****.

** ~tibetan-sentence-reset-structure--counts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 437
:SIGNATURE: (tibetan-sentence-reset-structure--counts buffer)
:END:

Return (SENT-COUNT . SEG-COUNT) — how many `*** Sentence N' headings
and how many `**** Segment M' (demoted) headings BUFFER contains.
Used by the reset command to show the user what it will touch.

** ~tibetan-sentence-reset-structure~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 454
:SIGNATURE: (tibetan-sentence-reset-structure)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Undo the effect of `tibetan-add-sentence-structure' on the current buffer.

Two reversible edits:
  1. Delete every `^\*\*\* Sentence N$' heading line (and the
     single trailing blank line that usually follows, if present).
  2. Re-promote every `^\*\*\*\* Segment M' back to
     `^\*\*\* Segment M' so the next
     `tibetan-add-sentence-structure' run can discover them again.

Prompts with a summary of how many headings will be removed /
promoted before touching the buffer.  Does NOT save; user reviews
the edit, then `C-x C-s' when satisfied.  Idempotent — safe to
run repeatedly.

Intended use: before running `C-c s S' a SECOND time on a file
whose structure is already in place.  The segment regex in the
auto-segmenter only matches three-asterisk `*** Segment', so
without resetting the demoted segments a re-run would silently
find zero segments and do nothing useful.

** ~tibetan-sentence-final-particles~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 49
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence-converb-particles~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 90
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-case-particle-syllables~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 109
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-topic-particle-syllables~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 118
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-non-boundary-two-syllable-patterns~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 122
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence-dialogue-single-syllables~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 132
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence-dialogue-two-syllable-patterns~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 141
:DEFAULT: '
:END:

/(no docstring)/


* doc-prep/tibetan-wylie-ingest.el
:PROPERTIES:
:CUSTOM_ID: doc-prep/tibetan-wylie-ingest.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/doc-prep/tibetan-wylie-ingest.el
:END:

** ~tibetan-wylie-ingest--python-available-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 70
:SIGNATURE: (tibetan-wylie-ingest--python-available-p)
:END:

Return non-nil when the configured Python interpreter is on PATH.

** ~tibetan-wylie-ingest--pyewts-available-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 76
:SIGNATURE: (tibetan-wylie-ingest--pyewts-available-p)
:END:

Return non-nil when `pyewts' is importable in the configured Python.
Runs `python3 -c "import pyewts"' and inspects the exit code.
Caches nothing — caller should bind the result once per wizard
run if the cost is a concern.

** ~tibetan-wylie-ingest--require-pyewts-or-error~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 85
:SIGNATURE: (tibetan-wylie-ingest--require-pyewts-or-error)
:END:

Signal a `user-error' when the Python prerequisites are missing.
Used by `tibetan-wylie-ingest-file' as its single pre-flight gate
so users see a clear hint instead of a stack trace from the
shelled-out script.

** ~tibetan-wylie-ingest--char-tibetan-unicode-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 114
:SIGNATURE: (tibetan-wylie-ingest--char-tibetan-unicode-p ch)
:END:

Return non-nil when CH is in the Tibetan Unicode block.

** ~tibetan-wylie-ingest--extract-tibetan-text-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 119
:SIGNATURE: (tibetan-wylie-ingest--extract-tibetan-text-body path)
:END:

Return the body under the `* Tibetan Text' heading in PATH.
Returns nil when the heading is absent.  Mirrors the Python
script's `split_source' logic — body runs from the heading to the
next top-level heading (or EOF).

** ~tibetan-wylie-ingest--scan-body-for-issues~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 137
:SIGNATURE: (tibetan-wylie-ingest--scan-body-for-issues body)
:END:

Walk BODY one char at a time, returning a plist of findings.
Plist keys:
  :tibetan-unicode-count   — number of chars in U+0F00..U+0FFF
  :non-ascii-count         — number of non-ASCII, non-Tibetan chars
  :first-tibetan-unicode-pos  / :first-non-ascii-pos  — 1-based
                              char index into BODY of the FIRST
                              occurrence (for user-friendly error
                              snippets), or nil when absent.

** ~tibetan-wylie-ingest--count-paragraphs-and-units~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 168
:SIGNATURE: (tibetan-wylie-ingest--count-paragraphs-and-units body)
:END:

Return a plist `(:paragraph-count N :estimated-segment-count N)'.
Paragraphs are blank-line-separated non-empty blocks (same rule
the Python script uses).  Segment count = total `//' (double-shad)
occurrences across the body, mirroring `split_paragraph_into_
segments' — each `//' boundary becomes one segment.  Estimate
slightly understates when a paragraph's trailing text has no `//'
(that residual becomes a final segment), so add 1 per paragraph.

** ~tibetan-wylie-ingest--count-pipe-shads~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 191
:SIGNATURE: (tibetan-wylie-ingest--count-pipe-shads body)
:END:

Return the count of `|' characters in BODY.  Used by the
validator to flag Wylie sources that use the pipe-shad
convention and would benefit from `--normalize-shads' before
ingest.

** ~tibetan-wylie-ingest--count-non-standard-folios~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 207
:SIGNATURE: (tibetan-wylie-ingest--count-non-standard-folios body)
:END:

Return the count of bracket-style folio markers in BODY that
are NOT already in the canonical `[F:D...]' form the Python
ingest script recognises.  Matches `[141a.6]' and `[141a6]'
variants.

** ~tibetan-wylie-ingest-validate-input~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 220
:SIGNATURE: (tibetan-wylie-ingest-validate-input path)
:END:

Scan the Wylie source at PATH for structural issues.
Returns a plist suitable for the wizard's pre-conversion report:

  :path                          absolute source path
  :has-tibetan-text-section      t  when `* Tibetan Text' is present
  :paragraph-count               number of blank-line paragraphs in body
  :estimated-segment-count       count of `//' (+ trailing) units in body
  :tibetan-unicode-count         0 for a clean Wylie source
  :non-ascii-count               0 for a clean Wylie source
  :first-tibetan-unicode-pos  / :first-non-ascii-pos  — see scanner
  :pipe-shad-count               count of `|' chars (suggests need for
                                 `--normalize-shads' before ingest)
  :non-standard-folio-count      count of `[NNNa.M]' markers that are
                                 NOT in the script's `[F:D...]' form
                                 (suggests `--normalize-folio-markers')

Signals a `user-error' when PATH does not exist or is unreadable.
Other-issue counts of zero indicate the file is structurally sound
for the converter;  any non-zero count is advisory — the user can
still proceed (the converter passes non-Wylie chars through to
pyewts which may emit replacement characters or simply skip them).

** ~tibetan-wylie-ingest--format-validation-report~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 260
:SIGNATURE: (tibetan-wylie-ingest--format-validation-report report)
:END:

Render REPORT (the plist from `--validate-input') as a string.

** ~tibetan-wylie-ingest-validate-input-interactive~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 299
:SIGNATURE: (tibetan-wylie-ingest-validate-input-interactive path)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Interactive wrapper around `tibetan-wylie-ingest-validate-input'.
Pops a `*Wylie Validate*' buffer with the report.

** ~tibetan-wylie-ingest--apply-to-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 313
:SIGNATURE: (tibetan-wylie-ingest--apply-to-body path fn)
:END:

Read PATH, apply FN to its `* Tibetan Text' body, write back.

FN is a one-argument function taking the body string (everything
between the `* Tibetan Text' heading and the next top-level `* '
heading) and returning a modified string.  Metadata headers,
title, and any post-body sections are untouched.

No-op (returns nil) when PATH has no `* Tibetan Text' heading.
Returns t when the body was actually rewritten.

** ~tibetan-wylie-ingest--normalize-shads-string~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 348
:SIGNATURE: (tibetan-wylie-ingest--normalize-shads-string body &optional class-mode)
:END:

Return BODY with Wylie pipe-shads normalised.

CLASS-MODE controls how the standard-Wylie double-shad `| |' is
treated;  it directly drives segment granularity downstream:

  · `reading' (default) — `| |' → `/ /' (two single shads with a
                          space).  The Python script's segment
                          splitter (`text.split("//")') doesn't
                          fire on `/ /', so the surrounding
                          paragraph stays as ONE segment.  Pyewts
                          still renders the two-shad sequence as
                          a Tibetan double-shad (`།  །') in the
                          output.  Right for reading-class flow
                          where one segment = one paragraph.

  · `grammar'           — `| |' → `//'.  The script splits at
                          every `//' boundary, so each `| |'-
                          delimited sentence becomes its own
                          segment.  Right for grammar-class
                          flow where one segment = one sentence.

Lone `|' (no adjacent partner) always maps to `/' in both modes —
within-segment shad, cosmetic at the pyewts level.

Pure-string operation;  used by `tibetan-wylie-ingest-normalize-
shads' on the body region and by the validator's counter.

** ~tibetan-wylie-ingest-normalize-shads~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 397
:SIGNATURE: (tibetan-wylie-ingest-normalize-shads path &optional class-mode)
:END:

Normalise Wylie pipe-shads in PATH's `* Tibetan Text' body.

CLASS-MODE (`reading' default, or `grammar') controls how the
double-shad `| |' is rewritten — see
`tibetan-wylie-ingest--normalize-shads-string' for the details.

Returns the count of `|' characters that were converted.
Idempotent — re-running on a body that no longer contains `|'
returns 0.

** ~tibetan-wylie-ingest--normalize-folio-markers-string~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 421
:SIGNATURE: (tibetan-wylie-ingest--normalize-folio-markers-string body)
:END:

Return BODY with bracket-style folio markers reshaped to
`[F:D<digits><ab><digits>]' so the Python script's
`FOLIO_RE = r"\[F:D(\d+[ab]\d+)\]"' regex picks them up.

** ~tibetan-wylie-ingest-normalize-folio-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 430
:SIGNATURE: (tibetan-wylie-ingest-normalize-folio-markers path)
:END:

Reshape inline folio markers in PATH's `* Tibetan Text' body
from `[141a.6]' to `[F:D141a6]' so the Python script extracts
them as `:FOLIO:' org properties instead of leaving them
embedded in the Wylie body (where pyewts would garble them).

Returns the count of markers rewritten.

** ~tibetan-wylie-ingest-wrap-bare-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 455
:SIGNATURE: (tibetan-wylie-ingest-wrap-bare-source path)
:END:

Wrap PATH (a bare Wylie source) in the canonical org structure
required by `tibetan-ybh-prep.py'.

A `bare Wylie source' is a `.org' file that contains Wylie body
content but has no `* Tibetan Text' heading — typically the
result of pasting raw Wylie into a fresh file or downloading a
plain-text Wylie transcription.

Mutates the file in place:
  #+TITLE: <basename-without-extension>

​  * Tibetan Text
  <existing content unchanged>

Returns PATH on success.  Idempotent — a file that ALREADY has
`* Tibetan Text' is left untouched and the function signals a
`user-error' so callers know to skip the wrap step.

** ~tibetan-wylie-ingest--build-argv~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 498
:SIGNATURE: (tibetan-wylie-ingest--build-argv path in-place)
:END:

Return the argv list for the Python converter.
PATH is the source `.org' file (absolute), IN-PLACE non-nil
threads the `--in-place' flag.

** ~tibetan-wylie-ingest--invoke-script~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 508
:SIGNATURE: (tibetan-wylie-ingest--invoke-script path in-place)
:END:

Run the converter on PATH.  Return a plist with :exit-code,
:stdout, :stderr.  Stubbed by tests via `cl-letf' on
`call-process'.

** ~tibetan-wylie-ingest-file~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 532
:SIGNATURE: (tibetan-wylie-ingest-file path &optional in-place skip-move)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Convert PATH from Wylie to Tibetan Unicode via the bundled script.

PATH is a source `.org' file carrying Wylie under `* Tibetan
Text'.  When IN-PLACE is non-nil the file is overwritten with the
converted content;  otherwise the converter's stdout is returned
as a string.

After a successful IN-PLACE conversion, the prepared file is
relocated into the configured work-in-progress folder via
`tibetan-doc-prep-move-to-work-in-progress' (sibling of the
source's parent dir, default name `work in progress').  Passing
SKIP-MOVE non-nil disables the relocation — useful from tests or
when the caller orchestrates the move itself.

Signals a `user-error' when `python3' or `pyewts' aren't
available, or when the script exits non-zero (stderr is included
in the error message for diagnosis).

Return value:
  IN-PLACE  → absolute path of the prepared file (post-move).
  stdout    → converter's stdout body as a string.

Interactive callers receive a prefix-arg → IN-PLACE.

** ~tibetan-wylie-ingest-python-executable~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 47
:TYPE: 'string
:GROUP: tibetan-wylie-ingest
:DEFAULT: "python3"
:END:

Python interpreter used to run the Wylie ingest script.
Must have the `pyewts' package importable.  The path is resolved
via `executable-find' at call time, so a virtualenv-relative
name is fine.

** ~tibetan-wylie-ingest-script-path~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 55
:TYPE: 'file
:GROUP: tibetan-wylie-ingest
:DEFAULT: (let ((dir (file-name-directory
              (or load-file-name buffer-file-name default-directory))))
    (expand-file
:END:

Absolute path to the bundled `tibetan-ybh-prep.py' converter.
Defaults to the script that ships alongside this Elisp module
under `doc-prep/'.  Override only when running from a non-standard
checkout.

** ~tibetan-wylie-ingest--tibetan-unicode-range~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 107
:DEFAULT: (cons #x0F00 #x0FFF)
:END:

Inclusive codepoint range (U+0F00..U+0FFF) of the Tibetan block.
Used by the validator to flag bodies that already contain Tibetan
Unicode — typically a sign the user accidentally pasted a converted
file in place of the Wylie source.

** ~tibetan-wylie-ingest--folio-regex~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 198
:DEFAULT: "\\[\\([0-9]+\\)\\([ab]\\)\\.?\\([0-9]+\\)\\]"
:END:

Match `[<digits><ab>.?<digits>]' — the common bracket-folio
convention in academic Tibetan transcriptions (e.g. `[141a.6]'
= folio 141a, line 6).  Pure-digit / pure-letter runs only, so
bibliographic ranges like `[141a.5–143b.4; pp. 285.5–290.4]'
(which contain dashes / semicolons) do NOT match and are left
intact.

** ~tibetan-wylie-ingest~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 42
:END:

Wylie → Tibetan Unicode ingest settings.


* persist/tibetan-analysis-claude.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-analysis-claude.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-analysis-claude.el
:END:

** ~tibetan-analysis--read-source-metadata~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 295
:SIGNATURE: (tibetan-analysis--read-source-metadata source-file)
:END:

Return a plist of prompt-relevant metadata extracted from SOURCE-FILE.
Keys:
  :title           value of the first `#+TITLE:' line
  :work            :WORK from the first :PROPERTIES: drawer
  :author          :AUTHOR from the first :PROPERTIES: drawer
  :sources         :SOURCES from the first :PROPERTIES: drawer
  :claude-context  list of all `#+TIBETAN_CLAUDE_CONTEXT:' values in order
  :vocab-file      value of `#+TIBETAN_VOCAB_FILE:' (relative to SOURCE-FILE)
  :corpus          value of `#+TIBETAN_CORPUS:' — the corpus identifier
                   used by the dictionary ranker to promote a
                   corpus-specific Steinert sub-dictionary (e.g.
                   `Yogacarabhumi' → `22-Yoghacharabhumi-glossary').
                   See `tibetan-vocab--corpus-source-map'.
  :target-lang     value of `#+TIBETAN_TARGET_LANG:' — the primary
                   visible translation language for this document
                   (`de' or `en', lowercased; nil when unset).
                   Drives `--prefer-target-lang' in the Interlinear
                   Gloss, the CAT Gloss, and the Claude Translation
                   prompt.  The Detailed Dictionary always keeps
                   the bilingual `DE // EN' form for reference.
  :source-mode     value of `#+SOURCE_MODE:' — the document's
                   reading mode (e.g. `parallel-sanskrit' for the
                   Yogācārabhūmi parallel-reading workflow where
                   Sanskrit is primary and Tibetan secondary).
                   nil when unset (today's default — Tibetan-only
                   source).  Phase 3 of sanskrit-parallel-workflow
                   (2026-04-27) injects a Sanskrit-primary system
                   block into the Claude prompt when this equals
                   `parallel-sanskrit'.  See
                   `core/tibetan-sanskrit-parallel.el' for the
                   inline reader used by `core/' callers without
                   the persist dependency.
  :dm-sanskrit-source  value of `#+DM_SANSKRIT_SOURCE:' — the
                   DharmaMitra source identifier for the Sanskrit-
                   side parallel work (e.g. `SA_T06_bsa034' for
                   Asaṅga's Bodhisattvabhūmi).  Phase 2 of the
                   dharmamitra-realign workflow (2026-04-27).
                   The realign command uses this to constrain
                   the search to the right corpus when re-aligning
                   `**** Sanskrit' siblings.  nil when unset.
  :dm-tibetan-source  value of `#+DM_TIBETAN_SOURCE:' — the DM
                   identifier for the Tibetan-side parallel
                   (e.g. `BO_T06_D4037' for the Derge
                   Bodhisattvabhūmi).  nil when unset.
  :text-type       value of `#+TIBETAN_TEXT_TYPE:' — canonical
                   Tibetan genre tag (`rnam-thar', `lam-rim',
                   `mgur', `mdo', `rgyud', `bstan-bcos',
                   `snyan-ngag', `gtam-rgyud', `'grel-pa',
                   `ti-ka', `gter-ma', `gdams-ngag', plus the
                   legacy values `classical' / `madhyamaka-
                   verse' / `sutra' / `commentary').  Threaded
                   into Claude system prompt by callers that
                   want to specialise the analysis to the
                   genre.  nil when unset.  §5.27 Phase 1
                   (2026-05-26).
  :class-mode      value of `#+TIBETAN_CLASS_MODE: grammar|reading'
                   — controls whether downstream commands default
                   to segment-focus (Tibetan grammar classes,
                   default) or sentence-focus (reading classes).
                   Persistent per-document;  command prefix-args
                   override ad-hoc.  §5.27 Phase 1 (2026-05-26).
  :sentence-detail value of `#+TIBETAN_SENTENCE_DETAIL:
                   compressed|detailed' — only meaningful when
                   `:class-mode' is `reading'.  `compressed' =
                   §5.22 default (Vocab + Translation + Grammar
                   + Provided Translations, ~2 A4 pages).
                   `detailed' = full §5.21 segment layout
                   (sentence file mirrors a segment file's
                   depth).  §5.27 Phase 1 (2026-05-26).

§5.22 final (2026-05-21):  the `:sentence-compressed' plist key
(briefly added in §5.22 initial as an opt-in for compressed
sentence layout) is RETIRED.  Sentence files are now
unconditionally compressed;  no per-source header is consumed.
The `#+TIBETAN_SENTENCE_COMPRESSED:' line is ignored if present.

§5.27 Phase 1 (2026-05-26):  `:author' reader extended.  In
addition to the existing PROPERTIES-drawer `:AUTHOR:' path, a
new `#+TIBETAN_AUTHOR:' single-line header is also recognised.
Drawer wins when both are present (backwards-compatible);
header-only sources (the wizard-created common case) get the
same `:author' slot.

Safe when SOURCE-FILE is nil or does not exist — returns an empty plist.

** ~tibetan-analysis-set-source-target-lang~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 498
:SIGNATURE: (tibetan-analysis-set-source-target-lang source-file lang)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Set `#+TIBETAN_TARGET_LANG:' to LANG on SOURCE-FILE.

LANG must be `"de"' or `"en"' — any other value signals a
user-error (the picker downstream only knows those two).

When the source already has a `#+TIBETAN_TARGET_LANG:' line the
value is replaced in place; otherwise a new line is inserted
immediately after the first `#+TITLE:' line so the metadata
block stays contiguous at the top of the file.

Re-analyses of this document will pick up the new value
automatically via `--read-source-metadata' — no manual reload
needed.  Interactive callers get progress + final-state messaging.

Errors when SOURCE-FILE is nil, non-existent, or the file cannot
be written to.

** ~tibetan-analysis--source-file-from-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 555
:SIGNATURE: (tibetan-analysis--source-file-from-analysis analysis-file)
:END:

Return the absolute source file referenced by ANALYSIS-FILE.
Reads the `#+SOURCE:' header (an org link of the form
`[[file:../foo.org::*Segment N][…]]') and resolves it relative to
the directory of ANALYSIS-FILE.  Returns nil if nothing is found.

** ~tibetan-analysis--match-resources-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 571
:SIGNATURE: (tibetan-analysis--match-resources-vocab tibetan-text vocab-file)
:END:

Return a list of (TERM . GLOSS) from VOCAB-FILE that occur in TIBETAN-TEXT.
VOCAB-FILE is an org file containing a table whose first column is the
Tibetan term and second column is the gloss.

** ~tibetan-analysis--collect-zettel-references~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 620
:SIGNATURE: (tibetan-analysis--collect-zettel-references tibetan-text)
:END:

Return a deduplicated list of thesaurus zettel plists for TIBETAN-TEXT.

Walks the tokenised words/compounds in TIBETAN-TEXT (via
`tibetan-tokenize-into-words'), looks each up in the thesaurus
(via `tibetan-thesaurus-lookup' using the Wylie key), and returns
the unique zettels found.  Plist shape per element:

  (:id ID :wylie WYLIE :sanskrit SKT :primary-en EN :primary-de DE
   :path PATH)

Deduplication is by `:id'.  Returns nil when the thesaurus module
isn't loaded, the directory isn't configured, or no zettel
matches.  Safe to call from prompt builders — wrapped in
`condition-case' so a malformed zettel doesn't crash the analysis.

** ~tibetan-analysis--format-zettel-references-block~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 659
:SIGNATURE: (tibetan-analysis--format-zettel-references-block zettels)
:END:

Format ZETTELS (list of plists) as an injectable user-prompt block.

When non-empty, returns a string starting with two newlines + a
header line + one bullet per zettel.  When ZETTELS is nil or
empty, returns the empty string so the caller can unconditionally
concat the result.

Each bullet shows the Wylie key, the org ZID, the Sanskrit
equivalent (if known), and the primary translation (English
preferred, German fallback) — enough for Claude to recognise the
term as it appears in the passage and to cite it correctly.

** ~tibetan-analysis--cross-link-zettels-in-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 698
:SIGNATURE: (tibetan-analysis--cross-link-zettels-in-body body)
:END:

Append `[[id:ZID][zettel ↗]]' links to bolded Tibetan terms in BODY.

BODY is a string (typically the Concept Notes section's body
returned by Claude).  For each line matching `- **<term>**…',
extract the leading word, look it up in the thesaurus by Wylie,
and append the zettel link to the line if the zettel exists AND
the line doesn't already carry a `[[id:…]]' link.

The leading-word extractor splits on whitespace / `(' so terms
like `**'dod yon (Skt. pañca kāmaguṇa) — five sensory…**' yield
the bare Wylie key `'dod yon' for lookup.  When Claude bolded the
Tibetan-script form (`མཐུ'), the function first tries the script
as-is (some thesauruses have script keys), then converts to Wylie
and re-tries.

Returns the modified BODY string.  No-op (returns BODY unchanged)
when BODY is nil/empty, when the thesaurus module isn't loaded,
or when no bolded terms match a zettel.  Wrapped in
`condition-case' so any error in the regex / lookup path falls
back to the unmodified body.

** ~tibetan-analysis--read-analysis-parser-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 766
:SIGNATURE: (tibetan-analysis--read-analysis-parser-sections analysis-file)
:END:

Return a plist of parser-output sections extracted from ANALYSIS-FILE.
Used to give Claude the tool's own grammatical analysis as grounding
for the Grammar section of the three-section response.  Keys:
  :grammatical-markers   body of `** Grammatical Markers'
  :clause-structure      body of `** Clause Structure'
  :verb-classification   body of `** Verb Classification (Hill 2010)'
  :sentence-structure    body of `** Sentence Structure'
Any missing section is nil.  Safe when ANALYSIS-FILE is nil or absent.

** ~tibetan-analysis--format-parser-grounding~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 808
:SIGNATURE: (tibetan-analysis--format-parser-grounding parser-sections)
:END:

Format PARSER-SECTIONS (plist from `--read-analysis-parser-sections')
as a single text block suitable for embedding in the Claude user
prompt.  Returns nil when every section is empty.

** ~tibetan-analysis--read-word-particle-list~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 835
:SIGNATURE: (tibetan-analysis--read-word-particle-list analysis-file)
:END:

Return the body of `** Word / Particle List' in ANALYSIS-FILE, or nil.
This is the compact, numbered vocabulary list the tool generates for
each segment: `N. Tibetan [wylie]  [tag] — short gloss'.  Safe when
ANALYSIS-FILE is nil or does not exist.

** ~tibetan-analysis--read-interlinear-glosses~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 862
:SIGNATURE: (tibetan-analysis--read-interlinear-glosses analysis-file)
:END:

Return content-word glosses from the `** Interlinear Gloss' section
of ANALYSIS-FILE as a newline-joined `  - wylie = gloss' list, or nil.

Parses each `[[term-SLUG][WYLIE]] [★] [GLOSS]' token, capturing the
Wylie surface form and its first bracketed gloss (the tool's layered
lookup result; Resources-curated entries are marked ★ in the source).

This replaces the reader for the §5.10-retired `** Word / Particle
List' section.  Without it the Claude vocabulary prompt received NO
dictionary grounding, so Claude guessed meanings for rare words
(Milarepa Segment 37: `phru rlog' = "farm work" in Resources, but
Claude hallucinated "hand-mill turning").  Safe when ANALYSIS-FILE is
nil or absent.

** ~tibetan-analysis--format-segment-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 911
:SIGNATURE: (tibetan-analysis--format-segment-vocabulary analysis-file)
:END:

Return the per-segment vocabulary block for the Claude user prompt.
Draws from the `** Word / Particle List' section of ANALYSIS-FILE
when present (legacy files), falling back to the `** Interlinear
Gloss' section (current layout — §5.10 retired Word / Particle List).
Either way the block carries the tool's own per-segment matches,
already enriched with Hill morphology and Resources entries.  Returns
nil when neither section yields content so the prompt builder can skip
it cleanly.

** ~tibetan-analysis--collect-paragraph-translations~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 949
:SIGNATURE: (tibetan-analysis--collect-paragraph-translations analysis-file)
:END:

Return alist of (label . body) for translations in ANALYSIS-FILE.
Sources, in order:
  - `** <Translator>' subsections under `* Reference Translations'
    (Lopez, Wangjié, future DharmaMitra…)
  - `* Working Translation' top-level body (the user's own German
    rendering), labeled `Working Translation', omitted when empty.
Returns nil when neither source has populated content.

** ~tibetan-analysis--build-comparison-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1004
:SIGNATURE: (tibetan-analysis--build-comparison-prompts tibetan-text translations)
:END:

Return (SYSTEM . USER) prompt cons for the translation comparison.
TIBETAN-TEXT is the source paragraph; TRANSLATIONS is alist of
(label . body) pairs as returned by
`tibetan-analysis--collect-paragraph-translations'.

The system prompt frames Claude as an evaluator scoring pairwise
content + grammatical similarity on a 0–1 scale.  The user prompt
lays out the source + each translation labeled, and requests two
markdown sections (`## Comparison Matrix' and `## Diagnostic').

** ~tibetan-analysis--parse-comparison-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1055
:SIGNATURE: (tibetan-analysis--parse-comparison-response response)
:END:

Parse Claude's translation-comparison RESPONSE.
Returns plist (:matrix STRING :diagnostic STRING) where:
  :matrix     is the markdown / org-compatible table block (the
              `| ... |' lines) extracted from the `## Comparison
              Matrix' section, or nil when no table was emitted.
  :diagnostic is the prose body of the `## Diagnostic' section
              (everything after the heading until end-of-response),
              or nil when absent.

** ~tibetan-analysis--write-comparison-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1078
:SIGNATURE: (tibetan-analysis--write-comparison-section analysis-file matrix-org diagnostic)
:END:

Write `* Translation Comparison' section into ANALYSIS-FILE.
MATRIX-ORG is the org-table body (string of `| ... |' rows).
DIAGNOSTIC is the prose body.  Either may be nil; the section is
still emitted if at least one is present.

Position invariant: the section sits between `* Working Translation'
and `* Auto-Analysis'.  If a `* Translation Comparison' section
already exists, it's replaced in place; otherwise inserted at the
canonical position.  Idempotent.

** ~tibetan-translation-comparison-refresh~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1128
:SIGNATURE: (tibetan-translation-comparison-refresh)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Refresh the `* Translation Comparison' section of the current par-NNN.org.
Collects all available translations (Lopez, Wangjié, future
Mitra, plus the user's `* Working Translation' if non-empty),
asks Claude to score them pairwise (0 = incomparable, 1 =
identical) and explain divergences, and rewrites the section.
Bound to `C-c u T'.

Requires gptel + a configured Anthropic API key.  Errors are
surfaced via `message' rather than signalled.

** ~tibetan-analysis--format-reference-translations~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1219
:SIGNATURE: (tibetan-analysis--format-reference-translations analysis-file)
:END:

Return the `* Reference Translations' block for the Claude user prompt.
Reads the top-level `* Reference Translations' section of
ANALYSIS-FILE and formats its `**'-subsections (one per translator —
Lopez, Wangjié, DharmaMitra, …) as labeled context blocks.  Returns
nil when:
  - the section is absent (e.g. seg-NNN.org without paragraph imports);
  - all `**'-subsection bodies are empty.
Instructs Claude to cross-check its translation against the references
and flag substantive divergences in the `## Grammar' section.

** ~tibetan-analysis--neighbor-analysis-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1277
:SIGNATURE: (tibetan-analysis--neighbor-analysis-file analysis-file seg-id)
:END:

Return the existing `seg-SEG-ID*.org' neighbor of ANALYSIS-FILE, or nil.
Looks in the same directory; if multiple variants exist (e.g. with a
short-title suffix), picks the one whose basename matches the exact
`seg-NNN' prefix first, otherwise falls back to directory-files
ordering.  SEG-ID is an integer.

** ~tibetan-analysis--format-neighbor-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1296
:SIGNATURE: (tibetan-analysis--format-neighbor-segment analysis-file seg-id offset)
:END:

Return a text block describing ANALYSIS-FILE's neighbor at OFFSET.
Reads `* Tibetan Text' (required) and `* Working Translation'
(optional) from the neighbor file.  OFFSET is the signed distance
(e.g. -1 for preceding, +1 for following).  Returns nil when the
neighbor does not exist or has no Tibetan text.

** ~tibetan-analysis--format-surrounding-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1323
:SIGNATURE: (tibetan-analysis--format-surrounding-segments analysis-file)
:END:

Return a ±2 surrounding-segments block for the Claude user prompt.
For each of offsets -2, -1, +1, +2 that resolves to an existing
neighbor file (see `--neighbor-analysis-file'), include its Tibetan
text and Working Translation (if non-empty).  Returns nil when no
neighbors could be resolved so the prompt stays lean for isolated
segments.

** ~tibetan-analysis--claude-static-system-blocks~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1373
:SIGNATURE: (tibetan-analysis--claude-static-system-blocks source-file)
:END:

Return the per-DOCUMENT static system-prompt suffix for SOURCE-FILE.
Concatenation of the source-metadata block, the Portfolio reference
block, and the target-language directive — everything appended to the
base system prompt that is CONSTANT across all segments/sentences of
one document.  Shared by the segment builder and the sentence-first
builder (§5.40) so the Anthropic prompt-cache constancy of BOTH is
guaranteed by construction, not by parallel maintenance.

** ~tibetan-analysis--build-claude-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1420
:SIGNATURE: (tibetan-analysis--build-claude-prompts tibetan-text source-file &optional analysis-file)
:END:

Build (SYSTEM . USER) Claude prompts for TIBETAN-TEXT.
SOURCE-FILE, if non-nil, supplies genre/author/context metadata and a
Resources vocabulary file.  ANALYSIS-FILE, if non-nil, supplies four
forms of grounding:
  1. the parser's own output (grammatical markers, clause structure,
     verb classification) for the Grammar section;
  2. the tool's per-segment `** Word / Particle List' matches as a
     vocabulary hint for the Translation section;
  3. ±2 surrounding segments (Tibetan + Working Translation if present)
     from the same analysis folder so Claude can resolve anaphora and
     discourse without over-interpreting an isolated line;
  4. the file's own seg-id (used to resolve the neighbors in (3)).

SANSKRIT-PLIST, if non-nil, is the parallel-mode walker plist
`(:iast STR :devanagari STR-or-nil :script-source SYM)' returned
by `tibetan-sanskrit-parallel-text-for-segment-id'.  When supplied
together with `#+SOURCE_MODE: parallel-sanskrit' on the source
file, the user prompt prepends a Sanskrit (primary) block above
the Tibetan passage and the system prompt gains a Sanskrit-primary
directive (Phase 3 of sanskrit-parallel-workflow, 2026-04-27).
Backward compatible with all existing callers that pass only the
first three args.

** ~tibetan-analysis--read-authinfo-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1526
:SIGNATURE: (tibetan-analysis--read-authinfo-key host)
:END:

Read password for HOST from ~/.authinfo or ~/.authinfo.gpg.
Parses the file directly for reliability.
Returns the password string or nil.

** ~tibetan-analysis--ensure-gptel-ready~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1545
:SIGNATURE: (tibetan-analysis--ensure-gptel-ready)
:END:

Ensure gptel is configured with Anthropic backend and API key.
Sets up the backend if claude-integration.el was not loaded.
Reads the API key from ~/.authinfo or environment.
Returns non-nil if gptel is ready to use.

** ~tibetan-analysis--write-claude-failure-stub~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1585
:SIGNATURE: (tibetan-analysis--write-claude-failure-stub analysis-file msg)
:END:

Write MSG into the *** Claude Translation section of ANALYSIS-FILE.
Only writes if that section is currently empty, missing, or already
holds a prior failure stub — never overwrites a real Claude response.
This is what callers should do from a queue :on-fail handler so the
user can see at a glance which segments still need a real Claude pass.

** ~tibetan-analysis--claude-status-rate-limited-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1619
:SIGNATURE: (tibetan-analysis--claude-status-rate-limited-p info)
:END:

Non-nil when gptel callback INFO indicates HTTP 429 (rate limited).

** ~tibetan-analysis--request-claude-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1624
:SIGNATURE: (tibetan-analysis--request-claude-translation tibetan-text analysis-file &optional source-file)
:END:

Request a Claude translation of TIBETAN-TEXT asynchronously.
When the response arrives, insert it into ANALYSIS-FILE under the
*** Claude heading in the Provided Translations section.

If SOURCE-FILE is given (or can be derived from ANALYSIS-FILE's
`#+SOURCE:' link), its `#+TIBETAN_CLAUDE_CONTEXT:' headers,
:WORK/:AUTHOR properties and a Resources vocabulary file are folded
into the prompts so Claude gets genre / author / glossary context.

Requests go through `tibetan-claude-queue' so concurrent requests
are capped (see `tibetan-claude-queue-concurrency') and HTTP 429
responses are retried with exponential backoff (see
`tibetan-claude-queue-max-retries').  When retries are exhausted, a
visible placeholder is written into the *** Claude Translation
section so the segment is easy to find and re-run later via C-c u R.

Requires gptel and a configured Anthropic API key.  Never signals —
failures are reported via `message' and the placeholder.

** ~tibetan-analysis--parse-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1724
:SIGNATURE: (tibetan-analysis--parse-claude-sections response)
:END:

Split RESPONSE on `## …' Translation / Vocabulary / Grammar /
Particles / `Concept Notes' (or legacy `Context') markdown headings.
Returns a plist with `:translation' / `:vocabulary' / `:grammar' /
`:particles' / `:concepts' string-or-nil values.

§5.24 (2026-05-22):  the legacy `## Context' heading is accepted
for backwards compatibility (older Claude responses still
delivered after a prompt change but before the model picks up
the rename) and routed to the `:concepts' slot.  The elisp key
`:context' is retired in favour of `:concepts'.

Missing sections are nil (not empty string) so the writer can leave
the old org body in place when Claude omitted a section.  When
RESPONSE contains no recognised heading, the whole (trimmed) string is
returned as `:translation' — this keeps backwards compatibility with
legacy single-translation responses.

`:particles' is a raw multi-line block of `word, particle, sub-id,
label' lines; parsing into structured tuples is downstream's job
(`tibetan-analysis--parse-claude-particles').

Phase 1 of two-language-parallel-analysis (2026-04-30):  the
suffixed `Translation (Sanskrit)' / `Translation (Combined)'
keys + the `Divergence' key (added in Phase B–D earlier today)
are retired.  Sanskrit-side and Combined-synthesis Claude output
is now produced by separate Claude calls (see
`persist/tibetan-analysis-sanskrit.el' and
`persist/tibetan-analysis-combined.el'), each with its own
parser.  This Tibetan parser goes back to its pre-Phase-B
five-key shape.

** ~tibetan-analysis--claude-heading-re~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1839
:SIGNATURE: (tibetan-analysis--claude-heading-re heading level)
:END:

Regexp that anchors `HEADING' at org LEVEL at beginning-of-line.

** ~tibetan-analysis--claude-stop-re~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1845
:SIGNATURE: (tibetan-analysis--claude-stop-re level)
:END:

Regexp matching the start of any heading at org LEVEL or shallower.

** ~tibetan-analysis--claude-segment-layout-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1857
:SIGNATURE: (tibetan-analysis--claude-segment-layout-p buffer)
:END:

Return non-nil if BUFFER uses the segment-level analysis layout.

After the sentence-§5.18 alignment (2026-05-18), both segment
files AND aligned sentence files share the SAME shape:
`* Tibetan Analysis' parent, `** Translation' at level-2, nested
`** Provided Translations'.  We treat any buffer carrying
`* Tibetan Analysis' as segment-layout — the writer then uses the
level-2 Translation slot, the renamer migrates legacy `** Claude
Translation' → `** Translation' in place, etc.

Legacy sentence layout (`* Auto-Analysis' parent + top-level
`* Provided Translations') still returns nil so the writer uses
the level-3 Claude headings expected by the old shape.

Brand-new / empty buffers still default to segment layout so
fresh per-segment scaffolds get Translation promoted to level 2.

** ~tibetan-analysis--claude-heading-inside-provided-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1897
:SIGNATURE: (tibetan-analysis--claude-heading-inside-provided-p)
:END:

Return non-nil if POINT is currently inside `* Provided Translations'.
Point should be on (or just after) a `*** Claude …' heading.  We
look up to find the nearest preceding `^\* ' level-1 heading
and check whether its text is `Provided Translations'.

** ~tibetan-analysis--reparent-orphaned-claude-subsections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1907
:SIGNATURE: (tibetan-analysis--reparent-orphaned-claude-subsections buffer)
:END:

Move level-3 Claude subsections that landed outside
`* Provided Translations' back into that section.

Earlier regenerate runs on sentence files (pre-reparent fix) could
append `*** Claude Translation' / `*** Claude Vocabulary' /
`*** Claude Grammar' at the end of the buffer (after `* Footnotes')
rather than inside Provided Translations.  This helper walks each
`^\*\*\* Claude \(Translation\|Vocabulary\|Grammar\|Context\)$'
heading, checks whether it sits inside `* Provided Translations',
and if not cuts the heading + body and re-inserts it at the end of
the Provided Translations section.  No-op when no Provided
Translations section exists or when no orphans are found.

** ~tibetan-analysis--claude-l2-has-real-content-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1969
:SIGNATURE: (tibetan-analysis--claude-l2-has-real-content-p heading-re buffer)
:END:

Return non-nil when a level-2 heading matching HEADING-RE in BUFFER
carries non-placeholder body content.

"Real content" = body is non-empty AND does not match a known
Claude placeholder marker (`[Awaiting`, `[Requesting`, `[Claude
unavailable`, `[Claude request failed`, `[Translation not
available`).  Used by `--migrate-legacy-claude-headings' to
decide whether a level-2 slot is effectively empty (placeholder
only) and therefore eligible to receive migrated legacy
level-3 content.

§5.21 Commit 7 / §5.22 follow-up (2026-05-21):  added because
the prior guard checked only heading EXISTENCE, which made the
migration skip when the new §5.21 scaffold had emitted a fresh
`[Awaiting Claude…]' placeholder at level 2 — stranding legacy
level-3 content under `** Provided Translations'.

** ~tibetan-analysis--delete-claude-l2-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2008
:SIGNATURE: (tibetan-analysis--delete-claude-l2-section heading-re buffer)
:END:

Delete the level-2 heading matching HEADING-RE and its body from BUFFER.
No-op when no matching heading is present.

§5.22 follow-up (2026-05-21):  used by the migration helper to
clear a placeholder-only level-2 slot before re-inserting it
with migrated content from a legacy level-3 location.

** ~tibetan-analysis--migrate-legacy-claude-headings~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2029
:SIGNATURE: (tibetan-analysis--migrate-legacy-claude-headings buffer)
:END:

Migrate legacy `*** Claude' / `*** Claude Translation' / `** Claude
Translation' headings in BUFFER.

Segment-layout buffers (with `** Wylie Transliteration'):
1. Rename bare `*** Claude' → `*** Claude Translation'.
2. Move `*** Claude Translation' (level-3, legacy placement under
   `** Provided Translations') to a new `** Translation'
   (level-2) directly after `** Wylie Transliteration', preserving
   its body.
3. Rename existing level-2 `** Claude Translation' → `** Translation'
   in place (Phase 1.3 of layout-revision §5.18, 2026-05-04 — the
   level-2 heading drops the redundant `Claude' qualifier).

Sentence-layout buffers (no `** Wylie Transliteration'):
1. Rename bare `*** Claude' → `*** Claude Translation'.
2. Reparent any level-3 Claude subsection that escaped
   `* Provided Translations' (e.g. appended after `* Footnotes' by
   a previous regenerate run) back into that section.

Both branches are no-ops when nothing to migrate.

** ~tibetan-analysis--insert-concept-notes-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2172
:SIGNATURE: (tibetan-analysis--insert-concept-notes-heading buffer body)
:END:

Insert `** Concept Notes' with BODY into BUFFER.

§5.24 (2026-05-22):  the heading is placed AFTER the existing
`** Translation' (Claude) and `** DharmaMitra Translation' if
present, BEFORE `** Grammar'.  Reader-flow:  read Translation
→ glance at Concept Notes for technical terms → read Grammar.

Placement fallbacks (in decreasing preference):
  · After `** DharmaMitra Translation' body.
  · After `** Translation' / `** Claude Translation' body.
  · After `** Claude Vocabulary' body.
  · After `** Wylie Transliteration' body.
  · End of buffer (defensive).

BODY may be empty — the heading is still created with a `[Awaiting
Claude…]' placeholder body.

** ~tibetan-analysis--insert-claude-vocabulary-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2237
:SIGNATURE: (tibetan-analysis--insert-claude-vocabulary-heading buffer body)
:END:

Insert `** Claude Vocabulary' with BODY into BUFFER.

Placement rule (§5.21 Commit 2/7, 2026-05-20):  prefer the slot
right after `** Interlinear Gloss' (the canonical scaffold
position — word-for-word trot → per-word annotations → fluent
translations).  Fallbacks in decreasing preference:  after
`** Wylie Transliteration', after `** Translation' / `** Claude
Translation', after the first `* ' top-level heading, end of
buffer.  BODY may be empty — the heading is still created with
two trailing newlines.

** ~tibetan-analysis--insert-claude-translation-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2297
:SIGNATURE: (tibetan-analysis--insert-claude-translation-heading buffer body)
:END:

Insert `** Translation' with BODY into BUFFER at the top.
Placement rule: right after `** Wylie Transliteration' and its body
if that heading exists; otherwise right after the first `* ' top-level
heading; otherwise at point-max.  BODY may be empty — the heading is
still created with two trailing newlines.

Phase 1.3 of layout-revision §5.18 (2026-05-04):  the heading is
now `** Translation' (was `** Claude Translation'); see
`tibetan-analysis--claude-section-order' for the rename rationale.

** ~tibetan-analysis--migrate-claude-grammar-to-u4~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2354
:SIGNATURE: (tibetan-analysis--migrate-claude-grammar-to-u4)
:END:

Relocate legacy Claude Grammar headings to the U4 target slot.
Three layouts exist in the wild:
  A (oldest): `*** Claude Grammar' at level 3 inside
      `** Provided Translations' (pre-Pass-6b).
  B: `** Claude Grammar' at level 2 (Pass 6b, 2026-04-22).
  C (U4 target): `*** Claude Grammar' at level 3 under
      `** Grammar' (2026-04-24).

Algorithm:
  1. Look for Layout B first (level-2).  If found, capture its body
     and delete the heading.
  2. Look for Layout A (level-3 inside Provided Translations).  Only
     treat as legacy if NO Layout-C heading is already in place under
     `** Grammar' (otherwise the Provided-Translations occurrence
     must already be a duplicate that `--extract-claude-grammar'
     missed — rare).  If found, capture body and delete.
  3. Ensure the U4 target `*** Claude Grammar' heading exists via
     `--place-claude-grammar-heading'.
  4. Drop the captured legacy body under the U4 heading, replacing
     any placeholder whitespace there.

Idempotent.  Operates on the CURRENT buffer — save-excursion is the
caller's responsibility.

** ~tibetan-analysis--place-claude-grammar-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2441
:SIGNATURE: (tibetan-analysis--place-claude-grammar-heading)
:END:

Insert `*** Claude Grammar' heading under `** Grammar' (U4, 2026-04-24).
Preferred slot: between `*** Particle Map' and `*** Particles in
This Segment', per the canonical U4 layout.  Fallbacks: after
`*** Particle Map' alone, or at end of `** Grammar'.  When
`** Grammar' itself is missing (legacy / pre-Pass-6b buffer, or
test fixture that never ran through `render-grammar-section'), a
minimal `** Grammar' heading is created first — Claude Grammar
always ends up nested so the workflow invariant holds.

Placement site: after `** Claude Translation' when that heading is
present (keeps Grammar in the priority-order neighbourhood);
otherwise after `** Wylie Transliteration'; otherwise at point-min.

Idempotent: caller must check the heading isn't already present
before invoking.

** ~tibetan-analysis--ensure-claude-headings~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2537
:SIGNATURE: (tibetan-analysis--ensure-claude-headings buffer)
:END:

Ensure the Claude Translation / Vocabulary / Grammar headings exist in BUFFER.

Segment-layout target (detected via `** Wylie Transliteration'):
  - `** Claude Translation'   at org level 2, right after Wylie.
  - `*** Claude Vocabulary'   at org level 3, inside
    `** Provided Translations' (after `*** DharmaMitra' if present).
  - `*** Claude Grammar'      at org level 3, nested UNDER
    `** Grammar', between `*** Particle Map' and `*** Particles in
    This Segment' (U4, 2026-04-24).  The reader flow inside Grammar
    is visual-map → prose-reading → per-particle Portfolio refs.
    Legacy files carrying `** Claude Grammar' at level 2 are
    migrated here — see the segment-layout migration block below.

Sentence-layout target (no `** Wylie Transliteration'):
  - `*** Claude Translation' at org level 3 (siblings under whatever
    parent the sentence scaffold provides).
  - `*** Claude Vocabulary'  at org level 3.
  - `*** Claude Grammar'     at org level 3.
  - `*** Claude Context'     at org level 3 — preserved for sentence
    files which still use the three/four-section layout.

Performs legacy-layout migration first (via
`tibetan-analysis--migrate-legacy-claude-headings'), then creates
whichever target heading is still missing.  Idempotent.

** ~tibetan-analysis--claude-body-md-h3-to-org~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2709
:SIGNATURE: (tibetan-analysis--claude-body-md-h3-to-org body parent-level)
:END:

Convert markdown `### foo' lines in BODY to org headings.

Each line that starts with `### ' (three hashes followed by a
space, line-anchored) is rewritten to a real org heading at
PARENT-LEVEL + 1 — one level deeper than the section the body
belongs to, so it nests cleanly underneath.

  Parent at level 2 (`** Claude Translation', segment layout)
    `### Tibetan Divergence' → `*** Tibetan Divergence'
  Parent at level 3 (`*** Claude Translation', sentence layout)
    `### Tibetan Divergence' → `**** Tibetan Divergence'

Mid-line `###' (e.g. `section ### 4.5 of the text') is preserved
as plain text — only line-anchored matches are converted.

Pure function — used by `--replace-claude-section-body' to render
optional Claude-emitted sub-headings (currently
`### Tibetan Divergence' from sanskrit-parallel mode, Phase 3 of
sanskrit-parallel-workflow, 2026-04-27) as foldable, navigable
org headings rather than flat markdown text in the analysis file.

If BODY contains no `### ' lines the input is returned unchanged,
so non-parallel Translation bodies (today's overwhelming majority)
are byte-identical pre- and post-Phase 4.

** ~tibetan-analysis--replace-claude-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2737
:SIGNATURE: (tibetan-analysis--replace-claude-section-body buffer heading body &optional level)
:END:

Replace the body under `HEADING' at org LEVEL in BUFFER with BODY.
LEVEL defaults to 3 for backwards compatibility.  Leaves the heading
itself in place; body is trimmed + terminated with one trailing blank
line.

Phase 4 of sanskrit-parallel-workflow (2026-04-27): before insert,
the body passes through `tibetan-analysis--claude-body-md-h3-to-org'
which rewrites any line-anchored `### foo' markdown sub-heading to
a real org heading at LEVEL + 1.  This is how Claude's optional
`### Tibetan Divergence' note (parallel-Sanskrit mode) becomes a
nested org heading rather than flat markdown text in the analysis
file.  Non-divergence bodies (no `### ' lines) are unchanged.

** ~tibetan-analysis--claude-effective-section-order~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2779
:SIGNATURE: (tibetan-analysis--claude-effective-section-order buffer)
:END:

Return the layout-appropriate Claude section-order for BUFFER.

Segment layout (per-segment analysis files):
  `** Claude Translation' and `** Claude Grammar' both at level 2
  (the workshop-agreed priority slots), plus `*** Claude
  Vocabulary' at level 3 inside `** Provided Translations'.
  Context is dropped — the segment workflow is three-section only.

Sentence / legacy layout (sentence analysis files, or any buffer
without the segment-layout marker):
  Translation / Grammar / Context all at level 3 — preserves the
  existing sentence-level three-section workflow unchanged.

Callers that write Claude output (insert, restore) use this list so
a single buffer's layout drives heading levels consistently.

** ~tibetan-analysis--insert-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2819
:SIGNATURE: (tibetan-analysis--insert-claude-sections response analysis-file)
:END:

Parse RESPONSE and write its sections into ANALYSIS-FILE.
RESPONSE is the raw markdown returned by Claude; it is split by
`tibetan-analysis--parse-claude-sections'.  Each section named in
the buffer's effective section-order (see
`tibetan-analysis--claude-effective-section-order') that the parser
filled in is written under its corresponding org heading at the
configured level.  Legacy `*** Claude' and `*** Claude Translation'
placements are migrated to the current two-section segment layout on
first write; sentence files keep the legacy three-section layout.

When Claude's response includes a `## Particles' block AND
`tibetan-analysis-auto-regen-on-claude-arrival' is non-nil, the
Grammar section is re-rendered from scratch so the per-occurrence
Bialek Portfolio snippets (§1.5.1 Place, §2.11.1 Sequential, ...)
land inline under each particle without requiring a manual
reanalyse.

** ~tibetan-analysis--parse-claude-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2918
:SIGNATURE: (tibetan-analysis--parse-claude-vocabulary vocab-text)
:END:

Parse VOCAB-TEXT (the `## Vocabulary' body) into an alist.
Each entry is (WYLIE-KEY . FULL-LINE) where WYLIE-KEY is the
lowercase, trimmed first field (before the first comma) and
FULL-LINE is the original line.  Blank lines and lines starting
with `---' are skipped.

** ~tibetan-analysis--read-claude-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2943
:SIGNATURE: (tibetan-analysis--read-claude-section-body filepath heading &optional level)
:END:

Return the non-placeholder body under `HEADING' in FILEPATH, or nil.
LEVEL is the org heading level to look for (defaults to 3 for
backwards compatibility).  When nil is passed explicitly as LEVEL,
the reader tries level 2 first and falls back to level 3 — this is
useful during the Claude Translation migration, when an old file may
still carry a level-3 heading.  Skips placeholder `[Requesting …]'
and known error markers so we don't re-persist dead content.

** ~tibetan-analysis--read-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2989
:SIGNATURE: (tibetan-analysis--read-claude-sections filepath)
:END:

Return preserved Claude content in FILEPATH as a plist.
Keys: `:translation', `:vocabulary', `:grammar', `:particles',
`:concepts'.  Each value is a non-empty string or nil.  Reads
from the current layout (Translation, Vocabulary, and Concept
Notes at level 2;  Grammar / Particles at level 3) and falls
back to the legacy level-3 placements so old analysis files do
not lose their work on reanalysis.

§5.24 (2026-05-22):  the `:context' slot is renamed to
`:concepts' and its heading promoted from level-3 `*** Claude
Context' to level-2 `** Concept Notes' (sentence files keep
level 3 — see `--claude-effective-section-order' else branch).
Reader does dual-name match so existing `*** Claude Context'
bodies migrate naturally on first regen.

Phase 1 of two-language-parallel-analysis (2026-04-30):  the
Phase B–D `:translation-sanskrit' / `:translation-combined' /
`:divergence' slots are retired.  Sanskrit + Combined +
Divergence content lives under separate top-level sections (`*
Sanskrit Analysis' / `* Combined Analysis') with their own
read / restore helpers in
`persist/tibetan-analysis-sanskrit.el' /
`persist/tibetan-analysis-combined.el'.

** ~tibetan-analysis--parse-claude-particles~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3069
:SIGNATURE: (tibetan-analysis--parse-claude-particles body)
:END:

Parse a `*** Claude Particles' BODY into a list of particle tuples.
Each line is `word, particle, portfolio-sub-id, label'; returns a
list of plists `(:word W :particle P :sub-id ID :label L)'.
Lines that don't match the 4-field shape are silently skipped
(Claude may emit commentary or sentinel `[none]' lines on short
passages — those shouldn't crash the caller).

** ~tibetan-analysis--read-claude-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3090
:SIGNATURE: (tibetan-analysis--read-claude-translation filepath)
:END:

Return the preserved Claude translation in FILEPATH, or nil.
Legacy wrapper around `tibetan-analysis--read-claude-sections' that
returns only the `:translation' slot for callers that have not been
migrated yet.

** ~tibetan-analysis--claude-needs-request-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3097
:SIGNATURE: (tibetan-analysis--claude-needs-request-p filepath)
:END:

Return non-nil when FILEPATH lacks populated Claude content.

"Populated" = at least Translation OR Vocabulary slot has a
non-placeholder body (the slots `tibetan-analysis--read-claude-
sections' reads and filters for placeholders).  When BOTH are nil
(missing or `[Awaiting…]' / `[Requesting…]' placeholders), the
file needs a Claude request.

§5.22 follow-up (2026-05-21):  used by the `:missing-only'
semantic of `re-request-claude' in `tibetan-analysis-reanalyze-
file' / `tibetan-sentence-reanalyze-file' to skip the API call
on files already populated.

** ~tibetan-analysis--should-fire-claude-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3115
:SIGNATURE: (tibetan-analysis--should-fire-claude-p re-request-claude filepath)
:END:

Decide whether to fire a Claude request for FILEPATH.

RE-REQUEST-CLAUDE controls the policy:
  · nil             — never fire (preserve existing content).
  · t               — always fire (legacy behaviour;  forces
                       a fresh Claude call regardless of
                       existing content).
  · `:missing-only' — fire only when the file's Claude
                       Translation/Vocabulary slots are missing
                       or placeholders.

Returns non-nil when a request SHOULD be dispatched.

** ~tibetan-analysis--restore-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3134
:SIGNATURE: (tibetan-analysis--restore-claude-sections filepath sections)
:END:

Write SECTIONS (a plist) back into FILEPATH's Claude headings.
SECTIONS has keys :translation, :vocabulary, :grammar, :particles,
:concepts;  any nil slot leaves the corresponding org body
untouched.  Creates the target headings if they are missing,
migrating legacy layouts on first encounter.

§5.24 (2026-05-22):  `:concepts' replaces the legacy `:context'
slot.  Heading promoted from `*** Claude Context' (L3) to
`** Concept Notes' (L2) for segment layout;  sentence layout keeps
L3 (via the else branch of `--claude-effective-section-order').

** ~tibetan-analysis--restore-claude-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3163
:SIGNATURE: (tibetan-analysis--restore-claude-translation filepath translation)
:END:

Write TRANSLATION back under `*** Claude Translation' in FILEPATH.
Legacy wrapper around `tibetan-analysis--restore-claude-sections'.

** ~tibetan-analysis-auto-regen-on-claude-arrival~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 2802
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

When non-nil, the Grammar section is re-rendered after Claude
populates `*** Claude Particles' so the Portfolio snippets appear
inline without requiring a manual `C-c u R'.

Pass 6c (2026-04-22) introduced `## Particles' tuples that let the
Grammar section's `*** Particles in This Segment' attach per-
occurrence Bialek Portfolio function snippets.  Those tuples
arrive in Claude's response async; without this auto-regen the
student has to re-run reanalyse by hand to see the enriched
Grammar section.

Set to nil to keep the Pass 6b compact Grammar shape and do the
enriched re-render only on explicit reanalyse.

** ~gptel-api-key~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 76
:END:

/(no docstring)/

** ~gptel-backend~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 77
:END:

/(no docstring)/

** ~gptel-model~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 78
:END:

/(no docstring)/

** ~gptel-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 79
:END:

/(no docstring)/

** ~tibetan-analysis--claude-system-prompt~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 98
:DEFAULT: "You are a specialist in Classical Tibetan (chos skad) translation \
and philology, acting as a teaching assistant for a
:END:

System prompt sent to Claude for segment-level three-section analysis.
Produces a markdown response (Translation / Vocabulary / Grammar)
parsed by `tibetan-analysis--parse-claude-sections' and placed into
`** Claude Translation', `*** Claude Vocabulary', and
`*** Claude Grammar'.  Vocabulary uses DharmaMitra-style
word-by-word format as the second tier (after provided vocabulary,
before Steinert entries).  Genre-specific assumptions come from the
source file's `#+TIBETAN_CLAUDE_CONTEXT:' headers.

** ~tibetan-analysis--claude-section-order~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 1799
:DEFAULT: '
:END:

/(no docstring)/


* persist/tibetan-analysis-combine.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-analysis-combine.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-analysis-combine.el
:END:

** ~tibetan-analysis-combine--read-auto-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 81
:SIGNATURE: (tibetan-analysis-combine--read-auto-sections filepath)
:END:

Return the list of (HEADING . BODY) cons cells inside the
`* Tibetan Analysis' subtree of FILEPATH (or legacy
`* Auto-Analysis' subtree on pre-migration files).  Nil if the
file is missing or has no auto-analysis subtree.

Phase 1.2 of layout-revision §5.18 (2026-05-04):  accepts both
heading names so combine works seamlessly during the
soft-migration window where some files have been reanalysed
into the new shape and others still carry the old shape.

** ~tibetan-analysis-combine--tibetan-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 107
:SIGNATURE: (tibetan-analysis-combine--tibetan-text filepath)
:END:

Return the body of `* Tibetan Text' in FILEPATH (trimmed, filtered).

The raw body often interleaves Tibetan with non-Tibetan cruft that
should never surface in the combined document:

  - Org `#+TITLE:' / `#+AUTHOR:' / `#+TIBETAN_CLAUDE_CONTEXT:'
    headers captured inside a segment when the `〔seg:N〕' marker
    was placed above the source-file preamble.
  - `#'-prefixed editorial / OCR comments.
  - Blank separators.

We reuse `tibetan-analysis--filter-to-tibetan-lines' (the same
pre-parser sanitiser the per-segment generator runs) to keep only
lines that contain at least one Tibetan character.  Returns nil
when the filtered result is empty — the caller uses that to decide
whether the segment should be rendered at all.

** ~tibetan-analysis-combine--snippet~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 131
:SIGNATURE: (tibetan-analysis-combine--snippet text)
:END:

Return a one-line snippet of TEXT suitable for a heading suffix.
Only Tibetan-script characters are kept (non-Tibetan lines are
dropped so OCR-comment debris never surfaces), and the result is
truncated to `tibetan-analysis-combine--snippet-max' characters
with a trailing ellipsis when longer.

** ~tibetan-analysis-combine--render-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 153
:SIGNATURE: (tibetan-analysis-combine--render-segment filepath seg-id)
:END:

Return a string containing the combined-doc block for one segment,
or nil when the segment has no Tibetan content to render.

FILEPATH points at the seg-NNN*.org file, SEG-ID is the numeric id.

A segment is skipped when `tibetan-analysis-combine--tibetan-text'
returns nil (the `* Tibetan Text' body had no Tibetan characters
once non-Tibetan lines were filtered out).  This typically happens
for seg-001 in sources where the `〔seg:1〕' marker sits above the
#+TITLE / #+AUTHOR / #+TIBETAN_CLAUDE_CONTEXT preamble — the
segment captures the preamble but no actual passage, so including
it in the combined document would only produce a run of
`[Not available]' placeholders.

** ~tibetan-analysis-combine--collect-appendix-bodies~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 197
:SIGNATURE: (tibetan-analysis-combine--collect-appendix-bodies analysis-files heading)
:END:

Walk ANALYSIS-FILES and return a list of (SEG-ID . BODY) for every
occurrence of HEADING (a level-2 heading string like
`** Grammatical Markers').  Empty bodies are skipped.

** ~tibetan-analysis-combine--split-grammatical-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 212
:SIGNATURE: (tibetan-analysis-combine--split-grammatical-entries body)
:END:

Split a Grammatical Markers BODY into individual bullet entries.

The renderer emits markers as a bulleted list:

    - དེ [de] in «...» → CONVERBIAL: COORDINATIVE … [Portfolio §2.4]
    - ལ [la] in «...» → DATIVE (DAT) …
    - …

We split on `^- ' so each particle annotation becomes its own entry,
which lets the dedup collapse common markers (every `ལ DATIVE' hit
across the corpus folds into a single appendix line with all the
source segment ids).  Continuation lines (indented lines belonging
to the previous bullet) are attached to whichever bullet preceded
them.  Empty or whitespace-only entries are dropped.

** ~tibetan-analysis-combine--split-detailed-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 252
:SIGNATURE: (tibetan-analysis-combine--split-detailed-entries body)
:END:

Split a Detailed Dictionary BODY into individual `◆'-prefixed
entries.  Text before the first `◆' is dropped (typically empty).

** ~tibetan-analysis-combine--entry-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 263
:SIGNATURE: (tibetan-analysis-combine--entry-key entry)
:END:

Return a stable dedup KEY for ENTRY.
For a Detailed Dictionary entry `◆ word [wylie] …' the key is the
`◆ word' head line.  For a Grammatical Markers entry the key is
the first non-empty line trimmed.

** ~tibetan-analysis-combine--dedup-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 271
:SIGNATURE: (tibetan-analysis-combine--dedup-entries per-seg-bodies splitter)
:END:

Dedup entries extracted from PER-SEG-BODIES by entry key.
PER-SEG-BODIES is the list returned by
`tibetan-analysis-combine--collect-appendix-bodies'.
SPLITTER is a function that converts one body string into a list of
entry strings (e.g. `--split-grammatical-entries').

Returns a list of plists in encounter order:
  (:key KEY :entry ENTRY :segments (SEG-ID ...))
SEG-IDs are sorted numerically ascending and deduplicated.

** ~tibetan-analysis-combine--render-appendix~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 307
:SIGNATURE: (tibetan-analysis-combine--render-appendix title entries)
:END:

Render TITLE (a full `* Appendix X — …' heading) followed by the
rendered ENTRIES list.  Each entry shows its body and a
`(segments: 3, 7, 12)' trailer.

** ~tibetan-analysis-combine-document~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 328
:SIGNATURE: (tibetan-analysis-combine-document &optional folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Build `analysis/combined.org' by stitching together every
`seg-NNN*.org' file in FOLDER, using the same priority section
order as the per-segment analysis view and appending a
consolidated dictionary and grammatical-markers appendix.

Interactive: FOLDER defaults to the analysis folder of the current
source file (via `tibetan-analysis-get-folder').  A universal
prefix argument prompts for the folder instead.

Writes `combined.org' in the folder and opens it in a side window.

** ~tibetan-analysis-combine--per-segment-order~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 54
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis-combine--appendix-section-names~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 68
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis-combine--snippet-max~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 73
:DEFAULT: 40
:END:

Maximum length of the Tibetan snippet appended to each segment
heading in the combined document, in characters.


* persist/tibetan-analysis-combined.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-analysis-combined.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-analysis-combined.el
:END:

** ~tibetan-analysis-combined--build-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 129
:SIGNATURE: (tibetan-analysis-combined--build-prompts tibetan-text sanskrit-plist tibetan-translation sanskrit-translation
                  source-file &optional _analysis-file)
:END:

Build (SYSTEM . USER) Claude prompts for the Combined call.

TIBETAN-TEXT, SANSKRIT-PLIST: the two raw sources for the segment.
TIBETAN-TRANSLATION, SANSKRIT-TRANSLATION: the bodies of the two
upstream Claude translations (strings).
SOURCE-FILE: source document (for metadata lookups).
ANALYSIS-FILE: reserved for future ±2 neighbour context.

Returns nil when any of:  tibetan-text empty, sanskrit-plist
nil / lacks IAST, either translation empty.  The Combined call
is meaningful only when all four inputs are present.

** ~tibetan-analysis-combined--parse-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 220
:SIGNATURE: (tibetan-analysis-combined--parse-claude-sections response)
:END:

Split a Combined Claude RESPONSE on `## Translation' /
`## Sanskrit-Tibetan Comparison' headings.

Returns a plist with `:translation' and `:comparison' keys.
Missing sections are nil.

Phase 3.1 of layout-revision §5.18 (2026-05-04):  the second
section is now always emitted (with `[Faithful — …]' for faithful
renderings).  Renamed from `:divergence' to `:comparison'.  The
parser accepts THREE heading aliases for the comparison body —
the new `## Sanskrit-Tibetan Comparison', the bare `## Comparison',
and the legacy `## Divergence' (so archived Claude responses still
round-trip).

** ~tibetan-analysis-combined--ensure-parent~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 292
:SIGNATURE: (tibetan-analysis-combined--ensure-parent buffer)
:END:

Ensure `* Combined Analysis' top-level heading exists in BUFFER.
Inserts at end of buffer when absent.  Idempotent.

** ~tibetan-analysis-combined--ensure-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 305
:SIGNATURE: (tibetan-analysis-combined--ensure-section buffer heading)
:END:

Ensure HEADING (a level-2 string) exists inside `* Combined Analysis'
in BUFFER.  Idempotent.

** ~tibetan-analysis-combined--replace-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 325
:SIGNATURE: (tibetan-analysis-combined--replace-section-body buffer heading body)
:END:

Replace the body under HEADING (a level-2 string) inside
`* Combined Analysis' in BUFFER with BODY.

** ~tibetan-analysis-combined--migrate-legacy-divergence-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 342
:SIGNATURE: (tibetan-analysis-combined--migrate-legacy-divergence-heading buffer)
:END:

Rename any legacy `** Divergence' inside `* Combined Analysis' →
`** Sanskrit-Tibetan Comparison' (Phase 3.2 of layout-revision
§5.18, 2026-05-04).  Idempotent — no-op when the file already uses
the new heading name.

Called at the start of `--insert-sections' / `--restore-sections'
so subsequent existence checks find the new name on legacy files.

** ~tibetan-analysis-combined--insert-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 361
:SIGNATURE: (tibetan-analysis-combined--insert-sections response analysis-file)
:END:

Parse a Combined Claude RESPONSE and write its sections into
ANALYSIS-FILE under `* Combined Analysis' (top-level).

Returns t on successful write, nil when no sections were
parsed or ANALYSIS-FILE is missing.

** ~tibetan-analysis-combined--read-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 394
:SIGNATURE: (tibetan-analysis-combined--read-section-body filepath heading)
:END:

Return the body under `** HEADING' inside `* Combined Analysis'
in FILEPATH.

** ~tibetan-analysis-combined--read-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 422
:SIGNATURE: (tibetan-analysis-combined--read-sections filepath)
:END:

Return preserved Combined-Claude content as a plist with
`:translation' / `:comparison' keys.

Phase 3.2 of layout-revision §5.18 (2026-05-04):  the `:comparison'
slot's body lookup tries the new heading `Sanskrit-Tibetan
Comparison' first and falls back to the legacy `Divergence' name
so files written before this commit still resolve cleanly.

** ~tibetan-analysis-combined--restore-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 439
:SIGNATURE: (tibetan-analysis-combined--restore-sections filepath sections)
:END:

Write SECTIONS (a plist) back into FILEPATH's `* Combined
Analysis' section.

** ~tibetan-analysis-combined--needs-fire-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 466
:SIGNATURE: (tibetan-analysis-combined--needs-fire-p tibetan-text sanskrit-plist tibetan-translation sanskrit-translation)
:END:

Return non-nil when the Combined call has all four inputs and
should be fired.  All four must be non-empty strings (with
SANSKRIT-PLIST carrying a non-empty `:iast').

** ~tibetan-analysis-combined--request-synthesis~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 487
:SIGNATURE: (tibetan-analysis-combined--request-synthesis tibetan-text sanskrit-plist tibetan-translation sanskrit-translation
                  source-file analysis-file &optional callback)
:AUTOLOAD: t
:END:

Fire an async Combined Claude call.

On response, parses sections + writes them under `* Combined
Analysis' in ANALYSIS-FILE.  CALLBACK (if non-nil) is called
with one arg — the parsed plist — after the write.

Goes through `tibetan-claude-queue' so the request shares the
rate-limit budget + retry-on-429 logic with the Tibetan and
Sanskrit calls (concurrency optimization, 2026-04-30:  before
this commit the Combined call bypassed the queue entirely).
The Combined call is naturally serial relative to Tibetan +
Sanskrit (it chains off the Sanskrit callback, runs only when
both translations are present), so queueing it adds rate-limit
safety without changing the per-segment latency profile.

No-op when `--needs-fire-p' returns nil or gptel /
tibetan-claude-queue are missing.

** ~gptel-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 59
:END:

/(no docstring)/

** ~tibetan-analysis-combined--system-prompt-base~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 74
:DEFAULT: "You are a comparative reader of parallel Sanskrit-Tibetan \
philological corpora, working with a graduate Yogācārabhūmi
:END:

Constant base for the Combined Claude system prompt.  Per-
document additions (target-lang directive, source genre) appended
at build time.  Constant per document → Anthropic-cache friendly.

** ~tibetan-analysis-combined--section-order~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 280
:DEFAULT: '
:END:

/(no docstring)/


* persist/tibetan-analysis-persist.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-analysis-persist.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-analysis-persist.el
:END:

** ~tibetan-analysis--clean-word-token~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 86
:SIGNATURE: (tibetan-analysis--clean-word-token word)
:END:

Normalise a single Tibetan word token for dictionary lookup / display.
Strips surrounding whitespace, then removes every shad glyph
`།༎༏༐༑༔' plus its Wylie stand-in `/' (anywhere in the token —
a shad inside a single word is always upstream-tokenizer noise).

Matches the character class already used by
`tibetan-analysis--get-grammatical-role' (≈line 1068) and
`tibetan-analysis--build-cat-translation' (≈line 1390).  Extracted
2026-04-24 after live review of seg-16 of gal-chen-nyi-shu.org
showed a third sibling site (the `enriched-vocab-pairs' loop in
`tibetan-analysis-generate-content') stripped only TRAILING shads,
letting verse-start `།བྱང་ཆུབ་...' tokens leak into the Interlinear
link label as `/byang chub ...'.

** ~tibetan-analysis--token-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-analysis--token-wylie word)
:END:

Return lowercase, trimmed Wylie for a single Tibetan WORD token.
Returns nil when `tibetan-to-wylie-fixed' is unavailable or the
conversion raises; the caller should fall back to dictionary-only
behaviour in that case.  Used by the Claude-override helpers that
need a canonical comparison key against Claude's Wylie-keyed data.

** ~tibetan-analysis--claude-particle-label-for-token~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-analysis--claude-particle-label-for-token word particles)
:END:

Return a short Claude-derived particle label for WORD, or nil.
WORD is a Tibetan token (shad-stripped).  PARTICLES is the parsed
Claude Particles list returned by
`tibetan-analysis--parse-claude-particles' — each entry is a plist
`(:word W :particle P :sub-id ID :label L)'.

Matches the token's Wylie exactly against the `:particle' field
(verse particles are always single-word).  When found, returns a
formatted string `LABEL (§SUB-ID)' suitable for use as the
Interlinear's `short-meaning'.  When not found, or when PARTICLES
is nil / empty, returns nil — the caller keeps its current gloss.

** ~tibetan-analysis--claude-vocab-gloss-for-token~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 173
:SIGNATURE: (tibetan-analysis--claude-vocab-gloss-for-token word vocab-alist)
:END:

Return the Claude-provided gloss for WORD from VOCAB-ALIST, or nil.
WORD is a Tibetan token.  VOCAB-ALIST is the parsed Claude
Vocabulary alist returned by `tibetan-analysis--parse-claude-vocabulary'
— each element is (WYLIE-KEY . FULL-LINE).  The line format produced
by Claude's system prompt is

    wylie-key, part-of-speech, "gloss", commentary

so the helper extracts the first double-quoted substring as the
short gloss.  Matches the token's Wylie as either equal to the
key or a space-bounded prefix of the key (so a stem-only token
matches a stem+particle MWU entry — `blo sbyangs' matches
`blo sbyangs na').  Returns nil when no match or no quoted gloss
field present.

** ~tibetan-analysis--claude-vocab-proper-noun-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 202
:SIGNATURE: (tibetan-analysis--claude-vocab-proper-noun-p word vocab-alist)
:END:

Return Claude's proper-noun gloss (the name) for WORD, or nil.
VOCAB-ALIST is the parsed Claude Vocabulary alist — each element
is (WYLIE-KEY . FULL-LINE) with the line shaped

    wylie-key, part-of-speech, "gloss", commentary

When Claude's part-of-speech field (everything BEFORE the first
double-quoted gloss) classifies the token as a proper noun /
proper name — it contains the word `proper' — the helper returns
the quoted gloss (the name).  Otherwise nil.

Used to override a dictionary common-noun gloss that masks a
proper name with no Steinert `<person>'/`<place>' tag (e.g. `rngog'
glossed "mane" by Rangjung-Yeshe but named rNgog in context —
Milarepa Segment 110).  Examining only the pre-quote prefix keeps
a comma inside the gloss (`"I, me"') from being mistaken for the
POS field.

** ~tibetan-analysis--apply-claude-vocab-override~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 238
:SIGNATURE: (tibetan-analysis--apply-claude-vocab-override word dict-gloss vocab-alist &optional curated)
:END:

Override a proper-noun DICT-GLOSS with Claude's reading.

Two override paths, both gated on Claude Vocabulary having an entry
for WORD:

1. DICT-GLOSS begins with `<person>' or `<place>' — the Steinert
   84000Dict proper-noun tag markers — → prefer Claude's gloss.
   The 84000 data indexes buddha-names and place-names under short
   Tibetan phrases that double as common words (`blo sbyangs' =
   "trained mind" but also the 547th buddha); Claude, which sees
   full-clause context, recovers the lexical reading.

2. DICT-GLOSS is an UNTAGGED common-noun reading but Claude
   classifies WORD as a proper noun → prefer Claude's name.  This
   catches homographs whose dictionary entry carries no tag — e.g.
   `rngog' glossed "mane" by Rangjung-Yeshe but named rNgog
   (Mar pa's disciple) in context (Milarepa Segment 110).

Otherwise DICT-GLOSS is returned unchanged.  The `<term>' tag
(84000's canonical-term marker) is left intact — it IS the right
gloss when present.

** ~tibetan-analysis--tibetan-script-matcher~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 492
:SIGNATURE: (tibetan-analysis--tibetan-script-matcher limit)
:END:

Font-lock matcher: move point over the next run of Tibetan script
(U+0F00–U+0FFF) up to LIMIT, setting match data.  Used instead of a
string matcher because a `[ༀ-࿿]' char-range as a STRING in
`font-lock-keywords' is silently dropped when other keywords are
present;  the same range works in this runtime `re-search-forward'.

** ~tibetan-analysis--shad-boundary-words~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 518
:SIGNATURE: (tibetan-analysis--shad-boundary-words tibetan-text)
:END:

Return shad boundaries for TIBETAN-TEXT as a list of
(WYLIE . OCCURRENCE) conses — the last syllable before each INTERNAL
shad (a shad with content after it) plus how many times that syllable
occurs in its unit, so the marker placers can land on the right
repeat (M3, Fable-5 audit: `ngo so la so' must mark the SECOND so).
༼…༽ abbreviation brackets are stripped before tokenizing (L6 — the
bracket glyph used to convert to "" and silently drop the
boundary).  A trailing shad ends the passage and is not a boundary.
nil when no internal boundary or the Wylie converter is unavailable.

** ~tibetan-analysis--strip-shad-marker-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 548
:SIGNATURE: (tibetan-analysis--strip-shad-marker-lines body)
:END:

Remove shad-marker lines from BODY (line-based; for the Vocabulary).

** ~tibetan-analysis--mark-shads-in-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 557
:SIGNATURE: (tibetan-analysis--mark-shads-in-vocab body tibetan-text)
:END:

Insert shad-boundary markers into a Claude Vocabulary BODY.
Idempotent: existing markers are stripped first.  Boundaries are
matched INDEPENDENTLY, in order, from an advancing scan position —
an unmatched boundary is skipped without blocking later ones (M3:
head-first consumption used to drop ALL subsequent markers), and a
boundary whose syllable also ends an earlier entry lands on its
OCCURRENCE ordinal, not the first match.

** ~tibetan-analysis--mark-shads-in-interlinear~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 597
:SIGNATURE: (tibetan-analysis--mark-shads-in-interlinear body tibetan-text)
:END:

Splice shad-boundary markers into a flowing Interlinear BODY.
Idempotent.  For each (WYLIE . OCCURRENCE) boundary, the marker goes
after that token's OCCURRENCE-th `[gloss]' from the current scan
position (M3); boundaries whose token is not found are skipped
without affecting later ones.

** ~tibetan-analysis--mark-l2-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 631
:SIGNATURE: (tibetan-analysis--mark-l2-section heading fn)
:END:

In the current buffer, replace the body of `** HEADING' with the
result of calling FN on the trimmed body.  No-op when HEADING is absent.

** ~tibetan-analysis--apply-shad-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 650
:SIGNATURE: (tibetan-analysis--apply-shad-markers filepath)
:END:

Insert / refresh shad-boundary markers in the Interlinear Gloss and
Claude Vocabulary sections of FILEPATH.

Idempotent (markers are stripped and re-placed on every call) and a
no-op when the segment has no INTERNAL shad boundary — so single-shad
Milarepa segments are untouched while multi-shad MA Reading segments
get a `──── ། ────' divider where each shad-unit ends.  Reads the
Tibetan from the file's own `* Tibetan Text' section.

** ~tibetan-analysis--in-claude-vocab-section-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 682
:SIGNATURE: (tibetan-analysis--in-claude-vocab-section-p)
:END:

Return non-nil when point's line sits under a `** Claude Vocabulary'
heading (and not under a later level-2 heading).

** ~tibetan-analysis--vocab-term-matcher~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 692
:SIGNATURE: (tibetan-analysis--vocab-term-matcher limit)
:END:

Font-lock matcher: set group 1 to the leading term of the next
`** Claude Vocabulary' entry line up to LIMIT.

An entry is `TERM, part-of-speech, "gloss", commentary'; the term is
the run of Wylie letters / spaces / apostrophes before the first comma.
Only fires inside the `** Claude Vocabulary' section, so identically
shaped lines in `*** Claude Particles' are left alone.  Returns nil
when no further term is found before LIMIT.

** ~tibetan-analysis-setup-faces~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 772
:SIGNATURE: (tibetan-analysis-setup-faces)
:END:

Setup faces for analysis buffers.
Ensures Tibetan text remains readable at all heading levels and in
verbatim/code blocks which otherwise might use smaller fonts.
Also differentiates case particles (magenta) from converbs (orange)
in the Particle Map via font-lock — bypassing org's emphasis
parser which doesn't fire for compound-embedded markers like
`mthu='i='.  Only applies once per buffer to prevent accumulation.

** ~tibetan-analysis-class-fold~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 867
:SIGNATURE: (tibetan-analysis-class-fold)
:INTERACTIVE: t
:END:

Fold the reference sections of the current analysis buffer.
Each heading in `tibetan-analysis-class-folded-sections' collapses to
its heading line (subtree hidden); everything else stays visible.

** ~tibetan-analysis-toggle-class-view~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 880
:SIGNATURE: (tibetan-analysis-toggle-class-view)
:INTERACTIVE: t
:END:

Toggle between the folded class view and the fully expanded view.

** ~tibetan-analysis--analysis-buffer-name-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 892
:SIGNATURE: (tibetan-analysis--analysis-buffer-name-p filename)
:END:

Non-nil when FILENAME is a per-segment or per-sentence analysis file.
Covers the §5.23 suffixed names (`seg-013-kbgp.org') and `sent-*.org'
— the old `seg-[0-9]+\.org$'-only match left MA Reading files
without the §5.31/§5.35 face setup.

** ~tibetan-analysis-mode-hook~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 902
:SIGNATURE: (tibetan-analysis-mode-hook)
:END:

Hook to run when entering an analysis buffer.

** ~tibetan-analysis-get-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 915
:SIGNATURE: (tibetan-analysis-get-folder)
:END:

Get the analysis folder path for current buffer.
Creates the folder if it doesn't exist.
Returns the folder path.

** ~tibetan-analysis--extract-segment-number~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 927
:SIGNATURE: (tibetan-analysis--extract-segment-number seg-id)
:END:

Extract segment number from SEG-ID.
SEG-ID can be:
- A number: 5
- A simple string: "5" or "seg-005"
- A compound string: "Segment 5" or "Sentence 1, Segment 5"
Returns the segment number as an integer.

** ~tibetan-analysis-segment-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 953
:SIGNATURE: (tibetan-analysis-segment-filename seg-id)
:END:

Generate analysis filename for SEG-ID.
SEG-ID can be a number or string like \='1\=' or \='seg-001\='.
Returns filename like \='seg-001.org\='.

** ~tibetan-analysis--file-source-basename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 960
:SIGNATURE: (tibetan-analysis--file-source-basename filepath)
:END:

Return the basename of the document named by FILEPATH's `#+SOURCE:'
org link (`[[file:PATH::…]…]'), or nil when the file has no parseable
SOURCE line.  Reads only the file head.

** ~tibetan-analysis--file-belongs-to-source-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 972
:SIGNATURE: (tibetan-analysis--file-belongs-to-source-p filepath source-file)
:END:

Return non-nil when FILEPATH's `#+SOURCE:' link names SOURCE-FILE.
EXACT basename comparison (M1, Fable-5 audit 2026-06-04 — the previous
substring match let source `rim.org' claim a `lam-rim.org' file,
re-opening the §5.23 cross-source overwrite class).  Safe when either
argument is nil or FILEPATH is absent; nil when FILEPATH has no
parseable SOURCE link.

** ~tibetan-analysis--resolve-filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 983
:SIGNATURE: (tibetan-analysis--resolve-filepath folder num short-name source-file)
:END:

Choose the analysis filepath for segment NUM in FOLDER.

Without SHORT-NAME: the un-suffixed `seg-NNN.org'.

With SHORT-NAME (derived from SOURCE-FILE): prefer an existing suffixed
`seg-NNN-SHORT.org'; else an existing un-suffixed `seg-NNN.org' that
belongs to the SAME source — a single-source folder not migrated to the
§5.23 suffix (Milarepa keeps bare filenames); else a new suffixed path.

The source-match guard keeps a multi-source folder safe: a bare file
left by source A is NOT reused for source B (which would collide), so
new B segments still get B's suffix.

** ~tibetan-analysis-get-filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1022
:SIGNATURE: (tibetan-analysis-get-filepath seg-id &optional source-file)
:END:

Get full filepath for analysis of SEG-ID.
If SOURCE-FILE is provided, includes a short-name suffix — unless an
un-suffixed `seg-NNN.org' for the same source already exists (an
un-migrated single-source folder), in which case the existing bare file
is reused so operations don't create a `-SHORT' duplicate.  See
`tibetan-analysis--resolve-filepath'.

** ~tibetan-analysis-paragraph-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1035
:SIGNATURE: (tibetan-analysis-paragraph-filename par-id)
:END:

Generate analysis filename for paragraph PAR-ID.
PAR-ID can be an integer (131), a bare numeric string ("131"),
or a §-prefixed string ("§131").  Returns `par-NNN.org' with
three-digit zero padding.

** ~tibetan-analysis-get-paragraph-filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1048
:SIGNATURE: (tibetan-analysis-get-paragraph-filepath par-id)
:END:

Return full filepath for paragraph analysis PAR-ID.

** ~tibetan-analysis-make-short-name~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1053
:SIGNATURE: (tibetan-analysis-make-short-name source-file)
:END:

Generate a short name suffix from SOURCE-FILE for analysis filenames.
E.g., \='Tigress-Story-BlockPrint-Class.org\=' -> \='tigress\='
      \='Reading-Sa-skya-legs-bshad.org\=' -> \='saskya\='
      \='Reading-05-Padma.org\=' -> \='padma\='.

** ~tibetan-analysis-compute-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1074
:SIGNATURE: (tibetan-analysis-compute-hash text)
:END:

Compute MD5 hash of TEXT for change detection.

** ~tibetan-analysis-create-paragraph-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1082
:SIGNATURE: (tibetan-analysis-create-paragraph-file par-id tibetan-text source-file auto-content &optional references)
:END:

Create a new paragraph analysis file for PAR-ID.
TIBETAN-TEXT is the paragraph body.  SOURCE-FILE is the path of the
comparative document.  AUTO-CONTENT is generated analysis content
(string).  REFERENCES, if non-nil, is an alist of (heading . body)
pairs from sibling translation sub-sections in the source paragraph
(e.g. `(("Lopez 2006" . "...") ("Wangjié & Mulligan" . "..."))');
they are written into a `* Reference Translations' top-level section
between `* Working Translation' and `* Auto-Analysis'.  Returns the
written filepath.

** ~tibetan-analysis-create-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1150
:SIGNATURE: (tibetan-analysis-create-file seg-id tibetan-text source-file auto-content &optional folio)
:END:

Create a new analysis file for SEG-ID.
TIBETAN-TEXT is the Tibetan content.
SOURCE-FILE is the path to the source document.
AUTO-CONTENT is the generated analysis content (string).

FOLIO (optional) is a folio reference (e.g. "D3a3" for
Yogācārabhūmi sources).  When non-nil, written as a `:PROPERTIES:'
drawer under the `* Tibetan Text' heading so the reader can see
which folio they're on.  P6 (CLAUDE.md §6, addressed 2026-05-05)
restores the folio that §5.8.2's drawer-aware extractor was
correctly stripping from the source segment body.

§5.23 (2026-05-22):  threads SOURCE-FILE through
`tibetan-analysis-get-filepath' so writes go to the SUFFIXED
path `seg-NNN-SHORT.org' (e.g. `seg-006-gal.org' for
`gal-chen-nyi-shu.org').  Was previously UNSUFFIXED — that
worked for single-source folders but caused silent overwrites
when multiple source files shared one analysis/ folder (MA
Readings: gal-chen / lam-rim / title-colophon all collided on
seg-1..seg-21).  The §5.8.3 fix that originally dropped this
argument was symmetric — to keep the skip-check + create-path
matching, the skip-check in
`tibetan-auto-analyze-document' has the matching change.

** ~tibetan-analysis-get-stored-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1250
:SIGNATURE: (tibetan-analysis-get-stored-hash filepath)
:END:

Get the stored Tibetan hash from analysis file at FILEPATH.

** ~tibetan-analysis-check-sync~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1259
:SIGNATURE: (tibetan-analysis-check-sync filepath tibetan-text)
:END:

Check if analysis at FILEPATH is in sync with TIBETAN-TEXT.
Returns t if in sync, nil if source has changed.

** ~tibetan-analysis-find-section-bounds~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1266
:SIGNATURE: (tibetan-analysis-find-section-bounds buffer section-name)
:END:

Find the start and end positions of SECTION-NAME in BUFFER.
Returns (START . END) or nil if not found.

** ~tibetan-analysis-get-user-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1284
:SIGNATURE: (tibetan-analysis-get-user-sections filepath)
:END:

Extract user-content + machine-generated top-level sections from
analysis file at FILEPATH.

Returns alist of (section-name . content) for sections present.
Sections that don't exist in the file are simply omitted.

Sections collected:
  - User-edited:  My Notes, Working Translation, Reference
    Translations, Translation Comparison, Apparatus, Footnotes.
  - Machine-generated, top-level (preserved across reanalyse
    when not re-firing the underlying Claude / DM call):
​    * Sanskrit Text                    (Phase 3 of two-language-
                                       parallel-analysis, 2026-04-30)
​    * Sanskrit Analysis                (Phase 2)
​    * Combined Analysis                (Phase 4)
​    * DharmaMitra Translation (Tibetan)   (Phase A.1)
​    * DharmaMitra Translation (Sanskrit)  (Phase A.2)
​    * Sanskrit (DharmaMitra)           (realign feature, 2026-04-30)

Caller (typically `tibetan-analysis-regenerate-auto') re-emits
these in canonical order; preservation prevents reanalyse from
silently destroying the upstream-Claude / DM artefacts.

** ~tibetan-analysis--get-unknown-top-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1365
:SIGNATURE: (tibetan-analysis--get-unknown-top-sections filepath)
:END:

Return the concatenated text of top-level sections in FILEPATH whose
heading is NOT in `tibetan-analysis--known-top-sections'.  Sections are
returned in file order, each ending with a blank line; nil when none.

** ~tibetan-analysis-migrate-suffix-in-folder~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1427
:SIGNATURE: (tibetan-analysis-migrate-suffix-in-folder &optional folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Walk FOLDER and rename unsuffixed analysis files to suffixed form.

For each `seg-NNN.org' or `sent-NNN.org' in FOLDER that lacks a
short-name suffix, reads the file's `#+SOURCE:' header to identify
the source document, derives a short-name via
`tibetan-analysis-make-short-name', and renames the file in place
to `seg-NNN-SHORT.org' / `sent-NNN-SHORT.org'.

Already-suffixed filenames are skipped (idempotent).  Files
without a `#+SOURCE:' link are skipped (no source to derive
short-name from).  Returns a plist summary:
  (:folder F :total N :renamed N-ren :skipped N-skip :failed N-bad)

FOLDER defaults to the analysis folder of the current buffer
(via `tibetan-analysis-get-folder', falling back to
`default-directory').  Interactive callers get a final-state
`message'.

§5.23 (2026-05-22) — companion to the create-file fix that
restored per-source filename suffixes.  Existing pre-§5.23
folders need a one-shot run of this command to migrate;  newly-
created files after the fix are already suffixed at creation.

** ~tibetan-analysis-strip-dangling-term-links-in-folder~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1518
:SIGNATURE: (tibetan-analysis-strip-dangling-term-links-in-folder &optional folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Walk FOLDER (default: analysis/ next to current source) and strip
dangling `[[term-X][...]]' links from every `seg-*.org' /
`sent-*.org' / `par-*.org' / `compound-*.org' analysis file.

Saves files that changed; leaves files with no dangles untouched.
Reports the total per-file dangle count + how many files were
modified.

Useful one-shot recovery for analysis files generated before
the 2026-05-18 dangling-link guard landed — without this command
the user would have to re-run `C-c u r' (batch reanalyse) on the
folder, which is much slower and re-fires Claude requests.

** ~tibetan-analysis--strip-dangling-term-links-in-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1559
:SIGNATURE: (tibetan-analysis--strip-dangling-term-links-in-buffer)
:END:

Strip dangling `[[term-X][label]]' links from the current buffer.

For each `[[term-X][label]]' org link whose `<<term-X>>' anchor
is absent from the SAME buffer, replace the link with just the
plain LABEL text.  Anchored links — those whose target exists —
are left untouched, as are non-`term-*' links (id:, file:, http:,
arbitrary internal anchors).

Returns the number of links stripped (for telemetry / tests).

Defensive post-pass run by `tibetan-analysis-create-file' and
`tibetan-analysis-regenerate-auto' so generated analysis files
are export-safe.  The root cause of the divergence — Interlinear
and Detailed Dictionary using independent MWU tokenization paths
that can disagree — is tracked separately.

** ~tibetan-analysis-regenerate-auto~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1601
:SIGNATURE: (tibetan-analysis-regenerate-auto filepath tibetan-text auto-content)
:END:

Regenerate the Auto-Analysis section in FILEPATH.
Preserves user sections (My Notes, Working Translation, Footnotes)
and reshapes the file into the canonical section order:

​  * Tibetan Text
​  * My Notes                     — user-edited (above Auto-Analysis
​  * Working Translation            so notes sit beside the Tibetan
                                   during class-prep reading)
​  * Auto-Analysis                — regenerated
​  * Footnotes                    — user-edited, footnote-like tail

Any pre-existing section content for My Notes / Working Translation
/ Footnotes is preserved verbatim; only its position in the file
may change.  Files already in canonical order are left untouched
except for Auto-Analysis + header metadata.  Idempotent: repeated
runs produce identical output.

Updates `#+TIBETAN_HASH' and `#+LAST_ANALYZED' as a side-effect.

** ~tibetan-analysis--read-dharmamitra-tibetan-nested-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1880
:SIGNATURE: (tibetan-analysis--read-dharmamitra-tibetan-nested-body filepath)
:END:

Return the trimmed body of `** DharmaMitra Translation' inside
`* Tibetan Analysis' in FILEPATH, or nil when absent / empty /
a placeholder.

Used by `tibetan-analysis-regenerate-auto' to preserve the
DharmaMitra Tibetan body across regenerates (the body sits
inside the auto-content region and would otherwise be erased
by the rewrite pass).

** ~tibetan-analysis--read-provided-translations-nested-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1916
:SIGNATURE: (tibetan-analysis--read-provided-translations-nested-body filepath)
:END:

Return the trimmed body of `** Provided Translations' inside
`* Tibetan Analysis' in FILEPATH, or nil when absent / empty.

§5.21 Commit 6/7 (2026-05-20):  used by
`tibetan-analysis-regenerate-auto' to preserve the user's hand-
pasted reference translations (Roehrich, Lopez, published English
versions, …) across regenerates.  Pattern modelled on the §5.20
`--read-dharmamitra-tibetan-nested-body' helper.

On legacy files (where the body still carries the now-retired
auto-children `*** CAT Gloss' / `*** Claude Vocabulary' / `***
Reference Translations'), this returns the body verbatim
including those subtrees;  the renderer no longer emits them,
so on subsequent regenerates they sit harmlessly in the user-
content slot.  Carsten can manually delete them on first inspect
if desired (one-shot manual sweep).

** ~tibetan-analysis--write-nested-provided-translations-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1960
:SIGNATURE: (tibetan-analysis--write-nested-provided-translations-body filepath body)
:END:

Write BODY into the nested `** Provided Translations' section of
FILEPATH (inside `* Tibetan Analysis'), replacing whatever empty
placeholder the renderer emitted.

§5.21 Commit 6/7 (2026-05-20):  called by
`tibetan-analysis-regenerate-auto' AFTER `save-buffer' to re-
inject the user's preserved Provided Translations body into the
freshly-regenerated file at the new nested location.  Mirror of
the §5.20 DM-Tibetan nested writer in
`tibetan-dharmamitra-translation.el'.

Pre-conditions:
  · FILEPATH exists and is in the §5.21 on-disk layout (carries
    `* Tibetan Analysis' parent + nested `** Provided
    Translations' placeholder).  No-op when the section is absent.
  · BODY is a non-empty string (caller skips the call when no
    preserved body exists).

** ~tibetan-analysis--extract-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2010
:SIGNATURE: (tibetan-analysis--extract-section-body section-text)
:END:

Extract just the body of a captured org section.

SECTION-TEXT is the full section text including heading line +
optional property drawer + body, as captured by
`tibetan-analysis-get-user-sections'.  Returns the trimmed body
(everything after the heading line and an optional `:PROPERTIES:
… :END:' drawer);  nil when SECTION-TEXT is nil / empty.

Used by the regenerate-auto migration path to extract just the
body of a captured top-level section so it can be re-inserted
at a different location (e.g. legacy `* DharmaMitra Translation
(Tibetan)' body → nested `** DharmaMitra Translation' under
`* Tibetan Analysis').

** ~tibetan-analysis--find-resources-dir~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2047
:SIGNATURE: (tibetan-analysis--find-resources-dir)
:END:

Find the Resources directory for the current document.
Walks up from the source file directory, checking each ancestor
for a Resources/ subfolder (up to 5 levels).  This handles typical
layouts where the analysis file lives several levels below the
Resources folder, e.g.
  Tibetisch IV/Resources/
  Tibetisch IV/work in progress/analysis/seg-004.org

** ~tibetan-analysis--extract-pdf-text~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2073
:SIGNATURE: (tibetan-analysis--extract-pdf-text pdf-path)
:END:

Extract text from a PDF file.
First checks for a cached .txt version (same name, .txt extension).
If no cache exists, tries pdftotext, then python3 + pymupdf.
Caches the result for future use.
Returns the full text as a string, or nil if extraction fails.

** ~tibetan-analysis--extract-segment-block~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2137
:SIGNATURE: (tibetan-analysis--extract-segment-block text seg-num)
:END:

Return only the block for SEG-NUM from multi-segment TEXT.
Recognizes org/markdown headings like `## Segment 4`, `** Segment 4`,
`# Segment 4`.  Matches up to the next heading at the same level, or
end of text.  Returns nil if no matching segment heading is found.

** ~tibetan-analysis--filename-segment-number~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2159
:SIGNATURE: (tibetan-analysis--filename-segment-number basename)
:END:

Return the segment number encoded in BASENAME, or nil.
Recognises markers like Segment-04, segment_4, seg-4.

** ~tibetan-analysis--find-reference-translations~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2167
:SIGNATURE: (tibetan-analysis--find-reference-translations _tibetan-text &optional seg-num source-text)
:END:

Search for reference translations for the current segment.

Two sources are consulted:
  (a) Inline 〔trans:N〕 … 〔/trans〕 blocks in SOURCE-TEXT (the raw
      class file), when it and SEG-NUM are provided.  These are the
      translations the user typed directly into the source file
      alongside each segment, and they take precedence because they
      represent the most current teaching/working translation for
      the passage.
  (b) External files in the Resources folder (.txt/.org/.md) and
      OCR'd PDFs whose filename hints at translation content.

If SEG-NUM is non-nil, Resources files are segment-scoped:
- Filenames containing `Segment-NN` / `seg-NN` are only included when NN
  matches SEG-NUM.
- Multi-segment files (like `Class-Translations.txt`) with `## Segment N`
  headings inside are narrowed to the matching block.
Files with neither filename marker nor internal segment heading are
included whole (e.g. the Blue Annals OCR).

Returns list of (source-name . translation-text) pairs.

** ~tibetan-analysis--ensure-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2275
:SIGNATURE: (tibetan-analysis--ensure-vocabulary)
:END:

Ensure vocabulary is loaded before analysis.

Short-circuits on the canonical `tibetan-glossaries-loaded' flag so a
batch reanalysis never re-emits the `Loading glossaries...' message
or replays the underlying I/O.  The hash-table-count fallback is
kept for sessions where the flag is not bound (older loaders).

§5.22 follow-up (2026-05-21):  also primes the per-document
Resources vocabulary (auto-detected from `Resources/' next to
the source buffer) and Custom vocabulary (pointed to by
`#+TIBETAN_VOCAB_FILE:').  Without these explicit loads, the
Interlinear Gloss's multisource ranker found
`tibetan-current-resources-vocab' / `tibetan-current-custom-vocab'
nil and silently fell through to rank 3 (Steinert) — surfacing
generic dictionary glosses where the user expected their curated
Resources entries.  Both loaders cache per-file, so repeated
calls are cheap.  Wrapped in `ignore-errors' because the loaders
read `(buffer-file-name)' and may signal in edge cases (e.g.
indirect or temp buffers).

** ~tibetan-analysis--get-particle-annotation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2321
:SIGNATURE: (tibetan-analysis--get-particle-annotation word)
:END:

Get compact particle annotation for WORD.
Returns string like \='(ERG)\=' or \='(LOC: if/when)\=' or nil.

** ~tibetan-analysis--format-word-with-wylie~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2366
:SIGNATURE: (tibetan-analysis--format-word-with-wylie word)
:END:

Return WORD in the canonical `SCRIPT [wylie]' display form.

Used by every generated-analysis section that mentions a Tibetan
word except the top-level `* Tibetan Text' section (which is kept
script-only by design).  Centralises the format so all sections
stay in lock-step; do NOT emit `script  wylie' pairs ad-hoc from
call sites.

Behaviour:
- If WORD is nil or empty, returns an empty string.
- If a Wylie conversion via `tibetan-to-wylie-fixed' is available
  and yields a non-empty string distinct from WORD, returns
  "WORD [wylie]".
- Otherwise returns WORD unchanged (e.g. WORD already Wylie, or
  converter unavailable, or converter echoes WORD).

** ~tibetan-analysis--bialek-type~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2397
:SIGNATURE: (tibetan-analysis--bialek-type word)
:END:

Return the Bialek classification type string for WORD, or nil.

Single source of truth for particle / converb terminology in this
module.  Delegates to `tibetan-analyze-grammar-bialek' so the
Word/Particle List and the Grammatical Markers section stay in
lock-step.

Because bialek splits its input on tsheg (`་'), a tsheg-joined
compound like `བྱས་པས' gets split into (`བྱས' `པས') and neither
half alone carries enough context for the causal-converb detector
to fire.  We therefore try BOTH the original word and the
tsheg-removed form before giving up — so the full compound can
still be classified as a whole.  Returns the `type' field (index
2) of the first matching tuple.

** ~tibetan-analysis--strip-drawers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2423
:SIGNATURE: (tibetan-analysis--strip-drawers text)
:END:

Remove org property drawers from TEXT (source child texts carry
the segment's :PROPERTIES: drawer).

** ~tibetan-analysis--sentence-tree-for-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2431
:SIGNATURE: (tibetan-analysis--sentence-tree-for-segment seg-id source-file)
:END:

Build the FULL-SENTENCE verb-first tree for the sentence containing
SEG-ID in SOURCE-FILE (Phase 5: the sentence is the analysis domain).

Each child segment is parsed SEPARATELY and the token streams are
concatenated with offset-shifted verb positions and MWU spans —
parsing the joined text instead merges tokens across the shad at
child boundaries (verified: `ནས། ཆོས' tokenized as one word).

Returns (:tree TREE :ranges ((SEG-NUM START END)...) :sent-num N
:seg-nums (..)) or nil when the segment isn't inside a multi-segment
sentence, the layout is flat, or any substrate is unavailable —
nil → the caller falls back to segment-local analysis.

** ~tibetan-analysis--render-sentence-slice~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2497
:SIGNATURE: (tibetan-analysis--render-sentence-slice slice seg-id)
:END:

Render SEG-ID's slice of the sentence-level SLICE plist, or nil.
The head segment (containing the main verb) gets the FULL tree under
a sentence-context header; a dependent segment gets its own clause
subtrees plus a chain note naming the main verb and its segment.

** ~tibetan-analysis--tree-np-string~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2567
:SIGNATURE: (tibetan-analysis--tree-np-string np)
:END:

Render NP's head as `SCRIPT [wylie]'.

** ~tibetan-analysis--render-tree-node~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2571
:SIGNATURE: (tibetan-analysis--render-tree-node tree indent header)
:END:

Render TREE (a `tibetan-analyze-sentence' node) at INDENT spaces.
HEADER is the verb line's label ("MAIN VERB", "COMPLEMENT VERB",
"VERB").  Never emits a line starting with `*' (C1 lesson).

** ~tibetan-analysis--render-sentence-tree~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2635
:SIGNATURE: (tibetan-analysis--render-sentence-tree words verbs multiword-units)
:END:

Return the verb-headed tree rendering for the `** Sentence
Structure' section (verb-first redesign, replaces the flat per-clause
blocks).  Empty string on empty inputs; a visible placeholder when no
clause is detected.  Vocab-fallback verbs are filtered (the §5.16
uddāna fix).

** ~tibetan-analysis--get-grammatical-role~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2656
:SIGNATURE: (tibetan-analysis--get-grammatical-role word root-form verb-table)
:END:

Determine grammatical role description for WORD with ROOT-FORM.

Uses VERB-TABLE for verb detection.  For particle / converb
classification, delegates to `tibetan-analyze-grammar-bialek' via
`tibetan-analysis--bialek-type' so the terminology matches the
Grammatical Markers section exactly (e.g. \='GENITIVE (GEN)\=',
\='CONVERBIAL: CAUSAL CONVERB\=').

Return value is a string such as \='Transitive verb, CONVERBIAL:
CAUSAL CONVERB\=', \='GENITIVE (GEN)\=', or \='Noun\=' — or
nil when nothing informative is available.

** ~tibetan-analysis--detect-verb-suffix~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2727
:SIGNATURE: (tibetan-analysis--detect-verb-suffix word _root-form)
:END:

Return a Bialek-terminology suffix label for WORD, or nil.

Thin adapter over `tibetan-analysis--bialek-type' so the suffix
label attached to a verb in the Sentence Structure / Word list
stays in lock-step with the Grammatical Markers section.  The
returned string, when non-nil, is prefixed with ", " so callers
can concatenate it directly after a verb-kind label, e.g.
\='Transitive verb, CONVERBIAL: CAUSAL CONVERB\='.  Returns nil
when bialek has no classification for the word's particle tail
(e.g. a bare nominaliser on a verb stem, or an unusual suffix
bialek does not cover).

** ~tibetan-analysis--verb-morphology-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2742
:SIGNATURE: (tibetan-analysis--verb-morphology-gloss root-form)
:END:

If ROOT-FORM (Tibetan script, particles stripped) is a verb in the
Hill 2010 database, return a morphology-aware English gloss.

Returned format, depending on whether ROOT-FORM is the lemma or an
inflected stem:

  Lemma form:        "to do, to make"
  Past stem:         "pf. of byed — to do, to make"
  Future stem:       "ft. of byed — to do, to make"
  Imperative stem:   "imp. of byed — to do, to make"
  Present stem:      (same as lemma when equal)

Returns nil if ROOT-FORM is not a verb. Used by the Word/Particle List
to make byas/byas pas/bstugs type entries show their underlying lemma
and canonical meaning, rather than falling back to dictionary glosses
like "verb: do" that hide the morphology.

** ~tibetan-analysis--wylie-like-token-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2806
:SIGNATURE: (tibetan-analysis--wylie-like-token-p tok)
:END:

Return non-nil if TOK looks like a bare Wylie syllable rather than
a natural English/German word.  Used to strip compound-reference
tails that leak out of user wordlists, e.g. "dice kha phung la thong"
→ only "dice" is the real gloss.

** ~tibetan-analysis--strip-wylie-tail~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2833
:SIGNATURE: (tibetan-analysis--strip-wylie-tail gloss)
:END:

Remove trailing Wylie-only example tokens from GLOSS.
Some user wordlists embed compound cross-references directly after
the actual gloss (e.g. "dice kha phung la thong" where only
"dice" is the English meaning).  Walks tokens from the end, strips
consecutive Wylie-like tokens, and returns the trimmed gloss — but
only if at least one plain content token remains.

** ~tibetan-analysis--render-particle-skeleton~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2856
:SIGNATURE: (tibetan-analysis--render-particle-skeleton tibetan-text _particles verbs)
:END:

Generate a visual particle map for TIBETAN-TEXT.
PARTICLES is the parsed particles list, VERBS is the verb list.
Returns an org-formatted string with particles highlighted:
- =PARTICLE= for case markers (genitive, dative, etc.)
- ~CONVERB~ for converb particles
- *PARTICLE* for sentence-final particles
- [Ø ROLE] for zero-marked arguments

The particle keeps its natural lowercase Wylie spelling inside the
markup (`='i='`, not `='I='`) since the magenta / orange face
remap in `tibetan-analysis-setup-faces' already makes it stand
out visually.  Earlier versions upcased for plain-text / PDF
fallback visibility; the face remap + customizable colours cover
those cases, and lowercase reads cleaner for apostrophe-prefixed
particles like `'i' / `'o'.

** ~tibetan-analysis--strip-folio-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2987
:SIGNATURE: (tibetan-analysis--strip-folio-markers text)
:END:

Strip folio markers like (12a2), (16b5) etc. from TEXT.
These are editorial annotations from the Tibetan block print and
should not be processed as Tibetan text.  Also removes extra
whitespace left behind.

** ~tibetan-analysis--filter-to-tibetan-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 2998
:SIGNATURE: (tibetan-analysis--filter-to-tibetan-lines text)
:END:

Return TEXT with non-Tibetan content lines dropped.

Source files often interleave the Tibetan body with editorial
material that the analyser must NOT treat as input:
  - Parenthetical English descriptions on their own line,
    e.g. `(Homage / author's dedication verses)'.
  - Org-mode comment lines starting with `#'.
  - Blank separator lines.
  - Folio-marker-only lines.

Lines that contain no Tibetan character (anywhere in the U+0F00
block) are dropped entirely; lines that contain ANY Tibetan
character are kept verbatim.  The surviving lines are joined with
a single tsheg (U+0F0B) so that:
  1. `tibetan-parse-enhanced' — which splits on tsheg — tokenises
     the first word of each continuation line cleanly (a raw
     newline does NOT split, producing spurious compound tokens
     like `ལོ༎\nརྒྱལ').
  2. `tibetan-to-wylie-fixed' emits a space between the two words
     (instead of the bare `lo//rgyal' it produces when the source
     contains only a newline, which looks like a typo to readers
     of the Wylie transliteration).

Without this filter, `tibetan-to-wylie-fixed' renders the
non-Tibetan portion as leading whitespace (producing a ~30-space
indent on sections like Wylie Transliteration) and
`tibetan-extract-vocabulary' tokenises the English words, matching
some of them against Wylie glossary entries (the English word
"on", for instance, matches the Wylie hash key and produces a
bogus "set" gloss).

** ~tibetan-analysis--format-bilingual-gloss~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3038
:SIGNATURE: (tibetan-analysis--format-bilingual-gloss gloss)
:END:

Reformat a German//English GLOSS for easy reading.
If GLOSS has the shape
  "{DE-stem-ref}; {DE-meaning} // {EN-stem-ref}; {EN-meaning}"
where both sides start with a matching verb-stem reference
("Pf. von ..." + "pf. of ...", etc.), returns a two-line string
  "{DE-stem-ref};\n{DE-meaning} // {EN-meaning}"
with the redundant English stem reference removed.
Plain "German // English" entries without a stem reference are
returned unchanged, since they're already compact.
Falls back to the original string when the pattern doesn't fit.

The stem-reference detection is deliberately conservative so we do
not mangle entries that happen to contain a period or semicolon for
unrelated reasons.

** ~tibetan-analysis--detailed-dict-is-particle-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3073
:SIGNATURE: (tibetan-analysis--detailed-dict-is-particle-p tibetan)
:END:

Return non-nil when TIBETAN is a pure case / converb particle.
Matches a single-syllable token (no tsheg / word break) whose text
appears in `tibetan-extract-vocab--particle-tails' — the same list
used by the MWU loop and vocabulary extractor.  Particle entries in
the Detailed Dictionary get a compact one-line rendering pointing
at the Particle Overview section instead of a verbose multi-source
dump.

** ~tibetan-analysis--render-detailed-dictionary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3087
:SIGNATURE: (tibetan-analysis--render-detailed-dictionary tibetan-text vocab-pairs &optional bialek-analysis)
:END:

Render the `** Detailed Dictionary' section for TIBETAN-TEXT.
Inserts into the current buffer.  TIBETAN-TEXT is the raw input
string; VOCAB-PAIRS is the segmentation's (word . gloss) alist used
as a legacy fallback when the multi-source helper is unavailable.
BIALEK-ANALYSIS, if non-nil, is the output of
`tibetan-analyze-grammar-bialek'; its grammatical tags let the
renderer split stem+particle compounds the same way the Interlinear
Gloss does, so a click on `[[term-mthu][mthu]]' in the Interlinear
lands on the `<<term-mthu>>' anchor emitted here (not a separate
`<<term-mthu-i>>' for the inflected form).  When the analysis is
omitted the renderer falls back to `tibetan-strip-particles' for a
narrower (unambiguous-particles-only) strip.

The section body is produced by `tibetan-vocab-extract-detailed'
when present, enriched via `tibetan-vocab-multisource-entries' for
the per-source breakdown (Resources / Steinert / Rangjung Yeshe /
DharmaMitra).  Entries backed by a curated Resources list get a
`★' marker on the head line.  Sanskrit is only emitted when the
source entry itself carries it.  The bilingual gloss formatter
keeps German // English pairs side by side.

** ~tibetan-analysis--particle-wylie-equivalent-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3319
:SIGNATURE: (tibetan-analysis--particle-wylie-equivalent-p a b)
:END:

Return non-nil if particle Wylie strings A and B denote the same
particle modulo the inherent-vowel representation.

Tibetan consonants carry an implicit `a' in their syllable form:
`ར' can be Wylie'd as either `r' (consonant alone, as Claude emits
when asked for the `particle' part of a compound like `der') or
`ra' (consonant + inherent vowel, as `tibetan-to-wylie-fixed'
produces when given the bare Tibetan syllable).  Without a
normaliser, the Grammar renderer's match `equal' loop considers
the two strings distinct and silently drops the Portfolio snippet
for every `ར' occurrence.

Normalisation: lowercase, trim, strip a trailing `a' when the
preceding char is a consonant (so `ra' → `r', `sa' → `s', `da' →
`d'; `la' stays `la' because `l' alone is ambiguous with the
ablative converb `la' vs the suffix `l' case — and Claude emits
`la' for the dative, matching bialek).  Compare the normalised
forms for equality.

** ~tibetan-analysis--particle-wylie-normalise~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3343
:SIGNATURE: (tibetan-analysis--particle-wylie-normalise s)
:END:

Normalise particle Wylie S for equivalence matching.
Lowercase, trim, and strip trailing `a' when preceded by a single
consonant so `ra' and `r' both map to `r'.  Multi-char particles
like `la', `ni', `dang' are preserved verbatim.

** ~tibetan-analysis--buddhist-term-entry-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3357
:SIGNATURE: (tibetan-analysis--buddhist-term-entry-p entry)
:END:

Return non-nil when ENTRY is an 84000 `<term>'-tagged Buddhist term.
ENTRY is a plist from `tibetan-vocab-multisource-entries'.  The
check matches on (a) `:source' containing `84000' (either 43-84000Dict
or 44-84000Definitions — both carry the canonical-term marker) AND
(b) `:primary' or `:detailed' starting with the literal `<term>' tag
that Steinert's 84000 tables emit for canonical Buddhist terminology.

`<person>' and `<place>' tags are intentionally NOT treated as
Buddhist terms here — those surface via B1's rename-override path
(commit e82e817), and the user-facing `*** Buddhist Terms' section
is reserved for terms the reader would look up in an encyclopaedia
of Buddhism (Four Immeasurables, Bodhicitta, Skandha, etc.).

** ~tibetan-analysis--collect-buddhist-terms~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3378
:SIGNATURE: (tibetan-analysis--collect-buddhist-terms enriched-vocab-pairs)
:END:

Return a list of (SCRIPT WYLIE DEFINITION) for Buddhist terms in
ENRICHED-VOCAB-PAIRS.  ENRICHED-VOCAB-PAIRS is the
((TIBETAN-CLEAN . SHORT-MEANING) ...) alist threaded out of the
generate-content vocab loop.

For each unique token, the helper calls
`tibetan-vocab-multisource-entries' and inspects each entry for
Steinert 84000 `<term>'-tagged content (see
`--buddhist-term-entry-p').  When found, the longest available
body (`:detailed' preferred over `:primary') is taken as the
definition — 84000Definitions routinely carries a paragraph-length
explanation of the term's meaning, provenance, and related
concepts, which is exactly what a student needs on first encounter.

Deduplicated by Tibetan script so a term appearing twice in the
segment surfaces only once.  Returns nil when no Buddhist terms
are present — the caller omits the subsection entirely in that
case.

** ~tibetan-analysis--format-buddhist-term-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3430
:SIGNATURE: (tibetan-analysis--format-buddhist-term-body body &optional max-len)
:END:

Trim BODY (the 84000Definitions `:detailed' text) for display.
Strips the leading `<term>' tag if present, converts literal `\n'
escape sequences to real newlines, flattens multi-paragraph bodies
to a single line with `/ ' separators, and truncates to MAX-LEN
characters (default 400) with an ellipsis.  The aim: a one-line
definition compact enough to sit under a `- SCRIPT [wylie]' entry
in the `*** Buddhist Terms' subsection.

** ~tibetan-analysis--render-buddhist-terms-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3452
:SIGNATURE: (tibetan-analysis--render-buddhist-terms-section terms)
:END:

Insert `*** Buddhist Terms' section into the current buffer for
TERMS.  TERMS is the list of `(SCRIPT WYLIE 84000-BODY)' triples
returned by `tibetan-analysis--collect-buddhist-terms'.

Phase 5 of zettel-in-translation-workflow (2026-04-26): for each
term, consults `tibetan-zettel--read-claude-explanation' first.
When a fresh cache exists (zettel exists AND `:prompt-version:'
matches the live system prompt's hash), the zettel's body is
rendered with a `[via zettel cache]' suffix.  When no cache or
stale, falls back to the truncated 84000Definitions body — same
behaviour as pre-Phase-5 U3.

The cache reader returns nil when the prompt-version is stale,
so the writer (Phase 5 extension to commit 2394c50's writer) can
overwrite on the next Claude response without manual
invalidation.

** ~tibetan-analysis--render-grammar-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3495
:SIGNATURE: (tibetan-analysis--render-grammar-section tibetan-text particles verbs
                                                              bialek-analysis
                                                              &optional
                                                              claude-particles
                                                              enriched-vocab-pairs)
:END:

Render the merged `** Grammar' section body to the current buffer.

Pass 6b replaces three redundant sections (`** Particle Map',
`** Particle Overview', `** Grammatical Markers') with a single
`** Grammar' containing:

  *** Particle Map — the Wylie transliteration with particle markers
      (=CASE= for case particles, ~CONVERB~ for converbs, Ø for
      zero-marked arguments) — a quick visual scan of the grammar.

  *** Claude Grammar (U4, 2026-04-24) — Claude's prose reading of
      the passage's syntactic structure.  Placeholder emitted here;
      body filled in by `--ensure-claude-headings' +
      `--restore-claude-sections' after Claude responds.

  *** Buddhist Terms (U3, 2026-04-24) — 84000Definitions paragraph
      for any fixed Buddhist term (Four Immeasurables, Bodhicitta,
      Skandha, ...) detected in the passage via the multi-source
      dictionary stack.  Omitted when no `<term>'-tagged entries
      are present.

  *** Particles in This Segment — one line per Bialek particle
      detection: type, top-level Portfolio reference, translation
      hint.  Pass 6c augments this with per-occurrence function
      sub-IDs tagged by Claude + the matching Portfolio snippet,
      making the section self-contained for readers without access
      to the user's Portfolio source file.

TIBETAN-TEXT feeds the Particle Map's annotated Wylie.  PARTICLES
and VERBS come from the Bialek / verb-extraction pipeline.
BIALEK-ANALYSIS is the result of `tibetan-analyze-grammar-bialek'
— drives the compact particles list.

CLAUDE-PARTICLES, when non-nil, is a list of plists (parsed via
`tibetan-analysis--parse-claude-particles') carrying
`(:word W :particle P :sub-id ID :label L)' for each per-occurrence
function Claude identified.  When passed, each Bialek line gains a
specific sub-function heading and the matching Portfolio snippet
text.  When nil, falls back to the compact parser-only list.

ENRICHED-VOCAB-PAIRS, when non-nil, is the
((TIBETAN-CLEAN . SHORT-MEANING) ...) alist built upstream by the
vocab-pairs loop — threaded in so the `*** Buddhist Terms'
subsection can look up per-token 84000Definitions bodies.  When
nil, the subsection is omitted.

** ~tibetan-analysis--render-particle-bullets~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3593
:SIGNATURE: (tibetan-analysis--render-particle-bullets bialek-analysis claude-particles)
:END:

Render per-particle bullets for `*** Particles' (§5.21 Commit 3/7).
Was the body of the standalone `*** Particles in This Segment'
section pre-§5.21.  Now part of the merged `*** Particles' that
also carries the visual skeleton at the top.

For each Bialek-detected particle in BIALEK-ANALYSIS:
  1. Header line:  particle (with context word when clitic) ·
     type · `[Bialek 2022 §X.Y; Portfolio §A.B]' reference
     bracket.  Bialek 2022 ref added Commit 4/7;  this commit
     emits only the Portfolio ref via the existing `portfolio'
     field on the bialek tuple.
  2. Try to match against CLAUDE-PARTICLES on particle wylie for
     a per-occurrence sub-ID + short label.  On hit, look up the
     Portfolio snippet via
     `tibetan-interlinear-portfolio-function-snippet'.
  3. On miss, fall back to the parser's generic translation
     hint.

** ~tibetan-analysis--split-level2-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3842
:SIGNATURE: (tibetan-analysis--split-level2-sections content)
:END:

Split CONTENT into an ordered list of level-2 section cons cells.
Each element is (HEADING . BODY), where HEADING is the full heading
line (e.g. `** Wylie Transliteration') and BODY is everything up to
the next `^\*\* ' heading or end of string.  Text that appears
before the first level-2 heading is returned under the special key
`t' (typically empty) so the caller can preserve it.

** ~tibetan-analysis--extract-claude-grammar~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3874
:SIGNATURE: (tibetan-analysis--extract-claude-grammar sections)
:END:

If SECTIONS contains a `** Provided Translations' whose body holds a
`*** Claude Grammar' sub-heading, extract that subsection and return
a new list with:
  - a new `** Claude Grammar' level-2 entry carrying the promoted body
  - the original `** Provided Translations' with that sub-heading
    removed (the rest of its body preserved)
When no `*** Claude Grammar' is present the sections are returned
unchanged.

** ~tibetan-analysis--reorder-auto-content~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3907
:SIGNATURE: (tibetan-analysis--reorder-auto-content content)
:END:

Reorder the level-2 sections of CONTENT into the priority order
defined by `tibetan-analysis--priority-section-order', keeping every
other section in its original relative position.

This also promotes `*** Claude Grammar' out of `** Provided
Translations' so it can slot into the priority list as a first-
class level-2 section.

** ~tibetan-analysis--render-sanskrit-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 3964
:SIGNATURE: (tibetan-analysis--render-sanskrit-source sanskrit-plist)
:END:

Return a top-level `* Sanskrit Text' org section as a string.

SANSKRIT-PLIST is the walker plist
`(:iast STRING :devanagari STRING-or-nil :script-source SYMBOL)'
or nil.  Returns:
  - empty string for nil input or empty `:iast', so callers can
    safely concatenate without guarding.
  - a complete top-level section for a populated plist:

​      * Sanskrit Text

      IAST: <iast>
      [Devanagari: <devanagari>]

The trailing blank line ensures the section concatenates cleanly
with the next section in the buffer.

Pure function — no buffer side effects, no metadata reads.  All
Sanskrit-text decisions (whether to emit, which lines to
include) happen here so callers stay simple.

Phase 3 of two-language-parallel-analysis (2026-04-30) promotes
this from the Phase-2 `** Sanskrit Source' (which lived inside
`* Auto-Analysis') to the new top-level `* Sanskrit Text'
section.  Top-level placement makes Sanskrit a peer of the
Tibetan analysis rather than a subsection of it; the same
walker-driven gate (placeholder bodies → nil) applies.

** ~tibetan-analysis-generate-content~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 4004
:SIGNATURE: (tibetan-analysis-generate-content tibetan-text &optional seg-id source-text source-file)
:END:

Generate auto-analysis content for TIBETAN-TEXT.
Returns the analysis as a string (org-mode formatted).

SEG-ID, if provided, enables segment-scoped reference translations
(per-segment Blue Annals files, narrowing multi-segment class files).

SOURCE-TEXT, if provided, is the full text of the source class file.
When SEG-ID resolves to a segment number, the 〔trans:SEG-NUM〕 block
inside SOURCE-TEXT is surfaced as an additional reference
translation.  This is how inline class-taught translations reach the
analysis buffer.  Batch callers that do not have the source buffer
handy may omit this argument; only the inline-trans surfacing is
affected, not the rest of the analysis.

SOURCE-FILE, if provided, is the absolute path to the source
document.  Its `#+TIBETAN_CORPUS:' header (if any) is read and
`tibetan-vocab--corpus' is dynamically bound for the duration of
analysis so the dictionary ranker promotes the matching Steinert
sub-dictionary into rank-3.  When omitted, the function falls back
to `buffer-file-name' (useful when called from inside a source-doc
buffer); when that too is nil, corpus-specific ranking simply
doesn't apply and the default ranking is used.

Claude-tagged per-particle functions (Pass 6c) are threaded in via
the dynamic `tibetan-analysis--claude-particles-for-render' — set
it with `let' around the call when Claude data is available.

§5.21 Commit 6/7 (2026-05-20):  SEG-ID and SOURCE-TEXT are kept
in the signature for caller compatibility but are no longer
consumed inside this function.  Their sole previous use was the
`*** Reference Translations' auto-fill nested under `**
Provided Translations';  that auto-fill was retired in the
same commit so PT could become a pristine user-content slot.
The `(ignore …)' form below silences the byte-compiler's
unused-arg warning without breaking the public API.

** ~tibetan-open-segment-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 4809
:SIGNATURE: (tibetan-open-segment-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open or create analysis, dispatched by cursor context (all 3 levels).
Priority is most-specific-wins, respecting nested org layouts:

  1. `*** Segment N' / `**** Segment N' heading (or its body)
     → classical segment-level analysis (seg-NNN.org)
  2. `*** Sentence N' heading (or its body between segments)
     → `tibetan-sentence-open-analysis' (sent-NNN.org)
  3. `** §N' paragraph heading (or any descendant when the subtree
     has no sentence/segment children)
     → `tibetan-open-paragraph-analysis' (par-NNN.org)
  4. Legacy 〔seg:…〕 or plain-text line
     → classical segment impl via `-any-format' detection.

Segment/sentence detectors only look at the nearest heading, so
they remain mutually exclusive.  The paragraph detector walks up
the outline (it's true anywhere inside a §-subtree), so checking
it last prevents paragraph from swallowing a nested segment or
sentence.

** ~tibetan--open-segment-analysis-impl~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 4848
:SIGNATURE: (tibetan--open-segment-analysis-impl)
:END:

Segment-level implementation; see `tibetan-open-segment-analysis'.

** ~tibetan-reanalyze-segment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 5012
:SIGNATURE: (tibetan-reanalyze-segment)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-analyze current segment / sentence / paragraph, preserving user notes.
Dispatches by context exactly like `tibetan-open-segment-analysis':
segment > sentence > paragraph > legacy, most-specific-wins.

** ~tibetan--reanalyze-segment-impl~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5034
:SIGNATURE: (tibetan--reanalyze-segment-impl)
:END:

Segment-level implementation; see `tibetan-reanalyze-segment'.

** ~tibetan-open-paragraph-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 5164
:SIGNATURE: (tibetan-open-paragraph-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open or create analysis for the current `** §N' paragraph.
Requires point to be anywhere inside a paragraph subtree whose
Tibetan text is carried by a `*** Tibetisch' (or
`*** Tibetisch (B2)') child.  If no analysis exists yet, generate
one via `tibetan-analysis-generate-content' (same pipeline as
`tibetan-open-segment-analysis'), write `par-NNN.org', and fire an
async Claude translation request.  The analysis file opens in a
right-side window.

** ~tibetan-reanalyze-paragraph~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 5221
:SIGNATURE: (tibetan-reanalyze-paragraph)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-analyze the current paragraph, preserving user sections.
Regenerates only `* Auto-Analysis'; `* My Notes',
`* Working Translation', and `* Footnotes' stay intact, matching
the segment-level `tibetan-reanalyze-segment' contract.

** ~tibetan-analysis--read-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5270
:SIGNATURE: (tibetan-analysis--read-section-body filepath section-name)
:END:

Return the body of `* SECTION-NAME' in FILEPATH (nil if missing).
The body excludes the heading line itself and any trailing blank line.
Works for top-level (* …) sections.

** ~tibetan-analysis--seg-id-from-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5289
:SIGNATURE: (tibetan-analysis--seg-id-from-filename filepath)
:END:

Return the numeric segment id encoded in FILEPATH's basename, or nil.

** ~tibetan-analysis--fire-parallel-claude-with-plist~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5299
:SIGNATURE: (tibetan-analysis--fire-parallel-claude-with-plist tibetan-text sanskrit-plist source-file analysis-file)
:END:

Fire Sanskrit + Combined Claude calls with a PRE-RESOLVED plist.

SANSKRIT-PLIST must already be the walker output (or a
sentence-level aggregation thereof) for the relevant segment
or sentence.  The Tibetan call is fired SEPARATELY by the
caller; this helper only dispatches the parallel-mode-specific
Sanskrit call (and chains Combined off its callback).

Soft-coded: when the Sanskrit / Combined modules aren't
loaded, the call is a clean no-op.  When SANSKRIT-PLIST is
nil — which the walker returns for non-parallel sources, no-
sibling segments, OR placeholder bodies — also a no-op.

Returns t when the Sanskrit call was dispatched, nil otherwise.

** ~tibetan-analysis--fire-parallel-mode-claude-calls~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5361
:SIGNATURE: (tibetan-analysis--fire-parallel-mode-claude-calls tibetan-text source-file analysis-file)
:END:

Fire the Sanskrit + Combined Claude calls for ANALYSIS-FILE
(SEGMENT-level analysis file — looks up the segment walker
plist by seg-id parsed from the filename).

Thin wrapper around
`tibetan-analysis--fire-parallel-claude-with-plist'.  Returns
nil when the source isn't parallel-mode or the segment's
Sanskrit sibling is a placeholder.

** ~tibetan-analysis-reanalyze-file~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 5383
:SIGNATURE: (tibetan-analysis-reanalyze-file filepath &key source-file re-request-claude dry-run)
:END:

Re-run auto-analysis on an existing analysis FILEPATH.

Tibetan text is read from the file's own `* Tibetan Text' section;
segment id is parsed from the filename (`seg-NNN[-short].org').
SOURCE-FILE, when supplied, is the path of the original class file —
its buffer contents are scanned for inline 〔trans:N〕 blocks.

RE-REQUEST-CLAUDE controls Claude / DM request dispatch:
  · nil             — never re-fire;  preserve existing content.
  · t               — always re-fire (legacy behaviour).
  · `:missing-only' — fire only when the file's Claude
                       Translation / Vocabulary slots are missing
                       or placeholders (§5.22 follow-up, 2026-05-21
                       — `tibetan-analysis--should-fire-claude-p').
                       DharmaMitra is fired in parallel under the
                       same gate.  Files already populated are
                       skipped, saving API spend and class-prep
                       time when batch-reanalysing for layout
                       updates.

With DRY-RUN non-nil, return a plist describing what would happen
without touching the file.  Otherwise return a plist:
  (:file F :seg-id ID :ok BOOL :error STR :claude-preserved BOOL).

** ~tibetan-analysis--folder-analysis-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 5616
:SIGNATURE: (tibetan-analysis--folder-analysis-files folder)
:END:

Return the list of seg-NNN*.org files under FOLDER, sorted by seg-id.

** ~tibetan-analysis-batch-reanalyze~  /function (cl)/  ⚡  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 5627
:SIGNATURE: (tibetan-analysis-batch-reanalyze &key folder source-file re-request-claude dry-run)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-run auto-analysis on every `seg-NNN*.org' file in FOLDER.

FOLDER defaults to the analysis folder of the current buffer (see
`tibetan-analysis-get-folder').  SOURCE-FILE defaults to the current
buffer's file.  With RE-REQUEST-CLAUDE non-nil, a fresh Claude request
is dispatched for each file; otherwise the stored `*** Claude'
translation is preserved verbatim.  With DRY-RUN non-nil, no files
are written — a report is returned without touching anything.

User sections (My Notes, Working Translation, Footnotes) and the
`*** Claude' translation are preserved on every file.

Returns a plist summary:
  (:folder F :total N :ok N-ok :failed N-bad :results RESULTS).

** ~tibetan-analysis-roman-scale~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 295
:TYPE: 'float
:GROUP: tibetan-cat
:DEFAULT: 0.65
:END:

Scale factor for non-Tibetan (roman) text in analysis buffers.
Values less than 1.0 make text smaller, e.g. 0.65 = 65% size.

** ~tibetan-analysis-show-clause-structure~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 301
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

When non-nil, the generated analysis file includes a `** Clause
Structure' section populated from `tibetan-analyze-round2'.

The section lists, per clause, the clause type (main vs. dependent
+ the converb that makes it dependent), the clause's main verb,
the NPs found inside it with their case tags (ERG/ABS/DAT/LOC/
TERM/GEN/INST/COM), and any semantic-role assignments derivable
from the verb's Hill-DB case frame (agent, patient, goal, …).

Set to nil to suppress the section; existing analysis files are
only affected the next time they are regenerated.

** ~tibetan-analysis-class-folded-sections~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 849
:TYPE: '(repeat string
:GROUP: tibetan-cat
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis-class-view-on-open~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 857
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

When non-nil, analysis buffers open in the class view
(reference sections folded).  Toggle per buffer with
`tibetan-analysis-toggle-class-view'.

** ~tibetan-vocab--corpus~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 50
:END:

/(no docstring)/

** ~tibetan-analysis--sanskrit-text-for-render~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 61
:END:

/(no docstring)/

** ~tibetan-analysis--target-lang~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 63
:DEFAULT: nil
:END:

Primary-visible translation language for the current analysis.
Two canonical values:
  `"de"' — prefer German half of bilingual `DE // EN' glosses,
            produce German Claude translation.
  `"en"' — prefer English (same as nil — backward compat).
nil   — unset, defaults to English-preferred behaviour throughout.

Dynamically bound by `tibetan-analysis-generate-content' based on
the source document's `#+TIBETAN_TARGET_LANG:' header, so every
downstream renderer (Interlinear Gloss, CAT Gloss, Claude prompt
builder) can consult a single value without receiving an explicit
argument.  Set per-buffer via `setq-local' to override the header
for ad-hoc analyses.

** ~tibetan-analysis-version~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 79
:DEFAULT: "1.0"
:END:

Version of the analysis file format.

** ~tibetan-analysis--claude-vocabulary-for-render~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 279
:DEFAULT: nil
:END:

Dynamic binding: parsed Claude-vocabulary alist for the in-flight render.
Parallel to `tibetan-analysis--claude-particles-for-render'.  Set by
callers that have just read the preserved `*** Claude Vocabulary'
body — `tibetan-analysis-reanalyze-file' does so before calling
`tibetan-analysis-generate-content'.  Consumed by the
enriched-vocab-pairs loop via
`tibetan-analysis--apply-claude-vocab-override' to rescue
polysemous tokens whose dictionary stack only offers proper-noun
glosses.  Nil → override is a no-op (first-time generate before
Claude has responded).

** ~tibetan-analysis--faces-setup~  /variable (buffer-local)/
:PROPERTIES:
:KIND: defvar-local
:LINE: 391
:DEFAULT: nil
:END:

Non-nil if faces have already been set up for this buffer.

** ~tibetan-analysis--particle-map-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 394
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--interlinear-gloss-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 447
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--term-anchor-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 468
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--tibetan-script-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 500
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--shad-marker~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 512
:DEFAULT: "──────── ། ────────"
:END:

Marker line inserted at shad (།) boundaries in word-list sections.
MA Reading `segments' often span several shad-delimited units; the
marker shows where each unit ends so the reader can analyse shad-by-shad
while reading the whole section.

** ~tibetan-analysis--vocabulary-term-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 728
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--segment-span-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 745
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--structure-font-lock-keywords~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 760
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--class-view-active~  /variable (buffer-local)/
:PROPERTIES:
:KIND: defvar-local
:LINE: 864
:DEFAULT: nil
:END:

Non-nil while the buffer is in the folded class view.

** ~tibetan-analysis--known-top-sections~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 1349
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--tree-role-labels~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 2554
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--priority-section-order~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 3768
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis--claude-particles-for-render~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 3936
:DEFAULT: nil
:END:

Dynamic binding: parsed Claude-particles list for the in-flight render.
Set by callers that have just read (or are about to re-insert) the
`*** Claude Particles' body — e.g. `tibetan-analysis-reanalyze-file'
reads the preserved Claude sections BEFORE calling
`tibetan-analysis-generate-content', so it can thread the parsed
particles through this var.  The Grammar renderer consults it to
attach per-occurrence Portfolio snippets; nil → compact parser-only
output, identical to Pass 6b behaviour.

** ~tibetan-analysis--sanskrit-text-for-render~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 3946
:DEFAULT: nil
:END:

Dynamic binding: Sanskrit-text plist for the in-flight render.

Set by callers that have located the segment's `**** Sanskrit'
sibling (typically via
`tibetan-sanskrit-parallel-text-for-segment-id' from
`core/tibetan-sanskrit-parallel.el').  When non-nil, the value
is the plist returned by that walker:
  (:iast STRING :devanagari STRING-or-nil :script-source SYMBOL).

The renderer `tibetan-analysis--render-sanskrit-source' reads
this var (indirectly, through the caller passing the value) and
emits a `** Sanskrit Source' level-2 section.  When nil — the
default — no Sanskrit section is emitted; today's Tibetan-only
documents are unchanged.

Phase 2 of the sanskrit-parallel-workflow (2026-04-27).

** ~tibetan-analysis-tibetan-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 316
:END:

/(no docstring)/

** ~tibetan-analysis-roman-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 328
:END:

/(no docstring)/

** ~tibetan-analysis-verb-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 333
:END:

/(no docstring)/

** ~tibetan-analysis-structure-role-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 342
:END:

/(no docstring)/

** ~tibetan-analysis-case-particle-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 351
:END:

/(no docstring)/

** ~tibetan-analysis-converb-particle-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 364
:END:

/(no docstring)/

** ~tibetan-analysis-interlinear-gloss-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 375
:END:

/(no docstring)/

** ~tibetan-analysis-vocabulary-term-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 671
:END:

/(no docstring)/

** ~tibetan-analysis-segment-span-face~  /face/
:PROPERTIES:
:KIND: defface
:LINE: 734
:END:

/(no docstring)/


* persist/tibetan-analysis-sanskrit.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-analysis-sanskrit.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-analysis-sanskrit.el
:END:

** ~tibetan-analysis-sanskrit--build-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-analysis-sanskrit--build-prompts sanskrit-plist source-file &optional _analysis-file)
:END:

Build (SYSTEM . USER) Claude prompts for SANSKRIT-PLIST.

SANSKRIT-PLIST is the walker plist
`(:iast STR :devanagari STR-or-nil :script-source SYM)' returned
by `tibetan-sanskrit-parallel-text-for-segment-id'.

SOURCE-FILE supplies metadata (target-lang, genre-context
headers) — read via
`tibetan-analysis--read-source-metadata' when available.

ANALYSIS-FILE (currently unused; reserved for future ±2
neighbour-Sanskrit context).

Returns nil when SANSKRIT-PLIST is nil / lacks `:iast'.  System
block is constant per document; user block carries IAST +
optional Devanagari.

Pure function — no buffer / network I/O.

** ~tibetan-analysis-sanskrit--parse-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 245
:SIGNATURE: (tibetan-analysis-sanskrit--parse-claude-sections response)
:END:

Split a Sanskrit Claude RESPONSE on `## …' markdown headings.

Returns a plist with keys `:translation' / `:devanagari' /
`:sandhi' / `:word-list' / `:grammar'.  Missing sections are
nil so the writer can leave the old org body in place when
Claude omitted a section (e.g. `## Devanagari' is conditional).

When RESPONSE is nil / empty / has no `## ' headings, returns
the all-nil plist.

** ~tibetan-analysis-sanskrit--heading-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 290
:SIGNATURE: (tibetan-analysis-sanskrit--heading-key heading-token)
:END:

Map a parsed Sanskrit `## …' HEADING-TOKEN to its plist key.
Special-cases `Word List' → `:word-list' (downcased name has a
space, which can't be a symbol); other tokens go through
naive downcase + intern.

** ~tibetan-analysis-sanskrit--missing-required-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 328
:SIGNATURE: (tibetan-analysis-sanskrit--missing-required-sections parsed)
:END:

Return the subset of `--required-keys' that are missing or empty
in PARSED.  Empty list → response is complete enough to write
without retry.

** ~tibetan-analysis-sanskrit--merge-parsed~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 339
:SIGNATURE: (tibetan-analysis-sanskrit--merge-parsed original retry)
:END:

Combine ORIGINAL and RETRY parsed plists.  For each section key
in `--section-order', prefer ORIGINAL's non-empty value;  fall
back to RETRY's value when ORIGINAL's slot is nil/empty.  Result
is a fresh plist (does not mutate inputs).

Original wins on collisions because the retry is FILL-MISSING,
not OVERWRITE — a verbose retry response should never clobber a
perfectly-good Word List from the first try.

** ~tibetan-analysis-sanskrit--key-to-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 365
:SIGNATURE: (tibetan-analysis-sanskrit--key-to-heading key)
:END:

Map a parsed plist KEY back to its `## ' heading token (for use
in retry prompts).  Inverse of `--heading-key'.

** ~tibetan-analysis-sanskrit--build-retry-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 376
:SIGNATURE: (tibetan-analysis-sanskrit--build-retry-prompts sanskrit-plist source-file missing-keys)
:END:

Build (SYSTEM . USER) prompts for a focused Sanskrit retry.

SYSTEM is byte-identical to `--build-prompts' so the Anthropic
prompt cache stays warm — only the user-block delta is billed at
full rate on the retry.

USER re-includes the original IAST (and Devanagari when present)
and explicitly names the sections the previous response did not
emit, instructing Claude to produce ONLY those.

Returns nil when SANSKRIT-PLIST lacks IAST or MISSING-KEYS is
empty.

** ~tibetan-analysis-sanskrit--ensure-parent~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 451
:SIGNATURE: (tibetan-analysis-sanskrit--ensure-parent buffer)
:END:

Ensure `* Sanskrit Analysis' top-level heading exists in BUFFER.
Inserts at end of buffer when absent.  Idempotent.

** ~tibetan-analysis-sanskrit--migrate-legacy-translation-heading~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 464
:SIGNATURE: (tibetan-analysis-sanskrit--migrate-legacy-translation-heading buffer)
:END:

Rename any legacy `** Claude Translation' inside `* Sanskrit
Analysis' → `** Translation' (Phase 1.4 of layout-revision §5.18,
2026-05-04).  Idempotent — no-op when the file already uses the
new heading name.

This runs at the start of `--insert-sections' /
`--restore-sections' so the existence checks downstream can search
for the new name and find it on legacy files.

** ~tibetan-analysis-sanskrit--ensure-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 484
:SIGNATURE: (tibetan-analysis-sanskrit--ensure-section buffer heading)
:END:

Ensure HEADING (a level-2 string) exists inside `* Sanskrit Analysis'
in BUFFER.  Appends at the end of the parent section if absent.
Idempotent.

** ~tibetan-analysis-sanskrit--replace-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 512
:SIGNATURE: (tibetan-analysis-sanskrit--replace-section-body buffer heading body)
:END:

Replace the body under HEADING (a level-2 string) inside `* Sanskrit
Analysis' in BUFFER with BODY.  Leaves the heading in place;
trims body + adds one trailing blank line.

** ~tibetan-analysis-sanskrit--write-from-plist~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 530
:SIGNATURE: (tibetan-analysis-sanskrit--write-from-plist sections analysis-file)
:END:

Write SECTIONS (a parsed plist) into ANALYSIS-FILE under `*
Sanskrit Analysis' (top-level).  Each non-nil entry in
`--section-order' is written under its heading; nil entries are
skipped (their existing org body stays).

Returns t on successful write, nil when ANALYSIS-FILE is missing
or SECTIONS has no populated slots.

Extracted from `--insert-sections' (2026-05-04, Issue 1) so the
post-fire retry path can write a MERGED plist (original + retry)
without round-tripping through a synthesised response string.

** ~tibetan-analysis-sanskrit--insert-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 561
:SIGNATURE: (tibetan-analysis-sanskrit--insert-sections response analysis-file)
:END:

Parse a Sanskrit Claude RESPONSE and write its sections into
ANALYSIS-FILE under `* Sanskrit Analysis' (top-level).  Each
section named in `tibetan-analysis-sanskrit--section-order'
that the parser populated is written under its heading;
missing sections are skipped (their existing body stays).

Thin wrapper around `--write-from-plist' (2026-05-04 refactor):
parse → plist → write.  Existing call sites unchanged.

Returns t on successful write, nil when ANALYSIS-FILE is
missing or RESPONSE is empty.

** ~tibetan-analysis-sanskrit--read-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 582
:SIGNATURE: (tibetan-analysis-sanskrit--read-section-body filepath heading)
:END:

Return the body under `** HEADING' inside `* Sanskrit Analysis'
in FILEPATH, trimmed.  Nil when the heading is absent or body
is empty.

** ~tibetan-analysis-sanskrit--read-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 611
:SIGNATURE: (tibetan-analysis-sanskrit--read-sections filepath)
:END:

Return preserved Sanskrit-Claude content in FILEPATH as a plist
with keys `:translation' / `:devanagari' / `:sandhi' /
`:word-list' / `:grammar'.  Each value is a non-empty string or
nil.

Phase 1.4 of layout-revision §5.18 (2026-05-04):  the
`:translation' lookup tries the new heading `Translation' first
and falls back to the legacy `Claude Translation' so files
written with the old heading still resolve cleanly.

** ~tibetan-analysis-sanskrit--restore-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 639
:SIGNATURE: (tibetan-analysis-sanskrit--restore-sections filepath sections)
:END:

Write SECTIONS (a plist) back into FILEPATH's `* Sanskrit Analysis'
section.  Any nil slot leaves the corresponding body
untouched.  Creates the parent `* Sanskrit Analysis' and
section headings if absent.

** ~tibetan-analysis-sanskrit--fire-retry-for-missing~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 666
:SIGNATURE: (tibetan-analysis-sanskrit--fire-retry-for-missing sanskrit-plist source-file analysis-file partial missing-keys label callback done)
:END:

Fire one focused Sanskrit Claude retry asking only for MISSING-KEYS.

PARTIAL is the original parsed plist (which slots were populated
by the first response).  On retry response, MERGE into PARTIAL
(original wins on collisions) and write the merged plist via
`--write-from-plist'.  CALLBACK is the user-supplied callback
from `--request-translation' (chains the Combined call).  DONE
is the queue thunk's done-callback.

When the retry prompt cannot be built, or the retry call itself
returns an empty response, fall back to writing PARTIAL alone —
the user gets whatever sections did make it on the first pass,
not nothing.  Done is always called exactly once per invocation
of this helper.

Caller stays inside the queue's in-flight thunk for the duration
of the retry — concurrency cap is correctly respected and the
rate-limit budget is shared with the Tibetan side.

** ~tibetan-analysis-sanskrit--request-translation~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 743
:SIGNATURE: (tibetan-analysis-sanskrit--request-translation sanskrit-plist source-file analysis-file &optional callback)
:AUTOLOAD: t
:END:

Fire an async Sanskrit Claude call for SANSKRIT-PLIST.

On response, parses sections + writes them to ANALYSIS-FILE
under `* Sanskrit Analysis'.  CALLBACK (if non-nil) is called
with one arg — the parsed plist — after the write.  Phase 5's
Combined dispatcher uses CALLBACK to chain.

Goes through `tibetan-claude-queue' so the request shares the
rate-limit budget + retry-on-429 logic with Tibetan calls
(concurrency optimization, 2026-04-30:  before this commit
the Sanskrit call bypassed the queue entirely, which meant a
batch of N segments fired N simultaneous Sanskrit requests
with no throttle and no 429 retry).  Per-segment
concurrency is preserved: with default concurrency cap of 3,
one segment's Tibetan + Sanskrit calls run in parallel, and
the chained Combined call follows once both finish.

No-op when:
  - SANSKRIT-PLIST is nil / lacks IAST.
  - `gptel' or `tibetan-claude-queue' is missing (soft-required).
  - Build prompts returns nil.

** ~gptel-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 58
:END:

/(no docstring)/

** ~tibetan-analysis-sanskrit--system-prompt-base~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 73
:DEFAULT: "You are a specialist in Classical Sanskrit philology, acting \
as a teaching assistant for a graduate Yogācārabhūmi rea
:END:

Constant base for the Sanskrit Claude system prompt.  Per-
document additions (target-lang directive, source genre context)
are appended at build time; the resulting full system prompt is
constant per document and so participates in Anthropic prompt
caching.

** ~tibetan-analysis-sanskrit--required-keys~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 321
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-analysis-sanskrit--section-order~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 433
:DEFAULT: '
:END:

/(no docstring)/


* persist/tibetan-auto-analysis.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-auto-analysis.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-auto-analysis.el
:END:

** ~tibetan-auto--report-progress~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 58
:SIGNATURE: (tibetan-auto--report-progress current total message)
:END:

Report progress: CURRENT of TOTAL, with MESSAGE.

** ~tibetan-auto--with-progress~  /macro/
:PROPERTIES:
:KIND: defmacro
:LINE: 65
:SIGNATURE: (tibetan-auto--with-progress callback _total &rest body)
:END:

Execute BODY with progress CALLBACK for TOTAL items.

** ~tibetan-auto--count-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 75
:SIGNATURE: (tibetan-auto--count-segments)
:END:

Count number of segments in current buffer.

** ~tibetan-auto--count-sentences~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 84
:SIGNATURE: (tibetan-auto--count-sentences)
:END:

Count number of sentences in current buffer.

** ~tibetan-auto--seg-id->number~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 97
:SIGNATURE: (tibetan-auto--seg-id->number seg-id)
:END:

Convert a segment id (string like "4" or "seg-4") to an integer.
Returns nil if no digits are found.

** ~tibetan-auto--collect-inline-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 107
:SIGNATURE: (tibetan-auto--collect-inline-segments)
:END:

Collect segments marked with inline 〔seg:N〕...〔/seg〕 markers.
Returns alist of (SEGMENT-NUMBER . TIBETAN-TEXT) sorted by number.

** ~tibetan-auto--collect-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 119
:SIGNATURE: (tibetan-auto--collect-segments)
:END:

Collect all segments from current buffer.

Supports both hierarchical org segments at any heading level (`***'
in the fresh-prep layout or `****' after `tibetan-add-sentence-structure'
demoted them under `*** Sentence N' wrappers) and inline `〔seg:N〕…〔/seg〕'
markers produced by `tibetan-doc-format'.  The hierarchical form wins
if present; otherwise the inline form is used.

The heading regex is `^\*+ Segment ' so both `*** Segment 1' and
`**** Segment 1' are picked up.  Sentence wrappers (`*** Sentence N')
are NOT matched because the literal word differs.

Returns alist of (SEGMENT-NUMBER . TIBETAN-TEXT) for the
hierarchical form, or — when at least one segment carries a
`:FOLIO:' drawer (Yogācārabhūmi-style sources) — a list of
(SEGMENT-NUMBER TIBETAN-TEXT FOLIO) triples so the create-file
caller can preserve the folio reference (P6, 2026-05-05).  The
inline-marker fallback always returns the cons-pair shape.

Mixed shape (pair vs triple) is fine for downstream:  callers
that want folio peek at the third element via `nth' / `caddr',
those that don't are unaware of it.

** ~tibetan-auto--collect-sentences~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 171
:SIGNATURE: (tibetan-auto--collect-sentences)
:END:

Collect all sentences from current buffer.

Heading regex `^\*+ Sentence ' to match both the legacy `** Sentence N'
layout produced by `tibetan-prepare-document' and the current
`*** Sentence N' layout produced by `tibetan-add-sentence-structure'.

Returns alist of (SENTENCE-NUMBER . COMBINED-TEXT).

** ~tibetan-auto--should-skip-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 200
:SIGNATURE: (tibetan-auto--should-skip-p filepath)
:END:

Return t if we should skip generating FILEPATH.

** ~tibetan-auto--sentence-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 209
:SIGNATURE: (tibetan-auto--sentence-filename sent-num source-file)
:END:

Generate filename for sentence SENT-NUM analysis.
SOURCE-FILE is used to generate a unique suffix.

** ~tibetan-auto--sentence-filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 217
:SIGNATURE: (tibetan-auto--sentence-filepath sent-num)
:END:

Get full filepath for sentence SENT-NUM analysis.

** ~tibetan-auto--generate-sentence-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 228
:SIGNATURE: (tibetan-auto--generate-sentence-analysis _sent-num tibetan-text)
:END:

Generate sentence analysis content for SENT-NUM with TIBETAN-TEXT.
Returns the analysis as an org-mode formatted string.

** ~tibetan-auto--create-sentence-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 265
:SIGNATURE: (tibetan-auto--create-sentence-file sent-num tibetan-text source-file)
:END:

Create sentence analysis file for SENT-NUM.
TIBETAN-TEXT is the combined sentence text.
SOURCE-FILE is the path to the source document.

** ~tibetan-auto-analyze-document~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 339
:SIGNATURE: (tibetan-auto-analyze-document &optional force)
:INTERACTIVE: t
:END:

Automatically generate analysis files for all segments and sentences.

With prefix argument FORCE, regenerate all files even if they exist.
Otherwise, skips existing files based on `tibetan-auto-skip-existing'.

Creates:
- analysis/seg-NNN-*.org for each segment (word-by-word analysis)
- analysis/sent-NNN-*.org for each sentence (clause analysis)

After the structural pass, if `tibetan-auto-fire-claude-on-create'
is non-nil (default), queues Claude translation requests for every
newly-created `seg-*.org'.  Requests are staggered by
`tibetan-auto-claude-request-delay' (1.5s) and fire asynchronously.

Progress is shown in the echo area.

** ~tibetan-auto--fire-dm-on-new-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 473
:SIGNATURE: (tibetan-auto--fire-dm-on-new-files filepath-and-text-pairs)
:END:

Queue DharmaMitra translation requests for FILEPATH-AND-TEXT-PAIRS.

PAIRS is a list of (filepath . tibetan-text) cons cells — same
shape as `tibetan-auto--fire-claude-on-new-files'.  Each pair's
filepath is mapped to its source-file (via
`tibetan-analysis--source-file-from-analysis') and seg-id (via
`tibetan-analysis--seg-id-from-filename') so the umbrella
`tibetan-dharmamitra-translation-fire-for-segment' can fire
both Tibetan AND Sanskrit (parallel-mode) variants.

Each request fires via `run-at-time' with an offset of
`tibetan-auto-dm-request-delay' × index.  Individual failures
are swallowed to `message' and do not abort the batch.

Phase A.3 of multi-translator-parallel-reading (2026-04-30).

** ~tibetan-auto--fire-claude-on-new-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 525
:SIGNATURE: (tibetan-auto--fire-claude-on-new-files filepath-and-text-pairs)
:END:

Queue Claude translation requests for FILEPATH-AND-TEXT-PAIRS.

PAIRS is a list of (filepath . tibetan-text) cons cells.  Each request
fires via `run-at-time' with an offset of
`tibetan-auto-claude-request-delay' × index, so 97 new segments take
97 × 1.5 ≈ 2.5 min of queue time.  Individual request failures are
swallowed to `message' and do not abort the batch.

** ~tibetan-auto--extract-tibetan-text-from-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 602
:SIGNATURE: (tibetan-auto--extract-tibetan-text-from-analysis filepath)
:END:

Read the Tibetan segment text out of the `* Tibetan Text' heading
of analysis file FILEPATH.  Returns a trimmed string, or nil.

** ~tibetan-auto--claude-needs-request-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 617
:SIGNATURE: (tibetan-auto--claude-needs-request-p filepath)
:END:

Non-nil if FILEPATH's Claude translation block is still a placeholder.

Matches the Claude Translation heading at any supported depth and form:
​  * `^\*+ Translation$'         (current — the §5.18 layout, 2026-05-04,
                                  dropped the `Claude' qualifier on the
                                  Tibetan-side translation heading)
​  * `^\*+ Claude Translation$'  (pre-§5.18 structured layout)
​  * `^\*+ Claude$'              (legacy — singular Claude block)

A translation is considered missing when the body under the first such
heading is blank, or begins with one of the standard placeholders:
`[Requesting translation...]', `[Awaiting Claude…]' (§5.21 scaffold),
`[Claude unavailable...]', `[Not available...]', or the `[Claude
request failed...]' fail-stub.

** ~tibetan-auto--analysis-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 652
:SIGNATURE: (tibetan-auto--analysis-files folder)
:END:

Return the list of per-segment analysis files under FOLDER.
Matches `seg-NNN*.org' — sentence files are deliberately excluded so
we focus the batch on the segments used in class.

** ~tibetan-auto-request-claude-translations~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 661
:SIGNATURE: (tibetan-auto-request-claude-translations &optional force)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Request Claude translations for every segment analysis that is missing one.

Walks the analysis folder for the current source file, finds every
`seg-NNN*.org' whose *** Claude section is still a placeholder (or an
`unavailable' error marker), and queues an asynchronous gptel request
for each.  Requests are spaced by
`tibetan-auto-claude-request-delay' seconds so we do not trip
rate-limit guards.

With a prefix argument (FORCE non-nil), re-request translations for
every segment file regardless of whether one is already present.  Use
this after you've changed the system prompt or switched model.

** ~tibetan-auto-skip-existing~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 34
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

If non-nil, skip analysis files that already exist.
Set to nil to regenerate all files.

** ~tibetan-auto-generate-sentences~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 40
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

If non-nil, also generate sentence-level clause analysis files.

** ~tibetan-auto-show-progress~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 45
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

If non-nil, show progress messages during batch analysis.

** ~tibetan-auto-fire-claude-on-create~  /customize variable/  ↗
:PROPERTIES:
:KIND: defcustom
:LINE: 298
:AUTOLOAD: t
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

Whether initial batch creation should fire Claude translation requests.

When non-nil (default), `tibetan-auto-analyze-document' and
`tibetan-sentence-create-all' queue staggered gptel requests for
every newly-created `seg-*.org' (resp. `sent-*.org') file immediately
after the structural analysis is written — so a single
`C-c u B' / `C-c s N' produces both the structural skeleton AND
Claude translations, matching what the single-file `C-c u A' /
`C-c s A' commands have always done.

Set to nil to skip the Claude step and rely on a subsequent
`tibetan-auto-request-claude-translations' run.  Pre-existing files
(skipped by the structural pass) are NOT re-fired regardless of this
setting — use `C-u C-c u r' / `C-u C-c s r' for that.

The fire is async and throttled by `tibetan-auto-claude-request-delay'
(default 1.5s per request); results arrive in the background.

** ~tibetan-auto-fire-dm-on-create~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 319
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

When non-nil, `tibetan-auto-analyze-document' fires DharmaMitra
translation for each newly-created segment file after Claude.

Phase A.3 of multi-translator-parallel-reading (2026-04-30).
Mirrors `tibetan-auto-fire-claude-on-create' for the DM
translator.  Set to nil to opt out of automatic DM translation
without disabling Claude.

** ~tibetan-auto-dm-request-delay~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 330
:TYPE: 'number
:GROUP: tibetan-cat
:DEFAULT: 0.5
:END:

Seconds between successive DharmaMitra translation requests
during batch fire.  DM's chat-translate is synchronous (~1-2 sec
per call), so 97 new segments take roughly 97 × (1.5 + 0.5) =
~3 min of queue time.  The 0.5-sec gap reduces the chance of
DM rate-limiting compared to back-to-back firing.

** ~tibetan-auto-claude-request-delay~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 593
:TYPE: 'number
:GROUP: tibetan-cat
:DEFAULT: 1.5
:END:

Seconds to wait between successive Claude translation requests.
Batched gptel requests are fire-and-forget asynchronous, but throwing
hundreds at once causes rate-limit or timeout errors.  The staggered
fire loop in `tibetan-auto-request-claude-translations' spaces them
out by this many seconds.

** ~tibetan-auto--progress-callback~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 54
:DEFAULT: nil
:END:

Callback function for progress reporting.
Called with (CURRENT TOTAL MESSAGE).


* persist/tibetan-claude-queue.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-claude-queue.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-claude-queue.el
:END:

** ~tibetan-claude-queue-status~  /function/  ↗
:PROPERTIES:
:KIND: defun
:LINE: 141
:SIGNATURE: (tibetan-claude-queue-status)
:AUTOLOAD: t
:END:

Return a plist describing current Claude queue state.
Keys: :pending :in-flight :total :succeeded :failed :retried.

** ~tibetan-claude-queue-show-status~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 152
:SIGNATURE: (tibetan-claude-queue-show-status)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Echo current Claude queue stats in the minibuffer.

** ~tibetan-claude-queue-cancel-pending~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 163
:SIGNATURE: (tibetan-claude-queue-cancel-pending)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Drop all not-yet-started jobs from the Claude queue.
In-flight requests are unaffected and will still run to completion
(or to retry exhaustion).

** ~tibetan-claude-queue-reset-stats~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 174
:SIGNATURE: (tibetan-claude-queue-reset-stats)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Reset the Claude queue's lifetime counters.
Does not touch pending or in-flight jobs.

** ~tibetan-claude-queue-submit~  /function (cl)/  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 190
:SIGNATURE: (tibetan-claude-queue-submit thunk &key on-done on-fail label)
:AUTOLOAD: t
:END:

Submit THUNK to the throttled Claude request queue.

THUNK must be a function of one argument DONE-FN.  THUNK starts the
asynchronous work (typically a `gptel-request' call) and, when the
result is known, calls (funcall DONE-FN STATUS) with one of:

  \='(:status ok)                — success; ON-DONE is invoked
  \='(:status rate-limited)      — HTTP 429; queue retries with
                                    exponential backoff up to
                                    `tibetan-claude-queue-max-retries'
  \='(:status error :error STR)  — non-retryable failure; ON-FAIL
                                    is invoked

Optional keyword args:
  :on-done FN  — called with the success status plist on success
  :on-fail FN  — called with the failure status plist when retries
                 are exhausted, or on a non-retryable error
  :label   STR — a short string used in diagnostic messages
                 (typically the analysis filename)

Synchronous errors raised inside THUNK are caught and reported as
\='(:status error :error STR).  Errors raised inside ON-DONE / ON-FAIL
are caught and logged so they do not break queue progress.

** ~tibetan-claude-queue--maybe-dispatch~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 226
:SIGNATURE: (tibetan-claude-queue--maybe-dispatch)
:END:

Dispatch jobs from the pending queue up to the concurrency cap.

** ~tibetan-claude-queue--start~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 234
:SIGNATURE: (tibetan-claude-queue--start job)
:END:

Actually run JOB.  Internal.

** ~tibetan-claude-queue--handle-finish~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 253
:SIGNATURE: (tibetan-claude-queue--handle-finish job status)
:END:

Called when JOB's thunk reports completion via STATUS.
Runs the appropriate user callback and dispatches the next pending
job.  Kept robust against repeated calls (idempotent on counters).

** ~tibetan-claude-queue--call-fail~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 319
:SIGNATURE: (tibetan-claude-queue--call-fail job status)
:END:

Invoke JOB's :on-fail callback (if any) with STATUS, swallowing errors.

** ~tibetan-claude-queue-concurrency~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 77
:TYPE: 'integer
:GROUP: tibetan-claude-queue
:DEFAULT: 3
:END:

Maximum number of Claude API requests in flight at once.
Lower this if you still see HTTP 429 (rate-limited) errors during
large batches; raise it if your Anthropic-API tier can sustain more
parallel requests and you want batches to finish faster.

** ~tibetan-claude-queue-max-retries~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 85
:TYPE: 'integer
:GROUP: tibetan-claude-queue
:DEFAULT: 4
:END:

Maximum number of retries for a rate-limited (HTTP 429) request.
After this many retries the job is considered failed and its
:on-fail callback runs.

** ~tibetan-claude-queue-base-delay~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 92
:TYPE: 'number
:GROUP: tibetan-claude-queue
:DEFAULT: 2.0
:END:

Base delay (seconds) for exponential backoff on HTTP 429.
The actual delay before retry N is roughly
  base * 2^N + ±25% jitter
capped at `tibetan-claude-queue-max-delay'.

** ~tibetan-claude-queue-max-delay~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 100
:TYPE: 'number
:GROUP: tibetan-claude-queue
:DEFAULT: 60.0
:END:

Upper bound on the per-retry backoff delay, in seconds.

** ~tibetan-claude-queue-verbose~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 105
:TYPE: 'boolean
:GROUP: tibetan-claude-queue
:DEFAULT: t
:END:

When non-nil, echo per-job lifecycle messages to *Messages*.
Useful while debugging or while watching a long batch run; set to
nil for quieter operation.

** ~tibetan-claude-queue--pending~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 116
:DEFAULT: nil
:END:

FIFO list of pending jobs, oldest first.
Each job is a plist (:thunk FN :attempt N
:on-done FN :on-fail FN :label STR).

** ~tibetan-claude-queue--in-flight~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 121
:DEFAULT: 0
:END:

Count of currently in-flight Claude requests.

** ~tibetan-claude-queue--total~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 124
:DEFAULT: 0
:END:

Total jobs ever submitted in this Emacs session.

** ~tibetan-claude-queue--succeeded~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 127
:DEFAULT: 0
:END:

Count of jobs that completed successfully.

** ~tibetan-claude-queue--failed~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 130
:DEFAULT: 0
:END:

Count of jobs that ultimately failed.

** ~tibetan-claude-queue--retried~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 133
:DEFAULT: 0
:END:

Total count of retries scheduled (across all jobs).

** ~tibetan-claude-queue~  /customize group/
:PROPERTIES:
:KIND: defgroup
:LINE: 72
:END:

Throttling and retry policy for Claude API requests.


* persist/tibetan-compound-analysis.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-compound-analysis.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-compound-analysis.el
:END:

** ~tibetan-compound-get-context~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 57
:SIGNATURE: (tibetan-compound-get-context)
:END:

Get the context configuration for current buffer.
Reads from #+TIBETAN_CONTEXT header or defaults to classical-prose.

** ~tibetan-compound-detect-verse-number~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 72
:SIGNATURE: (tibetan-compound-detect-verse-number)
:END:

Detect verse boundary by Tibetan numeral pattern.
Returns (start-line . end-line) or nil.
Looks for patterns like ། ༡༢ །

** ~tibetan-compound-detect-double-shad~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 110
:SIGNATURE: (tibetan-compound-detect-double-shad)
:END:

Detect compound boundary by double shad (།།).
Returns (start-line . end-line) or nil.

** ~tibetan-compound-detect-syllable-meter~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 133
:SIGNATURE: (tibetan-compound-detect-syllable-meter)
:END:

Detect verse by finding 4 consecutive 7-syllable lines.
Returns (start-line . end-line) or nil.

** ~tibetan-compound-detect-explicit-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 160
:SIGNATURE: (tibetan-compound-detect-explicit-markers)
:END:

Detect compound by explicit markers.
Supports multiple marker formats:
  - 〔verse:NNN〕 (legacy numbered)
  - 〔verse :num N〕...〔/verse〕 (new property-based)
  - 〔sent〕...〔/sent〕 (sentence blocks)
  - 〔prose :comment-on N〕...〔/prose〕 (commentary blocks)
Returns (start-line . end-line) or nil.

** ~tibetan-compound--detect-block-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 192
:SIGNATURE: (tibetan-compound--detect-block-markers marker-type)
:END:

Detect block bounded by 〔MARKER-TYPE...〕 and 〔/MARKER-TYPE〕.
MARKER-TYPE is one of: sent, verse, prose.
Returns (start-line . end-line) or nil.

** ~tibetan-compound-detect-region~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 211
:SIGNATURE: (tibetan-compound-detect-region)
:END:

Use active region as compound boundary.
Returns (start-line . end-line) or nil if no region.

** ~tibetan-compound-detect-boundaries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 219
:SIGNATURE: (tibetan-compound-detect-boundaries)
:END:

Detect compound boundaries using multiple strategies.
Tries in order: region, explicit markers, verse numbers, double shad, meter.
Returns (start-line . end-line) or nil.

** ~tibetan-compound--extract-tibetan-from-line~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 233
:SIGNATURE: (tibetan-compound--extract-tibetan-from-line)
:END:

Extract Tibetan text from current line.
Handles both plain lines and 〔seg:...〕 markers.

** ~tibetan-compound-extract-lines~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 257
:SIGNATURE: (tibetan-compound-extract-lines start-line end-line)
:END:

Extract Tibetan lines between START-LINE and END-LINE.
Returns list of (line-number . tibetan-text).

** ~tibetan-compound-get-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 276
:SIGNATURE: (tibetan-compound-get-folder)
:END:

Get the compound analysis folder path for current buffer.
Creates the folder if it doesn't exist.

** ~tibetan-compound-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 286
:SIGNATURE: (tibetan-compound-filename compound-id)
:END:

Generate filename for compound COMPOUND-ID.
Returns filename like ='compound-001.org='.

** ~tibetan-compound-get-next-id~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 291
:SIGNATURE: (tibetan-compound-get-next-id)
:END:

Get the next available compound ID.
Scans existing compound-NNN.org files in analysis folder.

** ~tibetan-compound-compute-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 304
:SIGNATURE: (tibetan-compound-compute-hash lines)
:END:

Compute MD5 hash for LINES (list of tibetan texts).
Used for change detection.

** ~tibetan-compound-analyze-connectors~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 313
:SIGNATURE: (tibetan-compound-analyze-connectors lines)
:END:

Analyze how LINES connect grammatically.
Returns list of connector analyses.

** ~tibetan-compound-analyze-topic-flow~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 368
:SIGNATURE: (tibetan-compound-analyze-topic-flow lines)
:END:

Analyze topic/subject flow across LINES.
Identifies shared topics and referent tracking.

** ~tibetan-compound-build-line-translation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 399
:SIGNATURE: (tibetan-compound-build-line-translation gloss verbs connectors line-idx total-lines)
:END:

Build a draft English translation for a single line.
GLOSS is alist of (tibetan . english) pairs.
VERBS is list of verb analyses.
CONNECTORS contains inter-line grammar.
LINE-IDX is current line number (1-based).
TOTAL-LINES is total number of lines.

** ~tibetan-compound-generate-translations-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 480
:SIGNATURE: (tibetan-compound-generate-translations-section lines connectors context)
:END:

Generate translation section for LINES.
CONNECTORS provides inter-line grammar analysis.
CONTEXT provides tradition-specific terminology.
Returns org-mode formatted string with actual draft translations.

** ~tibetan-compound-generate-content~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 667
:SIGNATURE: (tibetan-compound-generate-content lines context)
:END:

Generate compound analysis content for LINES with CONTEXT.
Returns org-mode formatted string.

** ~tibetan-compound-create-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 790
:SIGNATURE: (tibetan-compound-create-file compound-id lines source-file auto-content)
:END:

Create a new compound analysis file.
COMPOUND-ID is the numeric ID.
LINES is list of (line-num . tibetan-text).
SOURCE-FILE is path to source.
AUTO-CONTENT is generated analysis.
Returns the filepath.

** ~tibetan-compound-check-sync~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 845
:SIGNATURE: (tibetan-compound-check-sync filepath lines)
:END:

Check if compound analysis at FILEPATH is in sync with LINES.
Returns t if hashes match, nil otherwise.

** ~tibetan-compound-regenerate-auto~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 857
:SIGNATURE: (tibetan-compound-regenerate-auto filepath lines context)
:END:

Regenerate auto-analysis in FILEPATH for LINES with CONTEXT.
Preserves user sections.

** ~tibetan-compound-find-existing~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 918
:SIGNATURE: (tibetan-compound-find-existing lines)
:END:

Find existing compound analysis file for LINES.
Returns filepath if found, nil otherwise.

** ~tibetan-open-compound-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 938
:SIGNATURE: (tibetan-open-compound-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open or create persistent compound analysis.
Detects compound boundaries automatically or uses region.
Opens analysis in side window.

** ~tibetan-reanalyze-compound~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 981
:SIGNATURE: (tibetan-reanalyze-compound)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-analyze current compound, preserving user notes.
Must be called from within a compound analysis buffer.

** ~tibetan-compound-analyze-document-structure~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1029
:SIGNATURE: (tibetan-compound-analyze-document-structure)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Analyze the structure of the current document.
Returns a list of analysis results or nil if no structure found.

** ~tibetan-compound-analyze-verse-structure~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1041
:SIGNATURE: (tibetan-compound-analyze-verse-structure lines)
:END:

Analyze verse structure from LINES.
LINES should be a list of (line-num . text) cons cells.
Returns a list of verse analysis or nil.

** ~tibetan-compound-analysis-version~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 28
:DEFAULT: "1.0"
:END:

Version of the compound analysis file format.

** ~tibetan-compound-contexts~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 35
:DEFAULT: '
:END:

/(no docstring)/


* persist/tibetan-sentence-claude.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-sentence-claude.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-sentence-claude.el
:END:

** ~tibetan-sentence-claude--split-subsections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 42
:SIGNATURE: (tibetan-sentence-claude--split-subsections body)
:END:

Split BODY at `### Segment N' headers.
Returns (:preamble STR-or-nil :segments ((N . STR) ...)): the
preamble is the trimmed text before the first subsection (nil when
empty), each subsection body is trimmed.  nil-safe.

** ~tibetan-sentence-claude--strip-span-markers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 79
:SIGNATURE: (tibetan-sentence-claude--strip-span-markers s)
:END:

Remove every span-marker token from S.  nil-safe.
A raw `⟦' must NEVER land in a file — every consumer that does not
convert markers strips them unconditionally.

** ~tibetan-sentence-claude--render-marked-whole~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 86
:SIGNATURE: (tibetan-sentence-claude--render-marked-whole whole n)
:END:

Render WHOLE (the marked whole-sentence translation) for child N.
When WHOLE contains exactly one `⟦N⟧' before exactly one `⟦/N⟧', the
own span becomes `⟪…⟫' (split into one pair PER LINE — the font-lock
pattern is single-line) and all other marker tokens are stripped.
Any irregularity (missing, duplicated, inverted markers) degrades to
the fully stripped plain translation.  nil-safe.

** ~tibetan-sentence-claude--parse-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-sentence-claude--parse-response response seg-nums)
:END:

Parse a sentence-first RESPONSE for the expected SEG-NUMS.
Returns a plist:
  :translation-whole  the `## Translation' preamble (whole sentence)
  :grammar-preamble   the `## Grammar' cross-clause preamble
  :concepts           the `## Concept Notes' body, WHOLE (never split
                      — sentence-level by design, even if Claude
                      sneaks a subsection in)
  :per-segment        ((SEG-NUM . (:translation S :vocabulary S
                                   :grammar S :particles S)) ...)
                      only for SEG-NUMS; stray numbers are ignored
  :missing            SEG-NUMS with no subsection in ANY section —
                      the caller writes a VISIBLE stub for these,
                      never a silent blank.

** ~tibetan-sentence-claude--synthesize-segment-markdown~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 178
:SIGNATURE: (tibetan-sentence-claude--synthesize-segment-markdown plist)
:END:

Build a standard five-section markdown response from PLIST.
Slots (:translation :vocabulary :grammar :particles :concepts) that
are nil produce NO heading — the section writer then leaves the
existing body untouched (preserve semantics).

** ~tibetan-sentence-claude--synthesize-sentence-markdown~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 197
:SIGNATURE: (tibetan-sentence-claude--synthesize-sentence-markdown parsed)
:END:

Build the SENTENCE file's markdown from the PARSED response plist.
Translation = the whole-sentence rendering, Grammar = the cross-clause
preamble, Concept Notes = the sentence-level body.

** ~tibetan-sentence-claude--collect-children-vocab~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 259
:SIGNATURE: (tibetan-sentence-claude--collect-children-vocab seg-nums folder)
:END:

Per-child Interlinear grounding: `=== Segment N ===' labelled
`wylie = gloss' blocks from each child seg file.  nil when nothing.

** ~tibetan-sentence-claude--build-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 279
:SIGNATURE: (tibetan-sentence-claude--build-prompts sentence source-file
                                               &optional folder)
:END:

Build (SYSTEM . USER) for a sentence-first call.
SENTENCE is the walker plist from
`tibetan-sentence--sentence-for-segment' (:sent-num :seg-nums
:children :tibetan-text).  SOURCE-FILE supplies the per-document
static system blocks (metadata/Portfolio/target-lang — shared helper,
cache-constant).  FOLDER locates the child seg files for grounding.

** ~tibetan-sentence-claude--schedule-dm~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 379
:SIGNATURE: (tibetan-sentence-claude--schedule-dm sentence child-files
                                             sent-file force)
:END:

Schedule the sentence-level DM fire, staggered against the 10/min
rate limit.  Timers run during the batch drain loop's
`accept-process-output'.

** ~tibetan-sentence-claude-clear-inflight~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 409
:SIGNATURE: (tibetan-sentence-claude-clear-inflight)
:INTERACTIVE: t
:END:

Clear the sentence-level in-flight registry (crash recovery).

** ~tibetan-sentence-claude--claim~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 416
:SIGNATURE: (tibetan-sentence-claude--claim source-file sent-num label)
:END:

Claim (SOURCE-FILE . SENT-NUM); nil when already in flight.

** ~tibetan-sentence-claude--release~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 423
:SIGNATURE: (tibetan-sentence-claude--release key)
:END:

Release the in-flight claim KEY.

** ~tibetan-sentence-claude--segment-label~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 427
:SIGNATURE: (tibetan-sentence-claude--segment-label sent-num seg-nums)
:END:

The `(Sentence N — segments A–B)' label line.

** ~tibetan-sentence-claude--handle-response~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 432
:SIGNATURE: (tibetan-sentence-claude--handle-response response ctx)
:END:

Land a sentence-first RESPONSE into the files named by CTX.
CTX: (:sent-num N :seg-nums L :child-files L :sent-file F-or-nil
:force BOOL).  Per child: synthesize a standard five-section response
from its subsections and feed `tibetan-analysis--insert-claude-sections'
(landing-gated: non-FORCE skips children whose Translation/Vocabulary
is already populated — M7 discipline).  Concept Notes are copied to
every child under the sentence label.  Children missing from the
response get a VISIBLE stub via the failure-stub writer — never a
silent blank.  The sent file gets the whole-sentence pieces.

** ~tibetan-sentence-claude--request~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 502
:SIGNATURE: (tibetan-sentence-claude--request sentence child-files sent-file
                                         source-file folder force)
:END:

Queue the sentence-first Claude request.  Mirrors
`tibetan-analysis--request-claude-translation' (429 retry via the
queue; on exhaustion a visible failure stub lands in every child that
is still empty).

** ~tibetan-analysis--fire-sentence-level~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 580
:SIGNATURE: (tibetan-analysis--fire-sentence-level tibetan-text analysis-file
                                              source-file seg-id
                                              &optional force)
:END:

Fire ONE sentence-level Claude call for the sentence containing
SEG-ID, when applicable.  Returns nil when the sentence path does NOT
apply (flat layout, single-segment sentence, parallel-Sanskrit source,
missing child files, fire-gate says no) — the caller then runs the
existing per-segment fire UNCHANGED.  Returns `fired' or `dedup-hit'
when the sentence path owns this segment (caller must NOT also fire
per-segment Claude).

** ~tibetan-dharmamitra-sentence-request-delay~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 364
:TYPE: 'number
:GROUP: tibetan-cat
:DEFAULT: 6.5
:END:

Seconds between scheduled sentence-level DharmaMitra fires.
DharmaMitra rate-limits at ~10 requests/minute (§5.29 history); a
batch firing 57 sentences must stagger.  Each scheduled fire adds
this delay to a cumulative counter, reset by
`tibetan-sentence-claude-clear-inflight'.

** ~tibetan-sentence-claude--subsection-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 35
:DEFAULT: "^###[ \t]+Segment[ \t]+\\([0-9]+\\)[ \t]*$"
:END:

Regexp matching a `### Segment N' subsection header.
The standard section parser anchors on `^## ' exactly, so these
three-hash lines pass through into section bodies untouched —
the two-level parse composes cleanly.

** ~tibetan-sentence-claude--span-token-re~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 75
:DEFAULT: "⟦/?[0-9]+⟧"
:END:

Any segment-span marker token (`⟦N⟧' or `⟦/N⟧') in the
whole-sentence translation.

** ~tibetan-sentence-claude--system-addendum~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 224
:DEFAULT: "

SENTENCE-FIRST MODE — this request covers a COMPLETE SENTENCE that
spans several numbered segments (the shad-delimite
:END:

System-prompt addendum that turns the segment schema into the
sentence-first schema.  Appended to the base segment system prompt;
constant per document, so it forms a stable Anthropic cache prefix
(distinct from the segment-level prefix — the two coexist).

** ~gptel-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 362
:END:

/(no docstring)/

** ~tibetan-sentence-claude--dm-schedule-count~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 373
:DEFAULT: 0
:END:

Cumulative counter for staggering sentence-level DM fires.

** ~tibetan-sentence-claude--inflight~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 401
:DEFAULT: (make-hash-table :test #'equal)
:END:

In-flight sentence-level requests, keyed (TRUENAME . SENT-NUM).
A batch over 12 children of one sentence fires the call ONCE; the
later children see the claim and skip.  Entries are released in the
queue job's done path (ok AND fail).  Content gates — not this
registry — decide refiring on the next run, so a stale entry after a
crash only suppresses until `tibetan-sentence-claude-clear-inflight'.


* persist/tibetan-sentence-persist.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-sentence-persist.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-sentence-persist.el
:END:

** ~tibetan-sentence--get-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 95
:SIGNATURE: (tibetan-sentence--get-folder)
:END:

Return the analysis folder for the current source buffer.
Reuses `tibetan-analysis-get-folder' so segment and sentence files
land in the same `analysis/' directory.

** ~tibetan-sentence--filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (tibetan-sentence--filename sent-num)
:END:

Return basename `sent-NNN.org' for SENT-NUM (an integer).

** ~tibetan-sentence--filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 113
:SIGNATURE: (tibetan-sentence--filepath sent-num &optional folder)
:END:

Return absolute path of `sent-NNN.org' for SENT-NUM.
FOLDER defaults to `tibetan-sentence--get-folder'.

** ~tibetan-sentence--sent-id-from-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 120
:SIGNATURE: (tibetan-sentence--sent-id-from-filename filepath)
:END:

Extract numeric sentence id from FILEPATH's basename, or nil.

** ~tibetan-sentence--seg-id-from-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 126
:SIGNATURE: (tibetan-sentence--seg-id-from-filename filepath)
:END:

Extract numeric segment id from FILEPATH's basename, or nil.

** ~tibetan-sentence--current-sentence-bounds~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 136
:SIGNATURE: (tibetan-sentence--current-sentence-bounds)
:END:

Return the buffer position bounds of the current sentence as (BEG . END).
Works when point is on a `*** Sentence N' heading or anywhere inside
it (including under one of its `**** Segment M' children).  Returns
nil if no sentence is in scope.

** ~tibetan-sentence--current-sentence-num~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 166
:SIGNATURE: (tibetan-sentence--current-sentence-num)
:END:

Return the sentence number of the sentence containing point, or nil.

** ~tibetan-sentence--collect-children-in-range~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 177
:SIGNATURE: (tibetan-sentence--collect-children-in-range beg end)
:END:

Collect Segment children of the sentence between BEG and END.
Returns a list of plists `(:seg-num N :text STR :beg POS :end POS)'
in source order, or nil when no segments are found.  Segments are
matched by heading text (`Segment N'), not by level, so the function
works for any heading depth produced by `tibetan-add-sentence-structure'
or `tibetan-prepare-document'.

** ~tibetan-sentence--collect-current-sentence~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 214
:SIGNATURE: (tibetan-sentence--collect-current-sentence)
:END:

Collect data for the sentence at point.
Returns a plist
  (:sent-num N
   :children ((:seg-num M :text STR :beg POS :end POS) ...)
   :tibetan-text STR  ; concatenated child texts, blank-line separated
   :seg-nums (M1 M2 ...))
or nil when no sentence is in scope.

** ~tibetan-sentence--sentence-for-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 236
:SIGNATURE: (tibetan-sentence--sentence-for-segment seg-num source-file)
:END:

Find the Sentence containing Segment SEG-NUM in SOURCE-FILE.
Inverse of `tibetan-sentence--collect-from-source-buffer': walks UP
from the segment heading to its enclosing `Sentence' heading (Phase 5
of the verb-first redesign — the sentence is the analysis domain; the
segment file shows its slice).  Returns the same plist shape as
`tibetan-sentence--collect-current-sentence', or nil when the segment
is absent or the layout is flat (no Sentence level — pre-§2.12
sources fall back to segment-local analysis).  Takes SOURCE-FILE
explicitly — never depends on `buffer-file-name' (§5.34 lesson).

** ~tibetan-sentence--collect-from-source-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 257
:SIGNATURE: (tibetan-sentence--collect-from-source-buffer sent-num)
:END:

Re-scan the current buffer for sentence SENT-NUM and return its data.
Like `tibetan-sentence--collect-current-sentence' but driven by
SENT-NUM rather than point position.  Returns nil when SENT-NUM is
not present in the current buffer.

** ~tibetan-sentence--compute-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 273
:SIGNATURE: (tibetan-sentence--compute-hash concatenated-text)
:END:

Return MD5 of CONCATENATED-TEXT (utf-8) for change detection.
Reuses `tibetan-analysis-compute-hash' when the segment-persist
module is loaded so both hashing schemes stay identical.

** ~tibetan-sentence--get-stored-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 281
:SIGNATURE: (tibetan-sentence--get-stored-hash filepath)
:END:

Return the `#+TIBETAN_HASH:' header value stored in FILEPATH, or nil.

** ~tibetan-sentence--check-sync~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 290
:SIGNATURE: (tibetan-sentence--check-sync filepath concatenated-text)
:END:

Return non-nil if FILEPATH's stored hash matches CONCATENATED-TEXT.

** ~tibetan-sentence--find-section-bounds~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 300
:SIGNATURE: (tibetan-sentence--find-section-bounds buffer section-name)
:END:

Find positions of `* SECTION-NAME' in BUFFER.
Returns (START . END) or nil.

** ~tibetan-sentence--read-section-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 315
:SIGNATURE: (tibetan-sentence--read-section-body filepath section-name)
:END:

Return the trimmed body of `* SECTION-NAME' in FILEPATH, or nil.

** ~tibetan-sentence--read-third-level-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 333
:SIGNATURE: (tibetan-sentence--read-third-level-body filepath heading)
:END:

Return the trimmed body under `*** HEADING' in FILEPATH, or nil.
Skips placeholder text so we don't carry empty scaffolding around.

** ~tibetan-sentence--get-user-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 361
:SIGNATURE: (tibetan-sentence--get-user-sections filepath)
:END:

Read user-owned sections from FILEPATH.
Returns alist of (SECTION . CONTENT) for the trio that survives
re-analysis: My Notes, Working Translation, Footnotes.  Each CONTENT
covers the heading line through (just before) the next `* ' heading
or end-of-file, so it can be re-inserted verbatim if the section
gets clobbered by regeneration.

** ~tibetan-sentence--read-parallel-top-level-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 397
:SIGNATURE: (tibetan-sentence--read-parallel-top-level-sections filepath)
:END:

Return alist (HEADING-NAME . FULL-SECTION-TEXT) for the parallel-mode
top-level sections present in FILEPATH.

FULL-SECTION-TEXT covers the `* HEADING' line through just before the
next top-level heading (or end-of-file), so it can be re-appended
verbatim by the regenerate RESTORE phase.

** ~tibetan-sentence--append-top-level-section~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 418
:SIGNATURE: (tibetan-sentence--append-top-level-section buffer full-text)
:END:

Append FULL-TEXT (a `* Heading' … block) at the end of BUFFER.
Ensures a blank-line separator before the new section.  No-op when
FULL-TEXT is blank.

** ~tibetan-sentence--read-translation-bodies~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 430
:SIGNATURE: (tibetan-sentence--read-translation-bodies filepath)
:END:

Read user-owned third-level translation bodies from FILEPATH.
Returns plist `(:roehrich STR :class STR)' (each non-nil only when
the corresponding heading has real content, not placeholder text).

** ~tibetan-sentence--segment-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 494
:SIGNATURE: (tibetan-sentence--segment-claude-sections)
:END:

Return the list of level-2 headings to strip from segment-renderer
output before embedding it in a sentence file.

§5.27 Phase 5 (2026-05-26):  consults
`tibetan-sentence--detail-for-render':
  · `"detailed"'  → empty list (no stripping;  full §5.21
                       segment layout flows through).  Reader
                       gets all 11 L2 sections in the sentence
                       file — for thorough reading classes.
  · `"compressed"' / nil / anything else → `--strip-list'
                       (the §5.22 default 7-entry filter,
                       sentence file collapses to 4 L2 sections,
                       ~2 A4 pages).

Pre-§5.27:  always returned `--strip-list' unconditionally
(§5.22 final).  The dynamic-var gate restores per-document
control via the new `#+TIBETAN_SENTENCE_DETAIL:' header without
re-introducing the retired toggle command — the wizard sets the
header once, downstream renders honour it.

Consumed by `--strip-segment-claude-sections' below.

** ~tibetan-sentence--strip-segment-claude-sections~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 523
:SIGNATURE: (tibetan-sentence--strip-segment-claude-sections content)
:END:

Return CONTENT with segment-level Claude sections removed.
Uses the segment-module splitter so heading boundaries are
consistent with the rest of the tool.

§5.22 (2026-05-21):  the strip list is now sourced from the defun
`tibetan-sentence--segment-claude-sections', which consults the
dynamic var `tibetan-sentence--detail-for-render' (§5.27 Phase 5
renamed from `--compressed-for-render').  See that accessor's
docstring for the two-mode behaviour.

** ~tibetan-sentence--render-auto-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 547
:SIGNATURE: (tibetan-sentence--render-auto-analysis tibetan-text)
:END:

Return the body of the `* Auto-Analysis' block for TIBETAN-TEXT.

Runs the segment-level renderer
(`tibetan-analysis-generate-content') on the concatenated
sentence text and strips segment-level Claude scaffolding.  The
result is the same parser-driven analysis the per-segment files
carry (Wylie / Particle Map / Interlinear Gloss / Verb
Classification / Word-Particle List / Grammatical Markers /
Sentence Structure / Clause Structure / Detailed Dictionary),
but computed over the whole sentence so cross-segment compounds
and clause chains surface in one place.

Returns nil when the renderer is unavailable or the text has no
Tibetan content (so the caller can emit a placeholder instead of
a misleading empty section).  The Tibetan-content check reuses
`tibetan-analysis--filter-to-tibetan-lines' — a sentence whose
only text is Latin / `#' comments / blank lines gets no
auto-analysis block at all.

** ~tibetan-sentence--inject-sentence-l3-entries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 600
:SIGNATURE: (tibetan-sentence--inject-sentence-l3-entries buffer)
:END:

Inject sentence-only level-3 entries into the nested
`** Provided Translations' block inside `* Tibetan Analysis'.

For each `(HEADING . PLACEHOLDER)' in
`tibetan-sentence--scaffold-sentence-only-l3-entries' that is not
already present under the nested `** Provided Translations',
insert a `*** HEADING\nPLACEHOLDER\n\n' block.  Existing
populated entries are left untouched (idempotent).

When `** Provided Translations' itself is absent (segment renderer
didn't emit it, e.g. degraded test harness), this is a no-op.

Called from the scaffold (fresh files) and from `--regenerate'
(after relocation of preserved bodies, so already-populated
sentence-only entries don't get clobbered).

** ~tibetan-sentence--sentence-detail-from-source~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 646
:SIGNATURE: (tibetan-sentence--sentence-detail-from-source source-file)
:END:

Return the `:sentence-detail' string for SOURCE-FILE, or nil.
Reads via `tibetan-analysis--read-source-metadata' so the
heading is parsed by the canonical reader.  Safe when
SOURCE-FILE is nil or the metadata module isn't loaded.

** ~tibetan-sentence--scaffold~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 659
:SIGNATURE: (tibetan-sentence--scaffold sent-num seg-nums tibetan-text wylie source-file)
:END:

Return the scaffold body for a new sent-NNN.org file (as string).
SENT-NUM is the sentence number, SEG-NUMS the list of contained
segment numbers, TIBETAN-TEXT the concatenated child text,
WYLIE its transliteration (may be nil — retained as a fallback
when `tibetan-analysis-generate-content' is unavailable),
SOURCE-FILE the absolute path of the source org buffer.

The scaffold emits the segment-§5.18 aligned layout (2026-05-18):

​  * My Notes                          user-edited (top)
​  * Working Translation               user-edited (top)
​  * Tibetan Text                      concatenated child text
​  * Tibetan Analysis                  was `* Auto-Analysis'
    ** Wylie / Particle Map / Interlinear …
    ** Translation                    sentence-level Claude lands here
    ** Grammar (with *** Claude Grammar / *** Particles)
    ** Verb Classification / Sentence Structure / Clause Structure /
       Detailed Dictionary / Main Clause
    ** Provided Translations          NESTED — segment shape
      *** Roehrich                    sentence-specific
      *** Class Translation           sentence-specific
      *** Claude Context              sentence-specific
      *** Claude Vocabulary           from segment renderer
      *** Claude Particles            from segment renderer
​  * Footnotes                         user-edited (bottom)

Fallback:  when the segment renderer is unavailable
(`tibetan-analysis-generate-content' missing or no Tibetan
content), the analysis parent gets a minimal `* Wylie' block
under `* Tibetan Analysis' instead of the rich segment-renderer
body.  The outer shape (My Notes / Working Translation top,
Footnotes bottom) is preserved either way.

§5.22 final (2026-05-21):  the embedded segment-renderer output
is unconditionally stripped to the in-class compressed layout
(Vocabulary + Translation + Grammar + Provided Translations
only).  Per-segment seg-NNN.org files are unaffected;  only the
sent-NNN.org embedded body is compressed.  See
`tibetan-sentence--strip-list' for the dropped sections and
their rationale.

** ~tibetan-sentence--create-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 779
:SIGNATURE: (tibetan-sentence--create-file sent-num seg-nums tibetan-text source-file)
:END:

Create a new `sent-NNN.org' file for SENT-NUM, return its path.

** ~tibetan-sentence--read-l2-body~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 796
:SIGNATURE: (tibetan-sentence--read-l2-body filepath heading)
:END:

Read body under `** HEADING' anywhere in FILEPATH.  Returns the
trimmed string, or nil when heading is absent / body is empty or
a placeholder.

** ~tibetan-sentence--set-l3-body-in-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 822
:SIGNATURE: (tibetan-sentence--set-l3-body-in-buffer buffer heading body)
:END:

Set the body under `*** HEADING' in BUFFER to BODY (trimmed).
Heading must already exist in the buffer.

** ~tibetan-sentence--set-l2-body-in-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 841
:SIGNATURE: (tibetan-sentence--set-l2-body-in-buffer buffer heading body)
:END:

Set the body under `** HEADING' in BUFFER to BODY (trimmed).

** ~tibetan-sentence--set-top-level-body-in-buffer~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 859
:SIGNATURE: (tibetan-sentence--set-top-level-body-in-buffer buffer heading body)
:END:

Replace the FULL top-level `* HEADING' section in BUFFER with BODY.
BODY is the verbatim content captured by
`tibetan-sentence--get-user-sections' (heading line through next
top-level heading).  Idempotent — only replaces an existing section
in place;  callers ensure the heading is present first.

** ~tibetan-sentence--regenerate~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 873
:SIGNATURE: (tibetan-sentence--regenerate filepath sent-num seg-nums tibetan-text)
:END:

Regenerate FILEPATH in the new segment-§5.18-aligned layout.

PRESERVE phase:  reads all bodies worth keeping:
  - User sections (My Notes, Working Translation, Footnotes).
  - Reference bodies (Roehrich, Class Translation).
  - Sentence-level Claude bodies (Claude Translation @ level-3
    legacy OR `** Translation' @ level-2 new, Claude Grammar,
    Claude Context).

REBUILD phase:  scaffolds a fresh file via
`tibetan-sentence--scaffold' in the NEW layout (My Notes /
Working Translation top, `* Tibetan Analysis' parent, nested
`** Provided Translations', Footnotes bottom).

RESTORE phase:  injects each preserved body into its slot in the
new layout:
  - Legacy `*** Claude Translation' body → `** Translation'
    (level-2 promotion, matches segment §5.18 layout).
  - Roehrich / Class Translation / Claude Context → level-3
    inside nested `** Provided Translations'.
  - Claude Grammar → level-3 under `** Grammar'.
  - My Notes / Working Translation / Footnotes → top-level
    sections at their new positions (top / top / bottom).

Idempotent — re-running on an already-migrated file produces
identical output (modulo `#+LAST_ANALYZED:' timestamp).

Updates `#+SEGMENTS:' / `#+TIBETAN_HASH:' / `#+LAST_ANALYZED:'.

** ~tibetan-sentence--child-seg-filepath~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1160
:SIGNATURE: (tibetan-sentence--child-seg-filepath seg-num &optional folder)
:END:

Return absolute path to `seg-NNN.org' for SEG-NUM, or nil.

** ~tibetan-sentence--collect-children-grounding~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1167
:SIGNATURE: (tibetan-sentence--collect-children-grounding seg-nums &optional folder)
:END:

Collect parser-grounding text from the seg-NNN.org files of SEG-NUMS.
Returns a single concatenated string suitable for embedding in the
Claude user prompt, or nil when no child files exist or none have
parser content.

** ~tibetan-sentence--build-claude-prompts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1193
:SIGNATURE: (tibetan-sentence--build-claude-prompts tibetan-text seg-nums source-file &optional folder)
:END:

Build (SYSTEM . USER) Claude prompts for sentence-level analysis.
TIBETAN-TEXT is the concatenated sentence text; SEG-NUMS the list of
child segment numbers used to look up their per-segment analysis
files for parser grounding; SOURCE-FILE the source org buffer path
(used for genre/author metadata via the same helper as
`tibetan-analysis-persist'); FOLDER the analysis folder.

** ~tibetan-sentence--source-file-from-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1253
:SIGNATURE: (tibetan-sentence--source-file-from-analysis analysis-file)
:END:

Return absolute source file referenced by sent-NNN.org ANALYSIS-FILE.

** ~tibetan-sentence--seg-nums-from-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1266
:SIGNATURE: (tibetan-sentence--seg-nums-from-file filepath)
:END:

Read the `#+SEGMENTS:' header value from FILEPATH and return a list of ints.

** ~tibetan-sentence--request-claude~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1278
:SIGNATURE: (tibetan-sentence--request-claude tibetan-text seg-nums analysis-file &optional source-file folder)
:END:

Async-request a Claude sentence-level analysis of TIBETAN-TEXT.
On callback, parses the response and writes the three Claude
sections into ANALYSIS-FILE.  Reuses
`tibetan-analysis--insert-claude-sections' so the writer behaviour
stays identical to the segment-level flow.

Routed through `tibetan-claude-queue' so concurrent sentence-level
requests share the same throttle / retry budget as segment-level
requests, and a placeholder is written into the *** Claude
Translation section if retries are exhausted.

** ~tibetan-sentence-open-analysis~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1362
:SIGNATURE: (tibetan-sentence-open-analysis)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open or create analysis for the sentence at point in a side window.
Works from a `*** Sentence N' heading or any `**** Segment M' (or
`*** Segment M' in the fresh-prep layout) inside it.  When an
analysis file already exists, warn if its stored hash no longer
matches the current concatenated child text (i.e. the source has
changed).

** ~tibetan-sentence-reanalyze~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1445
:SIGNATURE: (tibetan-sentence-reanalyze)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-analyze the sentence at point.
Regenerates Tibetan/Wylie/SEGMENTS/HASH; preserves My Notes,
Working Translation, Footnotes, Roehrich, Class Translation, and
the three Claude sections.  Re-issues the Claude request only when
called with a prefix argument.

** ~tibetan-sentence-reanalyze-file~  /function (cl)/
:PROPERTIES:
:KIND: cl-defun
:LINE: 1506
:SIGNATURE: (tibetan-sentence-reanalyze-file filepath &key source-file re-request-claude dry-run)
:END:

Headless single-file re-analysis of a sent-NNN.org FILEPATH.
Tibetan text is freshly derived from SOURCE-FILE (or from the
file's `#+SOURCE:' link if not given) by re-scanning the source
buffer for the sentence number encoded in the filename.

When RE-REQUEST-CLAUDE is non-nil, a fresh Claude request is
dispatched after regeneration.  With DRY-RUN, no files are written.

Returns plist:
  (:file F :sent-id ID :ok BOOL :error STR :seg-nums (...)).

** ~tibetan-sentence--folder-sentence-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1587
:SIGNATURE: (tibetan-sentence--folder-sentence-files folder)
:END:

Return sent-NNN*.org files in FOLDER, sorted by sentence id.

** ~tibetan-sentence-batch-reanalyze~  /function (cl)/  ⚡  ↗
:PROPERTIES:
:KIND: cl-defun
:LINE: 1599
:SIGNATURE: (tibetan-sentence-batch-reanalyze &key folder source-file re-request-claude dry-run)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Re-run analysis on every `sent-NNN*.org' file in FOLDER.

FOLDER defaults to the analysis folder of the current buffer.
SOURCE-FILE defaults to the current buffer's file.  With
RE-REQUEST-CLAUDE non-nil, a fresh Claude request is dispatched
per file.  With DRY-RUN non-nil, nothing is written.

Returns plist:
  (:folder F :total N :ok N-ok :failed N-bad :results RESULTS).

** ~tibetan-sentence--analysis-folder~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1665
:SIGNATURE: (tibetan-sentence--analysis-folder)
:END:

Return the analysis folder path for the current source buffer.
Falls back to `<cwd>/analysis/' when not in a source buffer.

** ~tibetan-sentence-archive-analysis-folder~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1674
:SIGNATURE: (tibetan-sentence-archive-analysis-folder &optional folder)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Move every `sent-NNN*.org' in FOLDER into a timestamped archive subdir.

FOLDER defaults to the analysis folder of the current source
buffer.  Creates `<folder>/archive/YYYY-MM-DD-HHMMSS/' and moves
all `sent-*.org' files into it.  `seg-*.org' and any other files
are left untouched.  Returns the archive directory path.

Intended use: before re-segmenting a source file whose sentence
numbering is about to change.  Archiving preserves the old
analysis work so the user can compare or selectively restore
content (Roehrich pastes, Class translations, Claude bodies,
My Notes, Working Translation, Footnotes) into the freshly-
generated sent files afterwards.

** ~tibetan-sentence-create-all~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1722
:SIGNATURE: (tibetan-sentence-create-all)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Create a fresh `sent-NNN.org' analysis file for every `*** Sentence N'
heading in the current source buffer.

Iterates top-to-bottom, calling the same scaffold + create path
used by `C-c s A'.  Any `sent-NNN.org' that already exists in
the analysis folder is skipped (re-creation would clobber a file
the user may have hand-edited since last segment change).  To
regenerate from scratch, archive first with
`tibetan-sentence-archive-analysis-folder'.

Reports created / skipped counts on completion.

** ~tibetan-sentence--fire-claude-on-new-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 1821
:SIGNATURE: (tibetan-sentence--fire-claude-on-new-files plists source-file)
:END:

Queue Claude translation requests for sentence files in PLISTS.

Each PLIST has :sent-num, :seg-nums, :tibetan, :filepath.  Requests
are staggered by `tibetan-auto-claude-request-delay' (defaulting to
1.5s if the segment-level auto-analysis module isn't loaded), so 85
new sentences take 85 × 1.5 ≈ 2 min of async queue time.  Individual
failures are swallowed to `message'.

** ~tibetan-sentence-resegment~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 1876
:SIGNATURE: (tibetan-sentence-resegment)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

One-command re-segment workflow for an already-segmented source file.

Runs the four steps in order, with a single top-level confirmation:

  1. Archive `sent-NNN*.org' → `analysis/archive/<stamp>/'.
  2. Reset source-file structure (remove `*** Sentence N' headings,
     un-demote `**** Segment M' back to `*** Segment M').
  3. Re-run the auto-segmenter (`tibetan-add-sentence-structure').
  4. Create fresh `sent-NNN.org' for every new `*** Sentence N'.

Step 1 calls `tibetan-sentence-archive-analysis-folder'; the
individual commands' own interactive prompts are suppressed so
the user only sees ONE yes/no gate at the top.  Save the source
file yourself after reviewing the result — none of these steps
auto-save.

Intended use: after a boundary-detector upgrade (e.g. the
dialogue-framing step added on 2026-04-20) where the auto-
segmenter would now find more sentences than a previous pass.
Manual cleanup via regexp is viable but fiddly; this command
turns it into one confirmable operation.

** ~gptel-cache~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 75
:END:

/(no docstring)/

** ~tibetan-sentence-persist-version~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 88
:DEFAULT: "1.0"
:END:

Version of the sentence analysis file format.

** ~tibetan-sentence--parallel-top-level-sections~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 382
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence--strip-list~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 441
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence--detail-for-render~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 477
:DEFAULT: nil
:END:

Dynamic binding:  the `:sentence-detail' value for the in-flight
sentence-scaffold render.  When `detailed', the strip-list is
suppressed so the embedded segment-renderer output passes
through unmodified — sentence files mirror the full §5.21
segment layout (Wylie / Phonetics / Interlinear / etc. all
present).

§5.27 Phase 5 (2026-05-26):  let-bound by
`tibetan-sentence--create-file' and `tibetan-sentence--regenerate'
based on the source-document's `#+TIBETAN_SENTENCE_DETAIL:'
header.  Consumed by `--segment-claude-sections' below.

Nil → default compressed behaviour from §5.22 final
(backwards-compatible).  String `"detailed"' → full §5.21
layout.  Any other string treated as `compressed' (defensive).

** ~tibetan-sentence--scaffold-sentence-only-l3-entries~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 585
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-sentence--claude-system-prompt~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 1071
:DEFAULT: "You are a specialist in Classical Tibetan (chos skad) translation \
and philology, acting as a teaching assistant for a
:END:

System prompt for sentence-level (discourse) Claude analysis.
Mirrors the three-section schema of the segment-level prompt so the
existing parser (`tibetan-analysis--parse-claude-sections') can read
the response, but reframes the task at the sentence/discourse level
and tells Claude to defer to the per-segment parser output for
particle-level tagging.


* persist/tibetan-structure-reorg.el
:PROPERTIES:
:CUSTOM_ID: persist/tibetan-structure-reorg.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/persist/tibetan-structure-reorg.el
:END:

** ~tibetan-reorg--collect-document-segments~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 47
:SIGNATURE: (tibetan-reorg--collect-document-segments)
:END:

Collect inventory of all segments in current buffer.
Returns list of plists with :segment-num, :tibetan-text, :hash, :sentence-num.

** ~tibetan-reorg--collect-analysis-files~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 79
:SIGNATURE: (tibetan-reorg--collect-analysis-files directory)
:END:

Collect inventory of analysis files in DIRECTORY.
Returns list of plists with :filename, :filepath, :segment-num,
:hash, :has-notes.

** ~tibetan-reorg--file-has-user-content~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 100
:SIGNATURE: (tibetan-reorg--file-has-user-content filepath)
:END:

Check if analysis file at FILEPATH has user-added content.
Returns t if My Notes, Working Translation, or Footnotes have content.

** ~tibetan-reorg--find-analysis-by-hash~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 126
:SIGNATURE: (tibetan-reorg--find-analysis-by-hash hash directory)
:END:

Find analysis file in DIRECTORY with matching HASH.
Returns filepath or nil if not found.

** ~tibetan-reorg--plan-actions~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 144
:SIGNATURE: (tibetan-reorg--plan-actions doc-segments file-inventory)
:END:

Plan reorganization actions based on DOC-SEGMENTS and FILE-INVENTORY.
Returns list of action plists with :action, :source, :target, :segment-num, etc.

Actions:
- keep: File matches document, no change needed
- rename: File content matches but segment number changed
- create: New segment needs analysis file
- orphan: Analysis file has no matching segment

** ~tibetan-reorg--rename-analysis-file~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 207
:SIGNATURE: (tibetan-reorg--rename-analysis-file old-path new-path new-segment-num)
:END:

Rename analysis file from OLD-PATH to NEW-PATH.
Updates internal references to NEW-SEGMENT-NUM.

** ~tibetan-reorg--update-source-link~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 218
:SIGNATURE: (tibetan-reorg--update-source-link filepath new-segment-num)
:END:

Update the SOURCE link in FILEPATH to reference NEW-SEGMENT-NUM.

** ~tibetan-reorg--update-title~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 233
:SIGNATURE: (tibetan-reorg--update-title filepath new-segment-num)
:END:

Update the TITLE in FILEPATH to reference NEW-SEGMENT-NUM.

** ~tibetan-reorg--archive-orphan~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 244
:SIGNATURE: (tibetan-reorg--archive-orphan filepath)
:END:

Move orphaned analysis file at FILEPATH to archive directory.

** ~tibetan-reorg--generate-new-filename~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 265
:SIGNATURE: (tibetan-reorg--generate-new-filename segment-num source-file)
:END:

Generate filename for SEGMENT-NUM analysis.
SOURCE-FILE supplies the per-source short-name suffix (§5.23):
`seg-NNN-SHORT.org'.  Without it (or when no short name can be
derived) falls back to the bare `seg-NNN.org'.  Producing the
suffixed name keeps multi-source folders (e.g. MA Reading: gal-chen /
lam-rim / title-colophon) collision-free — the §5.23 fix applied here
to the reorg rename path, which previously always emitted the bare
name and reintroduced the silent-overwrite collision.

** ~tibetan-reorg--execute-actions~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 287
:SIGNATURE: (tibetan-reorg--execute-actions actions analysis-dir source-file &optional dry-run)
:END:

Execute reorganization ACTIONS in ANALYSIS-DIR.
SOURCE-FILE is used for generating new filenames.
If DRY-RUN is non-nil, only report what would be done.

Returns plist with :renamed, :created, :archived, :kept counts.

** ~tibetan-reorganize-analysis-files~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 373
:SIGNATURE: (tibetan-reorganize-analysis-files &optional dry-run)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Reorganize analysis files to match current document structure.

With prefix argument DRY-RUN, only show what would be done.

This command:
1. Collects segment inventory from the document
2. Collects analysis file inventory
3. Matches files to segments by content hash
4. Renames files whose segments moved
5. Archives files for deleted segments
6. Creates files for new segments (if enabled)

Returns plist with reorganization statistics.

** ~tibetan-preview-reorganization~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 451
:SIGNATURE: (tibetan-preview-reorganization)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Preview what reorganization would do without making changes.

** ~tibetan-reorg-archive-orphans~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 32
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

If non-nil, archive orphaned analysis files instead of deleting.
Orphans are moved to analysis/.archive/ subdirectory.

** ~tibetan-reorg-auto-create~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 38
:TYPE: 'boolean
:GROUP: tibetan-cat
:DEFAULT: t
:END:

If non-nil, automatically create analysis for new segments during reorg.


* philology/tibetan-bialek-textbook-refs.el
:PROPERTIES:
:CUSTOM_ID: philology/tibetan-bialek-textbook-refs.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/philology/tibetan-bialek-textbook-refs.el
:END:

** ~tibetan-bialek-textbook-ref~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 100
:SIGNATURE: (tibetan-bialek-textbook-ref type)
:END:

Return the canonical `Bialek 2022 §X.Y (Title)' reference for
TYPE, or nil when TYPE is not in the table.

TYPE is the ALL-CAPS string from `nth 2' of a
`tibetan-analyze-grammar-bialek' tuple (e.g. `GENITIVE (GEN)' or
`CONVERBIAL: ABLATIVE CONVERB').  Used by the merged `***
Particles' bullet renderer in
`tibetan-analysis--render-particle-bullets' to emit a
`[Bialek 2022 §X.Y …; Portfolio §A.B]' bracket alongside each
particle.

Returns nil rather than signalling when TYPE is unknown — the
renderer's bracket-building logic treats nil as `Bialek side
absent, emit Portfolio side only'.

** ~tibetan-bialek-textbook-refs~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 44
:DEFAULT: '
:END:

/(no docstring)/


* philology/tibetan-madhyamaka-terms.el
:PROPERTIES:
:CUSTOM_ID: philology/tibetan-madhyamaka-terms.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/philology/tibetan-madhyamaka-terms.el
:END:

** ~tibetan-initialize-madhyamaka-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 199
:SIGNATURE: (tibetan-initialize-madhyamaka-vocabulary)
:END:

Initialize the Madhyamaka vocabulary hash table from the term lists.

** ~tibetan-madhyamaka--term-present-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 215
:SIGNATURE: (tibetan-madhyamaka--term-present-p term text)
:END:

Non-nil when TERM occurs in TEXT as a whole, tsheg/shad/space-
delimited unit (not as a substring inside a larger syllable).

TERM is matched literally (regexp-quoted), bounded on each side by
string start/end or a Tibetan delimiter (tsheg ་, shad །༎༔, space,
tab, newline).  This prevents e.g. the core term `ལམ' matching inside
the unrelated syllable `ལམས'.

Note:  it does NOT resolve compound-internal matches — `ལམ' is a
genuine tsheg-delimited syllable inside `ལམ་རིམ', so it still matches
there.  Distinguishing word- from syllable-boundaries needs real
Tibetan word segmentation, out of scope here.

** ~tibetan-extract-madhyamaka-vocabulary~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 235
:SIGNATURE: (tibetan-extract-madhyamaka-vocabulary tibetan-text)
:END:

Extract Madhyamaka technical terms from TIBETAN-TEXT.
Returns list of (tibetan-term . english-explanation).  Terms match as
whole tsheg/shad/space-delimited units (see
`tibetan-madhyamaka--term-present-p').

** ~tibetan-madhyamaka-core-terms~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 23
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-gelugpa-specific-usage~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 181
:DEFAULT: '
:END:

/(no docstring)/

** ~tibetan-madhyamaka-vocabulary~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 195
:DEFAULT: nil
:END:

Hash table for Madhyamaka philosophical terminology.
Populated from tibetan-madhyamaka-core-terms and tibetan-gelugpa-specific-usage.


* philology/tibetan-verse-philology.el
:PROPERTIES:
:CUSTOM_ID: philology/tibetan-verse-philology.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/philology/tibetan-verse-philology.el
:END:

** ~tibetan-count-syllables~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 28
:SIGNATURE: (tibetan-count-syllables tibetan-text)
:END:

Count syllables in TIBETAN-TEXT.
Returns number of syllables (segments separated by tsheg ་).
Returns 0 if text is nil or empty.

** ~tibetan-syllable-breakdown~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 38
:SIGNATURE: (tibetan-syllable-breakdown tibetan-text)
:END:

Break down TIBETAN-TEXT into individual syllables.
Returns list of syllables.

** ~tibetan-format-syllables-numbered~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 45
:SIGNATURE: (tibetan-format-syllables-numbered tibetan-text)
:END:

Format TIBETAN-TEXT with numbered syllables.
Example output:
  བླ་  མ་  ལ་  ནི་  ཕྱག་  འཚལ་  ལོ།
  1    2   3   4    5     6      7

** ~tibetan-identify-metrical-fillers~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 66
:SIGNATURE: (tibetan-identify-metrical-fillers tibetan-text)
:END:

Identify potential metrical fillers in TIBETAN-TEXT.
Returns list of (syllable position is-filler-p).

** ~tibetan-extract-core-meaning~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 78
:SIGNATURE: (tibetan-extract-core-meaning tibetan-text)
:END:

Extract core meaning by removing likely metrical fillers.
Returns string with fillers marked.

** ~tibetan-parse-sa-bcad-number~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 97
:SIGNATURE: (tibetan-parse-sa-bcad-number sa-bcad-str)
:END:

Parse sa bcad outline number like '2.2.1.2.1.2.4'.
Returns list of integers.

** ~tibetan-sa-bcad-depth~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 102
:SIGNATURE: (tibetan-sa-bcad-depth sa-bcad-str)
:END:

Return depth level of sa bcad outline number.
Example: '2.2.1' → 3

** ~tibetan-analyze-verse-block~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 111
:SIGNATURE: (tibetan-analyze-verse-block verse-lines verse-number)
:END:

Analyze a block of VERSE-LINES with VERSE-NUMBER.
VERSE-LINES is list of lines (typically 4 lines per verse).
Returns formatted analysis.

** ~tibetan-format-apparatus-entry~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 142
:SIGNATURE: (tibetan-format-apparatus-entry lemma reading-a reading-b note)
:END:

Format a critical apparatus entry.
LEMMA: the word/phrase in question
READING-A: reading from auto-commentary
READING-B: reading from base text
NOTE: apparatus note (emend., conj., etc.)

** ~tibetan-display-apparatus~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 154
:SIGNATURE: (tibetan-display-apparatus apparatus-entries)
:END:

Display APPARATUS-ENTRIES in formatted style.
APPARATUS-ENTRIES is list of (lemma reading-a reading-b note).

** ~tibetan-get-current-verse-block~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 169
:SIGNATURE: (tibetan-get-current-verse-block)
:END:

Extract current verse block data from buffer.
Returns (verse-number . verse-lines) or nil if not in a verse block.

** ~tibetan-analyze-current-verse-interactive~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 200
:SIGNATURE: (tibetan-analyze-current-verse-interactive)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Analyze the verse block at point and display in side window.
Shows:
- Verse text with line-by-line syllable analysis
- Metrical analysis (7-syllable validation)
- Core meaning extraction (fillers marked)
- Vocabulary for all terms
- Madhyamaka terminology identification

Bound to C-c v v.

** ~tibetan-common-metrical-fillers~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 61
:DEFAULT: '
:END:

/(no docstring)/


* scripts/regenerate-tigress-analysis.el
:PROPERTIES:
:CUSTOM_ID: scripts/regenerate-tigress-analysis.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/scripts/regenerate-tigress-analysis.el
:END:

** ~regenerate-tigress-segment~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 52
:SIGNATURE: (regenerate-tigress-segment seg-id tibetan-text)
:END:

Regenerate analysis for SEG-ID with TIBETAN-TEXT.

** ~tibetan-cat-data-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 13
:DEFAULT: nil
:END:

Directory containing Tibetan CAT data files.

** ~tigress-segments~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 41
:DEFAULT: '
:END:

/(no docstring)/

** ~tigress-analysis-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 48
:DEFAULT: "/Users/cp/buddhist-studies/WS25-26/Tibetisch III/TigressStory/WorkInProgress/analysis/"
:END:

Directory containing analysis files.


* scripts/test-analysis-output.el
:PROPERTIES:
:CUSTOM_ID: scripts/test-analysis-output.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/scripts/test-analysis-output.el
:END:

** ~test-text~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 24
:DEFAULT: "སྔོན་འདས་པའི་དུས་བསྐལ་པ་གྲངས་མེད་པའི་ཕ་རོལ་ན།"
:END:

Seg-032 text.


* setup/tibetan-cat-setup.el
:PROPERTIES:
:CUSTOM_ID: setup/tibetan-cat-setup.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/setup/tibetan-cat-setup.el
:END:

** ~tibetan-cat-setup--check-emacs-version~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 38
:SIGNATURE: (tibetan-cat-setup--check-emacs-version)
:END:

Check that Emacs version is sufficient.

** ~tibetan-cat-setup--check-org~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 45
:SIGNATURE: (tibetan-cat-setup--check-org)
:END:

Check that org-mode is available.

** ~tibetan-cat-setup--check-fonts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 56
:SIGNATURE: (tibetan-cat-setup--check-fonts)
:END:

Check for Tibetan fonts.

** ~tibetan-cat-setup--check-glossaries~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 72
:SIGNATURE: (tibetan-cat-setup--check-glossaries)
:END:

Check that glossary data files exist.

** ~tibetan-cat-setup--ensure-packages~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 94
:SIGNATURE: (tibetan-cat-setup--ensure-packages)
:END:

Install required Emacs packages.

** ~tibetan-cat-setup--install-fonts-macos~  /function/  ⚡
:PROPERTIES:
:KIND: defun
:LINE: 125
:SIGNATURE: (tibetan-cat-setup--install-fonts-macos)
:INTERACTIVE: t
:END:

Install Tibetan fonts on macOS using Homebrew.

** ~tibetan-cat-setup--init-line~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 141
:SIGNATURE: (tibetan-cat-setup--init-line)
:END:

Generate the line to add to init.el.

** ~tibetan-cat-setup--already-in-init-p~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 145
:SIGNATURE: (tibetan-cat-setup--already-in-init-p)
:END:

Check if tibetan-cat is already configured in init.el.

** ~tibetan-cat-setup--add-to-init~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 154
:SIGNATURE: (tibetan-cat-setup--add-to-init)
:END:

Add tibetan-cat load line to init.el.

** ~tibetan-cat-setup--configure-fonts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 184
:SIGNATURE: (tibetan-cat-setup--configure-fonts)
:END:

Configure Tibetan font display.

** ~tibetan-cat-install~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 198
:SIGNATURE: (tibetan-cat-install &optional non-interactive)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Install and configure Tibetan CAT system.
If NON-INTERACTIVE is non-nil, skip prompts and use defaults.

** ~tibetan-cat-status~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 283
:SIGNATURE: (tibetan-cat-status)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Show Tibetan CAT system status and health check.

** ~tibetan-cat-setup-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 26
:DEFAULT: (file-name-directory (or load-file-name buffer-file-name))
:END:

Directory containing setup files.

** ~tibetan-cat-base-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 30
:DEFAULT: (file-name-directory (directory-file-name tibetan-cat-setup-dir))
:END:

Base directory of tibetan-cat.el installation.

** ~package-archive-contents~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 92
:END:

/(no docstring)/


* setup/tibetan-cat-ui.el
:PROPERTIES:
:CUSTOM_ID: setup/tibetan-cat-ui.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/setup/tibetan-cat-ui.el
:END:

** ~tibetan-cat-ui--ensure-package~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 68
:SIGNATURE: (tibetan-cat-ui--ensure-package pkg)
:END:

Ensure PKG is installed.

** ~tibetan-cat-ui--ensure-packages~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 75
:SIGNATURE: (tibetan-cat-ui--ensure-packages)
:END:

Install all UI packages.

** ~tibetan-cat-ui--setup-treemacs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 97
:SIGNATURE: (tibetan-cat-ui--setup-treemacs)
:END:

Configure treemacs file browser.

** ~tibetan-cat-ui--setup-modeline~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 119
:SIGNATURE: (tibetan-cat-ui--setup-modeline)
:END:

Configure doom modeline.

** ~tibetan-cat-ui--setup-dashboard~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 131
:SIGNATURE: (tibetan-cat-ui--setup-dashboard)
:END:

Configure startup dashboard.

** ~tibetan-cat-ui--setup-visuals~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 147
:SIGNATURE: (tibetan-cat-ui--setup-visuals)
:END:

Configure visual enhancements.

** ~tibetan-cat-ui--setup-completion~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 170
:SIGNATURE: (tibetan-cat-ui--setup-completion)
:END:

Configure modern completion.

** ~tibetan-cat-ui--setup-fonts~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 183
:SIGNATURE: (tibetan-cat-ui--setup-fonts)
:END:

Configure fonts for Tibetan script display.

** ~tibetan-cat-ui-install~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 202
:SIGNATURE: (tibetan-cat-ui-install)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Install all UI packages and configure them.

** ~tibetan-cat-ui-setup~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 211
:SIGNATURE: (tibetan-cat-ui-setup)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Activate all UI enhancements (packages must already be installed).

** ~package-archive-contents~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 34
:END:

/(no docstring)/

** ~treemacs-width~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 36
:END:

/(no docstring)/

** ~treemacs-show-hidden-files~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 37
:END:

/(no docstring)/

** ~treemacs-follow-mode~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 38
:END:

/(no docstring)/

** ~doom-modeline-height~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 42
:END:

/(no docstring)/

** ~doom-modeline-icon~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 43
:END:

/(no docstring)/

** ~doom-modeline-major-mode-icon~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 44
:END:

/(no docstring)/

** ~dashboard-banner-logo-title~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 47
:END:

/(no docstring)/

** ~dashboard-startup-banner~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 48
:END:

/(no docstring)/

** ~dashboard-center-content~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 49
:END:

/(no docstring)/

** ~dashboard-items~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 50
:END:

/(no docstring)/

** ~dashboard-set-heading-icons~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 51
:END:

/(no docstring)/

** ~dashboard-set-file-icons~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 52
:END:

/(no docstring)/


* spec/generate-docs.el
:PROPERTIES:
:CUSTOM_ID: spec/generate-docs.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/generate-docs.el
:END:

** ~tibetan-bdd-generate-docs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 11
:SIGNATURE: (tibetan-bdd-generate-docs)
:END:

Generate markdown documentation from all spec suites.


* spec/generate-living-doc.el
:PROPERTIES:
:CUSTOM_ID: spec/generate-living-doc.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/generate-living-doc.el
:END:

** ~tibetan-bdd--extract-all-suites~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 41
:SIGNATURE: (tibetan-bdd--extract-all-suites)
:END:

Extract all suite data as structured alists for report generation.

** ~tibetan-bdd--run-and-collect~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 66
:SIGNATURE: (tibetan-bdd--run-and-collect)
:END:

Run all specs and collect results keyed by suite+spec name.

** ~tibetan-bdd--html-escape~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 86
:SIGNATURE: (tibetan-bdd--html-escape str)
:END:

Escape HTML special characters in STR.

** ~tibetan-bdd--format-sexp~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 96
:SIGNATURE: (tibetan-bdd--format-sexp sexp)
:END:

Format SEXP as readable, escaped string.

** ~tibetan-bdd--tag-class~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 103
:SIGNATURE: (tibetan-bdd--tag-class tag)
:END:

Return CSS class for TAG keyword.

** ~tibetan-bdd--generate-html~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 115
:SIGNATURE: (tibetan-bdd--generate-html suites run-data)
:END:

Generate living documentation HTML from SUITES and RUN-DATA.

** ~tibetan-bdd--generate-suite-html~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 199
:SIGNATURE: (tibetan-bdd--generate-suite-html suite result-map)
:END:

Generate HTML for a single SUITE section using RESULT-MAP for status.

** ~tibetan-bdd--generate-spec-html~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 237
:SIGNATURE: (tibetan-bdd--generate-spec-html spec suite-name result-map)
:END:

Generate HTML for a single SPEC in SUITE-NAME using RESULT-MAP.

** ~tibetan-bdd--generate-css~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 315
:SIGNATURE: (tibetan-bdd--generate-css)
:END:

Generate embedded CSS for the living documentation.

** ~tibetan-bdd-generate-living-doc~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 479
:SIGNATURE: (tibetan-bdd-generate-living-doc)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Generate living documentation HTML report.
Runs all specs and produces a browsable HTML report.


* spec/run-specs.el
:PROPERTIES:
:CUSTOM_ID: spec/run-specs.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/run-specs.el
:END:

** ~tibetan-cat-data-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 23
:DEFAULT: nil
:END:

Directory containing Tibetan CAT data files.


* spec/suites/particle-analysis-spec.el
:PROPERTIES:
:CUSTOM_ID: spec/suites/particle-analysis-spec.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/suites/particle-analysis-spec.el
:END:

** ~tibetan-bdd--get-particle-annotation~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 12
:SIGNATURE: (tibetan-bdd--get-particle-annotation word)
:END:

Get particle annotation for WORD using analysis-persist function.


* spec/suites/tigress-story-spec.el
:PROPERTIES:
:CUSTOM_ID: spec/suites/tigress-story-spec.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/suites/tigress-story-spec.el
:END:

** ~tibetan-bdd-assert-particle-in-analysis~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 352
:SIGNATURE: (tibetan-bdd-assert-particle-in-analysis particle result)
:END:

Assert that PARTICLE appears in the grammar analysis RESULT.

** ~tibetan-bdd-get-particle-type~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 359
:SIGNATURE: (tibetan-bdd-get-particle-type particle result)
:END:

Extract the TYPE annotation for PARTICLE from RESULT.

** ~tibetan-bdd-get-particle-function~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 369
:SIGNATURE: (tibetan-bdd-get-particle-function particle result)
:END:

Extract the FUNCTION annotation for PARTICLE from RESULT.

** ~tibetan-bdd-assert-member~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 379
:SIGNATURE: (tibetan-bdd-assert-member expected list-result &optional msg)
:END:

Assert that EXPECTED is a member of LIST-RESULT.

** ~tibetan-bdd-assert-not-matches~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 383
:SIGNATURE: (tibetan-bdd-assert-not-matches pattern result &optional msg)
:END:

Assert that PATTERN does NOT match in RESULT.


* spec/tibetan-bdd.el
:PROPERTIES:
:CUSTOM_ID: spec/tibetan-bdd.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/spec/tibetan-bdd.el
:END:

** ~define-bdd-suite~  /macro/
:PROPERTIES:
:KIND: defmacro
:LINE: 50
:SIGNATURE: (define-bdd-suite name description &rest body)
:END:

Define a BDD suite NAME with DESCRIPTION.
BODY contains spec definitions.

** ~spec~  /macro/
:PROPERTIES:
:KIND: defmacro
:LINE: 62
:SIGNATURE: (spec name &rest clauses)
:END:

Define a spec NAME with GIVEN/WHEN/THEN clauses.
Adds to the current suite being defined.

Supported clauses:
  :given        — setup form (executed before WHEN)
  :when         — action form (its return value is bound to `result' in THEN)
  :then         — assertion form or list of assertion forms
  :skip-unless  — optional predicate;  when it evaluates to nil at run
                  time, the spec is recorded as `skipped' and neither
                  GIVEN nor WHEN nor THEN runs.  Use for environment-
                  dependent specs (e.g. `(tibetan-steinert-available-p)'
                  on Emacs versions without bundled SQLite).
  :example      — provenance string for documentation
  :tags         — list of keyword tags

** ~tibetan-bdd-assert-contains~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 109
:SIGNATURE: (tibetan-bdd-assert-contains haystack needle &optional message)
:END:

Assert that HAYSTACK contains NEEDLE.

** ~tibetan-bdd-assert-not-contains~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 117
:SIGNATURE: (tibetan-bdd-assert-not-contains haystack needle &optional message)
:END:

Assert that HAYSTACK does NOT contain NEEDLE.

** ~tibetan-bdd-assert-has-key~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 125
:SIGNATURE: (tibetan-bdd-assert-has-key result key &optional message)
:END:

Assert that RESULT alist has KEY.

** ~tibetan-bdd-assert-equal~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 132
:SIGNATURE: (tibetan-bdd-assert-equal expected actual &optional message)
:END:

Assert that EXPECTED equals ACTUAL.

** ~tibetan-bdd-assert-matches~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 139
:SIGNATURE: (tibetan-bdd-assert-matches pattern text &optional message)
:END:

Assert that TEXT matches regex PATTERN.

** ~tibetan-bdd-assert-count-gte~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 146
:SIGNATURE: (tibetan-bdd-assert-count-gte list min-count &optional message)
:END:

Assert that LIST has at least MIN-COUNT elements.

** ~tibetan-bdd-assert-truthy~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 153
:SIGNATURE: (tibetan-bdd-assert-truthy value &optional message)
:END:

Assert that VALUE is non-nil.

** ~tibetan-bdd-assert-nil~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 160
:SIGNATURE: (tibetan-bdd-assert-nil value &optional message)
:END:

Assert that VALUE is nil.

** ~tibetan-bdd-assert-not-nil~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 167
:SIGNATURE: (tibetan-bdd-assert-not-nil value &optional message)
:END:

Assert that VALUE is non-nil.

** ~tibetan-bdd-assert-member~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 173
:SIGNATURE: (tibetan-bdd-assert-member element list &optional message)
:END:

Assert that ELEMENT is a member of LIST.

** ~tibetan-bdd-assert-not-matches~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 180
:SIGNATURE: (tibetan-bdd-assert-not-matches pattern text &optional message)
:END:

Assert that TEXT does NOT match regex PATTERN.

** ~tibetan-bdd-run-spec~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 198
:SIGNATURE: (tibetan-bdd-run-spec spec)
:END:

Run a single SPEC, returning (name . result).

The result symbol is one of:
  `passed'    — all assertions held
  `skipped'   — `:skip-unless' predicate evaluated to nil
  `(failed REASON)' — an assertion or evaluation raised an error

** ~tibetan-bdd-run-suite~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 233
:SIGNATURE: (tibetan-bdd-run-suite suite-name)
:END:

Run all specs in SUITE-NAME, returning results.

** ~tibetan-bdd-run-all~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 250
:SIGNATURE: (tibetan-bdd-run-all)
:END:

Run all defined specs, returning summary.

** ~tibetan-bdd-report~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 274
:SIGNATURE: (tibetan-bdd-report results)
:END:

Print a report of RESULTS.

** ~tibetan-bdd-capture-verb-example~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 315
:SIGNATURE: (tibetan-bdd-capture-verb-example tibetan-text expected-verbs)
:END:

Capture a verb detection example.
TIBETAN-TEXT is the input, EXPECTED-VERBS is list of (verb . meaning).

** ~tibetan-bdd--extract-verbs~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 332
:SIGNATURE: (tibetan-bdd--extract-verbs text)
:END:

Helper to extract verbs from TEXT for testing.

** ~tibetan-bdd-suites~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 22
:DEFAULT: (make-hash-table :test 'eq)
:END:

Hash table of spec suites, keyed by suite name symbol.

** ~tibetan-bdd-results~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 25
:DEFAULT: nil
:END:

Results from last spec run.

** ~tibetan-bdd--current-result~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 195
:DEFAULT: nil
:END:

Holds the result of the WHEN clause for use in THEN assertions.


* tibetan-cat.el
:PROPERTIES:
:CUSTOM_ID: tibetan-cat.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/tibetan-cat.el
:END:

** ~tibetan-cat-setup~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 290
:SIGNATURE: (tibetan-cat-setup)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Setup Tibetan CAT system.
Loads glossaries and prepares the system for use.

** ~tibetan-cat-version~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 376
:SIGNATURE: (tibetan-cat-version)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Display Tibetan CAT version and feature summary.

** ~tibetan-cat--server-socket-path~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 419
:SIGNATURE: (tibetan-cat--server-socket-path server-name)
:END:

Return the socket path for SERVER-NAME if it exists, else nil.
Uses the `server-socket-dir' variable to resolve the path, then
checks for existence (the file is created by `server-start' and
removed when the server is stopped).

** ~tibetan-cat-open-zettel-for-word-at-point~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 430
:SIGNATURE: (tibetan-cat-open-zettel-for-word-at-point &optional arg)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open the zettelkasten entry for the Wylie term under point.

Tries (in order):
  1. `tibetan-thesaurus--wylie-at-point' to read a `[wylie]'-bracket
     token from the current line (Detailed Dictionary / Interlinear
     Gloss layout).
  2. Falls back to a `read-string' prompt when no bracketed token
     is on the line.

Resolves the term through `tibetan-zettel-lookup' which indexes the
zettelkasten by normalised Wylie.

Routing:
  - With prefix arg \[universal-argument]: open locally via `find-file'.
  - Otherwise: if the research Emacs server is running (socket file at
    \='(server--socket-dir)/tibetan-cat-zettel-target-server\='),
    route via `emacsclient --no-wait'.  This sends the zettel to the
    research instance — typical use case is a second monitor.
  - Otherwise (no server, no prefix): open locally via `find-file'.

When the lookup returns nil (no zettel for that Wylie), prompts the
user whether to create one and (if confirmed) delegates to `denote'
in the appropriate instance.

** ~tibetan-cat-zettel-target-server~  /customize variable/
:PROPERTIES:
:KIND: defcustom
:LINE: 399
:TYPE: 'string
:GROUP: tibetan-cat
:DEFAULT: "research-emacs"
:END:

Emacs server name of the Research instance for zettel lookups.

When this server is running, `tibetan-cat-open-zettel-for-word-at-point'
routes opens via emacsclient -s <server> so the zettel appears in the
research instance instead of the CAT instance.  Configure the research
instance to start the server with the matching name:

  (require ='server)
  (setq server-name "research-emacs")
  (unless (server-running-p) (server-start))

Empty string disables remote routing entirely.

** ~tibetan-cat-version~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 287
:END:

/(no docstring)/

** ~tibetan-cat-version~  /constant/
:PROPERTIES:
:KIND: defconst
:LINE: 372
:DEFAULT: "2.1.0"
:END:

Version of Tibetan CAT system.

** ~server-socket-dir~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 417
:END:

/(no docstring)/


* workspace/tibetan-sentence-workspace.el
:PROPERTIES:
:CUSTOM_ID: workspace/tibetan-sentence-workspace.el
:SOURCE_PATH: /Users/cp/tibetan-cat.el/workspace/tibetan-sentence-workspace.el
:END:

** ~tibetan-get-sentence-at-point-any-format~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 48
:SIGNATURE: (tibetan-get-sentence-at-point-any-format)
:END:

Get sentence data from ANY format (org structure or old markers).
Returns plist with :number, :segments, :start, :end.

** ~tibetan-get-sentence-at-point~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 72
:SIGNATURE: (tibetan-get-sentence-at-point)
:END:

Get the current sentence (segments between markers or
sentence boundaries).

** ~tibetan-create-sentence-workspace~  /function/
:PROPERTIES:
:KIND: defun
:LINE: 125
:SIGNATURE: (tibetan-create-sentence-workspace sentence-data)
:END:

Create workspace buffer for SENTENCE-DATA.

** ~tibetan-prepare-sentence~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 257
:SIGNATURE: (tibetan-prepare-sentence)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Open sentence preparation workspace.
Creates editable workspace with translation, notes, and vocabulary reference.
Works with both org structure (** Sentence) and old markers.

** ~tibetan-save-workspace~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 285
:SIGNATURE: (tibetan-save-workspace)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Save current workspace to file.

** ~tibetan-export-workspace-pdf~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 293
:SIGNATURE: (tibetan-export-workspace-pdf)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Export current workspace to PDF for classroom reference.
Uses minimal LaTeX setup - no title page, no TOC.

** ~tibetan-export-any-org-to-pdf~  /function/  ⚡  ↗
:PROPERTIES:
:KIND: defun
:LINE: 325
:SIGNATURE: (tibetan-export-any-org-to-pdf)
:INTERACTIVE: t
:AUTOLOAD: t
:END:

Export ANY org file to PDF with minimal LaTeX setup.
Works for any .org file, not just workspace files.
No title page, no TOC.

** ~tibetan-workspace-file~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 25
:DEFAULT: nil
:END:

Path to the current workspace file being edited.

** ~org-latex-classes~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 35
:END:

/(no docstring)/

** ~org-export-with-toc~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 36
:END:

/(no docstring)/

** ~org-export-with-title~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 37
:END:

/(no docstring)/

** ~org-export-with-author~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 38
:END:

/(no docstring)/

** ~org-export-with-date~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 39
:END:

/(no docstring)/

** ~org-latex-compiler~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 40
:END:

/(no docstring)/

** ~org-latex-pdf-process~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 41
:END:

/(no docstring)/

** ~org-latex-default-class~  /variable/
:PROPERTIES:
:KIND: defvar
:LINE: 42
:END:

/(no docstring)/

